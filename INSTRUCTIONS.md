# INSTRUCTIONS — ปรับระบบหาคำตอบบอทน้องใจดี (Spender Club)

> **สำหรับ Claude Code:** อ่านไฟล์นี้ + ไฟล์ Excel `spenderclub_knowledge_base_single.xlsx`
> (sheet ชื่อ **`line OA`**) ก่อนเริ่ม แล้วทำตามคำสั่งทีละเรื่อง
>
> **วิธีทำงาน:** ทำทีละเรื่อง (1 → 2 → 3) หยุดให้ผมรีวิวก่อนไปเรื่องถัดไป
> ก่อนลงมือ ให้ดูโครงสร้างโปรเจกต์และไฟล์ที่เกี่ยวกับ logic การตอบก่อน
> แล้วบอกผมว่าจะแก้ตรงไหนบ้าง

---

## ⛔ จุดห้ามแตะ (สำคัญที่สุด)

ส่วนเหล่านี้ทำงานดีอยู่แล้ว **ห้ามแก้** — แก้เฉพาะ logic "การหาคำตอบ" เท่านั้น:
- Admin Takeover, ระบบตรวจเวลา / greeting / off-hours
- `imei_protocol`, `repair_protocol`
- การจัดการรูปภาพ (logic 5 นาที, 4-ปุ่ม card)
- Redis keys เดิม, LINE / Facebook webhook

---

## 📋 โครงสร้างข้อมูล (sheet `line OA`)

ข้อมูลทั้งหมดอยู่ใน sheet เดียว 12 คอลัมน์ ใช้คอลัมน์ `type` แยกประเภท (`faq` / `product` / `license`):

| คอลัมน์ | หน้าที่ |
|---|---|
| `id` | รหัสแถว |
| `type` | ประเภท: faq / product / license |
| `category` | หมวด |
| `keywords` | คำค้น/คำพ้องที่ลูกค้าพิมพ์ (ใช้ match คำถาม) |
| `answer` | คำตอบ/ข้อมูลหลัก |
| `url` | ลิงก์อ้างอิง |
| `brand` | ยี่ห้อ (เฉพาะ product) |
| `model_code` | ชื่อรุ่น (เฉพาะ product) |
| `price` | ราคาขายจริง (เฉพาะ product) |
| `price_pack` | ราคาแพ็ค/โปร (เฉพาะ product) |
| `license_required` | ต้องขอ / ไม่ต้องขอ (เฉพาะ product) |
| `license_ref` | id แถว license ที่เกี่ยวข้อง (เฉพาะ product) |

> (แก้บั๊กแล้ว 2026-07-07) เดิมเอกสารนี้ยังมีคอลัมน์ `stock` (in/out) อยู่ ทั้งที่ชีตจริงและ `lib/sheet.ts` เอาคอลัมน์นี้ออกไปแล้วตั้งแต่ก่อนหน้านี้ ไม่มี logic เช็ค stock เหลืออยู่ในโค้ดเลย — ลบออกจากเอกสารให้ตรงกับของจริงแล้ว

**หลักการค้น:** ค้นจากคอลัมน์ `keywords` ก่อนเสมอ เจอแถวไหนดูที่ `type` แล้วตอบตามแบบของประเภทนั้น

---

## เรื่องที่ 1 — ตอบราคา/ข้อมูลสินค้า

เมื่อลูกค้าถามราคา/ข้อมูลรุ่นไหน → match `keywords` เจอแถว `type=product` → ตอบตาม template:

```
{brand} {model_code} ราคา {price} บาท ค่ะ 📻
• {answer}
• {license_text} รับประกัน 2 ปี
ดูรายละเอียด/ราคาล่าสุด: {url}
ลูกค้าสนใจสั่งเลยไหมคะ?
```

**ตัวอย่างจริง (TC-5M):**
```
Spender TC-5M ราคา 3,560 บาท ค่ะ 📻
• วิทยุใส่ซิม POC Network Radio ใช้งานทั่วไป
• ไม่ต้องขอใบอนุญาต ใช้ได้ถูกกฎหมาย รับประกัน 2 ปี
ดูรายละเอียด/ราคาล่าสุด: https://spenderclub.com/product/spender-tc-5m/
ลูกค้าสนใจสั่งเลยไหมคะ?
```

**กฎแปลง `license_required` → `license_text`:**
- `ไม่ต้องขอ` → "ไม่ต้องขอใบอนุญาต ใช้ได้ถูกกฎหมาย"
- `ต้องขอ` → "ถูกกฎหมาย (รุ่นนี้ต้องทำใบอนุญาต สอบถามรายละเอียดได้ค่ะ)"

**กรณีพิเศษ (ต้องจัดการ ไม่งั้นตอบพลาด):**

*1.1 ราคาเป็น `-` (ขายแค่แพ็ค):* ใช้ `price_pack` แทน
```
{brand} {model_code} — {price_pack} ค่ะ 📻
```
ตัวอย่าง: "Spender UBC-410T — แพ็คคู่ 2 เครื่อง 2,790 ค่ะ"

*1.2 มี `price_pack` เพิ่ม (มีทั้งเดี่ยวและแพ็ค):* ต่อท้าย "หรือ {price_pack} (โปรโมชั่นสุดคุ้ม)"

*1.3 กำกวม (match หลายรุ่น เช่น "245"):* อย่าเดา ให้เสนอตัวเลือก
```
รบกวนยืนยันรุ่นที่สนใจค่ะ มีหลายรุ่นใกล้เคียง:
1) {model_1}  2) {model_2}  3) {model_3}
แจ้งชื่อรุ่นได้เลยค่ะ
```

---

## เรื่องที่ 2 — ตอบใบอนุญาตเฉพาะรุ่น (เชื่อม 2 แถว)

เมื่อลูกค้าถาม "รุ่นนี้ต้องขอ/ต้องทำใบอนุญาตไหม" → ทำ 2 ขั้น:
1. อ่าน `license_required` จากแถวสินค้า → บอกต้องขอ/ไม่ต้องขอ
2. ตามคอลัมน์ `license_ref` → ไปดึง `answer` + `url` จากแถว `type=license` ที่ `id` ตรงกัน → ดึงค่าธรรมเนียม + ขั้นตอนมาตอบรวมกัน

**ห้ามตอบแค่ "ต้องขอค่ะ"** — ต้องครบ: ต้องขอไหม + ค่าธรรมเนียม + ขั้นตอน + ทางลัดถามแอดมิน

**กรณี "ต้องขอ":**
```
{brand} {model_code} รุ่นนี้ต้องทำใบอนุญาตค่ะ 📋
เป็น{category}
• ค่าธรรมเนียม {fee จากแถว license_ref}
• ดูขั้นตอนการขอใบอนุญาตได้ที่นี่ {url จากแถว license_ref}
  หรือสอบถามแอดมินช่วยดำเนินการได้ค่ะ
```
**ตัวอย่างจริง (TC-246H Plus → license_ref = lic-245-5w):**
```
Spender TC-246H Plus รุ่นนี้ต้องทำใบอนุญาตค่ะ 📋
เป็นวิทยุ 245MHz กำลังส่ง 5W (เครื่องมีทะเบียน)
• ค่าธรรมเนียม 535 บาท/เครื่อง
• ดูขั้นตอนการขอใบอนุญาตได้ที่นี่ https://rep.tccom.co.th/
  หรือสอบถามแอดมินช่วยดำเนินการได้ค่ะ
```

**กรณี "ไม่ต้องขอ":**
```
{brand} {model_code} ไม่ต้องขอใบอนุญาตค่ะ ใช้งานได้ถูกกฎหมายเลย 😊
เป็น{category} {answer}
```

> **กลไกเชื่อม:** คอลัมน์ `license_ref` ในแถวสินค้าชี้ไป id แถว license แล้ว
> (TC-246H→lic-245-5w, ราชการ→lic-gov, สมัครเล่น→lic-amateur, ใส่ซิม→lic-simradio)
> แค่ lookup ตาม id ไม่ต้องเดาประเภทเอง

---

## เรื่องที่ 3 — ค้นเว็บเมื่อไม่เจอในชีต (ก่อน handoff)

ถ้าค้นในชีตไม่เจอ **อย่าเพิ่งโยนแอดมิน** ให้ค้นเว็บก่อน:
```
ค้นชีต (keywords) ไม่เจอ
   → ค้น site:spenderclub.com และ site:spendernetwork.com
   → เจอ: ตอบ + แนบ url
   → ไม่เจอ/ไม่ชัด: handoff แอดมิน + log คำถาม
```

**Template เมื่อค้นเว็บเจอ:**
```
{ข้อมูลที่ได้จากเว็บ} ค่ะ 📻
ดูรายละเอียดเพิ่มเติม: {url ที่เจอบนเว็บ}
หากต้องการข้อมูลเพิ่มเติม สามารถสอบถามเพิ่มเติมได้เลยนะคะ
```

**Template เมื่อค้นเว็บแล้วยังไม่เจอ:**
```
ขออภัยค่ะ ยังไม่พบข้อมูลที่ตรงกับคำถาม 🙏
รบกวนแจ้งรายละเอียดเพิ่มเติม หรือรอแอดมินติดต่อกลับในเวลาทำการ 08:00–17:00 น. นะคะ
```

**กฎสำคัญ:**
1. **อ่านราคาให้ถูก** — ราคา**ขีดฆ่า** = ก่อนลด, ราคา**ไม่ขีดฆ่า** = ราคาขายจริง → ใช้ราคาขายจริงตอบ
2. **ห้ามแต่งข้อมูล** — เว็บไม่ชัด ห้ามเดา → handoff แทน
3. **กัน loop/ค้นซ้ำถี่** — cooldown ต่อ user 2 นาที + cache ผลค้นเว็บ 24 ชม.
4. **บันทึกคำถามที่ตอบไม่ได้** — เก็บ log ให้ Marketing เอาไปเติมชีต
5. **Auto-cache (ทำทีหลังได้ ไม่ต้องรีบ)** — ผลค้นเว็บเขียนกลับเข้าชีตแถวใหม่ ช่วงแรกแค่ค้นเว็บแล้วตอบก็พอ
6. **ห้ามเดาจากสินค้าใกล้เคียง** — ถ้าผลค้นหาไม่ตรงกับสิ่งที่ลูกค้าถามโดยตรง:
   - ห้ามหยิบสินค้ารุ่นอื่น/ใกล้เคียงมาตอบแทน
   - ห้ามแนบลิงก์ที่ไม่ตรงคำถาม
   - ห้ามสรุปสถานะ (หมด/มีของ/ราคา) ที่ผลค้นไม่ได้ระบุชัดว่าตรงกับสินค้านั้น
   - ให้ตอบว่าไม่พบข้อมูล + handoff อย่างเดียว

> URL pattern: สินค้า = `spenderclub.com/product/{brand}-{model}/`  
> URL ที่ส่งให้ลูกค้า: ตัด tracking parameters ออกก่อนเสมอ (เช่น `?srsltid=...`, `?utm_source=...`)

---

## เรื่องที่ 4 — แก้ flow นอกเวลาทำการ (18:00–07:59)

**ปัญหาเดิม:** ข้อความแรกของวันนอกเวลา บอทส่ง notice แล้วหยุด ไม่ตอบคำถาม
→ ลูกค้าถามราคาตอนกลางคืนแล้วโดนเงียบ ต้องพิมพ์ซ้ำ เสี่ยงเสียลูกค้า

**แก้เป็น:** prepend notice ไว้หน้าคำตอบ แล้วตอบคำถามต่อในข้อความเดียวกัน
(ใช้รูปแบบเดียวกับ Greeting Card ในเวลาทำการที่ prepend แล้วไปต่อ)

```
ข้อความแรกของวัน (นอกเวลา):
  off_hours:{userId} ไม่มี key → set key (23h)
  → prepend notice หน้าคำตอบ → ตอบคำถามต่อ (ข้อความเดียว)

ข้อความถัดไปในคืนเดียวกัน:
  off_hours:{userId} มีแล้ว → ไม่ prepend → ตอบปกติ
```

**notice ที่ใช้:**
```
ขณะนี้อยู่นอกเวลาทำการ แอดมินจะตอบกลับในเวลาทำการ 08:00–17:00 น. ค่ะ 🙏
ระหว่างนี้น้องใจดีช่วยดูแลก่อนนะคะ
```

> handoffMsg นอกเวลา + greeting card คงเดิม ไม่ต้องแก้
> **ทดสอบ:** ทักครั้งแรก 3 ทุ่ม "TC-246H ราคาเท่าไหร่" → ต้องได้ notice + ราคา 4,150 ในข้อความเดียว

---

## เรื่องที่ 5 — ปรับ flow อ่านภาพ (อุดช่องโหว่)

flow เดิมใช้ได้ แต่เพิ่ม 3 จุดนี้ให้ดีขึ้น:

**5.1 ตอบรับทันทีเมื่อได้รับรูป** (อย่าเงียบ 5 นาที)
เมื่อลูกค้าส่งรูป → ตอบรับสั้นๆ ทันที แล้วค่อยรอดูว่าจะพิมพ์ถามต่อไหม
```
ได้รับรูปแล้วค่ะ กำลังดูให้นะคะ 📷
```
(logic รอ 5 นาที + 4-ปุ่ม card คงเดิม แค่เพิ่มข้อความตอบรับนำหน้า)

**5.2 กรณี OCR อ่านรูปไม่ออก / ไม่ใช่รูปสินค้า**
ถ้า OCR ไม่เจอชื่อรุ่น → อย่าแสดง card เปล่า ให้ถามตรงๆ:
```
รบกวนพิมพ์ชื่อรุ่นที่สนใจได้ไหมคะ จะได้ช่วยหาข้อมูลให้ถูกต้องค่ะ
```

**5.3 4-ปุ่ม card ต้องผูกกับรุ่นที่ OCR เจอ**
เมื่อลูกค้ากด [ราคา]/[สเปค] บอทต้องรู้ว่าถามรุ่นไหน โดยดึง model_code
จากชื่อสินค้าที่เพิ่งบันทึกลง history ตอน OCR — ไม่ใช่ถามลอยๆ

---

## เรื่องที่ 6 — ปรับ flow IMEI (อุดช่องโหว่)

flow 4 ขั้นเดิมใช้ได้ แต่เพิ่ม 2 จุดนี้:

**6.1 ให้ยกเลิกกลางคันได้**
ระหว่างอยู่ใน imei_protocol ถ้าลูกค้าพิมพ์ "ยกเลิก" หรือถามเรื่องอื่นชัดเจน
→ ออกจาก protocol ล้าง state ที่ค้าง ไม่วนถามซ้ำ
```
ยกเลิกรายการแล้วค่ะ มีอะไรให้น้องใจดีช่วยอีกไหมคะ
```

**6.2 กัน "ใช่" ที่กำกวมในขั้นยืนยัน (สำคัญ)**
ขั้น 4 ถ้าลูกค้าตอบ "ใช่" พร้อมข้อมูลเพิ่ม/เปลี่ยน เช่น "ใช่ แต่เปลี่ยนเป็นกลุ่ม C"
หรือ "ใช่ แล้วเพิ่มอีกเครื่อง" → **อย่า handoff ทันที**
ให้จับว่ามีข้อมูลใหม่ แล้วกลับไปขั้น 3 สรุปยืนยันใหม่
handoff เฉพาะเมื่อลูกค้ายืนยันชัดเจนไม่มีเงื่อนไขเพิ่ม
→ กันแอดมินได้ข้อมูลผิด

> ขั้นตอน 1-4 เดิม (ตรวจ 15 หลัก, ถามประเภท, สรุป, HANDOFF:) คงไว้ทั้งหมด

---

## เรื่องที่ 7 — กันถาม/ตอบซ้ำ + spam (3 ชั้น)

**ปัญหา:** ลูกค้าถามเดิมซ้ำๆ บอทตอบเป๊ะเดิมทุกรอบ (ดูไม่ฉลาด) และมีคนพิมพ์เล่น/ยิงรัว

**หลักการสำคัญ:** ตอบครั้งแรกเต็มเสมอ แล้วค่อยปรับเมื่อซ้ำ — อย่าเงียบใส่ลูกค้าตั้งแต่แรก
(บางคนถามซ้ำเพราะเน็ตหลุด/ไม่เห็นคำตอบ ไม่ใช่กวน)

**ชั้นที่ 1 — กันถามซ้ำเป๊ะ (last_question)**
จำ**คำถามล่าสุด**ที่ลูกค้าพิมพ์ (ไม่ใช่คำตอบบอท) เช็ค**ก่อน**เรียก Gemini เลย ถ้าคำถามใหม่ตรงกับคำถามก่อนหน้าเป๊ะภายใน 2 นาที
→ ข้าม Gemini ไปเลย เปลี่ยนเป็นข้อความสั้นแทนการตอบยาวซ้ำ:
```
ราคา TC-246H อยู่ที่ 4,150 บาทตามที่แจ้งไปนะคะ มีอะไรให้ช่วยเพิ่มไหมคะ 😊
```

> (แก้บั๊กแล้ว 2026-07-11) เดิมเช็คหลังเรียก Gemini เสร็จ โดยเทียบ "คำตอบที่ได้" กับคำตอบก่อนหน้า (key `last_answer`) — Gemini ตั้ง `temperature: 1.0` ทำให้คำตอบไม่เหมือนเดิมทุกตัวอักษรได้แม้ถามคำถามคลุมเครือซ้ำเป๊ะ (เช่น "สอบถามเพิ่มเติม") เลยหลุดจากการเช็ค ตอบเต็มรูปแบบซ้ำไปเรื่อยๆ ไม่มีวันเงียบ ตอนนี้ย้ายมาเช็คก่อนเรียก Gemini แทน เทียบ "คำถามลูกค้า" (ค่าที่แน่นอน ไม่มีความสุ่ม) แทนคำตอบ — เปลี่ยน key เป็น `last_question` ทุกช่องทาง (LINE/Facebook ทั้ง text และ postback/Web Chat) แม่นยำกว่าเดิมและประหยัด token เพิ่มด้วยเพราะข้ามการเรียก Gemini ไปเลยถ้าตรวจพบว่าซ้ำ — พร้อมกันนี้แก้เพิ่มให้ Facebook postback (ปุ่มบนการ์ดสินค้า เช่น "สอบถามเพิ่มเติม") มีระบบกันซ้ำนี้ด้วย เดิมไม่มีเลยเพราะเป็นคนละ code path จาก text flow

> (แก้บั๊กแล้ว 2026-07-11 รอบ 2) ทดสอบจริงพบว่า "สอบถามเพิ่มเติม" ยังตอบซ้ำได้อยู่ทั้งที่แก้ข้างบนไปแล้ว — สาเหตุคือคำนี้ตรงกับ keyword ในชีตพอดี (แถว `product-recommend-14`: `สอบถามเพิ่มเติม/สอบถามเรื่องอื่นๆ`) เลยโดน **`findExactMatch()`** (เรื่องที่ 8) จับตอบทันทีตั้งแต่ก่อนถึงจุดกันซ้ำที่เพิ่งแก้ (จุดนั้นวางไว้แค่ก่อนเรียก Gemini เท่านั้น ไม่ครอบคลุม `findExactMatch()` เลย เพราะ `findExactMatch()` ทำงานก่อนหน้านั้นอีกที) ตอนนี้ย้ายจุดกันซ้ำมาเช็ค**ก่อน** `findExactMatch()` ด้วยแล้วในทั้ง 3 ไฟล์ (LINE, Facebook text, Web Chat) ครอบคลุมทั้ง 2 เส้นทาง (ชีต + Gemini) ในจุดเดียว — Facebook postback ไม่ต้องแก้เพิ่ม เพราะไม่เคยเรียก `findExactMatch()` เลยตั้งแต่แรก

**ชั้นที่ 2 — กัน spam ยิงรัว (rate limit)**
ถ้า user ส่งเกิน 5 ข้อความใน 10 วินาที → ชะลอ ตอบรวบครั้งเดียว หรือส่ง:
```
รอสักครู่นะคะ น้องใจดีกำลังอ่านข้อความค่ะ 🙏
```

**ชั้นที่ 3 — กันพิมพ์เล่น/ไร้สาระ (nonsense)**
ข้อความที่ไม่ใช่คำถาม (เช่น "5555", ".", สุ่มตัวอักษร) ซ้ำๆ
→ ตอบสั้นครั้งแรก แล้วเงียบถ้ายังกวนต่อ (ใช้ logic เดียวกับ out_of_domain เดิม)

**Redis keys เพิ่ม:**
| Key | TTL | หน้าที่ |
|---|---|---|
| `last_question:{userId}` | 2 นาที | จำคำถามล่าสุด กันถามซ้ำเป๊ะ (เช็คก่อนเรียก Gemini) |
| `msg_rate:{userId}` | 10 วินาที | นับความถี่ข้อความ กัน spam |
| `nonsense_count:{userId}` | 10 นาที | นับข้อความไร้สาระติดกัน |

> **จุดห้ามแตะ:** อย่าให้กลไกนี้บล็อกลูกค้าจริง — ตอบครั้งแรกเต็มเสมอ
> ใช้ rate limit/nonsense เฉพาะเมื่อมีรูปแบบซ้ำชัดเจน ไม่ใช่เงียบใส่ทุกข้อความสั้น

---

## เรื่องที่ 8 — Exact Keyword Match (ทางลัดตอบจากชีต ไม่ผ่าน Gemini)

> เพิ่มหลัง INSTRUCTIONS.md ฉบับแรก — ไฟล์: `lib/sheet.ts` (`findExactMatch()`) ใช้เหมือนกันทั้ง 3 ช่องทาง เช็คก่อนเรียก Gemini เสมอ

**เงื่อนไข (ต้องผ่านทั้ง 2 ชั้น):**
1. ข้อความ (lowercase, trim) ยาวไม่เกิน 40 ตัวอักษร และมี keyword จากคอลัมน์ `keywords` (คั่นด้วย comma/slash/pipe) เป็น substring โดยข้อความยาวไม่เกิน keyword ที่ match + 15 ตัวอักษร
2. ข้อความต้องไม่มีคำที่บ่งบอกความต้องการอื่น: `เสีย|ซ่อม|พัง|ปัญหา|เคลม|ต่างกัน|เปรียบเทียบ|ดีกว่า|ไม่เอา|ไม่ใช่|ยกเลิก|แนะนำ|งบ|เหมาะกับ`

**ผลลัพธ์:** match ได้พอดี 1 แถว → ตอบทันทีด้วย template ธรรมชาติ (`customerProduct()`/`customerFaq()`/`customerLicense()` — คนละชุดกับ `formatProduct()`/`formatFaq()`/`formatLicense()` ที่เป็นบริบทดิบให้ Gemini อ่าน) **ไม่เรียก Gemini เลย** · match 0 หรือ ≥2 แถว → ปล่อยผ่านไป Gemini ตามปกติ

> (แก้บั๊กแล้ว 2026-07-11) เดิม `findExactMatch()` ไม่มีระบบกันตอบซ้ำเลย — ถ้าข้อความลูกค้าตรง keyword ในชีตซ้ำๆ (พบจริงกับ "สอบถามเพิ่มเติม" ที่ตรงกับแถว `product-recommend-14`) จะตอบเต็มรูปแบบซ้ำเป๊ะไปเรื่อยๆ ไม่มีวันเงียบ ตอนนี้เพิ่มจุดกันถามซ้ำ (เทียบ `last_question` — ดูเรื่องที่ 7) ให้เช็ค**ก่อน** `findExactMatch()` ด้วยแล้วในทั้ง 3 ไฟล์ (LINE, Facebook text, Web Chat)

---

## เรื่องที่ 9 — ลูกค้ายืนยันสั่งซื้อ (Order Confirm)

> เพิ่มหลัง INSTRUCTIONS.md ฉบับแรก — ไฟล์: `line-webhook`/`fb-webhook` (`ORDER_CONFIRM_RE`) และ `web-chat` (`ORDER_RE`) เช็คก่อนเรียก Gemini

**เงื่อนไข:** ข้อความมีคำว่า "สั่งเลย, อยากสั่ง, ต้องการซื้อ, จะซื้อ, สั่งได้ไหม, ซื้อยังไง, โอนเงิน, ชำระเงิน, จ่ายเงิน"

| ช่วงเวลา | LINE/Facebook | Web Chat |
|---|---|---|
| ในเวลาทำการ | ตอบ "กรุณารอสักครู่นะคะ" ครั้งแรก แล้วเงียบถ้าพิมพ์ซ้ำภายใน 20 นาที | ตอบ FALLBACK_MSG ทุกครั้ง (ไม่กันซ้ำ) |
| นอกเวลาทำการ | ตอบ "ขณะนี้อยู่นอกเวลาทำการ โดยเจ้าหน้าที่จะเริ่มให้บริการในช่วงเวลา 08:00–17:00 น. นะคะ ลูกค้าสามารถพิมพ์ข้อความไว้โดยเจ้าหน้าที่จะรีบตอบกลับในวันทำการถัดไปนะคะ" ทุกครั้ง (ไม่กันซ้ำ) | ตอบ FALLBACK_MSG ทุกครั้ง (ไม่กันซ้ำ) |

---

## เรื่องที่ 10 — Web Chat Contact Suffix

> เพิ่มหลัง INSTRUCTIONS.md ฉบับแรก — ไฟล์: `app/api/web-chat/route.ts` (`CONTACT_SUFFIX`, `withSuffix()`)

Web Chat ไม่มีแอดมิน real-time เลยต่อท้ายข้อความชวนติดต่อแอดมินให้ลูกค้าเอง:
```

สอบถามรายละเอียดเพิ่มเติมติดต่อได้ที่
แอดไลน์ @spenderclub
เบอร์โทรติดต่อ: 085-222-9111, 089-661-2682
จันทร์–เสาร์ เวลา 08:00–17:00 น.ค่ะ
```

**เงื่อนไขต่อท้าย:**
- คำตอบมีคำว่า "บาท" (เช่น แจ้งราคา) → ต่อท้ายทุกครั้ง
- คำตอบไม่มีคำว่า "บาท" → ต่อท้ายแค่ครั้งแรกของ session เท่านั้น (จำสถานะด้วย Redis key `webchat:contact_shown:{sid}`)
- ไม่ต่อท้าย `FALLBACK_MSG` เพราะมีเบอร์ติดต่อฝังอยู่ในตัวข้อความแล้ว

---

## เรื่องที่ 11 — เคลม/ส่งซ่อม: เช็ค FAQ ก่อน + handoff แทนการวินิจฉัยเอง (ทุกช่องทาง)

> เพิ่มหลัง INSTRUCTIONS.md ฉบับแรก — ไฟล์: `lib/prompts.ts` (`<repair_protocol>`, `buildSystemPrompt(..., channel)`), `app/api/web-chat/route.ts` (`CLAIM_RE`, `CLAIM_MSG`)
> ปรับปรุงล่าสุด 2026-07-10 (รอบ 2) — เดิมทำเฉพาะ Web Chat (2026-07-07) → ปรับเป็น logic เดียวกันทุกช่องทาง (2026-07-10 รอบ 1) → ปรับ Web Chat เพิ่มอีกรอบให้ข้ามขั้นถามข้อมูลไปเลย (รอบ 2)
> ปรับปรุงเพิ่ม 2026-07-30 — LINE/Facebook ตัดคำถาม "ซื้อมานานแค่ไหน" ออก เหลือถามแค่รุ่น/อาการ 2 อย่าง พร้อมเพิ่มประโยค "เพื่อให้ช่างประเมินอาการเบื้องต้นให้นะคะ" — Web Chat ไม่กระทบ ยังข้ามขั้นถามเหมือนเดิม

**ขั้นที่ 1 — เช็ค `<faq>` ก่อนเสมอ (เหมือนกันทุกช่องทาง):** ถ้าเป็นคำถามขั้นตอน/นโยบายทั่วไป (ไม่ใช่แจ้งอาการเสียของเครื่องตัวเองโดยเฉพาะ) เช่น "จะส่งเครื่องเคลม" "เช็คสถานะซ่อม" "รับประกันกี่ปี" → ตอบจาก `<faq>` ทันที ไม่ต้องถามข้อมูลเพิ่ม (ในชีตมีคำตอบพร้อมอยู่แล้ว เช่น `warranty-repair-02`, `warranty-repair-04`)

**ขั้นที่ 2 — ถ้าแจ้งอาการเฉพาะเจาะจง (ไม่มีคำตอบตรงใน `<faq>`) — ต่างกันตาม channel:**

| ช่องทาง | บอททำ | ทำไมถึงต่าง |
|---|---|---|
| LINE / Facebook | ถามข้อมูล 2 อย่างก่อนเสมอ (รุ่น/อาการ) พร้อมบอกลูกค้าว่า "เพื่อให้ช่างประเมินอาการเบื้องต้นให้นะคะ" แล้วตอบ `HANDOFF: ซ่อม/เคลม \| รุ่น: [รุ่น] \| อาการ: [อาการ]` → `notifyAdmin()`/`notifyAdminFacebook()` ส่งสรุปเข้ากลุ่มแอดมินจริง + ตอบ handoffMsg | มี real-time admin คอยรับสรุป คุ้มที่จะถามเก็บบริบทไว้ |
| Web Chat | **ข้ามขั้นถามไปเลย** ตอบ `HANDOFF: ซ่อม/เคลม` ทันทีคำเดียว (ไม่มีรุ่น/อาการ/ระยะเวลาต่อท้าย) → ตรวจจับแล้วตอบ `CLAIM_MSG` | ไม่มี real-time admin ให้ส่งสรุปไปหา ถ้าถามแล้วข้อมูลไม่ถูกใช้ที่ไหนเลย เสียเวลาลูกค้าเปล่าๆ แล้วต้องอธิบายซ้ำเองตอนติดต่อแอดมินจริงอยู่ดี |

**ห้ามวิเคราะห์อาการหรือแนะนำแก้เองอีกแล้วในทุกช่องทาง** — ส่งต่อให้คนช่วยดูแลแทนเพื่อความถูกต้องแม่นยำที่สุดเสมอ

**Web Chat มีชั้นกรองเพิ่มอีกชั้นก่อนถึง Gemini:** ข้อความที่มีคำว่า "เคลม"/"ส่งซ่อม"/"claim" ตรงตัว (เช่น "จะส่งเครื่องเคลมครับ") จะถูก `CLAIM_RE` ดักไว้ ตอบทันทีไม่ผ่าน Gemini เลย:
```
เรื่องเคลม/ส่งซ่อมรบกวนติดต่อแอดมินโดยตรงเลยนะคะ
แอดไลน์ @spenderclub
เบอร์โทรติดต่อ: 085-222-9111, 089-661-2682
จันทร์–เสาร์ เวลา 08:00–17:00 น.ค่ะ
```
ข้อความที่แจ้งอาการโดยไม่มีคำเหล่านี้ (เช่น "PTT กดไม่ติดครับ") จะไม่ถูกดักตรงนี้ ไหลไปเรียก Gemini ตามขั้นที่ 1-2 ด้านบนแทน — ถ้าจบที่ `HANDOFF: ซ่อม/เคลม` ก็จะได้ข้อความเดียวกันนี้อยู่ดี (คนละเส้นทาง ผลลัพธ์เดียวกัน)

> (แก้บั๊กแล้ว 2026-07-10) เดิม `<repair_protocol>` เข้าทันทีเมื่อเจอคำว่า "เครื่องเสีย/ซ่อม/เคลม" แบบไม่มีเงื่อนไข ก่อนขั้นค้น FAQ เสมอ ทำให้คำถามทั่วไปที่ชีตตอบได้อยู่แล้วถูกถามข้อมูลซ้ำโดยไม่จำเป็น และเดิมมีขั้นตอนให้ Gemini วิเคราะห์อาการ/แนะนำแก้เอง/แนะนำที่อยู่ส่งซ่อมด้วยตัวเอง — ตอนนี้เปลี่ยนเป็นเช็ค FAQ ก่อนเสมอ และให้คนช่วยดูแลประเมินอาการเฉพาะเจาะจงแทน Gemini ทั้งหมด (Web Chat ยังปรับเพิ่มอีกรอบให้ข้ามขั้นถามข้อมูลไปเลย เพราะข้อมูลที่เก็บมาไม่มีที่ให้ใช้ต่อ — ไม่มี `notifyAdmin()` สำหรับ Web Chat)

### เพิ่ม 2026-07-31 — `<self_repair_protocol>` (เฉพาะ LINE/Facebook)

**ปัญหา:** ลูกค้าบางส่วนไม่ได้แจ้งว่าเครื่องเสีย แต่ถาม **"วิธีทำเอง"** เช่น "มีวิธีแกะเปลี่ยนกระจก TC-4M ไหม" — เป็นคนละกรณีกับ `repair_protocol` (ไม่ใช่การแจ้งอาการเสีย) เดิมไม่มี protocol รองรับ ปล่อยให้ Gemini ตอบเอง เสี่ยงสอนวิธีแกะเครื่องเองซึ่งอาจกระทบการรับประกันและความปลอดภัย

**ทำงานเฉพาะ LINE/Facebook เท่านั้น — Web Chat ไม่แตะเลย** (`channel === 'web'` → protocol นี้ว่างเปล่า ไม่ถูกใส่เข้า system prompt)

**พฤติกรรม:** เมื่อลูกค้าถามวิธีทำเอง (ไม่ใช่แจ้งเครื่องเสีย) ห้ามสอนวิธีทำเองเด็ดขาด ห้ามถามข้อมูลเพิ่มก่อน ตอบทันที:
```
HANDOFF: ขอวิธีทำเอง | รายละเอียด: [สิ่งที่ลูกค้าถาม]
```
โค้ด (`line-webhook`/`fb-webhook`) ตรวจจับ `HANDOFF: ขอวิธีทำเอง` แล้วตอบลูกค้าด้วยข้อความเฉพาะ (แทน `handoffMsg` ทั่วไป):
```
กรุณารอเจ้าหน้าที่เพื่อทำการตรวจสอบและแนะนำวิธีให้อีกครั้งนะคะ
```
พร้อม `notifyAdmin()`/`notifyAdminFacebook()` ส่งสรุปเข้ากลุ่มแอดมินเหมือน HANDOFF เคสอื่นๆ ปกติ

---

## เรื่องที่ 12 — Facebook: ปุ่มการ์ดสินค้า + กันตอบซ้ำใน postback

> เพิ่มหลัง INSTRUCTIONS.md ฉบับแรก (2026-07-11) — ไฟล์: `app/api/fb-webhook/route.ts` (`fbSendProductCard()`, postback handler)

**1. เปลี่ยนปุ่ม "ติดต่อแอดมิน" → "สอบถามเพิ่มเติม" บนการ์ดสินค้า**

การ์ดสินค้า (Generic Template ที่ส่งเมื่อคำตอบ Gemini มี URL `spenderclub.com`) มี 2 ปุ่ม: "ดูรายละเอียด/ราคา" (web_url) และปุ่มที่ 2 เดิมชื่อ "ติดต่อแอดมิน" (postback) — กดแล้วเข้า `shouldHandoff()` ตอบ handoffMsg ทันที **โดยไม่เคยเรียก `notifyAdminFacebook()` เลย** ลูกค้าเห็นข้อความ "รอแอดมินติดต่อกลับนะคะ" แต่ไม่มีใครในทีมรู้เรื่องจริง

แก้เป็น **"สอบถามเพิ่มเติม"** แทน (payload ยังเป็น `CONTACT_ADMIN` เหมือนเดิม — ไม่กระทบเพราะไม่ตรงกับ 5 payload ที่โค้ดรู้จักอยู่แล้ว) คำนี้ไม่ชนคำใน `shouldHandoff()` trigger list เลย จึงไหลไปเรียก Gemini จริง ได้คำตอบขยายความจากบริบทสินค้าที่เพิ่งแนะนำไป (conversation history เก็บคำตอบเต็มของ Gemini ไว้อยู่แล้วแม้ลูกค้าจะเห็นแค่การ์ดแบบย่อ)

**2. เพิ่มระบบกันตอบซ้ำใน postback handler**

พบว่าปุ่ม postback ทั้งหมด (ไม่ใช่แค่ "สอบถามเพิ่มเติม") ไม่มีระบบกันตอบซ้ำเลย ต่างจาก flow ข้อความพิมพ์ปกติที่มีอยู่แล้ว (เรื่องที่ 7 ชั้น 1) — กดปุ่มเดิมซ้ำกี่ครั้งก็เรียก Gemini ใหม่ทุกครั้ง ได้คำตอบเต็มซ้ำไปเรื่อยๆ ไม่มีวันเงียบ (ยืนยันจากการทดสอบจริง กดซ้ำ 3 ครั้งได้คำตอบเดิมเป๊ะทุกครั้ง)

ตอนนี้เพิ่ม logic กันซ้ำชุดเดียวกับ text flow เข้าไปในนี้ด้วยแล้ว (ใช้ Redis key เดียวกัน `last_question:{userId}`/`repeat_count:{userId}` เทียบ `queryText` ก่อนเรียก Gemini เลย — ครั้งที่ 2 ที่ซ้ำ: reminder สั้นๆ · ครั้งที่ 3+: เงียบ) ครอบคลุมทุกปุ่ม postback ไม่ใช่แค่ปุ่มที่เพิ่งแก้ (ดูเรื่องที่ 7 สำหรับรายละเอียดว่าทำไมถึงเปลี่ยนจากเทียบ `last_answer` เป็น `last_question`)

> **ทำไม LINE ไม่มีปัญหานี้:** ปุ่มใน LINE Flex Message ใช้ `action: { type: 'message' }` — กดแล้วส่งเป็นข้อความพิมพ์ปกติกลับมา ไหลผ่าน text handler เดียวกับข้อความทั่วไปที่มีระบบกันตอบซ้ำอยู่แล้วโดยอัตโนมัติ ไม่ต้องแก้อะไรเพิ่ม (Web Chat ไม่มีปุ่มแบบนี้เลยไม่เกี่ยวด้วย)

---

## เรื่องที่ 13 — กันบอทเงียบสนิทตอน Gemini ช้า/ค้าง (ทุกช่องทาง)

**ปัญหาที่เจอ:** ทดสอบใช้งานจริงพบ 2 เคส (LINE) ที่ลูกค้าถามคำถามปกติ (เช่น "เช่า ว.ซิม", "ราคา 5m") ที่มีคำตอบพร้อมอยู่ในชีตแล้ว แต่**บอทไม่ตอบอะไรเลยแม้แต่ข้อความ fallback** — เช็ครายงาน "ตอบไม่ได้" ก็ไม่มีบันทึกไว้เลย (รายงานนี้บันทึกเฉพาะตอน Gemini คืน `[NOT_FOUND]` เท่านั้น) ยืนยันว่าการประมวลผลถูกตัดตอนกลางทาง ไม่ใช่บอทหาคำตอบไม่เจอ

**สาเหตุที่สงสัยมากที่สุด:** เดิมจับ timeout เรียก Gemini ด้วย `Promise.race()` ธรรมดา (10 วินาที) — เมื่อ timeout ชนะ การเรียก Gemini ที่ "แพ้" **ไม่ได้ถูกยกเลิกจริง** ยังทำงานต่อในเบื้องหลังเงียบๆ เสี่ยงกินเวลาจนกระทบ `maxDuration` ทำให้ Vercel ฆ่า function ทิ้งกลางคันแบบไม่มี log อะไรเลย (log ของ Vercel เก็บได้แค่ ~1 ชม. เลยหาหลักฐานย้อนหลังไม่ได้)

**วิธีแก้ (2026-07-30):**
1. เปลี่ยนจาก `Promise.race()` เป็น **`AbortController`** — เมื่อครบ 10 วินาที (หรือ 7 วินาทีสำหรับ FB postback) จะสั่งยกเลิกการเชื่อมต่อ Gemini จริงผ่าน `abortSignal` ใน config ของ `ai.models.generateContent()` ทั้งใน `lib/gemini.ts`
2. เพิ่ม `maxDuration` จาก 30 เป็น **60 วินาที** ในทั้ง 3 ไฟล์ webhook เป็นตาข่ายนิรภัยเพิ่มเติม
3. เพิ่ม log **`webhook.message_received`** ทันทีที่ข้อความเข้ามา ก่อนเริ่มประมวลผลใดๆ ในทั้ง 3 ช่องทาง เผื่อเกิดปัญหาซ้ำจะได้มีหลักฐานว่าข้อความเข้าระบบจริงไหม ประมวลผลไปถึงขั้นไหนก่อนเงียบ

**ไม่กระทบ:** เวลารอคำตอบ (10s/7s), ข้อความ GEMINI_UNAVAILABLE, ต้นทุน token — ยังเท่าเดิมทุกอย่าง เป็นการแก้เชิงป้องกันเบื้องหลังล้วนๆ

### เพิ่ม 2026-07-31 — เจอสาเหตุที่แท้จริงอีกจุด: replyToken ของ LINE หมดอายุ/ใช้ซ้ำ

หลัง deploy ข้อแก้ด้านบนแล้ว ยังเจอเคสบอทเงียบซ้ำอีก 2 ครั้ง (LINE) — รอบนี้มี log ใหม่ (`webhook.message_received`) ช่วยไล่ดูได้ครบ พบว่า **Gemini ตอบสำเร็จปกติทุกครั้ง** (ไม่ได้ timeout เลย) แต่ขั้นตอนส่งข้อความกลับไปหาลูกค้าล้มเหลว:
```
gemini.reply (สำเร็จ) → reply.send_failed "400 - Bad Request" → (log ผิดว่า reply.sent สำเร็จ ทั้งที่จริงไม่สำเร็จ)
```
เกิดกับคำถามธรรมดา (~3 วิ) และเคสรูปภาพที่ใช้เวลานานกว่า (~14 วิ รวม OCR+ค้นเว็บ+Gemini) — ยิ่งใช้เวลานานยิ่งเจอบ่อย สอดคล้องกับพฤติกรรม **LINE replyToken ที่ใช้ได้ครั้งเดียวและมีอายุสั้น** (หมดอายุ หรือถูกใช้ไปแล้วถ้า LINE ส่ง webhook event ซ้ำเพราะเราตอบช้า)

**วิธีแก้ (เฉพาะ `line-webhook.ts` — Facebook ไม่มีปัญหานี้เพราะไม่ใช้ replyToken เลย ใช้ PSID ตรงๆ เหมือน push เสมอ):**
เพิ่ม fallback ใน `safeReply()` — ถ้า `lineClient.replyMessage()` ล้มเหลว ให้ลอง `lineClient.pushMessage({ to: userId, messages })` ทันที (ไม่ต้องพึ่ง replyToken เดิมที่มีปัญหา) พร้อม log แยกให้ชัดเจน (`reply.fallback_push_sent` / `reply.fallback_push_failed`) แทนที่จะปล่อยเงียบแบบเดิม

---

## เรื่องที่ 14 — ห้ามเดาเจตนา "เข้ากลุ่ม" จากข้อความที่ไม่มีคำขอชัดเจน (ทุกช่องทาง)

**ปัญหาที่เจอ:** ลูกค้าพิมพ์แค่ **"Nong Suea chang rescue (179557)"** (ชื่อ+ตัวเลขในวงเล็บ ไม่มีคำขอ/กริยาใดๆ) แต่ Gemini เดาเองว่าเป็นชื่อกลุ่ม+รหัสห้อง แล้วตอบยาวเรื่อง "เข้ากลุ่มสาธารณะที่แอป Spender Friends" ทันที (ดึงจาก FAQ `spender-network-15`) ทั้งที่ลูกค้าอาจไม่ได้ต้องการเรื่องนี้เลย — Gemini เห็นรูปแบบ "ชื่อ + ตัวเลขในวงเล็บ" คล้ายกับตัวอย่างชื่อกลุ่ม/รหัสห้องใน FAQ แล้วสรุปเจตนาเอาเองโดยไม่มีหลักฐานชัดเจน

**วิธีแก้ (2026-07-31):** เพิ่ม guardrail ใหม่ใน `<guardrails>` (`lib/prompts.ts`) ใช้ร่วมกันทุกช่องทาง — ถ้าข้อความลูกค้าไม่มีคำขอ/กริยาชัดเจน (ไม่มีคำว่า "เพิ่ม" "เข้ากลุ่ม" "ขอ" "ดึง" ฯลฯ) ห้ามเดาว่าต้องการเข้ากลุ่มสาธารณะ ให้ตอบถามกลับก่อนเสมอ:
```
ไม่ทราบว่าต้องการให้น้องใจดีช่วยเรื่องอะไรคะ 😊
หากลูกค้าต้องการเข้ากลุ่มสาธารณะหรือกลุ่มปิด โดยเจ้าหน้าที่จะให้บริการตั้งแต่เวลา 08:00-17:00 นะคะ
```
ยังคงใบ้เรื่องกลุ่มไว้ต่อท้ายเพื่อช่วยลูกค้าที่ตั้งใจเข้ากลุ่มจริงๆ พิมพ์ต่อได้ถูก แต่ไม่ตอบยาวแบบเดาเจตนาแทนลูกค้าอีกต่อไป — ถ้าข้อความมีคำขอชัดเจนอยู่แล้ว (เช่น "เพิ่มกลุ่ม Nong Suea chang rescue (179557) ให้หน่อย") ยังทำงานเหมือนเดิมทุกอย่าง ไม่กระทบ

---

## เรื่องที่ 15 — ประกาศวันหยุด (ทุกช่องทาง)

**ปัญหาที่เจอ:** ระบบ "ตั้งเวลาปิดบอทซ้ำทุกสัปดาห์" ที่มีอยู่ (`/admin/schedule`, `lib/schedule.ts`) รองรับแค่**วันซ้ำรายสัปดาห์** (เช่น "ทุกวันอังคาร") เท่านั้น ไม่รองรับวันหยุดนักขัตฤกษ์ที่เป็นวันที่เฉพาะเจาะจง (เช่น สงกรานต์ 13-15 เม.ย.) — และเมื่อ `isScheduledOff()` ตรงเงื่อนไข บอทจะ**เงียบสนิทไม่ตอบอะไรเลย** ทำให้ลูกค้าไม่รู้ว่าร้านปิดหรือระบบเสีย

**ฟีเจอร์ใหม่ (2026-07-31):** เพิ่ม **"ประกาศวันหยุด"** แยกต่างหากจากตารางเวลาซ้ำรายสัปดาห์เดิม — **บอทยังตอบคำถามลูกค้าตามปกติทุกช่องทาง ไม่ปิดเลย** แค่แนบข้อความแจ้งวันหยุดไปกับคำตอบครั้งแรกของแต่ละ session/วัน (คนละแนวคิดกับ `isScheduledOff` ที่ปิดสนิท)

**ไฟล์ที่เกี่ยวข้อง:**
- `lib/holidays.ts` — `HolidayRule` (startDate, endDate, label, message, enabled) + `getActiveHolidayNotice()` เช็ควันที่ปัจจุบัน (Bangkok) ตรงกับช่วงวันหยุดที่เปิดใช้งานไหม
- `app/api/admin/holidays/route.ts` — CRUD API (ใช้รหัสผ่านเดียวกับ `/admin/schedule` คือ `ADMIN_SCHEDULE_PASSWORD`)
- `app/admin/holidays/page.tsx` — **หน้าแอดมินให้เจ้าของร้านเพิ่ม/แก้ไข/ลบวันหยุดเองได้** ไม่ต้องรอให้นักพัฒนาแก้โค้ด — กรอกวันที่เริ่ม-สิ้นสุด, ชื่อวันหยุด, และพิมพ์ข้อความแจ้งลูกค้าเองได้อิสระ

**วิธีทำงานแต่ละช่องทาง:**
- **LINE** — แนบเข้า `txt()` helper (เหมือน off-hours notice) ก่อนข้อความตอบจริงทุกครั้งที่ยังไม่เคยแจ้งวันนั้น (flag `holiday_notified:{userId}` TTL 23 ชม.)
- **Facebook** — ส่งแยกก่อนคำตอบจริงที่จุดเดียวกับที่ส่ง `OFF_HOURS_NOTICE` ทั้ง 3 จุด (text flow, image flow, สเปค/ฟังก์ชัน flow)
- **Web Chat** — แนบเข้า `withHolidayNotice()` ซึ่งเรียกจาก `withContactSuffix()`/`withSuffix()` (ครอบคลุมคำตอบหลักเกือบทั้งหมด) flag ต่อ sessionId แทน userId เพราะ Web Chat ไม่มี userId ถาวร

**ไม่กระทบ:** `isScheduledOff()`/ตารางเวลาซ้ำรายสัปดาห์เดิม ยังทำงานแยกกันคนละระบบเหมือนเดิมทุกอย่าง ถ้าตั้งทั้ง 2 อย่างพร้อมกัน (เช่น ปิดบอทวันอังคารด้วย schedule + ประกาศวันหยุดสงกรานต์ด้วย holidays) ตารางเวลาซ้ำรายสัปดาห์จะทำงานก่อน (ปิดสนิท ไม่ถึงจุดที่เช็ควันหยุดเลย)

---

## 🎯 หลักการที่ต้องยึด

- **ใช้ template เดียว เติมค่าจากชีต ห้าม hardcode คำตอบรายรุ่น** — เพื่อให้ทีม Marketing เพิ่ม/แก้รุ่นในชีตเองได้ โดยไม่ต้องแก้โค้ด
- เป้าหมาย: เพิ่มรุ่นใหม่ = กรอกชีต 1 แถว บอทตอบได้เลย
- เมื่อทำเสร็จ ขอ commit แยกแต่ละเรื่อง + อธิบายสั้นๆ ว่าแก้ไฟล์ไหนบ้าง
- ทุกครั้งที่แก้โค้ดหรือเพิ่ม feature ให้อัปเดต SYSTEM_FLOW.html ให้ตรงกับสิ่งที่เพิ่งแก้ด้วยทุกครั้ง ถือเป็น standard workflow
- เมื่อมีการแจ้งว่ามีการแก้ไขอะไรก็ตาม (ไม่ว่าจะเป็นข้อความ flow template หรือข้อมูลใดๆ) ให้อัปเดต SYSTEM_FLOW.html, BOT_FLOW_SUMMARY.html, INSTRUCTIONS.md (ไฟล์นี้เอง) และ UNANSWERED_SUMMARY.html (เฉพาะเมื่อแก้ `app/admin/unanswered/page.tsx` หรือ endpoint ที่หน้านี้เรียกใช้) ให้ตรงกับสิ่งที่เปลี่ยนไปทันที โดยไม่ต้องรอให้สั่งแยก — ถือเป็นขั้นตอนสุดท้ายของทุกงานเสมอ

---

## เรื่องที่ 16 — Schedule Check ไม่ครอบคลุมรูปภาพ (LINE + Facebook)

**ปัญหาที่เจอ:** ลูกค้าส่ง**รูปภาพ**เข้ามาช่วงเวลาที่ตั้ง "ปิดบอท" ไว้ (`/admin/schedule`) แต่บอทยังตอบทันที ทั้งที่ตั้งใจให้เงียบช่วงนั้น

**สาเหตุ:** `isScheduledOff()` ถูกเรียกใช้แค่ใน branch ข้อความตัวอักษร (`msgType === 'text'` ของ LINE, `event.message?.text` ของ Facebook) เท่านั้น — branch จัดการรูปภาพ (`msgType === 'image'` ของ LINE ที่ [line-webhook.ts:603](app/api/line-webhook/route.ts#L603), `attachments...type === 'image'` ของ Facebook ที่ [fb-webhook.ts:548](app/api/fb-webhook/route.ts#L548)) ไม่เคยเช็คตารางเวลานี้เลยตั้งแต่แรก

**วิธีแก้ (2026-07-31):** เพิ่ม `isScheduledOff('line')`/`isScheduledOff('fb')` ที่ต้น IMAGE branch ของทั้ง 2 ไฟล์ ก่อนทำอะไรอื่นเลย (ก่อนแม้แต่ off-hours/holiday notice หรือ greeting) — ถ้า off จะ return เงียบทันทีเหมือนกับ text flow ทุกประการ

---

## เรื่องที่ 17 — คำว่า "แนะนำ" ตีความผิดบริบทเมื่อลูกค้าติดปัญหาอุปกรณ์เดิม (เฉพาะ LINE/Facebook)

**ปัญหาที่เจอ:** ลูกค้าอธิบายปัญหาอุปกรณ์ที่มีอยู่แล้วยาวๆ ต่อเนื่องหลายข้อความ (เช่น "ใส่ซิม Gomo แล้วยังใช้งานไม่ได้", "กับ Zello ขึ้นแบบนี้ครับ") แล้วจบด้วย **"ช่วยแนะนำด้วยครับ"** — Gemini จับแค่คำว่า "แนะนำ" ในข้อความล่าสุดตามเงื่อนไข `<reasoning_protocol>` ขั้นที่ 3 แล้วเข้า `<recommend_protocol>` ถามลักษณะการใช้งาน ("ร้านอาหาร โชว์รูม งานอีเวนต์...") ทั้งที่ไม่เกี่ยวกับปัญหาที่คุยมาเลย — คล้ายบั๊ก "จะซื้อ" ที่เจอก่อนหน้านี้ (เรื่องที่ 9) คือคำเดียวในบริบทยาวถูกตีความผิด

**วิธีแก้ (2026-07-31, เฉพาะ LINE/Facebook — Web Chat ไม่แตะเลย):** เพิ่มข้อยกเว้นในขั้นที่ 3 ของ `<reasoning_protocol>` (`lib/prompts.ts`, ตัวแปร `step3RecommendException`) — ถ้า conversation history ก่อนหน้ามีร่องรอยว่าลูกค้ากำลังติดปัญหาอุปกรณ์ที่มีอยู่แล้ว (พูดถึงชื่อแอป, ซิม, "ใช้งานไม่ได้", "เข้าไม่ได้", ข้อความ error) ห้ามตีความคำว่า "แนะนำ"/"ช่วยแนะนำ" เป็นการขอแนะนำสินค้าใหม่ ให้ข้าม `recommend_protocol` แล้วขอรายละเอียดเพิ่มก่อนส่งต่อแอดมินแทน

**ทำไมไม่กระทบคำถามแนะนำสินค้าจริง:** เงื่อนไขนี้ต้องมีร่องรอยปัญหาอุปกรณ์ในบทสนทนาก่อนหน้าด้วยเสมอ — คำถาม "แนะนำรุ่นหน่อยครับ" ที่ไม่มีบริบทปัญหาอุปกรณ์นำมาก่อน ยังเข้า `recommend_protocol` ตามปกติทุกอย่าง

---

## เรื่องที่ 18 — ลูกค้าส่งคำขอ IMEI ครบในข้อความเดียว บอทตอบข้อความแต่งเอง ไม่ส่งต่อแอดมิน (เฉพาะ LINE/Facebook)

**ปัญหาที่เจอ:** ลูกค้าส่งข้อความเดียวที่มีข้อมูลครบทุกอย่างสำหรับขอเปิด/เพิ่มกลุ่ม Spendernetwork (ชื่อ, อีเมล, เบอร์, บริษัท, รหัสกลุ่ม, ประเภทกลุ่ม, ประเภทสมาชิก, IMEI 15 หลัก) พร้อมคำขอชัดเจนว่า "เปิดระบบให้หน่อยครับ" — Gemini ไม่ได้ตอบในรูปแบบ `HANDOFF: ...` ตามที่ `<imei_protocol>` กำหนด แต่แต่งข้อความเองว่า "ขออภัยค่ะ น้องใจดีไม่พบข้อมูล ต้องการติดต่อแอดมินแจ้งได้เลยนะคะ" ซึ่งไม่ตรงกับ sentinel ใดๆ ที่โค้ดรู้จัก (`[NOT_FOUND]` หรือ `HANDOFF:`) — ผลคือ `notifyAdmin()`/`notifyAdminFacebook()` ไม่ถูกเรียกเลย คำขอลูกค้าหายไปโดยไม่มีแอดมินเห็น และไม่เข้า `unanswered_log` ด้วย (log นี้จับเฉพาะ path `[NOT_FOUND]` เท่านั้น)

**สาเหตุ:** `<imei_protocol>` เดิมออกแบบมาเป็น flow หลายรอบ (ตรวจ IMEI → ถามประเภทคำสั่ง → ให้ยืนยัน → ค่อยตอบ `HANDOFF:`) เมื่อลูกค้าให้ข้อมูลครบในข้อความเดียวตั้งแต่แรก Gemini ไม่มีคำสั่งชัดเจนให้ข้ามขั้นตอนย่อยไปเลย จึงเดาเอาเองว่าต้อง "บอกว่าไม่พบข้อมูล ให้ติดต่อแอดมิน" แทน

**วิธีแก้ (2026-07-31, เฉพาะ LINE/Facebook — Web Chat ไม่แตะเลย):**
1. เพิ่มข้อยกเว้น one-shot ท้าย `<imei_protocol>` (`lib/prompts.ts`, ตัวแปร `imeiOneShotNote`) — ถ้าข้อความเดียวมี IMEI ครบ 15 หลัก + คำขอชัดเจน + รายละเอียดกลุ่มครบ ห้าม Gemini ถามซ้ำหรือรอยืนยันตามขั้นที่ 2-3 ให้ข้ามไปตอบ `HANDOFF:` ทันทีตามรูปแบบขั้นที่ 4 และห้ามแต่งประโยค "ไม่พบข้อมูล" เด็ดขาด
2. **เฉพาะ Facebook:** เพิ่มฟังก์ชัน `getImeiHandoffMessage()` ใน [fb-webhook.ts](app/api/fb-webhook/route.ts) — เมื่อ HANDOFF summary มีคำว่า `"IMEI:"` จะตอบลูกค้าด้วยข้อความชี้ทางไปแอดไลน์ `@spenderclub` แทนข้อความ "รอแอดมิน" ทั่วไป เพราะงาน Spendernetwork ดูแลผ่านทีม LINE เป็นหลัก — มี 2 เวอร์ชันตามเวลา (ในเวลาทำการ / นอกเวลาทำการ 18:00–08:00 น. จะแจ้งเวลาทำการเพิ่ม)
3. **LINE:** ไม่ต้องเพิ่ม message ใหม่ — ใช้ `handoffMsg` เดิมที่ time-aware อยู่แล้วผ่าน `getHandoffMessage()`
4. ทุกกรณียังเรียก `notifyAdmin()`/`notifyAdminFacebook()` เหมือนเดิม — แก้แค่ข้อความที่ลูกค้าเห็น ไม่กระทบการแจ้งแอดมิน

---

## เรื่องที่ 19 — บอทตอบข้อความเดิมซ้ำติดกัน 2 รอบ (เฉพาะ LINE/Facebook)

**ปัญหาที่เจอ:** เจอ 2 เคสที่บอทส่งข้อความ**เหมือนกันเป๊ะ 2 รอบติดกัน** ทำให้ลูกค้ารู้สึกว่าบอทค้าง/ตอบผิดปกติ:
1. ลูกค้าพิมพ์ตัวเลขไม่มีคำขอชัดเจน 2 ข้อความติดกัน (เช่น รหัสสั่งซื้อ แล้วตามด้วยเลข 14 หลัก) → ทั้งคู่โดน guardrail "ไม่ทราบว่าต้องการให้น้องใจดีช่วยเรื่องอะไรคะ..." ([lib/prompts.ts:74](lib/prompts.ts#L74)) เหมือนกัน จึงได้คำตอบซ้ำ
2. ระหว่างขั้นยืนยัน IMEI (`<imei_protocol>` ขั้นที่ 3) ลูกค้าตอบอะไรที่ไม่ใช่ "ใช่" และไม่ใช่ข้อมูลกลุ่มใหม่ (เช่น บอกที่มาของเครื่องแทน) — ขั้นที่ 4 ไม่มีเงื่อนไขรองรับกรณีนี้ Gemini เลยแสดงข้อความสรุปยืนยันซ้ำคำต่อคำอีกรอบ

**สาเหตุร่วม:** ทั้ง 2 เคสเป็นคำตอบข้อความธรรมดาจาก Gemini (ไม่ใช่ sentinel เช่น `HANDOFF`/`NOT_FOUND`/`OUT_OF_DOMAIN` ที่มีกลไกกันซ้ำอยู่แล้ว) จึงไม่มีจุดไหนในโค้ดเช็คว่าคำตอบซ้ำกับรอบก่อนหน้าหรือไม่เลย

**วิธีแก้ (2026-08-01, เฉพาะ LINE/Facebook — Web Chat ไม่แตะเลย):** เพิ่ม Redis key `last_bot_reply:{userId}` เก็บคำตอบดิบจาก Gemini ของรอบล่าสุด — ก่อนส่งคำตอบทุกครั้ง (เฉพาะ path คำตอบทั่วไปที่ไม่ใช่ sentinel ใน [line-webhook.ts](app/api/line-webhook/route.ts) และ [fb-webhook.ts](app/api/fb-webhook/route.ts)) เทียบกับค่าที่เก็บไว้ — ถ้า**ตรงกันเป๊ะกับรอบติดกันล่าสุด**เท่านั้น จะสลับไปส่ง `"รับทราบค่ะ 🙏"` แทนข้อความเดิมทั้งดุ้น

**ทำไมไม่กระทบคำตอบอื่น:** เทียบแค่รอบติดกันล่าสุด 1 รอบ (ไม่เทียบย้อนหลังทั้งบทสนทนา) ไม่แตะ path ที่มีกลไกกันซ้ำอยู่แล้ว (`last_question`/`repeat_count` สำหรับคำถามซ้ำ, `nonsense_count` สำหรับ OUT_OF_DOMAIN, retry-once สำหรับ NOT_FOUND) — เป็นแค่ string compare + Redis GET/SET ไม่เรียก Gemini เพิ่ม ไม่เสีย token เพิ่ม

---

## เรื่องที่ 20 — ชี้ทางลูกค้าไปแอดไลน์เมื่อถามเรื่อง Spendernetwork/กลุ่มสาธารณะ (เฉพาะ Facebook)

**ที่มา:** เดิมมีแค่จุดเดียว (HANDOFF ที่มี IMEI แนบมาแล้ว, ดูเรื่องที่ 18) ที่ชี้ทางลูกค้าไปแอดไลน์ `@spenderclub` แทนข้อความรอแอดมินทั่วไป — แต่ยังมีอีก 3 สถานการณ์ที่เกี่ยวกับ Spendernetwork ที่ยังไม่ครอบคลุม: (1) ลูกค้าอยากเข้ากลุ่มสาธารณะ (2) อยากลบออกจากกลุ่ม (3) ถามการใช้งาน/แจ้งปัญหา Spendernetwork แบบทั่วไป (ยังไม่มี IMEI) — ทั้งหมดควรชี้ทางไปทีม LINE เหมือนกัน เพราะทีมที่ดูแลระบบ Spendernetwork อยู่ทาง LINE โดยเฉพาะ

**วิธีแก้ (2026-08-01, เฉพาะ Facebook — LINE ไม่ถูกแตะแม้แต่บรรทัดเดียว):**
1. เปลี่ยนชื่อ `getImeiHandoffMessage()` → `getSpendernetworkRedirectMessage()` ใน [fb-webhook.ts](app/api/fb-webhook/route.ts) และปรับข้อความใหม่ให้ครอบคลุมทั้งเรื่องกลุ่มและ Spendernetwork: *"เกี่ยวกับการเข้า/ลบกลุ่มสาธารณะ หรือการใช้งาน/ปัญหาระบบ Spendernetwork รบกวนลูกค้าแอดไลน์ @spenderclub ได้เลยนะคะ 😊 มีทีมซัพพอร์ตพร้อมดูแลลูกค้าโดยตรง ให้บริการตั้งแต่เวลา 08:00–17:00 น. ค่ะ"* (ยังคง time-aware เหมือนเดิม — นอกเวลาทำการจะเติมประโยคแจ้งนอกเวลาไว้หน้าข้อความ)
2. เพิ่มฟังก์ชันตรวจคำ `isSpendernetworkRequest()` **แยกไว้ในไฟล์ fb-webhook.ts เอง** (ไม่ไปเพิ่มใน `lib/handoff.ts` ที่ใช้ร่วมกับ LINE) เช็คคำ "เข้ากลุ่ม/ลบกลุ่ม/เพิ่มกลุ่ม/กลุ่มสาธารณะ/กลุ่มปิด/spendernetwork"
3. เช็คเงื่อนไขนี้แบบ **immediate ก่อนค้น FAQ/เรียก Gemini เลย** (ข้ามการค้นหาไปเลย เพราะเป็นเรื่องเฉพาะบัญชี/เฉพาะเครื่องที่ FAQ ตอบแทนไม่ได้อยู่ดี) — ไม่ใช่ handoff จริง จึงไม่ set `routed`/`takeover` และไม่เรียก `notifyAdminFacebook()` เพราะยังไม่มีข้อมูลอะไรให้แอดมินดำเนินการ กันตอบซ้ำด้วย `spendernetwork_notified:{userId}` (10 นาที, ตอบเตือนแค่ครั้งแรกแล้วเงียบ)
4. guardrail กำกวมเรื่องกลุ่ม ([lib/prompts.ts:74](lib/prompts.ts#L74), เดิมตอบ "ไม่ทราบว่าต้องการ...") เปลี่ยนเป็น channel-conditional (`groupIntentFallback`) — เฉพาะ `channel === 'facebook'` จะใช้ข้อความ redirect เดียวกับข้อ 1 แทน ส่วน LINE/Web Chat ยังใช้ข้อความเดิมทุกประการ

**ทำไมไม่กระทบ LINE เลย:** คำ trigger ใหม่ (`isSpendernetworkRequest`) อยู่แยกไฟล์ ไม่ได้เพิ่มใน `lib/handoff.ts` ที่ใช้ร่วมกัน · ข้อความ redirect ใหม่ผูกกับเงื่อนไข `channel === 'facebook'` เท่านั้นทั้ง 2 จุด (โค้ดและ prompt) · ไม่มีจุดไหนแก้ logic ที่ LINE ใช้งานอยู่เลย

---

## เรื่องที่ 21 — เอาข้อความแจ้งนอกเวลาทำการ "ครั้งแรกของวัน" ออก (LINE + Facebook)

**เดิม:** ตอนลูกค้าทักมาครั้งแรกในช่วงนอกเวลาทำการ (18:00–07:59 น. ตามเวลาไทย) บอทจะแทรกข้อความแจ้งนำหน้าคำตอบปกติ (จำไว้ 23 ชม./userId ผ่าน Redis key `off_hours:{userId}`):
- LINE: "ขณะนี้อยู่นอกเวลาทำการ แอดมินจะตอบกลับในเวลาทำการ 08:00–17:00 น. ค่ะ 🙏 ระหว่างนี้น้องใจดีช่วยดูแลก่อนนะคะ"
- Facebook: "ขณะนี้อยู่นอกเวลาทำการ โดยแอดมินจะตอบกลับในช่วงเวลาทำการ 08:00–17:00 น. ค่ะ🙏 ให้น้องใจดีช่วยดูแลนะคะ"

**วิธีแก้ (2026-08-01, ทั้ง LINE + Facebook):** เอาโค้ดส่วนตรวจจับ+ส่งข้อความนี้ออกทั้งหมด (ตัวแปร `offHoursNotice`, ค่าคงที่ `OFF_HOURS_NOTICE`/`OFF_HOURS_TTL`, และทุกจุดที่แทรกข้อความนี้ก่อนคำตอบ ทั้งใน text flow, image flow, greetFirst flow) — บอทจะตอบตามปกติทันทีไม่ว่าจะเป็นเวลาไหนก็ตาม ไม่มีข้อความแจ้งนอกเวลาทำการช่วงแรกของวันอีกต่อไป

**ไม่กระทบข้อความนอกเวลาทำการอื่นๆ ที่ยังคงอยู่:** ยืนยันสั่งซื้อ (`ORDER_CONFIRM_OFF_HOURS_MSG`), handoff ทั่วไป (`getHandoffMessage()`), ขอเจ้าของ (`OWNER_REQUEST_OFF_HOURS_MSG`), เรื่อง Spendernetwork ฝั่ง Facebook (`getSpendernetworkRedirectMessage()`) — เป็นคนละจุด คนละเงื่อนไข ยังทำงานเหมือนเดิมทุกประการ · ตารางเวลาที่ตั้งเองได้ (`/admin/schedule`, `isScheduledOff()`) ก็ไม่กระทบเช่นกัน (คนละระบบ)

---

## เรื่องที่ 22 — เอาข้อความ "ได้รับรูปแล้วค่ะ" ออก (LINE + Facebook)

**เดิม:** ทุกครั้งที่ลูกค้าส่งรูปภาพ บอทจะตอบทันที "ได้รับรูปแล้วค่ะ กำลังดูให้นะคะ 📷" ก่อนไปประมวลผลรูปต่อ (OCR/รอข้อความตามมา)

**วิธีแก้ (2026-08-01, ทั้ง LINE + Facebook):** เอาข้อความนี้ออก
- **Facebook:** ลบบรรทัด `fbSend(psid, 'ได้รับรูปแล้วค่ะ...')` ออกตรงๆ — ยังส่ง greet/holiday notice ตามปกติถ้ามี แล้วไปประมวลผลรูปต่อทันที (ดาวน์โหลด/OCR/caption) ไม่มีข้อความอื่นคั่นกลาง
- **LINE:** เดิมข้อความนี้รวมอยู่ใน `ackMsgs` เดียวกับ greet (ใช้ `safeReply()` ซึ่งต้องมีอย่างน้อย 1 ข้อความ) — เอาออกแล้วเหลือแค่เช็คว่า `greetFirst` ไหม ถ้าใช่ยังส่ง WELCOME_MSG ตามปกติ ถ้าไม่ใช่ **ไม่ส่งอะไรเลย** (ไม่เรียก `safeReply()` เลย ปล่อย replyToken ไม่ได้ใช้ไป ไม่มีปัญหาอะไรเพราะ LINE ไม่บังคับต้องใช้ทุก replyToken)

**ไม่กระทบ:** ข้อความ fallback ตอน Redis ล่ม (การ์ด `imageIntentCard()`) และข้อความตอน OCR หาไม่เจอ ("รบกวนพิมพ์ชื่อรุ่นที่สนใจ...") ยังอยู่เหมือนเดิมทุกจุด เพราะเป็นข้อความคนละจุดกัน

---

## เรื่องที่ 23 — ปรับข้อความทักทายใหม่ + คืนแจ้งนอกเวลาทำการแบบรวมข้อความเดียว (LINE + Facebook)

**เดิม:** `WELCOME_MSG` ([lib/greeting.ts](lib/greeting.ts)) เป็นข้อความคงที่ "Spender Club ยินดีต้อนรับค่ะ 😊 (ขณะนี้อยู่ระหว่างการทดสอบระบบตอบกลับอัตโนมัติค่ะ)..." ซึ่งยังมีวรรค "อยู่ระหว่างการทดสอบระบบ" ค้างมาตั้งแต่ช่วงทดสอบ ทั้งที่ใช้งานจริงมานานแล้ว

**วิธีแก้ (2026-08-01, เฉพาะ LINE + Facebook — Web Chat ไม่ใช้ WELCOME_MSG อยู่แล้วจึงไม่กระทบ):** แปลง `WELCOME_MSG` จากข้อความคงที่ (`const`) เป็นฟังก์ชัน **`getWelcomeMessage()`** ที่เช็คเวลาไทยเหมือน `getHandoffMessage()`:
- **ท่อนหลัก (แสดงทุกครั้ง):** "🤖 Spender CLUB ยินดีต้อนรับ (ขณะนี้เป็นระบบตอบกลับอัตโนมัติ) ขอบคุณที่สนใจวิทยุสื่อสาร SPENDER สามารถพิมพ์คำถามได้เลย เช่น 💰 สอบถามราคาสินค้า / 🌐 Spender Network / 🛠️ การใช้งาน น้องใจดีจะช่วยตอบเบื้องต้น และแอดมินจะดูแลต่อในเวลาทำการค่ะ 😊"
- **ท่อนนอกเวลาทำการ (แสดงเฉพาะ 18:00–07:59 น. ต่อไว้ด้านหน้าท่อนหลักในข้อความเดียวกัน):** "🙏 ขณะนี้อยู่นอกเวลาทำการ แอดมินจะตอบกลับในวันและเวลาทำการ 08:00–17:00 น. ค่ะ"

**ต่างจากเรื่องที่ 21 ยังไง:** เรื่องที่ 21 คือเอาข้อความนอกเวลาทำการ "แยกข้อความต่างหาก" ที่ส่งก่อนคำตอบทุกคำถามออก — ส่วนเรื่องนี้คือการนำแนวคิดนอกเวลาทำการกลับมาใหม่ แต่**ผูกรวมอยู่ในข้อความทักทายเดียว (WELCOME_MSG) ที่ส่งแค่ครั้งแรกของวันเท่านั้น** ไม่ใช่แจ้งซ้ำทุกข้อความเหมือนเดิม จึงไม่ขัดแย้งกัน

---

## เรื่องที่ 24 — ปรับข้อความ handoffMsg นอกเวลาทำการ (เฉพาะ LINE)

**เดิม:** ท่อนนอกเวลาทำการของ `getHandoffMessage()` ใน [line-webhook.ts](app/api/line-webhook/route.ts) ตอบว่า "ขณะนี้อยู่นอกเวลาทำการ รอแอดมินติดต่อกลับนะคะ 🙏 ทีมงานให้บริการในเวลาทำการ 08:00–17:00 น. ค่ะ"

**วิธีแก้ (2026-08-01, เฉพาะ LINE — Facebook ไม่แตะ ยังใช้ข้อความเดิม):** เปลี่ยนเป็น "น้องใจดีรับทราบค่ะ และจะแจ้งแอดมินให้ติดต่อกลับในเวลาทำการนะคะ 🙏 ทีมงานให้บริการในเวลาทำการ 08:00–17:00 น. ค่ะ" ให้ความรู้สึกว่าบอทรับเรื่องแล้วจะส่งต่อให้แอดมิน แทนที่จะบอกลูกค้าแค่ "รอ" เฉยๆ — ท่อนในเวลาทำการ ("รอแอดมินติดต่อกลับนะคะ 🙏 ทีมงานกำลังดูแลท่านอยู่ค่ะ") ไม่เปลี่ยน

---

## เรื่องที่ 25 — ตอบชื่อรุ่นระหว่าง repair_protocol หลุดไปตอบราคาสินค้าแทน (เฉพาะ LINE/Facebook)

**ปัญหาที่เจอ:** ลูกค้าแจ้ง "ลงทะเบียนแล้ว ยังใช้งานไม่ได้ครับ" → บอทเข้า `<repair_protocol>` ขั้นที่ 2 ถามรุ่น+อาการตามปกติ → ลูกค้าตอบกลับมาแค่ **"Spender tc-11hw"** (ตอบแค่ชื่อรุ่นเพื่อตอบคำถามที่บอทเพิ่งถาม อาการ "ใช้งานไม่ได้" มีอยู่แล้วจากข้อความแรก) — แต่บอทกลับหลุดออกจาก `repair_protocol` ไปเข้า `<product_template>` แทน ตอบราคาสินค้า TC-11HW เหมือนลูกค้ากำลังถามราคาสินค้าใหม่ ทั้งที่ควรสรุปเป็น `HANDOFF: ซ่อม/เคลม | รุ่น: TC-11HW | อาการ: ใช้งานไม่ได้`

**สาเหตุ:** ขั้นที่ 2 ของ `repair_protocol` ([lib/prompts.ts:17-21](lib/prompts.ts#L17-L21)) สั่งให้ถามรุ่น+อาการ แต่ไม่มีข้อยกเว้นบอกว่า "ถ้าลูกค้าตอบกลับมาแค่ชื่อรุ่นเดียวระหว่างอยู่ใน repair_protocol ห้ามตีความเป็นการถามราคาสินค้าใหม่" — พอ Gemini เห็นชื่อรุ่นตรงกับสินค้าในชีต กฎ `<product_template>` ("เมื่อพบข้อมูลสินค้า...ให้ตอบตาม template นี้เสมอ") เลยแทรกก่อน ทำให้หลุด flow การซ่อม

**วิธีแก้ (2026-08-01, เฉพาะ LINE/Facebook — Web Chat ไม่กระทบเพราะ step 2 ของ Web Chat ข้ามการถามรุ่น/อาการไปเลยอยู่แล้ว จึงไม่มีทางเกิดสถานการณ์นี้):** เพิ่มข้อยกเว้นท้าย `repairStep2` (ส่วน non-web) — ถ้าบอทเพิ่งถามรุ่น/อาการไปก่อนหน้า แล้วลูกค้าตอบกลับมาด้วยชื่อรุ่นสินค้าเพียงอย่างเดียว (ไม่มีคำว่า "ราคา" "สั่งซื้อ" "สนใจซื้อ" หรือคำขออื่น) ห้ามตีความเป็นถามราคาสินค้าใหม่เด็ดขาด ให้ถือเป็นคำตอบของคำถามที่ถามไปแล้ว สรุปเป็น HANDOFF ทันที

**ทำไมไม่กระทบคำถามราคาจริง:** เงื่อนไขต้องมีทั้งบริบท (บอทเพิ่งถามรุ่น/อาการ) และข้อความล่าสุดไม่มีคำขอราคา/ซื้อ — ถ้าลูกค้าพิมพ์ "TC-11HW ราคาเท่าไหร่" ชัดเจน ยังตอบราคาได้ตามปกติ ไม่ถูกบล็อก

**อัปเดต (2026-08-01 รอบ 2):** ลอง live พบว่าบั๊กยังเกิดซ้ำได้ในบางกรณี — สาเหตุคือ (1) ข้อความยกเว้นเดิมเขียนว่า "ชื่อรุ่นเพียงอย่างเดียว" เข้มงวดไป พอลูกค้าพิมพ์ "TC-11HW **ครับ**" (มีคำลงท้ายสุภาพ) Gemini อาจตีความว่าไม่เข้าเงื่อนไข (2) ถ้าข้อความเปิดของลูกค้ากำกวมไม่มีคำอาการชัดเจน (เช่น "กรณีแบบนี้เป็นอะไรและแก้ไขได้ไหมครับ") บริบทว่ากำลังอยู่ใน repair_protocol จะไม่มั่นคงตั้งแต่ต้น ทำให้ข้อยกเว้นทำงานไม่นิ่ง — ปรับแก้เพิ่ม (ยังแก้แค่ข้อความใน `repairStep2` บล็อกเดียว ไม่แตะโค้ด):
1. ผ่อนคำเป็น "ชื่อรุ่นสินค้า (จะมีคำลงท้ายสุภาพต่อท้าย เช่น ครับ/ค่ะ/นะครับ หรือไม่มีก็ได้ ไม่นับเป็นคำขออื่น)"
2. เพิ่มประโยคยืนยันว่า "ข้อยกเว้นนี้สำคัญกว่ากฎ product_template เสมอ" เพื่อแข่งกับคำว่า "เสมอ" ใน product_template
3. ถ้ายังไม่มีอาการชัดเจนในบทสนทนาก่อนหน้าเลย ให้ใส่ "อาการ: ไม่ระบุชัดเจน (รอช่างตรวจสอบเพิ่มเติม)" แทนการบังคับต้องมีอาการก่อนถึงจะสรุป HANDOFF ได้

**หมายเหตุ:** เป็นการแก้ด้วย prompt (isolated text change ในบล็อกเดียว ไม่กระทบโค้ด/ช่องทางอื่น) — ไม่การันตี 100% เพราะพฤติกรรม LLM ไม่ deterministic เสมอไป ถ้ายังเกิดซ้ำอีกหลังแก้รอบนี้ ต้องพิจารณาทางแก้ระดับโค้ดแทน (เช็ค context ว่าข้อความก่อนหน้าบอทเพิ่งถามรุ่น/อาการไปไหม)

---

## เรื่องที่ 26 — วิเคราะห์รูปภาพให้ครอบคลุมกว่า "แค่สินค้า" + สรุป+ถามยืนยันแทนเดาเอง (เฉพาะ LINE/Facebook)

**ปัญหาที่เจอ:** ลูกค้าส่งรูปได้หลากหลายมากในแต่ละวัน ไม่ใช่แค่รูปสินค้า — เช่น รูปฉลาก/หน้าจอแสดง IMEI, รูปหน้าจอ error/คำเตือนบนเครื่อง (เช่น "login timeout"), สกรีนช็อตแอป/เว็บที่ติดปัญหาลงทะเบียน, ใบเสร็จส่งพัสดุซ่อม/เคลม ฯลฯ — แต่ระบบเดิม (OCR fallback ตอนลูกค้าส่งรูปเดี่ยวๆ ไม่มีข้อความ/caption ประกอบ) ถูกออกแบบมาให้ถามหา **"ยี่ห้อและรุ่นสินค้า"** เท่านั้น ถ้าไม่เจอสินค้าจะตอบ "รบกวนพิมพ์ชื่อรุ่นที่สนใจได้ไหมคะ" เสมอ ไม่ว่ารูปจะเป็นอะไรก็ตาม — ไม่ตรงจุดกับรูปประเภทอื่นเลย

**วิธีแก้ (2026-08-01, เฉพาะ LINE/Facebook — Web Chat ไม่แตะ):**

1. เพิ่มฟังก์ชันใหม่ **`analyzeImageIntent()`** ใน [lib/gemini.ts](lib/gemini.ts) (เรียก Gemini รอบเดียว ไม่เพิ่มต้นทุน/เวลาแฝงจากเดิม) จำแนกรูปเป็น 3 แบบ:
   - `product` — รูปสินค้าชัดเจน ไม่มี error/IMEI ปน → คืนชื่อสินค้า (พฤติกรรมเดิมทุกประการ ใช้การ์ดปุ่มตัวเลือกเหมือนเดิม)
   - `other` — รูปประเภทอื่นที่พอเข้าใจเจตนาได้ (error/IMEI/สกรีนช็อต/เอกสาร) → Gemini **สรุปสั้นๆ ว่าเห็นอะไร + แต่งประโยคถามยืนยันกับลูกค้ากลับมาเลย** (ห้ามตอบคำถามหรือแนะนำวิธีแก้เอง แค่สรุป+ถามยืนยัน) เช่น `"เห็นว่าเครื่อง TC-5M ขึ้นข้อความ \"login timeout\" ใช่ไหมคะ ต้องการให้น้องใจดีช่วยดูเรื่องนี้ใช่ไหมคะ 😊"`
   - `unclear` — อ่านอะไรไม่ออกเลย → พฤติกรรมเดิม "รบกวนพิมพ์ชื่อรุ่นที่สนใจได้ไหมคะ"
2. กรณี `other` — บันทึกคำถามยืนยันนี้เข้า `saveHistory()` เป็น turn ของบอทด้วย เพื่อให้**ข้อความตอบกลับของลูกค้ารอบถัดไป (พิมพ์ปกติ) เดินเข้า `generateReply()` flow ข้อความปกติ** พร้อม context ครบ — ต่อยอดเข้า `repair_protocol`/`imei_protocol`/FAQ/HANDOFF ที่มีอยู่แล้วได้เองโดยไม่ต้องเขียน logic ซ้ำ ถ้าบอทหาคำตอบให้ไม่ได้ก็จะ handoff ไปหาแอดมินตามเวลาทำการเหมือนกับกรณีข้อความล้วนทุกประการ (ใช้ `handoffMsg` เดิม time-aware อยู่แล้ว)
3. ใช้แทนที่ OCR เดิม (ที่ถามแค่ "ยี่ห้อ/รุ่นสินค้า") ใน **LINE** ที่ branch "เกิน 5 นาทีไม่มีข้อความตามมา" ([line-webhook.ts](app/api/line-webhook/route.ts)) และ **Facebook** ที่ branch "ไม่มี caption ประกอบรูป" ([fb-webhook.ts](app/api/fb-webhook/route.ts))

**ทำไมไม่กระทบอย่างอื่น (แต่ไม่ใช่ zero-impact 100%):**
- ไม่แตะ `generateReply()` (ฟังก์ชันข้อความล้วน) หรือ `generateReplyWithImage()` (ใช้ตอนมี caption/ข้อความตามรูปอยู่แล้ว) เลย — บทสนทนาที่ไม่มีรูปภาพไม่ได้รับผลกระทบใดๆ
- เรียก Gemini รอบเดียวเหมือนเดิม (แค่ขยายคำถามในคำสั่งเดียวกัน) ไม่เพิ่มรอบ/ต้นทุน
- เคส `product` (รูปสินค้าชัดเจน) ยังคงพฤติกรรมเดิมทุกประการ ไม่มีขั้นถามยืนยันเพิ่ม
- **ผลกระทบจริงที่ตั้งใจ:** เฉพาะเคส "ส่งรูปเดี่ยวๆ ไม่มีข้อความ แล้วอ่านไม่ออกว่าเป็นสินค้า" — เดิมได้แค่ "รบกวนพิมพ์ชื่อรุ่น" เท่านั้น ตอนนี้ถ้าเป็นรูปประเภทอื่นที่เข้าใจเจตนาได้ จะได้คำถามยืนยันที่ตรงจุดกว่าแทน

**อัปเดต (2026-08-01 รอบ 2, ทดสอบด้วยสคริปต์แยกเฉพาะ):** พบว่ารูปสินค้า SPENDER ที่ไม่ใช่ตัววิทยุตรงๆ (เช่น อุปกรณ์เสริม/แท่นชาร์จ) ยังโดนจัดเป็น `unclear` เพราะเงื่อนไขข้อ `product` เดิมเขียนว่า "รูปสินค้า**วิทยุสื่อสาร** SPENDER" แคบเกินไป — ปรับเป็น **"รูปสินค้าแบรนด์ SPENDER ทุกประเภท (วิทยุสื่อสาร, อุปกรณ์เสริม, อะไหล่, แท่นชาร์จ ฯลฯ)"** แทน (แก้จุดเดียวในข้อความ prompt ของ `analyzeImageIntent()` ไม่กระทบเงื่อนไข `other`/`unclear` หรือส่วนอื่น)

**อัปเดต (2026-08-01 รอบ 3):** ทบทวนแล้วพบว่าเงื่อนไข `other` เดิมเขียนแคบเกินไป (จำกัดแค่ error/IMEI/สกรีนช็อต/เอกสาร) ไม่ตรงกับที่ตกลงกันไว้แต่แรกว่าต้องการให้ **"อ่านรูปภาพทั้งหมดที่เป็นรูปภาพเลย แล้วสรุป+ถามยืนยันกลับ"** ไม่จำกัดแบรนด์/ประเภท — ปรับเงื่อนไข `other` ให้เป็น catch-all ครอบคลุมทุกอย่างที่พอมองออก (รวมสินค้าแบรนด์อื่นที่ไม่ใช่ SPENDER เช่น T.C.COM, kaiwa ด้วย) และ**สงวน `unclear` ไว้เฉพาะรูปที่อ่าน/มองไม่ออกจริงๆ เท่านั้น** (มืด/เบลอ/ไม่มีข้อมูลให้อ่าน) — แก้ปัญหาเรื่องแบรนด์อื่นที่ค้างไว้ในรอบ 2 ไปในตัว ไม่ต้องแยกตัดสินใจเรื่อง T.C.COM/แบรนด์อื่นอีกต่อไป เพราะตอนนี้รูปแบรนด์ไหนก็ได้ที่พอมองออกจะเข้า `other` แล้วสรุป+ถามยืนยันเสมอ

**หมายเหตุการทดสอบ:** ระหว่างทดสอบรอบ 2-3 พบว่า `GEMINI_API_KEY` ใน `.env.local` ของเครื่อง dev เป็นค่า placeholder ที่ไม่เคยตั้งค่าจริง ทำให้ผลทดสอบ local ก่อนหน้านี้หลายรอบเป็น `unclear` ปลอมจากการเรียก Gemini ไม่สำเร็จ (ถูก catch แล้ว fallback) ไม่ใช่ผลการตัดสินใจของ AI จริง — ไม่กระทบ production เพราะ Vercel มี Environment Variables แยกต่างหากคนละชุดกับไฟล์นี้

**อัปเดต (2026-08-01 รอบ 4, ปรับถ้อยคำ):** ตัวอย่าง (few-shot) ของเงื่อนไข `other` เดิมใช้คำขึ้นต้น "เห็นว่า..." — ปรับเป็น **"สินค้าเป็น..."** แทนสำหรับกรณีที่ระบุชื่อสินค้า/อุปกรณ์ได้ชัดเจน (เช่น "สินค้าเป็นอะแดปเตอร์ชาร์จไฟ SPENDER รุ่น E2452 ใช่ไหมคะ ต้องการให้น้องใจดีช่วยเรื่องนี้ใช่ไหมคะ 😊") ให้ฟังดูเป็นธรรมชาติกว่า — ตัวอย่างเรื่อง error ("เครื่อง TC-5M ขึ้นข้อความ login timeout") ยังใช้ "เห็นว่า..." เหมือนเดิมเพราะเป็นคนละบริบทกัน (รายงานอาการ ไม่ใช่ระบุชื่อสินค้า)

**อัปเดต (2026-08-01 รอบ 5):** เพิ่มข้อยกเว้นใน `repairStep2` (เฉพาะ LINE/Facebook) — ถ้าประวัติสนทนามีข้อความขึ้นต้นด้วย `[ลูกค้าส่งรูปภาพ:` (แปลว่าอาการนี้มาจากการวิเคราะห์รูปภาพของบอทเอง ไม่ใช่ลูกค้าพิมพ์อาการเอง) ให้ **ลองค้น `<faq>`/`search_spender_specs` ก่อนเสมอ** แม้จะเป็นอาการเสียเฉพาะเจาะจงก็ตาม (ต่างจากกรณีลูกค้าพิมพ์อาการเองที่ยังคงข้าม FAQ ไป HANDOFF ทันทีเหมือนเดิม) — ถ้าเจอคำตอบตรงอาการให้ตอบได้เลย ถ้าไม่เจอค่อยเก็บรุ่น/อาการที่ขาดแล้วสรุป HANDOFF ตามปกติ

**หมายเหตุ tradeoff ที่ยอมรับแล้ว:** อาการเสียเดียวกันจะได้รับการดูแลต่างกันตามช่องทางป้อนข้อมูล — พิมพ์เองข้าม FAQ ไปหาผู้เชี่ยวชาญทันที ส่งรูปมาจะลองหาคำตอบสำเร็จรูปจาก FAQ ก่อน มีความเสี่ยงเล็กน้อยที่คำแนะนำทั่วไปใน FAQ อาจไม่ตรงปัญหาจริงเป๊ะเท่าผู้เชี่ยวชาญประเมิน แต่แลกกับตอบได้เร็วขึ้นโดยไม่ต้องรอแอดมินเสมอไป

---

## เรื่องที่ 27 — เพิ่มเวลา Gemini timeout จาก 10s เป็น 20s (LINE + Facebook)

**ปัญหาที่เจอ:** ลูกค้าถาม "ราคา TC4M ตอนนี้เท่าไรครับ" แล้วบอทตอบ fallback "ระบบกำลังประมวลผลนานกว่าปกติ" แทนราคาจริง — เช็คแล้วพบว่า Gemini timeout ที่ 10 วินาที ([line-webhook.ts](app/api/line-webhook/route.ts), [fb-webhook.ts](app/api/fb-webhook/route.ts)) น่าจะไม่พอ เพราะ system prompt ยาว/ซับซ้อนขึ้นเรื่อยๆ จากข้อยกเว้นที่เพิ่มสะสมมาตลอด (IMEI one-shot, "แนะนำ" context, repair_protocol 2 รอบ, FAQ-first จากรูปภาพ ฯลฯ) ทำให้ Gemini ใช้เวลา "คิด" นานขึ้น โดยเฉพาะคำถามที่กระตุ้นให้เรียก `search_spender_specs` (2 รอบการเรียก Gemini + ค้นเว็บคั่นกลาง) ยิ่งเสี่ยงเกิน 10 วินาทีง่ายขึ้น

**วิธีแก้ (2026-08-01, ทั้ง LINE + Facebook):** เพิ่ม timeout ของการเรียก `generateReply()` หลัก (คำถามข้อความทั่วไป) จาก **10000ms เป็น 20000ms** ในทั้ง 2 ไฟล์ — ไม่แตะ prompt/logic ใดๆ เลย แค่ปรับตัวเลข timeout จุดเดียวต่อไฟล์

**ทำไมปลอดภัย:** `maxDuration = 60` วินาทีต่อ request ยังเหลือ budget เพียงพอมาก (20s Gemini + ~4-5s ส่งข้อความตอบกลับ ยังห่างไกล 60s) · ไม่กระทบ timeout อื่นที่แยกกันอยู่แล้ว เช่น Facebook postback (7000ms), image analysis (15000-20000ms) ซึ่งเป็นคนละจุดคนละปัญหา

---

## เรื่องที่ 28 — Gemini พ่น reasoning ดิบออกมาเป็นคำตอบลูกค้า หลุด sentinel filter (LINE + Facebook + Web Chat)

**ปัญหาที่เจอ:** ลูกค้าพิมพ์ "1." (ไม่มีความหมาย) บนเว็บแชท บอทกลับส่งข้อความ **"คิดอย่างละเอียด: 1. ประเมินความเกี่ยวข้อง...2. ดำเนินการตามกฎ...3. สร้างคำตอบ...ดังนั้น คำตอบที่ถูกต้องคือ \"[OUT_OF_DOMAIN]\"\"[OUT_OF_DOMAIN]\""** ตรงๆ ให้ลูกค้าเห็น — Gemini ไม่ทำตามคำสั่ง "ไม่ต้องเขียนออก" ใน `<reasoning_protocol>` ([lib/prompts.ts:220-224](lib/prompts.ts#L220-L224)) เขียนขั้นตอนการคิดภายในทั้งหมดออกมาเป็นคำตอบจริง จบด้วย sentinel ที่พังซ้ำกัน

**ทำไมโค้ดไม่ดักไว้:** ทั้ง 3 ไฟล์ (LINE/Facebook/Web Chat) เดิมเช็คแบบ `reply === OUT_OF_DOMAIN || reply.startsWith(OUT_OF_DOMAIN)` — เช็คว่าคำตอบ**ขึ้นต้น**ด้วย sentinel เท่านั้น พอ sentinel ไปโผล่ท้ายข้อความยาวๆ แทน (ไม่ได้ขึ้นต้น) เช็คเลยไม่ผ่าน ข้อความ reasoning ดิบทั้งหมดเลยหลุดออกไปแสดงให้ลูกค้าเห็นตรงๆ

**วิธีแก้ (2026-08-01, ทั้ง LINE + Facebook + Web Chat):** เปลี่ยนเป็น `reply.includes(OUT_OF_DOMAIN)` (เช็คว่ามี sentinel อยู่ที่ไหนในคำตอบก็ได้ ไม่ใช่แค่ขึ้นต้น) — ถ้าเจอที่ไหนก็ตามให้ถือเป็น OUT_OF_DOMAIN ทันที ไม่สนใจข้อความที่หลุดมาก่อนหน้า

**ทำไมไม่กระทบอย่างอื่น (ตามที่ตั้งใจ — ทำแค่ระดับนี้ระดับเดียว ไม่แตะ prompt):** เป็นแค่การเปลี่ยนวิธีเช็ค string ไม่แตะ `<reasoning_protocol>` หรือ prompt ใดๆ เลย (ตั้งใจไม่แก้ระดับ prompt เพราะเสี่ยงเพิ่มความยาว prompt ซ้ำปัญหา timeout ที่เพิ่งแก้ไปในเรื่องที่ 27) — คำตอบปกติที่ไม่มี sentinel นี้ปนอยู่เลยไม่ได้รับผลกระทบใดๆ โอกาสที่คำตอบสินค้าจริงจะมีข้อความ `[OUT_OF_DOMAIN]` ปนอยู่โดยบังเอิญแทบเป็นศูนย์

**ยังไม่ได้แก้ (รอพิจารณาอนาคตถ้าจำเป็น):** sentinel ตัวอื่น (`NOT_FOUND`, `GEMINI_UNAVAILABLE`, `HANDOFF`) ใช้วิธีเช็คแบบ `startsWith` เหมือนกัน อาจมีความเสี่ยงแบบเดียวกันได้ในทางทฤษฎี แต่ยังไม่เจอเคสจริง จึงยังไม่แก้ตามขอบเขตที่ตกลงไว้

---

## เรื่องที่ 29 — รูป+ข้อความมาพร้อมกัน ให้สรุป+ถามยืนยันก่อนเสมอ แทนตอบทันที (LINE + Facebook)

**ปัญหาที่เจอ:** เดิมเมื่อลูกค้าส่งรูป+ข้อความมาพร้อมกัน (LINE: พิมพ์ตามภายใน 5 นาที / Facebook: มี caption ประกอบรูป) จะเรียก `generateReplyWithImage()` ตอบทันที ซึ่ง**ค้นเว็บ (Serper) แบบไม่มีเงื่อนไขทุกครั้ง** ก่อนที่ Gemini จะได้เห็น FAQ ด้วยซ้ำ (ต่างจาก flow ข้อความล้วนที่ Gemini เลือกเองผ่าน tool-calling ว่าจะค้นเว็บไหม เจอคำตอบใน FAQ ก็ไม่ต้องค้น) — ถ้าข้อความที่มากับรูปไม่เกี่ยวกับสเปกสินค้าเลย (เช่น "ขอเข้าห้อง DOPA BBT ด้วยครับ" มาพร้อมรูปฉลาก IMEI) การค้นเว็บนี้จะหาไม่เจออะไรเลย เสียเวลาไปเปล่าๆ แถมยังไม่แน่ใจว่า Gemini จะเชื่อมโยงข้อมูล (เช่น IMEI จากรูป) เข้ากับ `imei_protocol` ได้ถูกต้องหรือไม่ เพราะเงื่อนไข protocol เดิมอ้างอิงจาก "ข้อความลูกค้า" ไม่ใช่ข้อมูลที่มาจาก OCR ของรูป

**วิธีแก้ (2026-08-01, เฉพาะ LINE + Facebook):** เพิ่มฟังก์ชันใหม่ **`analyzeImageWithText()`** ใน [lib/gemini.ts](lib/gemini.ts) — อ่านรูป+ข้อความที่ลูกค้าส่งมาพร้อมกันในการเรียก Gemini ครั้งเดียว แล้ว:
- ถ้าพอเข้าใจได้ว่าลูกค้าต้องการอะไร (ไม่ว่าจะถามราคา/สเปก, แจ้งปัญหา, ขอเข้า/ลบกลุ่ม ฯลฯ) → **สรุปสั้นๆ + แต่งคำถามยืนยันกลับไปก่อนเสมอ** (ห้าม Gemini ตอบคำถามหรือแนะนำวิธีแก้ใดๆ เอง) เช่น `"สินค้าเป็น SPENDER TC-11HW ใช่ไหมคะ ต้องการทราบราคาใช่ไหมคะ 😊"` หรือ `"เห็น IMEI: 868078078912323 ใช่ไหมคะ ต้องการเพิ่มเข้ากลุ่ม DOPA BBT ใช่ไหมคะ 😊"`
- ถ้าทั้งรูปและข้อความไม่ให้ข้อมูลอะไรเลยจริงๆ → `unclear` (fallback เป็น `UNAVAILABLE_MSG`)
- บันทึกคำถามยืนยันนี้เข้า `saveHistory()` เป็น model turn ด้วย (เหมือน `analyzeImageIntent()` เดิม) เพื่อให้**ข้อความตอบกลับของลูกค้ารอบถัดไปเดินเข้า `generateReply()` flow ข้อความปกติ** (ค้นชีตก่อน ไม่เจอค่อยค้นเว็บ) ต่อยอด `repair_protocol`/`imei_protocol`/FAQ/HANDOFF ที่มีอยู่แล้วได้เอง

**Trade-off ที่ยอมรับแล้ว:** ทุกเคสรูป+ข้อความ (รวมถึงเคสถามราคาชัดเจนที่เคยตอบได้ในรอบเดียว) จะมีขั้นถามยืนยันเพิ่ม 1 รอบเสมอ ไม่มีการจำแนก "ชัดเจนพอข้ามยืนยันได้ไหม" — เลือกความเรียบง่าย/แน่นอนของระบบ (ไม่ต้องสอน AI แยกแยะ "ชัดเจน vs กำกวม" ซึ่งเสี่ยงตัดสินใจผิดได้) แลกกับความเร็วในเคสที่พบบ่อยที่สุด ถือว่าคุ้มเพราะขั้นยืนยันยังช่วยกันบอทตอบผิดจากการอ่านรูปพลาดด้วย

**ขอบเขตที่ตั้งใจไว้ (ไม่แตะ):**
- ปุ่ม "สอบถามสเปก" (LINE) / "สเปค/ฟังก์ชัน" (Facebook) — ยังเรียก `generateReplyWithImage()` ตรงๆ เหมือนเดิมทุกประการ เพราะเจตนาชัดเจนจากปุ่มอยู่แล้ว ไม่มีความกำกวมให้ต้องยืนยัน
- รูปเดี่ยวๆ ไม่มีข้อความ (เรื่องที่ 26) — ยังใช้ `analyzeImageIntent()` เดิมทุกประการ ไม่ถูกแตะเลย
- `generateReply()`/`generateReplyWithImage()` ตัวฟังก์ชันเอง — ไม่ถูกแก้ไขเลยแม้แต่บรรทัดเดียว เป็นแค่การเปลี่ยนว่า "จะเรียกฟังก์ชันไหนตอนไหน" ในโค้ด webhook เท่านั้น
- Web Chat — ไม่มีการรับรูปภาพอยู่แล้ว จึงไม่เกี่ยวข้อง

---

## เรื่องที่ 30 — บอทเงียบสนิทไม่ตอบลูกค้าเลย เพราะ reply/push ล้มเหลวซ้อนกัน (LINE + Facebook)

**ปัญหาที่เจอ:** ลูกค้าหลายคนพิมพ์มาแต่ไม่ได้รับคำตอบจากบอทเลยแม้แต่ตัวเดียว (เช็คจากหน้า `/admin/unanswered` แท็บ "ประวัติแชท" เห็นแค่ข้อความลูกค้า ไม่มีข้อความบอทตอบกลับ) ตรวจ Vercel Logs เจอ pattern ซ้ำ:
```
webhook.message_received
gemini.reply (ตอบสำเร็จ มีคำตอบแล้ว)
reply.send_failed         err: "400 - Bad Request"
reply.fallback_push_failed  err: "429 - Too Many Requests"
(กรณีลูกค้าใหม่ยังมี push.send_failed  err: "429 - Too Many Requests" ต่ออีก)
reply.sent   ← log ว่า "สำเร็จ" ทั้งที่ส่งไม่ได้เลยสักครั้ง
```
Gemini ตอบคำถามได้ถูกต้องเสมอ ปัญหาไม่ได้อยู่ที่ AI แต่อยู่ที่ "ส่งข้อความออกไปหาลูกค้าไม่สำเร็จ" — `replyMessage()` ล้มเหลว (400 น่าจะเพราะ replyToken ถูกใช้ไปแล้ว/หมดอายุ) แล้ว fallback `pushMessage()` ก็โดน rate limit (429) ตามมาอีก ล้มเหลวครบทุกช่องทาง แต่โค้ดเดิมดักจับ error ไว้ข้างในหมด (`safeReply`/`safePush`) ไม่ throw ออกมา เลย log `reply.sent` เหมือนสำเร็จทั้งที่จริงไม่ได้ส่งอะไรถึงลูกค้าเลย

**ทำไมเกิดถี่:** ต้นตอน่าจะมาจาก LINE/Facebook มีพฤติกรรม **ส่ง webhook event ซ้ำ (retry) ถ้าเซิร์ฟเวอร์ตอบกลับช้า** — ยิ่ง branch ไหนใช้เวลาประมวลผลนาน (เรียก Gemini ค้นเว็บ, วิเคราะห์รูปภาพ, หรือแม้แต่ข้อความทั่วไปที่เพิ่ง timeout เพิ่มเป็น 20s ในเรื่องที่ 27) ยิ่งเสี่ยงโดน retry เป็น event ซ้ำ 2 ชุดพร้อมกัน แย่ง replyToken/push กันเองจนล้มเหลวทั้งคู่ — ไม่ได้เกี่ยวกับฟีเจอร์ใดฟีเจอร์หนึ่งโดยเฉพาะ เป็นความเสี่ยงเชิงโครงสร้างของ webhook ทั้งระบบ ลูกค้าใหม่ (ที่ต้องส่งทั้งข้อความทักทาย + คำตอบแยกกัน 2 ก้อน) เสี่ยงโดนหนักกว่าลูกค้าเก่า

**วิธีแก้ (2026-08-04, ทั้ง LINE + Facebook — ไม่แตะ Web Chat เพราะไม่ใช่ webhook แบบมี retry จากภายนอก):**

1. **Dedupe by message ID** — เพิ่ม guard ที่จุดเริ่มต้นของการประมวลผลแต่ละ event ทั้ง [line-webhook.ts](app/api/line-webhook/route.ts) (ใช้ `event.message.id`) และ [fb-webhook.ts](app/api/fb-webhook/route.ts) (ใช้ `event.message.mid`) — เช็ค Redis (`set ... nx:true, ex:120`) ถ้าเคยเห็น message ID นี้แล้วภายใน 120 วิ ให้ `return` ทันที ไม่ประมวลผลซ้ำ กันปัญหาแย่ง replyToken/push กันเองตั้งแต่ต้นทาง
2. **แจ้งแอดมินเมื่อส่งไม่สำเร็จทุกช่องทาง** — เพิ่มเรียก `notifyAdmin()`/`notifyAdminFacebook()` ใน catch block ของ `safeReply()`, `safePush()` (LINE) และ `fbSend()` (Facebook) ตอน fallback ล้มเหลวครบทุกทาง — ยิงแจ้งกลุ่มแอดมินว่าส่งข้อความหาลูกค้าคนนี้ไม่สำเร็จ ให้ติดต่อเองแทน ไม่ปล่อยให้เงียบหายไปแบบไม่มีใครรู้อีกต่อไป (เป็น fire-and-forget เหมือน `logChat` ไม่กระทบความเร็ว response)

**ทำไมไม่กระทบอย่างอื่น:** ข้อ 1 เป็น guard ใหม่ที่ทำงานเฉพาะตอนเจอ message ID ซ้ำเป๊ะเท่านั้น (ครอบ try/catch แบบ fail-open — ถ้า Redis ล่มจะประมวลผลต่อตามปกติ ไม่เสี่ยงบอทเงียบเพิ่มจากตัว guard เอง) ข้อความปกติที่ไม่ซ้ำไม่ได้รับผลกระทบใดๆ · ข้อ 2 เป็นแค่เพิ่ม action ต่อท้ายใน catch block ที่เดิมมีแค่ log ไม่เปลี่ยน flow ที่ลูกค้าเห็นเลย (ลูกค้าก็ยังไม่ได้รับข้อความเหมือนเดิมเพราะส่งจริงไม่สำเร็จ) แค่เพิ่มการมองเห็นฝั่งแอดมิน · ไม่แตะ Web Chat เพราะเป็น API call ปกติที่ browser เรียกแล้วรอผลตอบกลับโดยตรง ไม่มีกลไก retry แบบ webhook จากภายนอกมาซ้อนทับ

**รอบ 2 (2026-08-04 — วันเดียวกัน):** เจอเคสใหม่ที่ยัง 400+429 อยู่ แต่ตรวจ Vercel Request Log แล้วพบว่า **มี `POST /api/line-webhook` แค่ครั้งเดียว ไม่มี event ซ้ำเลย** (บาง latency แค่ 3.4 วิ ผ่าน `findExactMatch()` ที่ไม่เรียก Gemini ด้วยซ้ำ) — พิสูจน์ว่า "LINE ส่ง event ซ้ำ" ไม่ใช่สาเหตุเดียว มีอีกกลไกหนึ่งคือ **ยิงเรียก LINE/Facebook API พร้อมกันหลายคำขอในจังหวะเดียว** (เช่น ลูกค้าพิมพ์ 2 ข้อความติดกันเร็วๆ ถูก LINE รวมมาใน events array เดียวกัน ประมวลผลพร้อมกันด้วย `Promise.all`) ชน rate limit ระยะสั้นของ LINE/Facebook (429) ได้แม้จะคนละข้อความ/คนละลูกค้ากันเลยก็ตาม เพราะ quota ผูกกับ channel access token เดียวของร้าน ใช้ร่วมกันทุกคน

**วิธีแก้เพิ่ม (retry-with-backoff เมื่อเจอ 429 เท่านั้น):**
- **LINE** ([line-webhook.ts](app/api/line-webhook/route.ts)) — เพิ่มฟังก์ชัน `pushWithRetry()` ใช้ร่วมกันทั้งใน fallback push ของ `safeReply()` และใน `safePush()` — ถ้า push ครั้งแรกเจอ error ที่มีคำว่า "429" ให้รอ 800ms แล้วลองใหม่**อีกครั้งเดียว** (ไม่ retry ซ้ำเรื่อยๆ) error อื่นที่ไม่ใช่ 429 (เช่น 400) โยนออกทันทีไม่ retry เพราะรอแล้วก็ไม่ช่วย
- **Facebook** ([fb-webhook.ts](app/api/fb-webhook/route.ts)) — พบช่องโหว่เพิ่ม: `fbSend()` เดิมใช้ `fetch().catch(...)` ตรงๆ แต่ `fetch()` **ไม่ throw error ตอนเจอ HTTP status ผิดพลาด** (throw เฉพาะ network ล่มจริงๆ) ทำให้ตอนนั้น Facebook ตอบ 429 กลับมา โค้ดจะคิดว่าส่งสำเร็จ ไม่มีการ log/แจ้งเตือนเลย — แก้โดยแยก `fbSendRequest()` ออกมา แล้วให้ `fbSend()` เช็ค `res.status`/`res.ok` เองตรงๆ ถ้าเจอ 429 รอ 800ms แล้วลองใหม่อีกครั้งเดียวเหมือน LINE ถ้ายังไม่ `ok` ถึง throw ให้ catch block ทำงาน (log error + `notifyAdminFacebook()` + **ไม่ logChat ว่าส่งสำเร็จอีกต่อไปถ้าจริงๆ ส่งไม่สำเร็จ**)

**ขอบเขตที่ตั้งใจไว้ (ไม่แตะ):** แก้เฉพาะ `fbSend()` (path ข้อความ text ปกติ ใช้บ่อยที่สุด) — `fbSendProductCard()`/`fbSendQuickReplies()` (การ์ดสินค้า/ปุ่มตัวเลือก) ยังไม่ได้ใส่ retry เดียวกัน เป็น branch ที่ใช้น้อยกว่า ทิ้งไว้พิจารณาเพิ่มทีหลังถ้าเจอปัญหาจริง · ไม่แตะ Web Chat เหมือนรอบแรก

**ทำไมปลอดภัย:** retry สูงสุด **1 ครั้งต่อการส่ง 1 ครั้งเท่านั้น** ไม่มีทาง loop ไม่รู้จบ · delay 800ms รวมกับ timeout เดิม (4-5s) ยังห่างไกล `maxDuration=60s` มาก · เช็คเฉพาะ error message ที่มีคำว่า "429" เท่านั้นถึง retry (error อื่นไม่เสียเวลารอเปล่าๆ) · ไม่แตะ logic อื่นนอกเหนือจากจุดส่งข้อความเลย

**รอบ 3 (2026-08-04 — วันเดียวกัน):** เจอเคสจริงที่ retry แค่ 1 ครั้ง (รอบ 2) ยังไม่พอ — เคส 21:04 `analyzeImageWithText()` ใช้เวลา **14.5 วิ** (ใกล้ timeout 15 วิเต็มที่) พอจะ `replyMessage()` replyToken ผ่านมา ~13 วิแล้ว หมดอายุ (400) แล้ว fallback push **โดน 429 ซ้ำแม้ retry ไปแล้ว 1 ครั้งตามรอบ 2** แปลว่า rate limit รอบนั้นรุนแรง/นานกว่า 800ms ที่ตั้งไว้

**วิธีแก้เพิ่ม 2 อย่าง:**
1. **เพิ่มจำนวน retry จาก 1 เป็น 2 ครั้ง พร้อม backoff เพิ่มขึ้นเรื่อยๆ** (800ms → 1500ms, รวมสูงสุด 3 ครั้งต่อการส่ง 1 ครั้ง) — ทั้ง `pushWithRetry()` (LINE) และ `fbSend()` (Facebook)
2. **ข้าม `replyMessage()` ไปเลยสำหรับ branch ที่ประมวลผลช้า (วิเคราะห์รูป+ข้อความ)** — เพิ่มฟังก์ชัน `pushOnly()` ใน [line-webhook.ts](app/api/line-webhook/route.ts) ใช้แทน `ans()` เฉพาะใน branch `analyzeImageWithText()` (LINE เท่านั้น เพราะ Facebook ไม่มี replyToken อยู่แล้ว ใช้ push-equivalent เสมอ) — เหตุผล: ถ้าประมวลผลไปแล้ว 7-15 วิ replyToken มีโอกาสหมดอายุสูงมาก การลอง `replyMessage()` ก่อนเสียเวลาไปเปล่าๆ ก่อนจะ fallback ไป push อยู่ดี ตัดขั้นตอนที่รู้อยู่แล้วว่าจะพังทิ้งไปเลย ประหยัดเวลา + ลดจำนวนคำขอ API ที่ยิงไปเปล่าๆ

**ข้อควรรู้ (บอกลูกค้าตรงๆ ก่อนแก้):** การแก้นี้**ไม่การันตี 100%**ว่าจะไม่เกิดซ้ำอีก — ถ้า rate limit รุนแรง/นานกว่า retry ทั้งหมดรวมกัน (800+1500=2300ms) ก็ยังมีโอกาสล้มเหลวได้อยู่ดี แต่ลดโอกาสได้มากกว่ารอบ 2 มาก และยังมี `notifyAdmin()`/`notifyAdminFacebook()` เป็นตาข่ายรองรับสุดท้ายเสมอ

**ขอบเขตที่ตั้งใจไว้ (ไม่แตะ):** `pushOnly()` ใช้เฉพาะ branch `analyzeImageWithText()` เท่านั้น — branch `analyzeImageIntent()` (รูปเดี่ยวไม่มีข้อความ) ยังใช้ `ans()` เดิม (ไม่มีหลักฐานว่าช้าเท่ากัน ยังไม่ปรับ รอพิจารณาถ้าเจอปัญหาจริง) · ไม่แตะ Web Chat เหมือน 2 รอบก่อน

**รอบ 4 (2026-08-05) — พบต้นตอที่แท้จริง: ข้อความ 2 อันจากลูกค้าคนเดียวกัน "ชนกันเอง"**
เจอเคสที่หักล้างทฤษฎี "ประมวลผลช้า" ทั้งหมด — ข้อความที่ 2 ("ขอเอกสารคู่มือ...") Gemini ตอบเร็วมาก (1.27 วิ ไม่ค้นเว็บ) แต่ก็ยัง 400+429(retry exhausted) เหมือนข้อความแรก ("มีที่ชาร์จ...") ที่ช้า (13.5 วิ) — เช็คเวลาแล้วพบว่า **ทั้ง 2 ข้อความ (ลูกค้าคนเดียวกัน ห่างกันแค่ 7 วิ) ประมวลผล "ซ้อนทับเวลากัน" เกือบทั้งหมด** เพราะ LINE ส่งเป็น event แยกกันแต่โค้ดประมวลผลพร้อมกันด้วย `Promise.all` — ทั้งคู่พยายามยิง LINE Send API ในช่วงเวลาเดียวกัน ชนโควตาเดียวกัน (channel access token เดียวของร้าน) แม้แต่ตอน retry ก็ยัง retry พร้อมกันจึงชนซ้ำได้อีกในทุกรอบ — อธิบายได้ว่าทำไม retry 3 ครั้ง (รอบ 3) ยังไม่พอ เพราะสู้กับ "อีกข้อความที่ยิงชนกันตลอดเวลา" ไม่ใช่แค่ rate limit ทั่วไป

**วิธีแก้ (ไฟล์ใหม่ [lib/sendLock.ts](lib/sendLock.ts)):** เพิ่ม `withUserSendLock(userId, fn)` — distributed lock ผ่าน Redis (`SET key NX PX`, key = `lock:send:{userId}`, TTL 15 วิ) บังคับให้ข้อความจากลูกค้าคนเดียวกันส่งเรียงลำดับทีละข้อความเท่านั้น (ไม่พร้อมกัน):
- ถ้า lock ถูกถืออยู่ → poll ทุก 300ms รอสูงสุด 12 วิ
- เกิน 12 วิยัง lock ไม่ว่าง → **bypass ไปทำงานเลย** กัน deadlock ค้างตลอดไป (log `sendlock.bypassed`)
- ทำงานเสร็จ → ปล่อย lock ทันที (ไม่รอ TTL หมดเอง) log `sendlock.released`
- Redis ล่ม → bypass ทันที (fail-open เหมือน guard อื่นในระบบ)

**จุดที่ห่อ lock:** ห่อทั้งฟังก์ชันส่งระดับบนสุด (ไม่ใช่ raw API call แยกจุด กัน lock ถูกปล่อย-จับใหม่ระหว่าง reply กับ fallback push ของข้อความเดียวกัน):
- LINE ([line-webhook.ts](app/api/line-webhook/route.ts)): `safeReply()`, `safePush()` — ครอบคลุมทุกจุดที่เรียก `replyMessage()`/`pushMessage()` (รวม `pushWithRetry()`/`pushOnly()` เพราะเรียกผ่าน 2 ฟังก์ชันนี้อยู่แล้ว ไม่ห่อซ้ำกันเองเพราะจะ deadlock ตัวเอง — Redis SET NX ไม่ reentrant)
- Facebook ([fb-webhook.ts](app/api/fb-webhook/route.ts)): `fbSend()`, `fbSendProductCard()`, `fbSendQuickReplies()` — lock key ใช้ `fb:{psid}` แยก namespace จาก LINE (`userId` ตรงๆ) กันชนกันโดยบังเอิญ

**ทดสอบก่อน deploy (local, ไม่แตะ production):** เขียนสคริปต์เดี่ยวจำลอง 2 ข้อความจาก userId เดียวกันห่างกัน 3 วิ (mock Redis เพราะ `.env.local` ไม่มี UPSTASH credentials จริงให้ทดสอบ — logic การ acquire/poll/bypass/release เหมือน `lib/sendLock.ts` เป๊ะ):
- เทสต์ 1 (serialize ปกติ): ข้อความ 1 ถือ lock 5 วิ → ข้อความ 2 รอจริง (waited ~2.1 วิ) ก่อนเริ่มทำงาน ✅
- เทสต์ 2 (bypass กัน deadlock): ข้อความ 1 ถือ lock นาน 13 วิ (เกิน MAX_WAIT_MS) → ข้อความ 2 bypass ที่ ~12 วิพอดี ไม่ค้างรอตลอดไป ✅

**ทำไมไม่กระทบอย่างอื่น:** lock ครอบคลุมแค่ "ช่วงพยายามส่งข้อความ" เท่านั้น ไม่ครอบ Gemini/Serper/ค้นชีต (ยังทำงานคู่ขนานได้ปกติ ไม่ช้าลง) · ถ้าเป็นลูกค้าคนละคน ไม่มีทางชน lock กันเลย (key แยกตาม userId) · Redis ล่ม fail-open ไม่เสี่ยงบอทเงียบเพิ่มจากตัว lock เอง · ไม่แตะ Web Chat

Commit+push แล้ว (`bb2c6fc`)

**รอบ 5 (2026-08-05) — พบสาเหตุรากที่แท้จริง: โควตาข้อความ LINE OA หมด ไม่ใช่ปัญหาโค้ดเลย**
หลัง deploy รอบ 4 แล้วยังเจอ 400+429 ต่อเนื่องอีกหลายเคส (บางเคสไม่มีการชนกันเลย แม้แต่ข้อความเดี่ยวๆ ที่ Gemini ตอบเร็วมาก 2.4 วิ ก็ยัง push พังหลัง retry ครบ 3 ครั้ง) — เช็คหน้า **LINE Official Account Manager → Dashboard** พบว่า **"Free messages sent: 300/300"** และ **"Additional messages sent: Unavailable in current plan"** ยืนยันว่า**โควตา push message ฟรีรายเดือนหมดเกลี้ยงแล้วจริง** แพลน Free ไม่มีระบบซื้อเพิ่มแบบ pay-per-use ต้องรออัปเกรดแพลนหรือรอรอบบิลใหม่เท่านั้น — อธิบายได้ครบทุกเคสที่เจอมาทั้งวัน (พังทุก branch เหมือนกันหมด ไม่ว่าช้า/เร็ว/ชนกัน/ไม่ชนกัน เพราะเป็นเพดานเดียวกันทั้งบัญชี ไม่ใช่ rate limit ชั่วคราว)

**สำคัญ:** โค้ดที่แก้ไปทั้ง 4 รอบ (dedupe, retry-backoff, sendLock, pushOnly) **ไม่ได้ผิดพลาดหรือเสียเปล่า** — ยังจำเป็นและทำงานถูกต้องตามที่ออกแบบไว้ทันทีที่โควตากลับมาใช้ได้ เพราะแก้ปัญหาคนละชั้นกัน (event ซ้ำ/ชนกันเอง/rate limit ชั่วคราว) ที่เป็นปัญหาจริงควบคู่กันไปด้วย เพียงแต่ทั้งหมดนี้ **ไม่มีทางแก้ปัญหา "โควตาหมด" ได้เลย** เพราะเป็นเพดานที่ LINE บล็อกไว้ระดับบัญชี ไม่ใช่ปัญหาทางเทคนิค — การตัดสินใจอัปเกรดแพลนเป็นเรื่องธุรกิจที่ต้องให้เจ้าของร้านตัดสินใจเอง ไม่ใช่สิ่งที่แก้ด้วยโค้ดได้

---

## เรื่องที่ 31 — ออกแบบใหม่ repair_protocol: ไม่ถามรุ่นแยก ใช้ยืนยันอาการ + ค้นชีต/เว็บก่อน HANDOFF (เฉพาะ LINE/Facebook)

**ปัญหาที่เจอ:** บั๊กเดิมจากเรื่องที่ 25 (ลูกค้าตอบแค่ชื่อรุ่นเปล่าๆ ระหว่าง repair_protocol แล้วบอทหลุดไปตอบราคาสินค้าแทนที่จะสรุป HANDOFF) **กลับมาเกิดซ้ำอีก** แม้จะแก้ prompt ไป 2 รอบแล้ว (`b0e948e`, `7f1bf9a`) — เช็ค prompt แล้วเงื่อนไขครอบคลุมเคสนี้ถูกต้องทุกจุด แต่ Gemini ไม่ทำตามคำสั่งในบางครั้ง (เป็นข้อจำกัดโดยธรรมชาติของการสั่งงาน AI ผ่าน prompt ไม่ใช่ deterministic code — temperature=1.0 ของ `generateReply()` ทำให้มีโอกาสหลุดได้แม้เงื่อนไขจะตรงเป๊ะ)

**วิธีแก้ (ออกแบบใหม่ทั้ง flow แทนการแพตช์เพิ่ม):** แทนที่จะถาม "รุ่นวิทยุที่มีปัญหา" แยกเป็นคำถามต่างหาก (ซึ่งเป็นต้นตอของความกำกวมเมื่อลูกค้าตอบกลับมาแค่ชื่อรุ่น) เปลี่ยนเป็น:
1. ใช้อาการที่ลูกค้าอธิบายมาแล้วเป็นหลัก ไม่ถามรุ่นแยก — ถ้าไม่แน่ใจว่าเข้าใจอาการถูกไหม ให้สรุปอาการที่เข้าใจแล้ว**ถามยืนยันกลับ**แทน เช่น "เข้าใจว่า [สรุปอาการ] ใช่ไหมคะ"
2. เมื่อลูกค้ายืนยัน ("ใช่") ให้ค้นหาคำตอบตามลำดับ: **ค้นชีต (`<faq>`) ก่อนเสมอ → ไม่เจอค่อยค้นเว็บ (`search_spender_specs`) → ไม่เจอทั้งคู่ค่อยสรุปเป็น `HANDOFF: ...`** (รูปแบบเดิม ใส่ "รุ่น: ไม่ระบุ (รอช่างตรวจสอบเพิ่มเติม)" ถ้าไม่มีรุ่นในบทสนทนา)
3. คำตอบที่เจอต้อง**ตรงกับอาการที่แจ้งโดยตรงเท่านั้น** ห้ามเดา/ประยุกต์ใช้คำตอบใกล้เคียง ถ้าไม่มั่นใจให้ถือว่าไม่เจอแล้วไป HANDOFF เสมอ
4. เก็บข้อยกเว้น "ลูกค้าตอบชื่อรุ่นเปล่าๆ" ไว้เป็นตาข่ายรองรับ (ปรับ context ใหม่: ถ้าบอทเพิ่งถามยืนยันอาการไป แล้วลูกค้าตอบชื่อรุ่นเปล่าๆ ให้ถือเป็นการระบุรุ่นเพิ่มให้อาการที่คุยค้างอยู่ ไม่ใช่ถามราคาใหม่)

**ทำไมแก้บั๊กเดิมได้จริง (ไม่ใช่แค่ความน่าจะเป็น):** ต้นตอบั๊กคือ "บอทถามรุ่นแยกเป็นคำถามเปิด" ทำให้ลูกค้าตอบกลับมาแบบกำกวม (คำตอบเดิม vs คำถามใหม่) — พอไม่มีจังหวะที่ต้องถามรุ่นแยกอีกต่อไป (ใช้คำถามยืนยัน ใช่/ไม่ใช่ แทน) สถานการณ์กำกวมที่ทำให้เกิดบั๊กจะไม่เกิดขึ้นตั้งแต่ต้น ไม่ใช่แค่ "สั่ง Gemini ให้ระวังมากขึ้น" แบบ 2 รอบก่อน

**Trade-off ที่ยอมรับแล้ว:** เดิม `repair_protocol` ตั้งใจข้าม FAQ/เว็บไปเลยสำหรับอาการเสียเฉพาะเจาะจง (หลักการ "ให้ผู้เชี่ยวชาญประเมินเพื่อความแม่นยำ") — ตอนนี้เปลี่ยนเป็นค้นชีต/เว็บก่อนเสมอเหมือนคำถามทั่วไป มีความเสี่ยงที่คำตอบใกล้เคียงแต่ไม่ตรงเป๊ะจะถูกใช้ตอบผิดจุดได้ (ลดความเสี่ยงด้วยกฎ "ต้องตรงโดยตรงเท่านั้น ไม่มั่นใจให้ไป HANDOFF" แต่ไม่ได้หายไป 100%) — **แลกกับการมี fallback HANDOFF เป็นตาข่ายสุดท้ายเสมอถ้าหาคำตอบไม่เจอจริงๆ** เหมือนเดิมทุกประการ ไม่ได้ตัดออก

**ผลพลอยได้:** ข้อยกเว้น "อาการที่มาจากรูปภาพ" (เดิมต้องมีแยกต่างหากเพราะรูปภาพข้าม flow มาตรฐาน) **ไม่จำเป็นอีกต่อไป ถูกลบออก** เพราะ flow ใหม่ค้นชีต/เว็บก่อนเป็นค่าเริ่มต้นอยู่แล้วสำหรับทุกกรณี ไม่ต้องแยกเขียนซ้ำ

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) จุดเดียว (`repairStep2` เฉพาะ branch `channel !== 'web'`) — เพราะ LINE และ Facebook ใช้ prompt ก้อนเดียวกัน (แยกแค่ `channel === 'web'` เท่านั้น) จึงมีผลกับทั้ง LINE และ Facebook พร้อมกันโดยไม่ต้องแก้ webhook ไฟล์ไหนเลย · **branch `channel === 'web'` ไม่ถูกแตะเลยแม้แต่บรรทัดเดียว** (Web Chat ยังข้าม FAQ/เว็บไปที่ HANDOFF ทันทีเหมือนเดิมทุกประการ) · ขนาดไฟล์เพิ่มขึ้นเล็กน้อย (~250 bytes) ไม่กระทบ latency อย่างมีนัยสำคัญ

### เพิ่มเติม (2026-08-10) — 2 บั๊กจากเคสจริง: ถามรุ่น+อาการซ้ำ + "ยินดีให้บริการค่ะ" ตัดหน้าคำยืนยัน (LINE + Facebook)

**เคสจริงที่เจอ:** ลูกค้าแจ้งอาการ TC-80H ตัดระบบเองบ่อยๆ → บอทสรุป+ถามยืนยันถูกต้องตาม repairStep2 → ลูกค้าตอบ **"ใช่ครับผม"** → บอทตอบ **"ยินดีให้บริการค่ะ"** เฉยๆ (ไม่ไปต่อ) → ลูกค้าถามต่อ "ต้องส่งเครื่องไปตรวจเช็คซ่อมไหมครับ" → บอทถามรุ่น+อาการใหม่ทั้งหมดราวกับไม่เคยคุยกันมาก่อน ลูกค้าต้องพิมพ์อธิบายอาการซ้ำ

**สาเหตุที่ 1 (ทำไมตอบ "ยินดีให้บริการค่ะ"):** `<reasoning_protocol>` ข้อ 1 (กฎ acknowledgment-shortcut: เจอคำอย่าง "ครับ"/"ค่ะ" → ตอบ "ยินดีให้บริการค่ะ" แล้วหยุดทันที) เช็ค**ก่อน**ข้อ 4 (พาเข้า `<repair_protocol>`) เสมอ — "ใช่ครับผม" มีคำว่า "ครับ" ปนอยู่ ดันเข้าเงื่อนไขข้อ 1 พอดี ตัดจบสนทนาก่อนที่ repairStep2's "เมื่อลูกค้ายืนยัน (ตอบ 'ใช่'...) ให้หาคำตอบตามลำดับ..." จะได้ประมวลผลเลย

**สาเหตุที่ 2 (ทำไมถามรุ่น+อาการซ้ำ):** `<repair_protocol>` ขั้นที่ 1 (คำถามนโยบายทั่วไป เช่น "จะส่งเครื่องเคลม") ตอบจาก `<faq>` แบบ stateless ล้วนๆ ไม่เคยเช็ค conversation history เลยว่าลูกค้าเคยแจ้ง/ยืนยันรุ่น+อาการเฉพาะของเครื่องตัวเองไปแล้วหรือยัง — พอ Gemini จัดคำถามต่อเนื่อง ("ต้องส่งเครื่องไปตรวจเช็คซ่อมไหม") เข้าขั้นที่ 1 แทนที่จะมองว่าเป็นการต่อยอดจากขั้นที่ 2 ที่คุยค้างอยู่ จึงตอบ FAQ คำถามนโยบายทั่วไปซึ่งมีเนื้อหาขอรุ่น+อาการซ้ำ

**วิธีแก้ (ทั้งคู่จำกัดเฉพาะ branch `channel !== 'web'` ใน [lib/prompts.ts](lib/prompts.ts)):**
1. **`<reasoning_protocol>` ข้อ 1** — เพิ่มข้อยกเว้น (`step1AckException`): ถ้าเทิร์นล่าสุดของบอทเป็นคำถามที่กำลังรอคำยืนยัน (`<repair_protocol>` ขั้นที่ 2 / `<self_repair_protocol>` / `<imei_protocol>` ขั้นที่ 3) และข้อความล่าสุดของลูกค้าเป็นคำตอบยืนยัน (เช่น "ใช่" "ใช่ครับ" "ใช่ค่ะ" แม้มีคำลงท้ายสุภาพติดมาด้วย) ห้ามใช้กฎ acknowledgment-shortcut ตัดจบ ให้ส่งต่อให้ protocol ที่ค้างอยู่ประมวลผลต่อทันที — กรณีขอบคุณปิดท้ายบทสนทนาทั่วไปที่ไม่มี protocol ค้างยังตอบ "ยินดีให้บริการค่ะ" เหมือนเดิมทุกประการ
2. **`<repair_protocol>` ขั้นที่ 1** — เพิ่มการเช็ค history ก่อนตอบจาก `<faq>` (`repairStep1` แยก branch ตาม channel เหมือน `repairStep2`): ถ้าเคยแจ้ง/ยืนยันอาการเฉพาะของเครื่องตัวเองไปแล้วในบทสนทนา ห้ามตอบคำถามนโยบายทั่วไปจาก `<faq>` ซ้ำ ห้ามถามรุ่น/อาการอีก ให้ข้ามไปขั้นที่ 2 ทันที (ค้นคำตอบตามลำดับ 1-3 โดยใช้รุ่น+อาการที่มีอยู่แล้ว) — ถ้ายังไม่เคยแจ้งอาการมาก่อนเลย ยังทำงานแบบเดิมทุกประการ (ค้นหาคำตอบใน `<faq>` ก่อนตามปกติ)

**ที่ตั้งใจไม่แตะ:** `<self_repair_protocol>` core logic (immediate `HANDOFF: ขอวิธีทำเอง` ไม่มีขั้นค้นชีต/เว็บ) — เป็นการออกแบบตั้งใจจากเรื่องที่ 31 เดิม (กันประกันขาด/อันตรายจากการสอนวิธีทำเอง) ไม่ใช่บั๊ก จึงไม่ต้องเพิ่ม flow "ยืนยันแล้วค้นคำตอบ 2 ทาง" ให้ · pre-handoff trigger keywords ใน [lib/handoff.ts](lib/handoff.ts) (`shouldHandoffDeferred`/`shouldHandoffImmediate`) — ตรวจสอบแล้วไม่มีคำว่า "ส่งซ่อม"/"ตรวจเช็ค" อยู่ในลิสต์เลย บั๊กจริงอยู่ที่ `<repair_protocol>` ขั้นที่ 1 ใน prompt เท่านั้น ไม่ใช่ trigger keyword แยกต่างหาก · Web Chat ไม่ถูกแตะเลย (branch `channel === 'web'` ของทั้ง 2 จุดเหมือนเดิมทุกประการ)

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. ยืนยันอาการด้วย "ใช่ครับผม" ระหว่าง repair_protocol ขั้นที่ 2 → ผลจริง: ค้นเว็บแล้วตอบ `HANDOFF: ซ่อม/เคลม | รุ่น: TC-80H | อาการ: ...` ทันที (ไม่ใช่ "ยินดีให้บริการค่ะ" อีกต่อไป) — **ผ่าน**
2. ถามต่อ "ต้องส่งเครื่องไปตรวจเช็คซ่อมไหมครับ" หลังยืนยันอาการแล้ว → ผลจริง: ไม่ถามรุ่น/อาการซ้ำ ตอบต่อด้วย `HANDOFF: ซ่อม/เคลม | รุ่น: TC-80H | อาการ: ...` โดยใช้ข้อมูลจาก history เดิม — **ผ่าน**
3. Regression — บทสนทนาใหม่ ไม่เคยยืนยันอาการมาก่อน (message เดียวมีทั้งรุ่น+อาการ+คำถามส่งซ่อม) → ผลจริง: ค้นแล้วตอบ HANDOFF ตรงไปตรงมา ไม่มีการถามซ้ำหรือค้างคา — พฤติกรรมปกติ ไม่กระทบจาก fix
4. Regression — "ขอบคุณครับ" ปิดท้ายบทสนทนาทั่วไปที่ไม่มี protocol ค้าง → ผลจริง: ตอบ "ยินดีให้บริการค่ะ" เหมือนเดิมทุกประการ — **ผ่าน** (ยืนยันว่าไม่กระทบ acknowledgment-shortcut กรณีปกติ)
5. ยืนยันอาการอีกเคส (TM-80H ไม่มีเสียงลำโพง) ด้วย "ใช่ค่ะ" → ผลจริง: ค้นเว็บแล้วตอบ HANDOFF ทันที ไม่ใช่ "ยินดีให้บริการค่ะ" — **ผ่าน** (ยืนยันความสม่ำเสมอ)

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) 2 จุด (`step1AckException` ใน `<reasoning_protocol>`, `repairStep1` ใน `<repair_protocol>`) ทั้งคู่ channel-conditional แบบเดียวกับ `repairStep2` เดิม — ไม่แตะ webhook ไฟล์ไหนเลย มีผลกับทั้ง LINE และ Facebook พร้อมกันโดยอัตโนมัติ

### เพิ่มเติม (2026-08-10 รอบ 2) — บอทถามชื่อรุ่นซ้ำ กลายเป็น "รับทราบค่ะ" ทางตัน เมื่อลูกค้าอ้างถึงสินค้าแบบกำกวม

**เคสจริงที่เจอ:** บอทถามยืนยันชื่อรุ่นไปแล้ว ("ลูกค้าสนใจเป็นรุ่นไหนคะ") ลูกค้าตอบกำกวม **"รุ่นนี้ราคาเท่าไหร่ครับ"** โดยไม่ได้ระบุชื่อรุ่นจริง (ตั้งใจจะส่งรูปตามมาทีหลัง) — Gemini ไม่มีชื่อรุ่นให้ค้น เลยพยายามถามคำถามเดิมซ้ำ ("ลูกค้าสนใจเป็นรุ่นไหนคะ") ซึ่งข้อความตรงกับที่เพิ่งส่งไปเป๊ะ ทำให้กลไก "กันบอทตอบข้อความเดิมซ้ำติดกัน" (`last_bot_reply` เทียบ exact string match ที่มีอยู่แล้วใน [line-webhook.ts:704-706](app/api/line-webhook/route.ts#L704-L706)/[fb-webhook.ts](app/api/fb-webhook/route.ts) เดิม — ไม่ใช่โค้ดที่แก้รอบนี้) สลับคำตอบเป็น **"รับทราบค่ะ 🙏"** แทน ซึ่งเป็นทางตัน ไม่ชวนลูกค้าทำอะไรต่อเลย ทั้งที่ลูกค้าตั้งใจจะส่งรูปตามมาอยู่แล้วจริงๆ (ลูกค้าส่งรูปเองในเทิร์นถัดไปโดยไม่มีใครชวน)

**แนวทางที่พิจารณาแล้วไม่เลือก:** เดิมเสนอให้ `analyzeImageIntent()` รับ conversation history เพิ่ม เพื่อให้รูปที่ส่งตามมาเชื่อมโยงกับคำถามราคาที่ค้างไว้ได้เอง — พิจารณาแล้วว่าซับซ้อนเกินความจำเป็น (ต้องสร้างกลไกจำสถานะ "คำถามค้างรอรูปยืนยัน" ใหม่ทั้งชุด ทิศทางตรงข้ามกับ `img_list` ที่มีอยู่ ซึ่งเป็น "รูปมาก่อนรอข้อความ") — เลือกวิธีง่ายกว่าแทน

**วิธีแก้ (เฉพาะ LINE/Facebook, ไม่แตะ Web Chat):** เพิ่มข้อยกเว้นใน `<reasoning_protocol>` ข้อ 2 (`step2AmbiguousRefException` ใน [lib/prompts.ts](lib/prompts.ts), channel-conditional เหมือนข้อยกเว้นอื่นๆ) — ถ้าลูกค้าอ้างถึงสินค้าด้วยคำกำกวม ("รุ่นนี้"/"ตัวนี้"/"อันนี้") **และ**เข้าเงื่อนไขทั้ง 2 ข้อพร้อมกัน: (1) ไม่มีชื่อรุ่น/รูปภาพสินค้าใดๆ ในบทสนทนาก่อนหน้าให้อ้างอิงได้เลย (2) ไม่ได้อยู่ระหว่าง `<recommend_protocol>` ที่เคยเสนอตัวเลือกไว้แล้ว (กันชนกับ refinement flow เดิม) — ห้ามถามคำถามเดิมซ้ำ ให้ตอบด้วยข้อความที่ชวนทำต่อได้จริงแทนเสมอ: **"รบกวนแจ้งชื่อรุ่น หรือส่งรูปสินค้ามาให้ดูด้วยนะคะ 📷"** — ข้อความใหม่นี้ไม่ตรงกับคำถามเดิมที่เคยส่ง จึงไม่โดนกลไกกันซ้ำตัดหน้าเป็น "รับทราบค่ะ" อีก

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะกลไกกันบอทตอบซ้ำ (`last_bot_reply`) ในเว็บฮุคทั้ง 2 ไฟล์เลย — แก้แค่ให้ Gemini ตอบข้อความที่ต่างจากเดิมแทน (ไม่ต้องมาโดนกลไกกันซ้ำตั้งแต่ต้น) · เงื่อนไข "ไม่มีรุ่น/รูปให้อ้างอิง" กันไม่ให้ชนกับกรณีที่มีบริบทจริงอยู่แล้ว (เช่น เพิ่งยืนยันรุ่นจากรูปไปแล้ว) · เงื่อนไข "ไม่ได้อยู่ระหว่าง recommend_protocol" กันไม่ให้ชนกับ refinement flow (เช่น "ตัวนี้ถูกกว่าไหม" ระหว่างเปรียบเทียบสินค้าหลายตัว) · ไม่แตะ repair_protocol/imei_protocol/self_repair_protocol เลย (คนละโดเมน)

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. เคสจริง: บอทถามชื่อรุ่นไปแล้ว ลูกค้าตอบกำกวม "รุ่นนี้ราคาเท่าไหร่ครับ" → ผลจริง: `"รบกวนแจ้งชื่อรุ่น หรือส่งรูปสินค้ามาให้ดูด้วยนะคะ 📷"` ตรงตามที่ตั้งใจ ไม่ถามชื่อรุ่นซ้ำ — **ผ่าน**
2. regression: "รุ่นนี้ราคาเท่าไหร่" แต่มีบริบทรุ่นชัดเจนอยู่แล้ว (เพิ่งยืนยันจากรูปว่าเป็น TC-11HW) → ผลจริง: ไปค้นราคา TC-11HW ตามปกติ ไม่เข้าเงื่อนไขคำกำกวม (มีรุ่นให้อ้างอิงแล้ว) — **ผ่าน**
3. regression: refinement ระหว่าง recommend_protocol ("ตัวนี้ถูกกว่าไหม" หลังเสนอตัวเลือกไปแล้ว) → ผลจริง: ไปต่อ product_template's "match หลายรุ่น" ถามยืนยันรุ่นที่สนใจตามปกติ ไม่โดนกฎใหม่ตัดหน้า — **ผ่าน**
4. regression: ลูกค้าให้ชื่อรุ่นชัดเจนตั้งแต่ต้น ("TC-11HW ราคาเท่าไหร่") → ผลจริง: ไปค้น FAQ/เว็บตามปกติ ไม่เข้าเงื่อนไขคำกำกวม — **ผ่าน**
5. regression: repair_protocol's bare-model-name exception เดิม (ยืนยันอาการด้วย "ใช่ค่ะ") → ผลจริง: ไปต่อ HANDOFF ตามปกติเหมือนที่ทดสอบผ่านมาก่อนหน้า ไม่กระทบจากกฎใหม่ — **ผ่าน**

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) จุดเดียว (`step2AmbiguousRefException` ใน `<reasoning_protocol>` ข้อ 2) channel-conditional เหมือนข้อยกเว้นอื่นๆ ในไฟล์นี้ — ไม่แตะ webhook ไฟล์ไหนเลย มีผลกับทั้ง LINE และ Facebook พร้อมกันโดยอัตโนมัติ ไม่แตะ Web Chat เลยแม้แต่บรรทัดเดียว

---

## เรื่องที่ 32 — บั๊กรูปหายเมื่อลูกค้าส่งหลายรูปติดกัน (LINE + Facebook)

**ปัญหาที่เจอ:** ระบบเก็บ "รูปที่รอวิเคราะห์" ด้วย `redis.set()` (key เดี่ยว: `img:{userId}`/`img_data:{userId}` ฝั่ง LINE, `fb_img_url:{userId}` ฝั่ง Facebook) — พอลูกค้าส่ง **2 รูปติดกันภายใน 5 นาที** รูปที่ 2 จะทับข้อมูลรูปแรกใน Redis ทันที ทำให้บอทเห็นแค่รูปล่าสุดตอนวิเคราะห์ รูปแรกหายไปจากการวิเคราะห์เงียบๆ (เจอจากเคสจริง: ลูกค้าส่งรูปฉลาก S/N 2 ใบ ตามด้วยคำถาม บอทวิเคราะห์ได้แค่รูปที่ 2 รูปเดียว)

**เจอเพิ่มระหว่างสำรวจ (Facebook):** `event.message.attachments?.find(a => a.type === 'image')` ดึงมาแค่**รูปแรกรูปเดียว**จากอัลบั้ม — ถ้าลูกค้าส่งหลายรูปพร้อมกันในข้อความเดียว (Facebook รองรับ) รูปที่ 2 เป็นต้นไปหายไปตั้งแต่ต้นทางเลย ไม่ผ่าน Redis ด้วยซ้ำ คนละกลไกกับบั๊กหลักแต่ผลลัพธ์เดียวกัน

**วิธีแก้:**
1. **LINE** ([line-webhook.ts](app/api/line-webhook/route.ts)) — รวม `img:`+`img_data:` เป็น `img_list:{userId}` (Redis list, `rpush`+`ltrim` เก็บ 5 รายการล่าสุด+`expire` reset TTL 300s ทุกครั้งที่มีรูปใหม่) ทั้ง 2 branch เดิม (elapsed<5min → `analyzeImageWithText`, elapsed≥5min → `analyzeImageIntent`) อ่านทั้ง list แทนตัวเดียว · ปุ่ม "สอบถามสเปก" อ่านรูปล่าสุดจาก list (ยังใช้ `generateReplyWithImage()` แบบรูปเดียวเดิม ไม่แตะ)
2. **Facebook** ([fb-webhook.ts](app/api/fb-webhook/route.ts)) — `.find()` → `.filter()` ดึงทุกรูปในอัลบั้ม · `fb_img_url:` → `fb_img_list:{userId}` (list) เฉพาะกรณีไม่มี caption (กรณีมี caption ประมวลผลทันทีในคำขอเดียว ไม่ผ่าน Redis อยู่แล้ว ไม่มีบั๊กนี้)
3. **lib/gemini.ts** — `analyzeImageWithText()`/`analyzeImageIntent()` เปลี่ยนรับ `base64Images: string[]` แทนรูปเดียว ส่งเข้า Gemini พร้อมกันในคำขอเดียว (หลาย `inlineData` part) ปรับ prompt ให้สรุปแยกรายรูป ("รูปที่ 1... รูปที่ 2...") เมื่อมีมากกว่า 1 รูป — `analyzeImageIntent()` ใช้ `kind:"product"` ได้เฉพาะกรณีทุกรูปเป็นสินค้าตัวเดียวกันชัดเจนเท่านั้น ถ้าคนละสินค้ากันตกไป `kind:"other"` แทน (กันเมนู 3 ปุ่มที่ผูกกับสินค้าเดียวไปจับคู่ผิดกับรูปที่ไม่ตรงกัน) · timeout dynamic ตามจำนวนรูป: LINE `15000 + 8000*(n-1)` ms, Facebook `10000 + 8000*(n-1)` ms (คำนวณฝั่ง webhook ที่ `Promise.race`)

**ทำไมไม่กระทบอย่างอื่น:** เป็นการแก้ชั้น storage/prompt ล้วนๆ ไม่แตะ `imei_protocol`/`repair_protocol`/`generateReply()`/`generateReplyWithImage()` เลย · ไม่แตะ Web Chat (ไม่มีการรับรูปภาพอยู่แล้ว) · รูปเดี่ยว (กรณีส่วนใหญ่) ยังทำงานเหมือนเดิมทุกประการ ต่างแค่ตอนนี้รองรับหลายรูปเพิ่มด้วย

**ขอบเขตที่ตั้งใจไว้ (ไม่แตะ ตามที่ตกลงไว้ก่อนแก้):** เรื่องการจำแนกรูปละเอียดขึ้น (6 ประเภท A-F ตามความเสี่ยง) และเรื่องลำดับการค้นหาคำตอบ **ยกไปเป็นงานแยก** — ไม่สร้าง JSON output format/search-then-handoff logic ใหม่ซ้ำกับ `imei_protocol`/`repair_protocol` ที่มีอยู่แล้ว (เรื่องที่ 31 ทำหน้าที่ค้นชีต→เว็บ→HANDOFF ให้ทุกกรณีอยู่แล้ว รวมถึงที่มาจากรูปภาพด้วย)

**ทดสอบก่อน commit (จำลองด้วย Gemini API จริง + mock Redis สำหรับ storage):**
- **เจอบั๊กเพิ่มระหว่างทดสอบจริง:** `maxOutputTokens: 400` (ค่าเดิมของทั้ง `analyzeImageIntent()`/`analyzeImageWithText()`) **ไม่พอสำหรับกรณีหลายรูป** — ทดสอบส่ง 2 รูปจริงเข้า Gemini แล้วเจอ `finishReason: MAX_TOKENS` ตอบ JSON ไม่จบ (`thoughtsTokenCount` สูงถึง 600-670 โดยไม่มี cap เพราะทั้ง 2 ฟังก์ชันนี้ไม่เคยตั้ง `thinkingConfig` มาก่อนเลย ต่างจาก `generateReply()` ที่ตั้ง `thinkingBudget: 1024` ไว้อยู่แล้ว) — แก้เพิ่มโดยเพิ่ม `thinkingConfig: { thinkingBudget: 300 }` และขยับ `maxOutputTokens` เป็น 800 ให้ทั้ง 2 ฟังก์ชัน ทดสอบซ้ำแล้ว `finishReason: STOP` ตอบ JSON ครบทุกครั้ง — **เผลอไปแก้ `generateReplyWithImage()`'s OCR call (บรรทัดที่ดันมีค่า `maxOutputTokens: 400` ตรงกันพอดี) ไปด้วยตอนใช้ replace_all แบบไม่ระวัง รีบแก้กลับเป็นค่าเดิมก่อน commit** (ฟังก์ชันนี้อยู่นอกขอบเขตที่ตกลงไว้ ไม่ควรถูกแตะเลย)
- **เทสต์ storage (mock Redis):** ยืนยันพฤติกรรมเดิม (บั๊ก) ส่ง 3 รูปติดกันเหลือแค่รูปล่าสุด 1 รูปจริง เทียบกับพฤติกรรมใหม่เก็บครบทั้ง 3 รูป + `ltrim` จำกัด 5 รายการล่าสุดถูกต้องเมื่อส่ง 8 รูป
- **เทสต์ Gemini จริง (ผ่าน `GEMINI_API_KEY` ที่มีอยู่ใน `.env.local`):** เรียก `analyzeImageIntent()`/`analyzeImageWithText()` จริงจาก `lib/gemini.ts` (ไม่ใช่โค้ดจำลอง) ด้วยรูปทดสอบ 2 ใบที่มีข้อความต่างกัน (ยืนยันว่า Gemini เห็น+อ่านเนื้อหาได้ทั้ง 2 รูปจริง ไม่ใช่แค่รูปเดียว, จำแนก `other`/`confirm` แยกรายรูปตามที่ตั้งใจ, รูปเดี่ยวยัง backward-compatible ตอบ `product` ปกติ) — ไฟล์ทดสอบทั้งหมดลบออกจาก repo แล้ว ไม่ commit

### เพิ่มเติม (2026-08-10) — เจอเคสจริง timeout dynamic ไม่พอ ทำให้ตกไปตอบ "ระบบกำลังประมวลผลนานกว่าปกติ"

**ปัญหาที่พบจากการใช้งานจริง:** ลูกค้าส่งรูปถ่ายจริง 2 รูป (ป้าย S/N บนตัวเครื่อง) ตามด้วยข้อความยาวระบุรายละเอียด 6 ห้อง/กลุ่ม — บอทตอบ `UNAVAILABLE_MSG` ("ขออภัยค่ะ ระบบกำลังประมวลผลนานกว่าปกติ 🙏") แทนที่จะสรุป+ถามยืนยันตามปกติ

**วิเคราะห์สาเหตุ:** ข้อความ+รูปนี้ไหลเข้า flow "รูป+ข้อความ" (`analyzeImageWithText()`) ซึ่งใน 1 ครั้งที่เรียก Gemini แนบไปด้วย: รูป 2 รูป (ต้องอ่านเลข S/N ละเอียดตามกฎ Type A) + prompt คำสั่งจำแนก 7 ประเภท A-G ที่ยาวมาก + ข้อมูลสินค้า/ราคาทั้งชีต (FAQ เต็ม) + ข้อความลูกค้าที่ยาว — payload หนักกว่ากรณีทั่วไปมาก แต่ `dynamicTimeout` เดิม (`15000 + 8000*(n-1)` ของ LINE) คำนวณจาก "จำนวนรูป" อย่างเดียว ไม่ได้คำนึงถึงน้ำหนักจาก FAQ เต็ม/ข้อความยาวเลย — สำหรับ 2 รูป = 23 วิ ซึ่งไม่พอสำหรับเคสนี้

**ยืนยันว่าไม่ใช่ปัญหาเดียวกับเรื่องที่ 41 (Keep-warm):** Keep-warm ป้องกันแค่ "เครื่อง Vercel เย็นเพราะไม่มีคนใช้นาน" (cold start ตอนคำขอแรก) — เคสนี้เกิดจากคำขอที่ "หนักเกินไปจนประมวลผลไม่ทันเวลาที่กำหนด" ซึ่ง Keep-warm ช่วยอะไรไม่ได้เลย เป็นคนละต้นเหตุ

**ทางเลือกที่พิจารณา (เลือกทางเลือกที่ 1):**
1. **เพิ่มเวลารอให้กว้างขึ้น** (เลือกใช้) — แก้จุดเดียว เสี่ยงต่ำสุด ไม่กระทบ logic อื่น
2. ลดขนาดข้อมูลที่ส่งให้ Gemini (เช่น ไม่แนบ FAQ เต็มทุกครั้ง) — แก้ที่ต้นเหตุน้ำหนักคำขอมากกว่า แต่ซับซ้อนกว่าและอาจกระทบความแม่นยำ Type F+สต็อกที่เคยทดสอบผ่านมาแล้ว **ไม่เลือกทำรอบนี้**
3. ตอบรับทันที + วิเคราะห์เบื้องหลังแล้วส่งผลตามทีหลัง — ทนทานสุดแต่เปลี่ยนโครงสร้างเยอะที่สุด **ไม่เลือกทำรอบนี้**

**วิธีแก้:** [line-webhook.ts](app/api/line-webhook/route.ts) + [fb-webhook.ts](app/api/fb-webhook/route.ts) — ปรับสูตร `dynamicTimeout` จาก `15000 + 8000*(n-1)` (LINE) / `10000 + 8000*(n-1)` (Facebook — เดิมฐานต่ำกว่า LINE โดยไม่มีเหตุผลชัดเจน) เป็นสูตรเดียวกันทั้ง 2 ช่องทาง: `Math.min(25000 + 10000*(n-1), 45000)` — เพิ่มฐานเป็น 25 วิ (จาก 15/10 วิ) + เพิ่มต่อรูปเป็น 10 วิ (จาก 8 วิ) พร้อม **cap ที่ 45 วิ** กันเกินงบ `maxDuration=60s` เมื่อส่งมาหลายรูป (เหลือ buffer ~15 วิให้ดาวน์โหลดรูปจาก LINE/FB + ส่งข้อความตอบ)

**ข้อจำกัดของการทดสอบ (แจ้งให้ทราบตรงๆ):** ทดสอบด้วยรูปสังเคราะห์ (PNG แบบ noise สร้างเองด้วย `zlib`, ไม่มี lib ภายนอก) 2 รูป + ข้อความจริงจากเคส + FAQ จำลองขนาดใกล้เคียงจริง (~14,000 ตัวอักษร เพราะ sandbox นี้ fetch `SHEET_CSV_URL` จริงไม่ได้) ผ่าน Gemini จริง 3 รอบ ได้เวลา 7.2-9.6 วิ **ต่ำกว่าที่คาดมาก ไม่สะท้อนความหนักของเคสจริงที่ล้มเหลว** (รูปถ่ายจริงจากกล้องมือถือหนักกว่ารูป noise สังเคราะห์มาก ทั้งขนาดไฟล์และการที่ Gemini ต้อง "คิด" อ่านตัวเลขละเอียดจากภาพจริงตามกฎ Type A ซึ่งรูป noise ไม่มีอะไรให้อ่านจริง) — เลือกค่าใหม่แบบมี margin กว้างจากการประมาณเชิงเหตุผล (ไม่ใช่ตัวเลขที่ยืนยันจาก benchmark จริงได้ 100%) แทนการยึดตามผลทดสอบสังเคราะห์เพียงอย่างเดียว

**ทดสอบก่อน commit:**
- `tsc --noEmit` ผ่านไม่มี error ✅
- ยืนยันค่า timeout ที่คำนวณได้ทุกจำนวนรูป (n=1 ถึง 5) อยู่ต่ำกว่า `maxDuration=60000ms` เสมอ (25s/35s/45s/45s/45s) ✅
- เรียก `analyzeImageWithText()` จริงด้วยรูปเดียว+ข้อความสั้นปกติ (เคสทั่วไป ไม่หนัก) → เสร็จใน 6.3 วิ อยู่ในงบใหม่สบายๆ ไม่มี regression ✅
- grep ยืนยันแก้แค่ 2 บรรทัด (LINE+FB) ไม่กระทบ timeout จุดอื่น (`generateReply()` 20s, "สอบถามสเปก" 15s, ฯลฯ) ✅

---

## เรื่องที่ 33 — จำแนกรูปภาพ 6 ประเภท (A-F) + handoff ทันทีสำหรับรูปที่มีข้อมูลบุคคลที่สาม (LINE + Facebook)

**เป้าหมาย:** ขั้น "สรุป+ถามยืนยัน" เดิม (เรื่องที่ 32) ไม่ได้แยกแยะความเสี่ยงของรูปที่ลูกค้าส่งมาเลย — เพิ่มการจำแนกภายใน 6 ประเภทก่อนตอบเสมอ เพื่อจับเคสเสี่ยงสูง (รูปมีข้อมูลส่วนตัวของบุคคลที่สามปน เช่น สกรีนช็อตแชท/ฟอร์มขอข้อมูลส่วนตัว) แล้ว **handoff ทันทีโดยไม่อ่าน/สรุป/เปิดเผยเนื้อหานั้นเลย** แทนที่จะไหลเข้าขั้นยืนยันตามปกติ — ไม่เปลี่ยน output format เป็น JSON เต็มรูปแบบ ยังเป็น text สรุปที่ไหลเข้า flow เดิมเหมือนก่อน

**6 ประเภท (ใช้พิจารณาภายในเท่านั้น ไม่โผล่ในคำตอบลูกค้า):**
- **A** = ป้าย/สติกเกอร์ S/N หรือ IMEI บนตัวเครื่อง — อ่านให้ครบทุกหลัก ไม่มั่นใจให้บอก+ขอยืนยัน/ถ่ายใหม่
- **B** = หน้าจอเมนู/ตั้งค่าทั่วไป — ตอบตรงไปตรงมา ความเสี่ยงต่ำ
- **C** = หน้าจอแสดงรหัสระบุเครื่อง (ICCID/IMEI) ผ่านซอฟต์แวร์ — จับคู่ label กับค่าให้ตรง ห้ามเดา/สลับ
- **D** = ภาพ error/ปัญหาการใช้งาน — สรุปอาการให้แม่น ไหลเข้า repair protocol ตามปกติ (ไม่มี logic ใหม่)
- **E** = ภาพที่มีข้อความ/แบบฟอร์ม/บทสนทนาของบุคคลที่สาม — **handoff ทันที ไม่ผ่านขั้นยืนยัน ไม่ไหลเข้า protocol ไหนเลย**
- **F** = รูปสินค้าทั่วไป — ระบุรุ่น เสนอตัวเลือกถามต่อตามเดิม

**ลำดับความสำคัญเมื่อมีหลายรูปคนละประเภทปนกันในชุดเดียวกัน:** `E > A/C > D > B/F` — ประเภทที่มาก่อนตัดสินการตอบทั้งชุด (มี E ปนแม้แต่รูปเดียว ตอบ handoff ทันที ไม่พิจารณารูปอื่นเลย)

**สัญญาณที่ใช้ตัดสิน Type E (เพิ่มหลังพบปัญหาความแม่นยำระหว่างทดสอบ — ดูหัวข้อทดสอบด้านล่าง):** มีข้อใดข้อหนึ่งก็นับเป็น E แล้ว ไม่ต้องครบทุกข้อ
- กรอบข้อความ/ฟองแชท (chat bubble), รูปโปรไฟล์วงกลม (avatar), หรือ timestamp แบบแอปแชท (LINE/Messenger/SMS)
- ลิงก์ URL แบบย่อหรือ redirect (bit.ly, tinyurl, ลิงก์ปลอมให้กดยืนยัน/ล็อกอิน)
- ข้อความขอข้อมูลส่วนตัว เช่น ที่อยู่, เลขบัตรประชาชน, รหัสผ่าน/OTP, เลขบัญชีธนาคาร
- เอกสาร/ฟอร์มที่มีชื่อ-ข้อมูลส่วนตัวของบุคคลอื่น (ไม่ใช่ตัวเครื่อง/สินค้า) ปนอยู่

**วิธีแก้:**
1. **lib/gemini.ts** — เพิ่ม `kind: 'handoff'` เข้า union type ของทั้ง `ImageIntent` และ `ImageWithTextResult` · ปรับ prompt ของ `analyzeImageIntent()`/`analyzeImageWithText()` ให้จำแนก 6 ประเภทก่อนตอบเสมอ (เงื่อนไข 0 เช็ค E ก่อนเงื่อนไขอื่นทั้งหมด) · parsing เช็ค `json.kind === 'handoff'` เป็นอันดับแรกก่อน `product`/`other`/`confirm`
2. **LINE** ([line-webhook.ts](app/api/line-webhook/route.ts)) — เพิ่มค่าคงที่ `IMAGE_HANDOFF_MSG = 'รบกวนรอแอดมินตรวจสอบและติดต่อกลับนะคะ 🙏'` · ทั้ง 2 branch (elapsed<5min → `analyzeImageWithText`, elapsed≥5min → `analyzeImageIntent`) เช็ค `kind === 'handoff'` แล้วส่งข้อความนี้ทันที + `notifyAdmin()` แจ้งแอดมินแยก ไม่บันทึก history เป็น pending-confirm เพราะไม่ไหลเข้า protocol ไหนต่อ
3. **Facebook** ([fb-webhook.ts](app/api/fb-webhook/route.ts)) — เหมือนกันทุกประการ ใช้ `notifyAdminFacebook()` แทน

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะ `imei_protocol`/`repair_protocol` internal logic เลย (Type D ยังไหลเข้า repair_protocol ตามปกติ ไม่มี search/handoff logic ใหม่ซ้ำ) · ไม่แตะ Web Chat, spam guard, scheduler, admin panel, Google Sheet schema · output format ยังเป็น text สรุปเดิมทุกกรณียกเว้น Type E ที่ตัดจบด้วยข้อความ handoff คงที่

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo หมดแล้ว):**
- **รอบแรก (5 เคสหลัก, รูปทดสอบสังเคราะห์แบบข้อความล้วน):** ผ่านทั้ง 5 เคส (รูปเดียว regression / Type A / Type B / Type E เดี่ยว / A+E ปนกัน) — แต่ทดสอบ Type E ซ้ำ 5 รอบเพิ่มเติมพบความแม่นยำแค่ ~50-60% (บางรอบตกไป `unclear` แทน `handoff`) — **จุดที่ปลอดภัยไว้: ทุกครั้งที่พลาด ตกไป `unclear` เท่านั้น ไม่เคยหลุดไปเปิดเผยเนื้อหาบุคคลที่สาม (`other`/`confirm`) เลยสักครั้ง**
- **ปรับปรุง 2 จุดตามที่ตกลง:** (1) สร้างรูปทดสอบ Type E ใหม่ให้สมจริงขึ้น (SVG→PNG ด้วย `@resvg/resvg-js`: มีกรอบแชท, avatar วงกลม, timestamp, ลิงก์ย่อ `bit.ly`, ข้อความขอเลขบัตรประชาชน+ที่อยู่ — คล้ายสกรีนช็อตแชทจริง) (2) เพิ่มสัญญาณ Type E ที่ชัดเจนขึ้นใน prompt (4 ข้อด้านบน) แทนคำอธิบายกว้างๆ เดิม
- **ทดสอบซ้ำ Type E เดี่ยว 10 รอบหลังปรับ:** `handoff` ครบ 10/10 = 100% (เกินเป้า 80%+ ที่ตั้งไว้)
- **ทดสอบซ้ำ 4 เคสอื่น (regression/A/B) + A+E ปนกัน หลังปรับ prompt:** ผ่านทุกเคส ข้อความจริงไม่เปลี่ยนพฤติกรรม ยังคงตอบถูกต้องตามประเภทเดิม ยืนยันว่าการเพิ่มสัญญาณ Type E ไม่กระทบการจำแนกประเภทอื่น

**ข้อความจริงที่ลูกค้าเห็น (ทดสอบล่าสุดหลังปรับปรุง):**
1. รูปเดียว (regression): `สินค้าเป็น SPENDER รุ่น TC-11HW ใช่ไหมคะ ต้องการให้น้องใจดีช่วยเรื่องนี้ใช่ไหมคะ 😊`
2. Type A (ป้าย S/N/IMEI): `เห็นหน้าจอแสดง S/N: B26023022S018876 และ IMEI: 863456789012345 ใช่ไหมคะ ต้องการให้น้องใจดีช่วยดูเรื่องนี้ใช่ไหมคะ 😊`
3. Type B (หน้าจอเมนู): `เห็นหน้าจอเมนูการตั้งค่าของอุปกรณ์ แสดงสถานะ WiFi: Connected, Volume: 75%, Language: Thai ใช่ไหมคะ ต้องการให้น้องใจดีช่วยดูเรื่องนี้ใช่ไหมคะ 😊`
4. Type E เดี่ยว (สกรีนช็อตแชทสมจริง): `รบกวนรอแอดมินตรวจสอบและติดต่อกลับนะคะ 🙏`
5. หลายรูปปนกัน A+E: `รบกวนรอแอดมินตรวจสอบและติดต่อกลับนะคะ 🙏` (E override ถูกต้อง ไม่พิจารณารูป A เลย)

---

## เรื่องที่ 34 — ขยาย Type D (ความเสียหายทางกายภาพ) + เพิ่ม Type G (ใบเสร็จ/สลิปขนส่ง) + แยกคำถามสต็อกจากราคาใน Type F (LINE + Facebook)

**เป้าหมาย:** ต่อยอดจากเรื่องที่ 33 (จำแนกรูป 6 ประเภท) เพิ่ม 3 อย่าง:
1. **Type D ขยายนิยาม** ให้ครอบคลุมความเสียหายทางกายภาพของตัวเครื่อง (เคสแตก ปุ่มหลุด สายชาร์จขาด ตัวเครื่องบุบ) ไม่ใช่แค่ error บนหน้าจอเหมือนเดิม — ใช้ flow เดียวกับ Type D เดิมทุกประการ (สรุป+ยืนยันอาการ → ไหลเข้า repair_protocol ค้นชีต→เว็บ→handoff ปกติ) ไม่มี path แยก
2. **Type G ใหม่** — ใบเสร็จ/สลิปขนส่ง/หลักฐานการโอนเงิน/เอกสารธุรกิจ: รับทราบทันที+ดึงเลขอ้างอิงที่เห็นในรูปมาระบุในคำตอบ (เลขที่ใบนำส่ง/บริษัทขนส่ง/จำนวนเงินโอน) แล้ว handoff เสมอ ห้ามบอกว่า "ดำเนินการให้แล้ว" ไม่ผ่านขั้นยืนยัน ไม่ไหลเข้า protocol ไหนทั้งสิ้น (คล้าย Type E แต่ข้อความเป็น dynamic ไม่ใช่ข้อความคงที่)
3. **Type F แยกคำถามสต็อกจากราคา/สเปค** — ถามราคา/สเปคยังตอบจาก flow เดิมทุกประการ (confirm→generateReply() ค้นชีต) แต่ถ้าถามสต็อกคงเหลือ/มีของไหม (ไม่ว่าจะถามราคาด้วยหรือไม่) ต้อง handoff เฉพาะส่วนสต็อกทันที เพราะ Sheet ไม่มีคอลัมน์สต็อก — ถ้าถามราคาด้วยให้ตอบราคาจริงไปเลย (ไม่ใช่แค่ถามยืนยัน) ต่อท้ายด้วย handoff เรื่องสต็อก

**ลำดับความสำคัญเมื่อมีหลายรูปคนละประเภทปนกัน (อัปเดตจากเรื่องที่ 33):** `E > G > A/C > D > B/F`

**วิธีแก้:**
1. **lib/gemini.ts** — เพิ่ม `kind: 'ackHandoff'; message: string` เข้า union type ของทั้ง `ImageIntent` และ `ImageWithTextResult` (ข้อความสำเร็จรูปพร้อมส่ง ต่างจาก `kind:'handoff'` เดิมที่เป็นข้อความคงที่ทั่วไป) · ขยาย prompt ทั้ง `analyzeImageIntent()`/`analyzeImageWithText()` ให้จำแนก 7 ประเภท A-G · parsing เช็ค `ackHandoff` ต่อจาก `handoff` ก่อน `product`/`other`/`confirm`
2. **analyzeImageWithText() เพิ่ม parameter `faqText: string`** (ค่าเริ่มต้น `''`) — ใช้เฉพาะกรณี Type F + ถามสต็อก เพื่อค้นราคาจริงจากข้อมูลเดียวกับที่ระบบใช้อยู่แล้ว (ไม่ใช่ search logic ใหม่ แค่ส่ง context เดียวกับที่ `generateReply()` ใช้อยู่แล้วเข้าไปให้ Gemini อ่าน) — ป้องกันการเดาราคาที่ไม่มีในข้อมูลด้วย prompt กำกับชัดเจน
3. **LINE** ([line-webhook.ts](app/api/line-webhook/route.ts)) — fetch `faqText` ก่อนเรียก `analyzeImageWithText()` (จุดที่มีข้อความประกอบ) · เพิ่ม branch `kind === 'ackHandoff'` ทั้ง 2 จุด (มี/ไม่มีข้อความประกอบ): ส่ง `message` ตรงๆ + `notifyAdmin()` แจ้งแอดมิน ไม่บันทึก pending-confirm history (เหตุผลเดียวกับ Type E — ไม่ไหลเข้า protocol ไหนต่อ)
4. **Facebook** ([fb-webhook.ts](app/api/fb-webhook/route.ts)) — เหมือนกันทุกประการ ใช้ `notifyAdminFacebook()` แทน

**ทำไมไม่กระทบอย่างอื่น:** Type D ยังไหลเข้า repair_protocol ปกติทุกประการ ไม่มี search/handoff logic ใหม่ซ้ำ · Type F ถามราคา/สเปคอย่างเดียว (ไม่ถามสต็อก) พฤติกรรมเหมือนเดิม 100% ยืนยันด้วยการทดสอบซ้ำ · ไม่แตะ `imei_protocol`/`repair_protocol` internal logic, Web Chat, spam guard, scheduler, admin panel, Google Sheet schema

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, รูปทดสอบสังเคราะห์ด้วย SVG→PNG, ไฟล์ทดสอบลบออกจาก repo หมดแล้ว):**
- **Type D ขยาย (เคสแตก+ปุ่มหลุด+สายชาร์จขาด) + ถามอาการ:** 5/5 ตอบ `confirm` ถูกต้อง สรุปความเสียหายทางกายภาพครบถ้วน
- **Type G (สลิปขนส่ง) + แจ้งส่งของ:** 5/5 ตอบ `ackHandoff` ถูกต้อง ดึงเลขที่พัสดุ+บริษัทขนส่งได้ครบ
- **Type F + ถามสต็อก+ราคาพร้อมกัน:** 5/5 ตอบ `ackHandoff` ถูกต้อง ตอบราคาจริงจากข้อมูลที่ให้ไป + handoff เรื่องสต็อก
- **Type F + ถามราคาอย่างเดียว (regression):** 4/5 `confirm` + 1/5 `unclear` ในรอบแรก — ตรวจสอบเพิ่มด้วย diagnostic logging (`finishReason`/token usage) รัน 8 รอบเพิ่มเติมได้ `finishReason: STOP` ทุกครั้ง token usage ต่ำกว่างบมาก (~241/800) **ไม่ใช่ปัญหา MAX_TOKENS** สรุปว่าเป็นความแปรปรวนปกติของโมเดล (รวม 12/13 ≈ 92%) ไม่ใช่ regression จากการแก้ครั้งนี้ — ลบ diagnostic logging ออกจากโค้ดก่อน commit แล้ว
- **สอบทาน `analyzeImageIntent()` (ไม่มีข้อความประกอบ) แยกต่างหาก:** Type D เดี่ยว 3/3 `other` ถูกต้อง, Type G เดี่ยว 3/3 `ackHandoff` ถูกต้อง — ยืนยันทั้ง 2 ฟังก์ชันทำงานสอดคล้องกัน

**ข้อความจริงที่ลูกค้าเห็น:**
1. Type D ขยาย (เคสแตก+ปุ่มหลุด+สายชาร์จขาด) + ถามอาการ: `เห็นว่าเครื่อง SPENDER TC-11HW เคสแตกมุมล่างซ้าย ปุ่ม PTT หลุด และสายชาร์จขาดใช่ไหมคะ ต้องการให้น้องใจดีช่วยดูเรื่องนี้ใช่ไหมคะ 😊`
2. Type G (สลิปขนส่ง) + แจ้งส่งของ: `รับทราบค่ะ ขอบคุณที่แจ้งนะคะ 🙏 เห็นใบนำส่งพัสดุ Kerry Express เลขที่ TH1234567890 แล้วค่ะ รบกวนรอแอดมินตรวจสอบและยืนยันอีกครั้งนะคะ`
3. Type F + ถามสต็อก+ราคา: `ราคา 3,900 บาทค่ะ ส่วนเรื่องสต็อกคงเหลือ รบกวนรอแอดมินยืนยันอีกครั้งนะคะ 🙏`
4. Type F + ถามราคาอย่างเดียว (regression): `สินค้าเป็น SPENDER TC-11HW ใช่ไหมคะ ต้องการทราบราคาใช่ไหมคะ 😊`

---

## เรื่องที่ 35 — Default Fallback ในระบบจำแนกรูปภาพ (Type D เป็น catch-all แทน unclear) (LINE + Facebook)

**ปัญหาที่เจอ:** เคสที่ไม่เข้าเงื่อนไข Type A/B/C/F/G ชัดเจน (เช่น รูปแอปมือถือที่ไม่คุ้นเคย/ไม่แน่ใจว่าเกี่ยวกับ SPENDER หรือไม่ พร้อมคำถามเฉพาะทาง) เดิมมีความเสี่ยงตกไปที่ `unclear` ("รบกวนพิมพ์ชื่อรุ่นที่สนใจ") ซึ่งผิดบริบทกับคำถามที่ไม่เกี่ยวกับสินค้าเลย

**วิธีแก้ (ปรับ prompt เท่านั้น ไม่มี kind ใหม่ ไม่มี code เปลี่ยนใน webhook):**
1. **Type D ขยายบทบาทเป็น default fallback** — ครอบคลุมทุกกรณีที่ไม่เข้า A/B/C/F/G อย่างชัดเจน (รวมแอป/สถานการณ์ที่ไม่คุ้นเคย) ไม่ใช่แค่ error/ความเสียหายเหมือนเดิม — เมื่อเข้าเงื่อนไขนี้ยังคงไหลเข้า flow เดิมทุกประการ (สรุป+ยืนยัน → บันทึก history → ลูกค้ายืนยันแล้วเดินเข้า `generateReply()` ปกติ ซึ่ง**ค้นชีต→ค้นเว็บ→handoff อยู่แล้วโดยไม่ต้องเพิ่ม code**) — ไม่สร้าง search logic ใหม่ซ้ำ
2. **ข้อยกเว้น Type E ยังคง priority สูงสุดเสมอ** — เพิ่มคำเตือนชัดเจนในprompt ว่าแม้เนื้อหา/คำถามจะดูเหมือนเรื่องทั่วไปไม่อันตราย (อาจเป็นการพรางคำถาม) ก็ให้ยึดสัญญาณ E เป็นหลักเสมอ
3. **ห้ามเดาคำตอบเทคนิคที่ไม่มีข้อมูลจริงรองรับ** (ชื่อ Account, รหัสผ่านเริ่มต้น, ขั้นตอนเฉพาะทาง) เพิ่มเป็นกฎชัดเจนในทั้งกฎของ Type D และเงื่อนไข catch-all ของทั้ง 2 ฟังก์ชัน — แม้จะดูมั่นใจแค่ไหนก็ตาม ให้แค่สรุป+ถามยืนยัน ปล่อยให้ `generateReply()` ค้นหาคำตอบจริงในขั้นถัดไป
4. **ทำให้ `unclear` เข้าถึงยากขึ้น** — เพิ่มคำสั่งชัดเจนว่าห้ามใช้ unclear แค่เพราะไม่แน่ใจว่าเกี่ยวกับ SPENDER หรือไม่/ไม่คุ้นเคย ตราบใดที่รูปยังพอบอกได้ว่าเห็นอะไร ให้ใช้ default fallback (ข้อ 3) แทนเสมอ — สงวน unclear ไว้เฉพาะรูปที่อ่าน/มองไม่ออกจริงๆ เท่านั้น
5. **อัปเดตลำดับความสำคัญ** เมื่อหลายรูปปนกัน: `E > G > A/C > D (รวม fallback) > B/F`

**ทำไมไม่กระทบอย่างอื่น:** ไม่เพิ่ม kind ใหม่ ไม่แตะ webhook เลยทั้ง 2 ไฟล์ (LINE/Facebook ใช้ `lib/gemini.ts` ร่วมกัน ได้ผลอัตโนมัติทั้งคู่) ไม่แตะ `imei_protocol`/`repair_protocol`/`generateReply()`/`<search_tool>` ภายใน `lib/prompts.ts` เลย — การค้นชีต→เว็บ→handoff ที่เกิดขึ้นหลังลูกค้ายืนยัน เป็นกลไกเดิมที่มีอยู่แล้ว (เหมือนที่ Type D ใช้อยู่ก่อนหน้านี้) ไม่ใช่ logic ใหม่

**การตัดสินใจร่วมกับผู้ใช้:** เดิมมีข้อเสนอให้เพิ่ม proactive `notifyAdmin()` เมื่อค้นไม่เจอทั้ง FAQ/เว็บ แต่หลังพิจารณาพบว่ากลไก fallback ทั่วไปของ `<search_tool>` (คำตอบ "ยังไม่พบข้อมูล...รอแอดมินติดต่อกลับ") ไม่เคย proactive push แจ้งแอดมินอยู่แล้วสำหรับคำถามข้อความทั่วไปทุกแบบ (ไม่ใช่เฉพาะรูปภาพ) — การเพิ่ม proactive notify เฉพาะเคสนี้จะต้องแตะ `<search_tool>`/webhook หลักที่ใช้ร่วมกับข้อความทุกประเภท ผู้ใช้เลือก **ไม่เพิ่ม** เพื่อคงพฤติกรรมเดิมเป๊ะๆ (แอดมินยังเห็นบริบทได้จาก LINE Manager backend ตามปกติ)

**ทดสอบก่อน commit (Gemini API จริง, รูปทดสอบสังเคราะห์ด้วย SVG→PNG):**
- **รูปแอปมือถือที่ไม่รู้จัก + ถามรหัสผ่านเริ่มต้น (คำถามเฉพาะทาง):** 4/5 `confirm` ถูกต้อง (ไม่เดารหัสผ่านเลยสักครั้ง) + 1/5 `unclear` รอบแรก — ตรวจเพิ่มด้วย diagnostic logging รัน 8 รอบเพิ่มเติมได้ `confirm` ครบ 8/8 `finishReason: STOP` ทุกครั้ง token usage ต่ำกว่างบมาก (~232/800) ไม่ใช่ปัญหา MAX_TOKENS สรุปเป็นความแปรปรวนปกติของโมเดล (รวม 12/13 ≈ 92%) — **สำคัญ: ไม่มีรอบไหนเดารหัสผ่านเลยแม้แต่ครั้งเดียว** ลบ diagnostic logging ออกก่อน commit แล้ว
- **รูปสถานการณ์แปลก (เสาอากาศติดตั้งบนหลังคา) ไม่เข้า A-G เลย ไม่มีข้อความประกอบ:** 5/5 `other` ถูกต้อง ไม่ตกไป unclear
- **Regression — รูปว่างเปล่าจริงๆ (ไม่มีอะไรให้เห็นเลย):** 3/3 `unclear` ถูกต้อง ยืนยันว่า unclear ยังทำงานถูกต้องสำหรับกรณีที่สมควรใช้จริงๆ

**ข้อความจริงที่ลูกค้าเห็น:**
1. รูปแอปไม่คุ้นเคย + ถามรหัสผ่านเริ่มต้น: `เห็นหน้าจอ login ของแอป NetLink Pro ใช่ไหมคะ ต้องการทราบรหัสผ่านเริ่มต้นใช่ไหมคะ 😊` (ไม่มีการเดารหัสผ่านใดๆ)
2. รูปเสาอากาศ (ไม่เข้า A-G เลย): `เห็นภาพประกอบเสาอากาศติดตั้งบนหลังคาบ้าน พร้อมข้อความระบุความสูงจากพื้นดาดฟ้าประมาณ 3 เมตร ใช่ไหมคะ ต้องการให้น้องใจดีช่วยดูเรื่องนี้ใช่ไหมคะ 😊`

---

## เรื่องที่ 36 — บั๊ก HANDOFF: ดิบหลุดไปให้ลูกค้าเห็น (LINE + Facebook, ข้อความล้วน)

**ปัญหาที่เจอ:** ลูกค้าเห็นข้อความจริง (จาก screenshot):
```
น้องใจดีรับทราบอาการที่แจ้งว่าแบตเตอรี่ชาร์จแล้วเปิดได้แปปเดียวค่ะ
HANDOFF: ซ่อม/เคลม | รุ่น: ไม่ระบุ (รอช่างตรวจสอบเพิ่มเติม) | อาการ: แบตเตอรี่ชาร์จแล้วเปิดได้แปปเดียว
```
สาเหตุ: webhook ตรวจจับ HANDOFF ด้วย `reply.toUpperCase().startsWith('HANDOFF')` — Gemini เผลอใส่ประโยคนำหน้ามาก่อน "HANDOFF:" (ทั้งที่ prompt สั่งห้ามไว้ที่ `lib/prompts.ts:25/47/151-152` ว่า "ห้ามเพิ่มข้อความอื่นนอกจากรูปแบบนี้") ทำให้ `startsWith('HANDOFF')` เป็น false เงื่อนไขนี้เลยไม่ถูกเรียก ข้อความดิบทั้งก้อน (ประโยคนำหน้า + `HANDOFF: ...`) จึงหลุดไปให้ลูกค้าเห็นตรงๆ แทนที่จะถูกแทนด้วย `handoffMsg` ตามดีไซน์เดิม

**วิธีแก้ (แก้เฉพาะ logic ตรวจจับใน webhook เท่านั้น ไม่แก้ prompt เพราะพึ่ง prompt อย่างเดียวไม่แม่นยำพอ):**
1. [line-webhook.ts:594-620](app/api/line-webhook/route.ts#L594-L620) และ [fb-webhook.ts:535-561](app/api/fb-webhook/route.ts#L535-L561) — เปลี่ยนเงื่อนไขตรวจจับจาก `startsWith('HANDOFF')` เป็น `reply.toUpperCase().includes('HANDOFF:')` (ค้นหาที่ไหนก็ได้ในข้อความ ไม่ anchor ที่ต้น)
2. Regex สกัด `summary` เปลี่ยนจาก `/^HANDOFF[:\s]*/i` (anchor ที่ต้น) เป็น `/^[\s\S]*?HANDOFF:?\s*/i` (non-greedy ตัดทุกอย่างก่อน "HANDOFF:" ทิ้งไปด้วย รวมประโยคนำหน้าถ้ามี) — ยังคงส่ง `summary` ที่สะอาดเข้า `notifyAdmin()`/`notifyAdminFacebook()` เหมือนเดิมทุกประการ
3. เช็ค self_repair_protocol (`HANDOFF: ขอวิธีทำเอง`) เปลี่ยนจาก `/^HANDOFF:\s*ขอวิธีทำเอง/i` เป็น `/HANDOFF:\s*ขอวิธีทำเอง/i` (ไม่ anchor ที่ต้นเช่นกัน) เพื่อความสอดคล้อง — ใช้ logic เดียวกันนี้ครอบคลุมทั้ง 3 จุดที่มี HANDOFF format (repair_protocol, self_repair_protocol, imei_protocol) เพราะรวมกันเป็นเงื่อนไขเดียวอยู่แล้ว
4. `replyMsg` ที่ส่งกลับลูกค้ายังคงเป็น `handoffMsg`/ข้อความควบคุมเสมอตามดีไซน์เดิม (ไม่เคยเป็น `reply` ดิบ) — การแก้นี้แค่ทำให้เงื่อนไขตรวจจับเข้าถึงบล็อกที่สร้าง `replyMsg` ปลอดภัยได้แม่นยำขึ้นเท่านั้น

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะ `lib/gemini.ts`/ระบบจำแนกรูปภาพ Type A-G เลย (`kind:'handoff'`/`kind:'ackHandoff'` ใช้กลไกคนละแบบ) — ยกเว้น Type D ที่ลูกค้ายืนยันแล้วไหลเข้า `generateReply()` ปกติจะได้รับผลดีจากการแก้นี้โดยอัตโนมัติ (ใช้ path เดียวกับข้อความล้วน) เป็นผลข้างเคียงที่ต้องการ

**ทดสอบก่อน commit (pure logic, ไม่ใช้ Gemini API):** จำลอง `reply` string 7 เคส (บั๊กจริงจาก repair_protocol/self_repair_protocol/imei_protocol + regression ปกติ 3 แบบ + bare `"HANDOFF"`) ผ่านทุกเคส — เคสบั๊กทุกแบบได้ `replyMsg = handoffMsg`/ข้อความควบคุมเท่านั้น (ไม่มีข้อความดิบหลุด) พร้อม `summary` สะอาดเข้า `notifyAdmin()` ถูกต้อง เคส regression ทำงานเหมือนเดิมทุกประการ

**ข้อความจริงที่ลูกค้าเห็น + summary ที่แอดมินได้รับ (หลังแก้):**
1. **เคสบั๊กจริง** (ประโยคนำหน้าก่อน HANDOFF: — repair_protocol):
   - ลูกค้าเห็น: `รอแอดมินติดต่อกลับนะคะ 🙏 ทีมงานกำลังดูแลท่านอยู่ค่ะ` (หรือเวอร์ชันนอกเวลาทำการ)
   - แอดมินเห็น (ผ่าน `notifyAdmin`): `[สรุปคำสั่ง]: ซ่อม/เคลม | รุ่น: ไม่ระบุ (รอช่างตรวจสอบเพิ่มเติม) | อาการ: แบตเตอรี่ชาร์จแล้วเปิดได้แปปเดียว`
2. **Regression** (`HANDOFF: ซ่อม/เคลม | ...` ตรงๆ): เหมือนข้อ 1 ทุกประการ (พฤติกรรมไม่เปลี่ยน)
3. **เคสบั๊ก self_repair_protocol** (ประโยคนำหน้าก่อน `HANDOFF: ขอวิธีทำเอง`):
   - ลูกค้าเห็น: `กรุณารอเจ้าหน้าที่เพื่อทำการตรวจสอบและแนะนำวิธีให้อีกครั้งนะคะ`
   - แอดมินเห็น: `[สรุปคำสั่ง]: ขอวิธีทำเอง | รายละเอียด: อยากรู้วิธีเปลี่ยนแบตเอง`
4. **เคสบั๊ก imei_protocol** (ประโยคนำหน้าก่อน `HANDOFF: ย้ายกลุ่ม`): ลูกค้าเห็น `handoffMsg` ปกติ (LINE) / `getSpendernetworkRedirectMessage()` (Facebook เพราะ summary มี "IMEI:") — แอดมินเห็น `[สรุปคำสั่ง]: ย้ายกลุ่ม | IMEI: 863598080691487 | A → B` ถูกต้องครบถ้วน

---

## เรื่องที่ 37 — ปิด notifyAdmin() สำหรับ Handoff ฝั่งข้อความล้วน (LINE + Facebook)

**เหตุผล:** แอดมินเช็คแชทเองทุกวันเป็นปกติอยู่แล้ว ไม่จำเป็นต้องพึ่ง push notification — ให้พฤติกรรมเหมือนกับที่ตัดสินใจไว้แล้วสำหรับ Type D fallback ฝั่งรูปภาพ (เรื่องที่ 35)

**วิธีแก้:** เอาการเรียก `notifyAdmin()`/`notifyAdminFacebook()` ออกจากบล็อกจัดการ HANDOFF ของข้อความล้วน (repair_protocol/self_repair_protocol/imei_protocol) ที่ [line-webhook.ts:594-620](app/api/line-webhook/route.ts#L594-L620) และ [fb-webhook.ts:535-560](app/api/fb-webhook/route.ts#L535-L560) — คงส่วน redis state (`routed`/`takeover`/`handoff_notified`) ไว้เหมือนเดิม เปลี่ยนแค่ catch block log event name จาก `handoff.notify_failed` เป็น `handoff.state_failed` เพื่อสะท้อนว่าตอนนี้ catch แค่ error จาก redis เท่านั้น — `replyMsg` ที่ลูกค้าเห็น (`handoffMsg`/ข้อความควบคุมตามเวลาทำการ) **ไม่เปลี่ยนแปลงเลย**

**ไม่แตะ `lib/handoff.ts`** — ฟังก์ชัน `notifyAdmin()`/`notifyAdminFacebook()` เองยังอยู่ครบ ยังใช้งานอยู่ที่ Type E/G/F+สต็อก ฝั่งรูปภาพตามปกติ (line-webhook.ts:313/323/362/370, fb-webhook.ts:708/717/745/753) — ยืนยันแล้วว่าเป็นจุดเรียกคนละที่ ไม่มี code path ร่วมกับบล็อก HANDOFF ข้อความล้วนเลย

**ทดสอบก่อน commit:** ตรวจ diff โดยตรง + grep ยืนยันว่าไม่มีการเรียก `notifyAdmin`/`notifyAdminFacebook` เหลืออยู่ในช่วงบรรทัดของบล็อก HANDOFF ข้อความล้วนทั้ง 2 ไฟล์ (มีแค่ comment อธิบาย ไม่ใช่ function call จริง) — `tsc` ผ่าน — บรรทัดที่กำหนดค่า `replyMsg` ไม่ถูกแก้เลย (ยืนยันจาก diff ว่าลูกค้ายังได้ `handoffMsg` เหมือนเดิมทุกประการ)

---

## เรื่องที่ 38 — เว้นบรรทัดในคำตอบหายไป (LINE + Facebook + Web Chat, ทั้ง Sheet FAQ และคำตอบค้นเว็บ)

**ปัญหาที่พบ:** แก้เซลล์ Sheet ให้เว้นบรรทัด (Alt+Enter) แล้ว แต่บอทตอบจริงยังติดกันเป็นก้อนเดียว

**การวินิจฉัย (ตามที่สั่ง):**
1. **Redis cache Sheet:** ไม่มี — `lib/sheet.ts` ใช้ **in-memory cache** ระดับ module เท่านั้น (`let cache: Sheet | null`, `CACHE_TTL_MS = 60_000` = 60 วิ ตรงตาม CLAUDE.md ที่ห้ามแคชเกิน 60s) ไม่เกี่ยวกับ Redis เลย
2. **จุดที่ลบ `\n` ทิ้ง — เจอต้นตอ:** [lib/sheet.ts:233-235](lib/sheet.ts#L233-L235) (เดิม) มีฟังก์ชัน `clean()` — `text.replace(/\r?\n\s*/g, ' ')...` **ลบ `\n` ทั้งหมดทิ้ง แทนด้วยช่องว่าง** ใช้อยู่ใน 5 จุด: `buildLicenseMap()` (112), `formatLicense()` (136), `formatFaq()` (142), `formatProduct()` INFO field (179), และ `customerProduct()` bullet (216) — 4 จุดแรกสร้าง context `<faq>` ที่ป้อนให้ Gemini อ่าน (`lib/prompts.ts:270`), จุดที่ 5 เป็นข้อความส่งลูกค้าตรงในเส้นทาง `findExactMatch()` fast-path
3. **ผ่าน Gemini เรียบเรียงใหม่หรือไม่:** ใช่ — เส้นทางหลัก (`generateReply()`) ให้ Gemini อ่าน `<faq>` แล้วแต่งประโยคตอบเองเสมอ (ไม่ได้ copy คำตอบดิบส่งตรง) **ปัญหาคือ Gemini ไม่เคยเห็นการเว้นบรรทัดต้นฉบับเลย** เพราะถูกลบไปตั้งแต่ก่อนสร้าง context — ต่อให้สั่ง prompt ให้รักษาบรรทัดยังไงก็ทำไม่ได้ในเมื่อข้อมูลที่เห็นไม่มี `\n` อยู่แล้ว (เส้นทาง fast-path `findExactMatch()` มีฟังก์ชัน `cleanKeepLines()` แยกไว้อยู่แล้วซึ่งรักษา `\n` ถูกต้อง แต่ใช้แค่ 2 ใน 3 formatter คือ `customerFaq()`/`customerLicense()` — `customerProduct()` ยังใช้ `clean()` ตัวที่ลบทิ้งอยู่)
4. **Debug เทียบค่าจริง:** ยืนยันด้วยการจำลอง CSV ที่มี Alt+Enter จริง (เครือข่าย sandbox เข้าถึง Google Sheet จริงไม่ได้ช่วงทดสอบ — ติด interstitial page ของเครือข่าย ไม่ใช่บั๊กโค้ด) → CSV ดิบมี `\n` ในเซลล์ → `fetchFAQ()` (ก่อนแก้) คืนข้อความที่ `\n` หายหมด → หลังแก้คืนข้อความที่มี `\n` ครบ → Gemini ตอบกลับมาพร้อม `\n` ตรงกับโครงสร้างต้นฉบับ (ดูผลทดสอบด้านล่าง)

**วิธีแก้:**
1. [lib/sheet.ts](lib/sheet.ts) — เปลี่ยนทั้ง 5 จุดจาก `clean()` เป็น `cleanKeepLines()` (รักษา `\n` แต่ยังลบช่องว่างซ้ำในแต่ละบรรทัด) ลบฟังก์ชัน `clean()` ที่ไม่มีที่ใช้แล้วออก
2. [lib/prompts.ts](lib/prompts.ts) `<output_format>` — เพิ่ม 2 กฎ: (ก) ถ้าคำตอบมีหลายประเด็น/ขั้นตอน ให้ขึ้นบรรทัดใหม่แยกแต่ละประเด็นเสมอ ห้ามเขียนพารากราฟยาวติดกัน ไม่ว่าจะมาจาก `<faq>` หรือค้นเว็บ (`search_spender_specs`) ก็ตาม (ข) ถ้า `<faq>` มีการเว้นบรรทัดอยู่แล้ว ให้คงไว้ตามเดิม ไม่รวบเป็นย่อหน้าเดียว
3. **ไม่แก้ scope การค้นหา** — `searchSpenderSites()` ยังค้นทั้ง spenderclub.com และ spendernetwork.com เหมือนเดิมทุกประการ แก้แค่การจัดรูปแบบคำตอบที่ได้

**เช็ค 3 ช่องทางว่ารองรับการขึ้นบรรทัดใหม่แบบไหน (research เท่านั้น ไม่ต้องแก้โค้ดช่องทางเลย):**
- **LINE** (`@line/bot-sdk`) — `txt()`/`safeReply()`/`safePush()` ส่ง `\n` ตรงๆ ไม่มีการแปลง LINE app render `\n` เป็นบรรทัดใหม่ตามธรรมชาติอยู่แล้ว
- **Facebook** — `fbSend()`/`fbSendRequest()` ส่ง `text` ตรงๆ ไม่มีการแปลง Messenger render `\n` เป็นบรรทัดใหม่ตามธรรมชาติเช่นกัน (ยกเว้น `fbSendProductCard()` ที่ [fb-webhook.ts:118](app/api/fb-webhook/route.ts#L118) ซึ่งยุบ `\n{2,}`→`\n` เดียว แต่ขอบเขตแค่ title/subtitle ของการ์ด Facebook Generic Template เท่านั้น ไม่ใช่ข้อความ plain text ทั่วไป — ไม่แตะ เพราะ card title ควรเป็นบรรทัดเดียวตามธรรมชาติของ UI element นี้อยู่แล้ว)
- **Web Chat widget** ([public/widget.js](public/widget.js)) — render ข้อความผ่าน `bbl.innerHTML = linkify(text)` (บรรทัด 155) ซึ่งเป็น HTML แต่ CSS ของ `.nj-bbl` ตั้ง **`white-space:pre-wrap`** ไว้แล้ว (บรรทัด 63) — ทำให้ `\n` ดิบที่อยู่ใน text แสดงผลเป็นบรรทัดใหม่ได้เลยโดยไม่ต้องแปลงเป็น `<br>` **สรุป: ทั้ง 3 ช่องทางรองรับ `\n` ถูกต้องอยู่แล้วโดยไม่ต้องแก้โค้ดฝั่ง render เลยสักช่องทาง** — ต้นตอปัญหาอยู่ที่ต้นทาง (`lib/sheet.ts`) เพียงจุดเดียว

**ทดสอบก่อน commit (Gemini API จริง — เครือข่าย sandbox เข้าถึง Google Sheet/Serper จริงไม่ได้ จึงจำลอง CSV และ web-search function-response ด้วยเนื้อหาจริงแทน แต่เรียก Gemini จริงทุกครั้ง):**
- **FAQ multi-line (Alt+Enter 3 ขั้นตอน):** context ที่ Gemini เห็นมี `\n` ครบ → Gemini ตอบกลับมาพร้อม `\n` ตรงกับ 3 ขั้นตอนต้นฉบับ
- **Product multi-line (จุดเด่น 3 ข้อ):** เหมือนกัน Gemini คงโครงสร้างหลายบรรทัดไว้ในคำตอบ
- **Web search formatting:** ส่งผลค้นหาสังเคราะห์ที่เขียนเป็นพารากราฟยาวติดกัน (ไม่มี `\n` เลยแม้แต่ตัวเดียว) ให้ Gemini สรุป → Gemini จัดกลุ่มเป็นหัวข้อแยกสินค้า + bullet แต่ละประเด็น เว้นบรรทัดถูกต้องเองตามกฎใหม่ (ดูตัวอย่างด้านล่าง)

**ข้อความจริงที่ลูกค้าเห็น (หลังแก้ — ทุกช่องทางได้ข้อความเดียวกันจาก `generateReply()` แล้ว render ผ่าน `\n` แบบเดียวกัน):**

1. คำถาม: "เช็คสถานะซ่อมยังไงคะ" (FAQ multi-line)
```
ในการเช็คสถานะซ่อม มีขั้นตอนดังนี้นะคะ
ขั้นตอนที่ 1: แจ้งเลขที่ใบรับซ่อมกับแอดมิน
ขั้นตอนที่ 2: รอตรวจสอบ 1-3 วันทำการ
ขั้นตอนที่ 3: แอดมินแจ้งผลกลับทางไลน์ค่ะ
```

2. คำถาม: "TC-99TEST ราคาเท่าไหร่คะ" (Product multi-line)
```
SPENDER TC-99TEST ราคา 2990 บาทค่ะ 📻
• จุดเด่นข้อ 1: กันน้ำกันฝุ่นมาตรฐาน IP67
จุดเด่นข้อ 2: แบตอึดใช้งานต่อเนื่อง 20 ชม.
จุดเด่นข้อ 3: ระยะส่งสัญญาณไกลสุด 5 กม.
• ไม่ต้องขอใบอนุญาต ใช้ได้ถูกกฎหมาย รับประกัน 2 ปี
https://spenderclub.com/tc-99test
ลูกค้าสนใจสั่งเลยไหมคะ?
```

3. คำถาม: "SPENDER TC-11HW กับ TC-15HW ต่างกันยังไงบ้างคะ เปรียบเทียบให้หน่อย" (ค้นเว็บ — ผลค้นหาต้นฉบับเป็นพารากราฟยาวติดกันไม่มี `\n` เลย)
```
น้องใจดีเปรียบเทียบ SPENDER TC-11HW กับ SPENDER TC-15HW ให้ฟังนะคะ 📻
SPENDER TC-11HW
• กำลังส่ง 5 วัตต์
• กันน้ำกันฝุ่น IP54
• แบตเตอรี่ 1800mAh ใช้งานต่อเนื่องได้ประมาณ 10 ชั่วโมง
• น้ำหนัก 180 กรัม ราคา 2,500 บาท
• เหมาะกับงานในร่มหรือพื้นที่ไม่กว้างมาก

SPENDER TC-15HW
• กำลังส่ง 8 วัตต์
• กันน้ำกันฝุ่น IP67 ระดับสูงกว่า
• แบตเตอรี่ 2600mAh ใช้งานต่อเนื่องได้ประมาณ 16 ชั่วโมง
• น้ำหนัก 220 กรัม ราคา 3,200 บาท
• เหมาะกับงานกลางแจ้งหรือพื้นที่กว้าง

ทั้งสองรุ่นไม่ต้องขอใบอนุญาต ใช้งานได้ถูกกฎหมายเลยค่ะ 😊
ดูรายละเอียดเพิ่มเติม: https://spenderclub.com/compare-tc11-tc15
หากต้องการข้อมูลเพิ่มเติม สามารถสอบถามเพิ่มเติมได้เลยนะคะ
```

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะ logic การค้นหา/จำแนกรูปภาพ (Type A-G) หรือ handoff logic (เรื่องที่ 36-37) เลย — แก้แค่การจัดรูปแบบข้อความ 2 ไฟล์ (`lib/sheet.ts`, `lib/prompts.ts`) ไม่แตะ webhook ทั้ง 3 ช่องทางเลยเพราะพิสูจน์แล้วว่าไม่ใช่ต้นตอปัญหา

**ต่อยอด (2026-08-07 เพิ่มภายหลัง): สำนวนขึ้นต้นมาตรฐานก่อน list/เปรียบเทียบ** — เดิมปล่อยให้ Gemini เลือกคำเองตามธรรมชาติ เจอ "ให้ฟังนะคะ" ซึ่งเป็นภาษาพูดเกินไป เพิ่มกฎใน `<output_format>` บังคับใช้แค่ "ดังนี้นะคะ" หรือ "ตามนี้ค่ะ" (สลับตามบริบทได้ ห้ามใช้คำอื่น) ก่อนขึ้น list/bullet/การเปรียบเทียบทุกครั้ง ไม่ว่าคำตอบจะมาจาก `<faq>` หรือค้นเว็บ — ทดสอบ Gemini จริง 5 รอบ (เปรียบเทียบสินค้า 2 คู่ x หลายรอบ + FAQ multi-step) ผ่านครบ ใช้สำนวน "ดังนี้นะคะ" ทุกรอบ ไม่หลุดไปใช้คำอื่นเลย

---

## เรื่องที่ 39 — เก็บ history ไว้ช่วงปิดบอทตามตารางเวลา + TTL แบบไดนามิก (LINE + Facebook, ไม่เกี่ยวกับ Web Chat)

**ปัญหาที่พบ:** ช่วง `isScheduledOff() = true` (บอทปิดตามตารางเวลา แอดมินดูแลเอง) ทั้ง 4 จุดตรวจ (LINE TEXT/IMAGE, Facebook TEXT/IMAGE) return เงียบทันทีโดยไม่บันทึกอะไรเข้า `saveHistory()` เลย — มีแค่ `logChat()` (log แยกต่างหาก อายุ 30 วัน ไม่เกี่ยวกับ Gemini context) ที่ถูกเรียกใน TEXT branch เท่านั้น ไม่มีเลยใน IMAGE branch ทั้ง 2 ช่องทาง — ตัดสินใจแล้วว่าจะ**เก็บประวัติไว้เฉยๆ รอลูกค้าทักมาใหม่** (ไม่ auto-reply)

**ความเสี่ยงที่ตรวจสอบก่อนแก้:** `saveHistory()` เดิม TTL แค่ 10 นาที (`HISTORY_TTL = 600` วิ) — ถ้าใช้ TTL เดิมตรงๆ ข้อความที่ลูกค้าพิมพ์ตอนปิดบอทตอนเช้าจะหายไปก่อนบอทจะเปิดตอนเย็นอยู่ดี ไม่ช่วยอะไร — และถ้าลูกค้าพิมพ์หลายข้อความติดกันช่วงปิดบอท (ไม่มี `'model'` turn คั่นเลย) ต้องเช็คว่า Gemini API ยอมรับ history ที่มี role `'user'` ติดกันหลายรอบไหม — **ทดสอบแล้วยอมรับปกติ ไม่ error** (Gemini `contents` เป็น array ของ turn เฉยๆ ไม่บังคับ strict alternation) จึงไม่ต้องออกแบบพิเศษ (ไม่ต้องรวม turn หรือใส่ placeholder model คั่น) — append ตรงไปตรงมาได้เลย

**วิธีแก้:**
1. [lib/history.ts](lib/history.ts) — เพิ่ม parameter `ttlSeconds: number = HISTORY_TTL` ให้ `saveHistory()` (optional, ไม่ระบุ = พฤติกรรมเดิมทุกประการ ทุก call site เดิมไม่กระทบเลย) — **ไม่แตะ `MAX_TURNS`** ตามที่ตกลงไว้
2. เพิ่มฟังก์ชัน `computeOffHoursHistoryTtl(channel)` เป็น local helper **ในแต่ละ webhook file** (ไม่ export จาก `lib/schedule.ts` — อ่าน `getRules()` ที่มีอยู่แล้วมาคำนวณเท่านั้น ไม่แก้ `isScheduledOff()`/logic การเช็คตารางเลย มิเรอร์วิธีจับคู่ day/time แบบเดียวกันทุกประการ): หา rule ที่ตรงกับเวลาปัจจุบัน คำนวณนาทีที่เหลือจนกว่าจะถึง `endTime` (รองรับทั้งช่วงในวันเดียวกันและข้ามคืน) + buffer 30 นาที — ถ้าคำนวณไม่ได้ (rules ว่าง/ไม่มี rule ตรง) fallback 12 ชม.
3. แก้ 4 จุดให้เรียก `saveHistory(userId, [...history, {role:'user', text: userMessage}], ttl)` ก่อน `return`:
   - [line-webhook.ts](app/api/line-webhook/route.ts) TEXT branch — `history`/`userMessage` มีอยู่แล้วในสโคป
   - line-webhook.ts IMAGE branch — เพิ่ม `getHistory(userId)` ก่อน (ไม่เคยถูกเรียกใน branch นี้เลย) บันทึกด้วย placeholder `[ลูกค้าส่งรูปภาพ]` (ยังไม่วิเคราะห์รูป ณ จุดนี้)
   - [fb-webhook.ts](app/api/fb-webhook/route.ts) TEXT branch — เหมือน LINE
   - fb-webhook.ts IMAGE branch — เหมือน LINE (เพิ่ม `getHistory()` ก่อน)
4. วันที่เปิดบอท 24 ชม. (ไม่มี rule ตรงกับวันนั้น) → `isScheduledOff()` คืน false ตั้งแต่ต้น → ไม่เข้า branch นี้เลย → ใช้ `saveHistory()` ปกติ (TTL 10 นาทีเดิมโดยอัตโนมัติ ไม่ต้องเขียน special case)

**การตัดสินใจร่วมกับผู้ใช้ (นอกขอบเขต "แก้ 4 จุด" เดิม):** เพื่อให้ `saveHistory()` รับ TTL แบบ dynamic ได้ ต้องแก้ `lib/history.ts` เพิ่มเติมด้วย (ไม่ได้อยู่ใน "แก้ 4 จุด" เดิมที่ระบุไว้) — แจ้งผู้ใช้ก่อนแล้วเลือก **เพิ่ม optional parameter ใน `saveHistory()`** (backward-compatible เต็มรูปแบบ) แทนการก็อป logic `slice(-MAX_TURNS)`+serialize ซ้ำในเว็บฮุคทั้ง 2 ไฟล์

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะ `MAX_TURNS`, `logChat()`/`lib/chatlog.ts`, ตัวฟังก์ชัน `isScheduledOff()` เอง, Admin Takeover, ข้อความ off-hours/greeting, `imei_protocol`/`repair_protocol`/`self_repair_protocol`, ระบบจำแนกรูปภาพ Type A-G, handoff logic (เรื่องที่ 36-37), sendLock/retry-backoff (เรื่องที่ 30), Web Chat เลยแม้แต่จุดเดียว — ทุก call site เดิมของ `saveHistory()` (ไม่ระบุ ttlSeconds) ได้ TTL 600 วิเหมือนเดิมทุกประการ

**ทดสอบก่อน commit:**
- ข้อความเข้าตอน 10:00 (ปิดบอท 08:00-18:00) → TTL 8 ชม. 30 นาที (8 ชม.เหลือ + buffer 30 นาที) ✅
- ข้อความเข้าตอน 17:45 (ใกล้ปิดช่วงปิดบอท) → TTL 45 นาที (15 นาทีเหลือ + buffer 30 นาที) ✅
- วันเปิด 24 ชม. (ไม่มี rule ตรงกับวันนั้น) → `computeOffHoursHistoryTtl()` ไม่ถูกเรียกเลย ยืนยัน fallback ปลอดภัย 12 ชม. แม้ถูกเรียกผิดจังหวะ ✅
- rules ว่างเปล่า/คำนวณไม่ได้ → fallback 12 ชม. ✅
- rule ข้ามคืน (เผื่อ schema รองรับ แม้ config จริงไม่ใช้แบบนี้) เช่น 22:00-06:00 ตอนนี้ 23:00 → TTL 7 ชม. 30 นาที ✅
- `saveHistory()` ไม่ระบุ ttlSeconds → default 600 วิ (10 นาทีเดิม) ✅ / ระบุ ttlSeconds → ใช้ค่าที่ส่งเข้าไปจริง ✅
- ลูกค้าพิมพ์ 4 ข้อความติดกันช่วงปิดบอท (append `role:'user'` ล้วนไม่มี `'model'` คั่น) → ไม่ error ✅ แล้วเรียก `generateReply()` จริงด้วย history ชุดนี้หลังบอทเปิด → ไม่ error ตอบราคาสินค้าได้ถูกต้องปกติ ✅

### ยกเลิกบางส่วน (2026-08-10) — เจอปัญหาจริงจากการใช้งาน: บอทดึงเรื่องเก่าที่แอดมินคุยจบไปแล้วนอกระบบ

**ปัญหาที่พบจากการใช้งานจริง:** ระหว่างที่บอทปิดตามตารางเวลา แอดมินมักคุยกับลูกค้าจบเรื่องไปแล้วเองนอกระบบ (โทร/ไลน์ส่วนตัว/หน้าร้าน) โดยไม่ผ่านบอทเลย — แต่ history ที่เรื่องที่ 39 บันทึกไว้ระหว่างปิดบอท (พร้อม TTL ยืดยาวจนกว่าจะเปิดร้าน) ยังไม่หมดอายุ พอบอทเปิดใหม่แล้วลูกค้าทักมาอีกครั้ง Gemini จะดึง history เก่าที่ "จบไปแล้วนอกระบบ" กลับมาต่อบทสนทนา ทำให้ลูกค้างงว่าทำไมบอทพูดถึงเรื่องที่คุยจบไปแล้ว

**การตัดสินใจ:** เรื่องที่ 39 จริงๆ แล้วรวม 2 การเปลี่ยนแปลงไว้ด้วยกันโดยไม่ได้แยกตั้งแต่ต้น — (ก) เพิ่มการเช็ค `isScheduledOff()` เข้าไปในสาขา IMAGE เป็นครั้งแรก (แก้บั๊กจริงที่ไม่เกี่ยวกับปัญหานี้: เดิมบอทตอบรูปภาพได้แม้ตั้งเวลาปิดไว้) กับ (ข) บันทึก history ระหว่างปิดบอท + ยืด TTL แบบไดนามิก (ต้นเหตุปัญหานี้) — ยกเลิกเฉพาะข้อ (ข) เท่านั้น คง (ก) ไว้ไม่ให้บั๊กเดิมกลับมา

**วิธีแก้:**
1. [lib/history.ts](lib/history.ts) — เอาพารามิเตอร์ `ttlSeconds` ออกจาก `saveHistory()` กลับไปใช้ `HISTORY_TTL` (600 วิ) ตายตัวเสมอเหมือนก่อนเรื่องที่ 39
2. [line-webhook.ts](app/api/line-webhook/route.ts) / [fb-webhook.ts](app/api/fb-webhook/route.ts) — ลบฟังก์ชัน `computeOffHoursHistoryTtl()` ทิ้งทั้ง 2 ไฟล์ (ไม่มีใครเรียกใช้แล้ว) + เอา `getRules` ออกจาก import ที่ไม่ใช้แล้ว
3. ทั้ง 4 จุด (LINE TEXT/IMAGE, Facebook TEXT/IMAGE) — เปลี่ยนจาก "เรียก `saveHistory()` พร้อม TTL ไดนามิกก่อน return" กลับเป็น **`return` เงียบๆ ทันที ไม่บันทึกอะไรเลย** เหมือนก่อนเรื่องที่ 39 ทุกประการ — **สาขา IMAGE ยังคงมีการเช็ค `isScheduledOff()` อยู่เหมือนเดิม** (ไม่ลบการเช็คนี้ทิ้ง) แค่เอาส่วนบันทึก history ออกเท่านั้น

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะ `logChat()` (เรื่องที่ 40 — เรียกแยกคนละบรรทัด ก่อน `isScheduledOff()` check อยู่แล้วทั้ง 2 ไฟล์ ยังบันทึกว่าลูกค้าส่งรูปมาแม้บอทปิดเหมือนเดิม) · ไม่แตะ `isScheduledOff()`/`lib/schedule.ts` เอง (ฟังก์ชันเช็คตารางเวลายังทำงานเหมือนเดิมทุกจุด) · ไม่แตะ `MAX_TURNS`, Admin Takeover, `imei_protocol`/`repair_protocol`/`self_repair_protocol`, ระบบจำแนกรูปภาพ Type A-G, handoff logic, Web Chat เลยแม้แต่จุดเดียว

**ทดสอบก่อน commit:**
- `tsc --noEmit` ผ่านไม่มี error หลังลบ `computeOffHoursHistoryTtl()`/`ttlSeconds` ออกทั้งหมด ✅
- grep ยืนยันไม่มี `computeOffHoursHistoryTtl` หรือ `saveHistory(...ttl)` (3 args) เหลืออยู่ในโค้ดเลยสักจุด ✅
- grep ยืนยัน `isScheduledOff()` ยังอยู่ครบทั้ง 4 จุด (LINE TEXT/IMAGE, FB TEXT/IMAGE) — สาขา IMAGE ไม่ได้ถูกลบการเช็คตารางเวลาทิ้งไปด้วย ✅
- เรียก `saveHistory()`/`getHistory()` จริงด้วย signature ใหม่ (2 args) → ไม่ throw runtime error ✅

---

## เรื่องที่ 40 — เพิ่ม logChat() ให้ครอบคลุมรูปภาพ (LINE + Facebook เท่านั้น)

**ปัญหาที่พบ:** branch รูปภาพไม่เคยเรียก `logChat()` เลย ไม่ว่าบอทเปิดหรือปิด — แอดมินเปิดหน้าประวัติแชทย้อนหลังไม่เห็นว่าลูกค้าเคยส่งรูปมาบ้าง

**การตรวจสอบก่อนแก้ (ตามที่สั่ง):**
1. `logChat()` ใน TEXT branch เรียก 2 ครั้งต่อ 1 รอบสนทนาจริง (ฝั่งลูกค้า + ฝั่งบอท) แต่**ไม่ได้เขียน manual ทั้งคู่** — ฝั่งลูกค้าเรียกทันทีตอนรับข้อความ ([line-webhook.ts:224](app/api/line-webhook/route.ts#L224) / [fb-webhook.ts:322](app/api/fb-webhook/route.ts#L322)) ส่วนฝั่งบอท**ฝังอยู่ในฟังก์ชันส่งข้อความกลาง** — ไม่ใช่ manual call ที่แต่ละจุดตอบ
2. ฝั่งบอทเรียกที่ `safeReply()`/`safePush()` (LINE, บรรทัด 176/185/273) และ `fbSend()`/`fbSendProductCard()` (Facebook, บรรทัด 139/188) — เรียก**หลังส่งข้อความสำเร็จจริง**เท่านั้น (หลัง `replyMessage()`/`pushMessage()`/Graph API สำเร็จ)
3. รูปแบบข้อมูล: `{ userId, channel: 'LINE'|'Facebook', role: 'user'|'bot', message: string, ts: number }` (`ChatLogEntry` ใน `lib/chatlog.ts`)

**สิ่งที่พบเพิ่มก่อนแก้ (กระทบแผนเดิม):** ทุกจุดที่ตอบเรื่องรูปภาพ (unclear/handoff/ackHandoff/confirm/สอบถามสเปก/รับทราบ) ส่งผ่าน `pushOnly()`→`safePush()`, `ans()`→`safeReply()`/`safePush()` (LINE) และ `fbSend()`/`fbSendReply()` (Facebook) อยู่แล้วทุกจุด — **ซึ่งมี `logChat({role:'bot',...})` ฝังอยู่แล้วอัตโนมัติ** — แจ้งผู้ใช้ก่อนแล้วตัดสินใจ**ไม่เพิ่ม logChat() ซ้ำสำหรับคำตอบบอท** (จะทำให้ log ซ้ำ 2 รอบต่อข้อความ) — เพิ่มแค่ **1 จุดต่อไฟล์** สำหรับฝั่งลูกค้าเท่านั้น

**วิธีแก้:** เพิ่ม `logChat({userId, channel, role:'user', message:'[ลูกค้าส่งรูปภาพ]', ts:Date.now()})` ที่ต้นสุดของ branch รูปภาพ **ก่อน** `isScheduledOff()` check ([line-webhook.ts:754](app/api/line-webhook/route.ts#L754) / [fb-webhook.ts:689](app/api/fb-webhook/route.ts#L689)) — ใช้ placeholder เดียวกับที่ `saveHistory()` ใช้อยู่แล้วจากเรื่องที่ 39 — ทำให้บันทึกไม่ว่าบอทเปิดหรือปิดด้วยจุดเดียว

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะ `saveHistory()`/`getHistory()` (เรื่องที่ 39), ระบบจำแนกรูปภาพ Type A-G, `isScheduledOff()`/`lib/schedule.ts`, `MAX_TURNS`/`lib/history.ts`, handoff logic (เรื่องที่ 36-37), Web Chat เลยแม้แต่จุดเดียว — TEXT branch ไม่ถูกแตะเลย (ยืนยันด้วยการอ่านโค้ดจริง ยังมี `logChat(userMessage)` แค่ 1 จุดเดิมทั้ง 2 ไฟล์)

**ทดสอบก่อน commit:**
- ลูกค้าส่งรูปช่วงบอทเปิด → `logChat()` เรียกครบ 2 ครั้ง (user 1 + bot 1 จาก shared helper, ไม่ซ้ำ) ✅
- ลูกค้าส่งรูปช่วงบอทปิด → บันทึก placeholder ไว้ 1 ครั้งแม้บอทไม่ตอบ ✅
- Facebook เหมือน LINE ทุกประการ ✅
- เรียก `logChat()` จริงด้วย argument shape ที่โค้ดใหม่ใช้ → ไม่ throw (fire-and-forget ตามดีไซน์เดิม) ✅
- regression: TEXT branch `logChat(userMessage)` ยังมีแค่ 1 จุดเดิมทั้ง 2 ไฟล์ ไม่ถูกแตะเลย ✅

---

## เรื่องที่ 41 — Keep-warm endpoint กัน Cold Start (GEMINI_UNAVAILABLE) — ⚠️ ลบออกแล้ว (2026-08-14)

> **อัปเดต 2026-08-14:** ผู้ใช้เลิกใช้ external cron service ที่เคย ping endpoint นี้แล้ว จึงลบ [app/api/cron/keep-warm/route.ts](app/api/cron/keep-warm/route.ts) ทิ้ง — ผู้ใช้รับทราบและยอมรับความเสี่ยงว่าปัญหา cold start (`GEMINI_UNAVAILABLE`) เดิมที่หัวข้อนี้เคยแก้ไว้อาจกลับมาเกิดได้อีกถ้าไม่มีกลไกอื่นทดแทน — เนื้อหาด้านล่างเก็บไว้เป็นบันทึกประวัติเท่านั้น ไม่ใช่สถานะปัจจุบันของระบบ

**ปัญหาที่พบ:** ผู้ใช้รายงาน `GEMINI_UNAVAILABLE` (timeout) หลายเคสจริง (07:50-07:51 วันหนึ่ง — ข้อความแรกของวัน หลังไม่มี traffic นาน) สงสัย cold start — สำรวจ Vercel Runtime Logs ย้อนหลัง 7 วันไม่ได้ (ไม่มี `vercel` CLI/token/`.vercel/` link ในสภาพแวดล้อมนี้ และ `log.*` เขียนแค่ `console.log()` ไม่มี log store อื่นให้ query) จึงแก้เชิงป้องกันแทนโดยไม่ต้องรอข้อมูลย้อนหลัง

**การตรวจสอบก่อนแก้ (ตามที่สั่ง):** โปรเจกต์นี้อยู่บน **Vercel Hobby tier** (ระบุไว้ใน `CLAUDE.md`) — Vercel Cron บน Hobby จำกัดความถี่สูงสุดแค่ 1 ครั้ง/วัน ไม่พอสำหรับ ping ทุก 5 นาทีตามที่ต้องการ — แจ้งผู้ใช้ก่อนแล้วเลือกใช้ **external cron service** (เช่น cron-job.org/GitHub Actions scheduled workflow ยิงจากภายนอก repo) แทน Vercel Cron ทั้งหมด — จึง**ไม่แก้ `vercel.json` เลย** (ไม่มี config ฝั่ง Vercel ให้เพิ่มเมื่อไม่ได้ใช้ Vercel Cron)

**วิธีแก้:** สร้างไฟล์ใหม่ [app/api/cron/keep-warm/route.ts](app/api/cron/keep-warm/route.ts) — endpoint เบาที่สุดเท่าที่ทำได้ ไม่เรียก Gemini/Redis/Sheet ใดๆ เลย แค่ตรวจ header `Authorization: Bearer ${'{'}CRON_SECRET{'}'}` (env var ใหม่ ต้องตั้งค่าใน Vercel Environment Variables + ตั้งใน external cron service ให้ส่ง header เดียวกันมา) ตรงหรือไม่ → ตรง = ตอบ `200 OK` ทันที / ไม่ตรงหรือไม่ได้ตั้ง `CRON_SECRET` เลย = ตอบ `401 Unauthorized` กันถูกเรียกจากภายนอกที่ไม่รู้ secret

**ทำไมไม่กระทบอย่างอื่น:** เป็นไฟล์ใหม่ล้วนๆ ไม่แก้ไฟล์เดิมเลยสักไฟล์ (ไม่แก้ `vercel.json` ด้วยตามเหตุผลข้างต้น) — ไม่แตะ `line-webhook.ts`/`fb-webhook.ts`/`web-chat/route.ts`, `lib/gemini.ts` (timeout ทุกค่าคงเดิม: 20s ข้อความล้วน, 7s FB postback, 15s สอบถามสเปก, dynamic รูป+ข้อความ), `lib/prompts.ts`, `lib/history.ts`, `lib/schedule.ts`, `lib/handoff.ts`, `lib/sendLock.ts`, `lib/sheet.ts`, `lib/chatlog.ts`, `lib/greeting.ts`, `lib/holidays.ts` เลยแม้แต่บรรทัดเดียว — ไม่เพิ่ม dependency ใหม่ (ใช้แค่ `next/server` ที่มีอยู่แล้ว)

**ทดสอบก่อน commit:** เรียก endpoint จริงผ่าน route handler ด้วย `NextRequest` จำลอง 4 เคส — ไม่ตั้ง `CRON_SECRET` เลย → 401 ✅ · ไม่มี header `Authorization` → 401 ✅ · secret ผิด → 401 ✅ · secret ถูกต้อง → 200 OK, body `"OK"`, ตอบเร็วมาก (0ms, ไม่มี network call ใดๆ ในโค้ดเลย) ✅

**สิ่งที่ผู้ใช้ต้องทำต่อ (นอกเหนือจากโค้ด — ไม่ใช่ส่วนที่ Claude ทำได้เอง):** ตั้งค่า `CRON_SECRET` เป็น env var ใน Vercel Project Settings (สุ่มค่าเอง) แล้วตั้ง external cron service ให้ยิง GET ไปที่ `https://<domain>/api/cron/keep-warm` ทุก 5 นาที พร้อมแนบ header `Authorization: Bearer <CRON_SECRET เดียวกัน>`

---

## เรื่องที่ 42 — แก้ปัญหา "แอดมินลืมคืนบอท" (Admin Takeover, LINE เท่านั้น)

**การสำรวจก่อนแก้ (ทำแยกต่างหากก่อนหน้านี้แล้ว):** พบว่าคำสั่ง `คืนบอท:{userId}` ([line-webhook.ts](app/api/line-webhook/route.ts)) **ไม่มีการตรวจสอบสิทธิ์ผู้ส่งเลย** — ใครก็ได้ที่คุยกับบอทตรงๆ (ไม่ต้องอยู่ในกลุ่มแอดมิน) พิมพ์ `คืนบอท:{userId ของคนอื่น}` ก็ปลด takeover ของคนอื่นได้ทันที — เป็นช่องโหว่จริงที่ควรปิด

**การเปลี่ยนแปลง:**
1. **ลบคำสั่ง `คืนบอท:{userId}` ทิ้งทั้งหมด** — ข้อความที่ขึ้นต้นด้วยคำนี้ไหลเข้า flow ข้อความปกติเหมือนข้อความอื่นๆ ไม่มีความหมายพิเศษอีกต่อไป ปิดช่องโหว่โดยตรงเพราะไม่มีคำสั่งให้ใช้ประโยชน์ได้อีกแล้ว
2. **`TAKEOVER_TTL` ลดจาก 2 ชั่วโมง (7200 วิ) เหลือ 1 ชั่วโมง (3600 วิ)** ([line-webhook.ts:22-24](app/api/line-webhook/route.ts#L22-L24)) — auto-expire เป็น**ทางออกทางเดียว**จาก takeover state แล้ว (ไม่มีทาง manual อีกต่อไป) จึงลด TTL ให้กลับมาตอบเร็วขึ้นกันแอดมินลืม
3. **ลบข้อความสอนแอดมินพิมพ์ `คืนบอท:{userId}`** ออกจาก `notifyAdmin()` ([lib/handoff.ts:133](lib/handoff.ts#L133)) — เหลือแค่ข้อความแจ้งบริบทเคส + ลิงก์ไป manager.line.biz
4. **ปิด `notifyAdmin()`/`notifyAdminFacebook()` สำหรับ pre-handoff trigger path ด้วย** ([line-webhook.ts:482-497](app/api/line-webhook/route.ts#L482-L497), [fb-webhook.ts:397-419](app/api/fb-webhook/route.ts#L397-L419)) — จุดนี้เป็นกลไกแยกจาก HANDOFF sentinel ที่ปิดไปแล้วในเรื่องที่ 37 (`shouldHandoffDeferred`/`shouldHandoffImmediate` — "ขายส่ง"/"ใบเสนอราคา"/"ตัวแทนจำหน่าย"/"คุยกับคน"/"ร้องเรียน") ตรวจสอบก่อนแล้วพบว่ายังเรียกอยู่ทั้ง 2 ช่องทาง ปิดให้สม่ำเสมอกันทั้งหมด (แอดมินเช็คแชทเองทุกเรื่องเหมือนกัน) — redis state (`routed`/`takeover`/`handoff_notified` LINE, `routed`/`handoff_notified` FB) ยังคงเดิมทุกประการ

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะระบบจำแนกรูปภาพ Type A-G, `saveHistory()`/`getHistory()`, `logChat()`, `isScheduledOff()` เลย — ไม่แตะ `routed:{userId}` (คนละ key คนละหน้าที่) — ไม่แตะ Facebook takeover (ไม่มีกลไกนี้อยู่แล้วตามที่สำรวจไว้) — ไม่แก้จุด (ก) ที่รูปภาพไม่ suppress ระหว่าง takeover (แยกเป็นงานอื่น) — ไม่แตะขั้นตอนยืนยันที่ลูกค้าเห็นก่อนสรุปเป็น HANDOFF (ข้อความยืนยัน IMEI/กลุ่ม ยังทำงานปกติทุกประการ) — `replyMsg`/`handoffMsg` ที่ลูกค้าเห็นไม่เปลี่ยนแปลงเลย

**ทดสอบก่อน commit:** จำลอง Redis พร้อม TTL semantics จริง (in-memory mock เดินเวลาได้) — takeover set แล้วผ่านไป 59 นาทียังไม่หมดอายุ ✅ ผ่านไป 61 นาที auto-expire แล้ว ✅ · ยืนยันจาก source จริงว่าไม่มี `startsWith('คืนบอท:')` เหลืออยู่เลยในโค้ด ✅ และไม่มีข้อความสอน `คืนบอท:` ใน `lib/handoff.ts` เหลืออยู่เลย (ตรวจ template literal จริง ไม่ใช่แค่คอมเมนต์) ✅ · pre-handoff trigger (จำลอง "ขายส่ง"): redis state (`routed`/`takeover`) set ถูกต้อง `notifyAdmin` เรียก 0 ครั้ง ✅ · regression: takeover ยัง set ถูกต้องทั้ง 2 จุด (pre-handoff + HANDOFF sentinel) ✅ · regression: `handoffMsg` ที่ลูกค้าเห็นไม่เปลี่ยนแปลง ✅

---

## เรื่องที่ 43 — ขยาย MAX_TURNS + แยก S/N ออกจาก IMEI (LINE + Facebook เท่านั้น)

**เคสจริงที่พบ:** ลูกค้าแจ้งอาการเครื่องเสีย (TC-7G ตกพื้น มีเสียงแก็ก) พร้อมถามเรื่องประกัน — บอทตอบดีตอนแรก (ให้ที่อยู่ร้านซ่อมจาก FAQ) แต่ลูกค้าพิมพ์ S/N ของเครื่อง ("W2324301S000150") ตามมาเป็นข้อความแยก บอทกลับถามหา "IMEI 15 หลัก" แล้วถามว่าต้องการทำรายการกลุ่มอะไร ทำให้บทสนทนาหลุดจากเรื่องซ่อม/ประกันไปเข้า `imei_protocol` ทั้งที่ไม่ได้ตั้งใจ — ต่อมาเมื่อถามเรื่องประกันอีกครั้ง บอทถามชื่อรุ่นสินค้าซ้ำ ทั้งที่ลูกค้าบอก "TC-7G" ไปแล้วตั้งแต่ข้อความแรก

**สาเหตุที่ 1 — `MAX_TURNS` เล็กเกินไปสำหรับลูกค้าที่พิมพ์ถี่:** ระบบเก็บ history แค่ 6 เทิร์นล่าสุด (3 รอบสนทนา) — ลูกค้าคนนี้พิมพ์แยกเป็นหลายข้อความสั้นๆ ติดกันในเวลาไม่กี่นาที (แจ้งอาการ, ให้ S/N, ให้ IMEI, ตอบคำถามยืนยัน, ส่งรูป, ยืนยันอีกครั้ง) แต่ละข้อความคือ 1 exchange ทำให้โควตา 6 เทิร์นหมดเร็วกว่าที่ควร ข้อความแรกที่บอกรุ่น "TC-7G" เลยหลุดออกจากหน้าต่างความจำไปทั้งที่ยังอยู่ในบทสนทนาเดียวกันต่อเนื่อง (ไม่ใช่ปัญหา TTL/เวลา แต่เป็นปัญหาจำนวนเทิร์น)

**สาเหตุที่ 2 — `imei_protocol` ถูกกระตุ้นผิดจาก S/N ที่ไม่ใช่ IMEI:** "W2324301S000150" มีตัวอักษรปนตัวเลข **ไม่ตรงกับเงื่อนไข imei_protocol ที่กำหนดไว้จริง (ตัวเลขล้วน 14-16 หลักติดกัน) เลย** แต่ Gemini ตีความว่าเป็นความพยายามพิมพ์ IMEI ที่ผิดรูปแบบ แล้วขอให้พิมพ์ใหม่ ทำให้บทสนทนาเบี่ยงออกจากเรื่องที่คุยอยู่ไปเข้า imei_protocol ทั้งที่ลูกค้าไม่ได้ตั้งใจจะจัดการกลุ่ม IMEI เลย

**วิธีแก้:**
1. **[lib/history.ts](lib/history.ts)** — เพิ่ม `MAX_TURNS_EXTENDED = 12` (เดิม `MAX_TURNS = 6` ยังคงอยู่เหมือนเดิมทุกประการ) พร้อมฟังก์ชันใหม่ `saveHistoryExtended()` แยกต่างหากจาก `saveHistory()` เดิม — **`saveHistory()`/`MAX_TURNS` เดิมไม่ถูกแก้แม้แต่บรรทัดเดียว** เพราะ Web Chat ([app/api/web-chat/route.ts](app/api/web-chat/route.ts)) มีฟังก์ชัน `saveHistory()` ของตัวเองแยกต่างหากอยู่แล้ว (ไม่ได้ import จากไฟล์นี้) จึงไม่ถูกกระทบทั้งทางตรงและทางอ้อม
2. **[line-webhook.ts](app/api/line-webhook/route.ts) + [fb-webhook.ts](app/api/fb-webhook/route.ts)** — เปลี่ยน import และเรียกใช้ `saveHistoryExtended()` แทน `saveHistory()` ทุกจุด (17 จุดใน LINE, 21 จุดใน Facebook) — ไม่แตะ `getHistory()` เลย (แค่อ่านค่าที่มีอยู่ใน Redis เฉยๆ ไม่เกี่ยวกับการตัด/เก็บเทิร์น)
3. **[lib/prompts.ts](lib/prompts.ts)** — เพิ่มข้อยกเว้น `imeiSnDistinctionNote` ใน `<imei_protocol>` (channel-conditional, `channel !== 'web'` เหมือนข้อยกเว้นอื่นๆ ในไฟล์นี้): ถ้าข้อความมีตัวอักษรปนกับตัวเลข (ไม่ใช่ตัวเลขล้วน) ให้ถือว่าเป็น S/N ไม่ใช่ IMEI ห้ามเข้า `<imei_protocol>` หรือขอให้พิมพ์ IMEI ใหม่เด็ดขาด ให้ถือเป็นข้อมูลประกอบบริบทของ protocol อื่นที่กำลังคุยอยู่แทน

**ที่ตั้งใจไม่แตะ:** เรื่องเช็คสถานะประกัน (ตกลงกับผู้ใช้แล้วว่าให้แอดมินดูแลเองตอนคุยกับลูกค้าโดยตรง ไม่ต้องปรับ flow ส่วนนี้) · `TAKEOVER_TTL` (ตกลงแล้วว่าคงไว้ที่ 1 ชม. เหมือนเดิมทุกประการ ไม่แตะ) · Web Chat ไม่ถูกแตะเลยแม้แต่บรรทัดเดียว (ทั้ง `saveHistory()`/`MAX_TURNS` เดิม และ `imei_protocol` branch `channel === 'web'`)

**ทดสอบก่อน commit:**
- `tsc --noEmit` ผ่านไม่มี error ✅
- ตรวจ logic การ slice ของ `MAX_TURNS_EXTENDED` โดยตรง (array 14 เทิร์น → เหลือ 12 เทิร์นล่าสุดถูกต้องตามสูตร) ✅ — ไม่ได้ทดสอบ round-trip ผ่าน Redis จริงเพราะ sandbox ไม่มี Redis ให้เชื่อมต่อ
- เคสจริง (Gemini จริง): ลูกค้าแจ้งอาการซ่อมแล้วพิมพ์ S/N "W2324301S000150" ตามมา → ผลจริง: `HANDOFF: ซ่อม/เคลม | รุ่น: SPENDER TC-7G (S/N: W2324301S000150) | อาการ: ...` — ไม่ถามหา IMEI 15 หลัก ไม่หลุดเข้า imei_protocol เลย ใช้ S/N เป็นข้อมูลประกอบ HANDOFF แทน — **ผ่าน**
- regression: IMEI แท้ 15 หลักล้วนพร้อมเจตนาชัดเจนในข้อความเดียว ("ขอเพิ่มกลุ่ม DOPA BBT... IMEI 867361044192209") → ผลจริง: `HANDOFF: เพิ่มกลุ่ม | IMEI: 867361044192209 | DOPA BBT` ตามปกติ — **ผ่าน**
- regression: บอทเพิ่งถาม IMEI ไปจาก imei_protocol เอง ลูกค้าตอบ IMEI มา (2 เทิร์น) → ผลจริง: ถามกลุ่มต้นทาง/ปลายทางต่อตามขั้นตอนปกติ ไม่ถูกกันด้วยกฎใหม่ — **ผ่าน**
- regression: S/N ปนตัวอักษรแบบไม่มีบริบทอื่นเลย (ข้อความแรกสุด) → ผลจริง: ไม่ถามหา IMEI 15 หลัก ตกไปที่ guardrail "ไม่มีคำขอชัดเจน" แทน (ปลอดภัย ไม่ผิดจุดประสงค์) — **ผ่าน**

**ขอบเขต:** แก้ 4 ไฟล์ (`lib/history.ts`, `line-webhook.ts`, `fb-webhook.ts`, `lib/prompts.ts`) ทั้งหมด LINE/Facebook เท่านั้น ไม่แตะ Web Chat เลยแม้แต่บรรทัดเดียว (ทั้งทางตรงและทางอ้อม)

---

## เรื่องที่ 44 — ตัด reasoning ที่หลุดถึงลูกค้า + แก้ acknowledgment-shortcut กว้างเกินไป (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าพิมพ์ "สอบถามอาการเครื่องหน่อยครับ" — บอทตอบข้อความที่มี**กระบวนการคิดภายในของ Gemini หลุดออกมาปนกับคำตอบจริง**ตรงๆ:
> "คิดก่อน: ลูกค้าทักทายมา. การตอบสนองที่เหมาะสมที่สุดคือการทักทายกลับและถามว่าต้องการให้ช่วยอะไร. นี่ไม่ใช่คำถามเกี่ยวกับสินค้า/บริการโดยตรง, ไม่ต้องใช้เครื่องมือ.\n\nสวัสดีค่ะ น้องใจดี ยินดีให้บริการค่ะ 😊 มีอะไรให้น้องใจดีช่วยดูแลคะ"

ต่อมาลูกค้าพิมพ์ "TC-7G" (หมายถึงรุ่นที่มีอาการเสียที่เพิ่งจะถาม) — บอทกลับตอบ**ราคาสินค้า**แทนที่จะถามอาการต่อ ทั้งที่ลูกค้าไม่ได้ถามราคาเลย

**สาเหตุที่ 1 — reasoning หลุด ไม่มีตาข่ายรองรับ:** เคยแก้บั๊ก reasoning หลุดมาก่อน (เรื่องที่ 28) แต่ครั้งนั้นแก้เฉพาะกรณีหลุดมาปนกับ **sentinel `[OUT_OF_DOMAIN]`** เท่านั้น (เปลี่ยนเช็คจาก `startsWith` เป็น `includes`) — เคสนี้**ไม่มี sentinel เลย** เป็นคำตอบธรรมดาที่ดันมี "คิดก่อน:" ติดหัวมา ระบบไม่มีตัวกรองสำหรับข้อความที่ไม่ใช่ sentinel เลย ส่งตรงถึงลูกค้าทันที

**สาเหตุที่ 2 — acknowledgment-shortcut trigger word กว้างเกินไป (ยังไม่ครอบคลุมพอจากที่แก้ไปเรื่องที่ 31):** `<reasoning_protocol>` ข้อ 1 มี "ครับ"/"ค่ะ" อยู่ใน trigger word list ตรงๆ — "สอบถามอาการเครื่องหน่อยครับ" มี "ครับ" ต่อท้าย (คำลงท้ายสุภาพปกติที่ติดแทบทุกประโยคภาษาไทย) ทำให้ Gemini เข้าใจผิดว่าเป็นแค่คำทักทาย/ตอบรับ (ยืนยันจาก reasoning ที่หลุดออกมาเอง: "ลูกค้าทักทายมา...นี่ไม่ใช่คำถามเกี่ยวกับสินค้า/บริการโดยตรง") ทั้งที่เป็นประโยคเนื้อหาจริงจัง — ครั้งก่อน (เรื่องที่ 31 เพิ่มเติม) แก้แค่กรณี "ลูกค้ายืนยันคำถามที่ protocol ค้างอยู่" (`step1AckException`) แต่เคสนี้ลูกค้า**เริ่ม**ประโยคใหม่ด้วยเนื้อหาจริงจัง ไม่ใช่แค่ตอบยืนยัน จึงไม่เข้าเงื่อนไขเดิม

**วิธีแก้:**
1. **[lib/gemini.ts](lib/gemini.ts)** — เพิ่มฟังก์ชัน `stripLeakedReasoning()` เป็นตาข่ายรองรับชั้นสุดท้าย (นอกเหนือจากคำสั่งใน prompt) ตัดข้อความที่ขึ้นต้นด้วย "คิดก่อน:" ตามด้วยย่อหน้าเหตุผลแล้วเว้นบรรทัดก่อนคำตอบจริงทิ้ง — ใช้ regex เฉพาะรูปแบบนี้เท่านั้น กันตัดข้อความปกติผิดพลาด — ใช้ใน `generateReply()` ทั้ง 2 จุดคืนค่า (ตอบตรง + ตอบหลังค้นเว็บ) **ใช้ร่วมกันทุกช่องทาง** (LINE/Facebook/Web Chat) เพราะเป็นการป้องกันข้อมูลรั่วไหลระดับพื้นฐาน ไม่ใช่การเปลี่ยนพฤติกรรม protocol เฉพาะช่องทาง
2. **[lib/prompts.ts](lib/prompts.ts)** — เขียน `<reasoning_protocol>` ข้อ 1 ใหม่ให้ชัดว่ากฎนี้ใช้เฉพาะ**ข้อความทั้งข้อความเป็นแค่คำตอบรับล้วนๆ ไม่มีคำขอ/คำถามอื่นแฝงอยู่เลย** เท่านั้น — ถ้ามีเนื้อหาอื่นปนอยู่แม้จะมีคำลงท้ายสุภาพติดมาด้วยก็ตาม ห้ามใช้กฎนี้เด็ดขาด พร้อมย้ำคำสั่ง "ห้ามเขียนขั้นตอนการคิดออกมาในคำตอบ" ให้ชัดเจนขึ้น (ไม่ใช่แค่ prompt-level fix เดี่ยวๆ แต่เสริมคู่กับตาข่ายโค้ดในข้อ 1) — **ใช้ร่วมกันทุกช่องทางเช่นกัน** เพราะข้อ 1 เดิมไม่มี channel-conditional wrapper อยู่แล้ว (ต่างจาก `step1AckException` ที่ยังคง LINE/Facebook เท่านั้นเหมือนเดิม ไม่ได้แตะ)

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะ `step1AckException` เดิม (ยังทำงานเหมือนเดิมทุกประการ เป็นแค่ข้อยกเว้นเพิ่มเติมต่อจากกฎที่แก้ใหม่) · ไม่แตะ sentinel detection อื่นๆ (`HANDOFF:`, `[OUT_OF_DOMAIN]`, `[NOT_FOUND]`) เลย · regex การตัด reasoning เจาะจงมาก (ต้องขึ้นต้นด้วย "คิดก่อน:" พอดีตามด้วยเว้นบรรทัด) กันตัดข้อความปกติที่ไม่เกี่ยวข้องผิดพลาด

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. logic การตัด reasoning: ข้อความจำลองที่มี "คิดก่อน:...\n\n" นำหน้า → ตัดเหลือแค่คำตอบจริงถูกต้องเป๊ะ ✅ / ข้อความปกติไม่มี "คิดก่อน:" → ไม่ถูกตัด (ไม่มี false positive) ✅
2. เคสจริง: "สอบถามอาการเครื่องหน่อยครับ" → ผลจริง: "ไม่ทราบว่าเครื่องมีอาการอย่างไรคะ รบกวนแจ้งรายละเอียดเพิ่มเติม..." ไม่มี "คิดก่อน:" หลุดมา ไม่ตอบ "ยินดีให้บริการค่ะ" เฉยๆ — **ผ่าน**
3. regression: "ครับ" เดี่ยวๆ ล้วนๆ (ตอบรับจริง) → ยังตอบ "ยินดีให้บริการค่ะ" เหมือนเดิม — **ผ่าน**
4. regression: "ขอบคุณครับ" ปิดท้ายบทสนทนาทั่วไป → ยังตอบ "ยินดีให้บริการค่ะ" เหมือนเดิม — **ผ่าน**
5. regression: `step1AckException` เดิม (ยืนยันอาการด้วย "ใช่ครับผม" ระหว่าง repair_protocol) → ยังไปต่อ HANDOFF ตามปกติ ไม่กระทบจากกฎใหม่ — **ผ่าน**

**ขอบเขต:** แก้ 2 ไฟล์ (`lib/gemini.ts`, `lib/prompts.ts`) — **มีผลทุกช่องทาง (LINE/Facebook/Web Chat)** ต่างจากงานส่วนใหญ่ในเซสชันนี้ที่จำกัดแค่ LINE/Facebook เพราะเป็นการแก้บั๊กพื้นฐานที่กระทบทุกช่องทางเท่ากัน ไม่ใช่การเพิ่มพฤติกรรม protocol เฉพาะทาง

### เพิ่มเติม (2026-08-12 รอบ 2) — เพิ่ม "ค่ะ"/"ครับ" เดี่ยวๆ เข้า step1AckException กันความกำกวมที่เหลืออยู่

**เคสจริงที่พบ:** ลูกค้าส่งรูป+ถามวิธีแกะเครื่อง — บอทยืนยันรุ่น (TCCOM TCM-246) + เจตนาถูกต้องผ่าน `<self_repair_protocol>` ลูกค้าตอบ **"ค่ะ" เดี่ยวๆ** (ไม่มี "ใช่" นำหน้า) — บอทกลับถามรุ่นซ้ำ ("ไม่ทราบว่าวิทยุสื่อสารรุ่นไหนคะ") ทั้งที่เพิ่งระบุไปแล้วเมื่อครู่

**สาเหตุ:** `step1AckException` (เรื่องที่ 44 ด้านบน) ระบุตัวอย่างคำยืนยันไว้แค่แบบที่มี "ใช่" นำหน้าเสมอ (`"ใช่" "ใช่ครับ" "ใช่ค่ะ" "ใช่ครับผม" "ถูกต้อง"`) — ไม่มีตัวอย่าง "ค่ะ"/"ครับ" เดี่ยวๆ ทั้งที่ภาษาไทยจริงตอบแบบนี้ต่อคำถามใช่/ไม่ใช่ก็หมายถึง "ใช่" เหมือนกัน — ทดสอบด้วยโค้ดปัจจุบัน (ก่อนแก้รอบนี้) reproduce บั๊กไม่ได้เลย (4/4 ถูกต้อง) แต่ตรวจพบว่าเคสจริงในภาพเกิดขึ้น**ก่อน**การ push เรื่องที่ 44 ไม่ใช่บั๊กที่ยังเหลืออยู่จริง — แต่เนื่องจากพบช่องว่างของ example list ที่แท้จริง (ไม่ครอบคลุมคำยืนยันแบบไม่มี "ใช่") จึงเพิ่มปิดไว้กันเหตุในอนาคต แม้ non-deterministic จะทำให้ยังไม่เจอบั๊กจริงตอนทดสอบก็ตาม

**วิธีแก้:** เพิ่มตัวอย่าง "ค่ะ"/"ครับ" เดี่ยวๆ (ไม่มี "ใช่" นำหน้า) เข้าไปใน `step1AckException` ([lib/prompts.ts](lib/prompts.ts)) พร้อมอธิบายเหตุผลว่าภาษาไทยจริงตอบแบบนี้ก็หมายถึงยืนยัน — จุดเดียว ไม่กระทบไฟล์อื่น

**ทดสอบก่อน commit (Gemini API จริง):**
- เคสจริง: บอทยืนยันรุ่น+เจตนาจากรูปภาพ ลูกค้าตอบ "ค่ะ" เดี่ยวๆ x5 รอบ → ผลจริง: `HANDOFF: ขอวิธีทำเอง | รายละเอียด: วิธีแกะเครื่อง TCCOM TCM-246` ครบ 5/5 ไม่ถามรุ่นซ้ำเลย — **ผ่าน**
- regression: "ครับ" เดี่ยวๆ ล้วนๆ ไม่มี protocol ค้าง (ตอบรับจริง) → ยังตอบ "ยินดีให้บริการค่ะ" เหมือนเดิม — **ผ่าน**
- regression: repair_protocol ยืนยันด้วย "ใช่ครับผม" (เดิม) → ยังไปต่อ HANDOFF ปกติ ไม่กระทบ — **ผ่าน**

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) จุดเดียว (แก้แค่ตัวอย่างในกฎเดิม ไม่เปลี่ยน logic) branch `channel !== 'web'` เหมือนเดิม

---

## เรื่องที่ 45 — คำตอบยืนยัน protocol อื่นโดนรูปค้างแย่งตีความ (LINE เท่านั้น)

**เคสจริงที่พบ:** บอทถาม `<imei_protocol>` ขั้นที่ 3 "ยืนยันการย้ายกลุ่ม IMEI: ... ถูกต้องไหมคะ?" — ลูกค้ามีรูปที่ส่งไว้ก่อนหน้า (เช่น แนบภาพหน้าจอ IMEI เผื่อไว้) ยังค้างอยู่ใน `img_list` ไม่ถึง 5 นาที แล้วพิมพ์ "ค่ะ" เพื่อยืนยัน — บอทกลับตอบสรุปรูปภาพ+ถามยืนยันเรื่องใหม่แทน ไม่เดินต่อ imei_protocol เลย (ทางตัน) เกิดกับ `repair_protocol` ขั้นที่ 2 และ `self_repair_protocol` ได้เหมือนกัน เพราะทั้งหมดถามยืนยันด้วยรูปแบบประโยคคล้ายกัน

**สาเหตุ:** ขั้นตอน Pending Image Check ใน [app/api/line-webhook/route.ts](app/api/line-webhook/route.ts) เช็คแค่ `if (imgList.length > 0)` — ถือว่าข้อความใดๆ ที่มาหลังรูปภายใน 5 นาทีคือคำบรรยายรูปเสมอ ไม่เคยเช็คเลยว่าข้อความนั้นกำลังตอบคำถามยืนยันที่ protocol อื่นค้างไว้อยู่หรือไม่ — พอเข้าเงื่อนไขนี้ ข้อความยืนยันจะถูกดึงไปรวมกับรูปแล้วยิงเข้า `analyzeImageWithText()` ซึ่งเป็นการเรียก Gemini คนละรอบ คนละ system prompt กับ `generateReply()` ที่มี protocol ทั้งหมดอยู่ ไม่รู้จัก imei_protocol/repair_protocol ที่ค้างอยู่เลย

**วิธีแก้:** เพิ่มฟังก์ชัน `looksLikePendingConfirmReply(history, userMessage)` ใน [app/api/line-webhook/route.ts](app/api/line-webhook/route.ts) เช็คก่อนเข้า Pending Image Check ทั้งขั้น (ครอบทั้ง 2 branch `elapsed </≥ 300000ms`) คืนค่า `true` เมื่อครบ 2 เงื่อนไข:
1. ข้อความบอทล่าสุด (`history` เทิร์นสุดท้ายที่ `role === 'model'`) มี "ใช่ไหมคะ"/"ถูกต้องไหมคะ"/"ยืนยัน...ไหมคะ" — เป็นคำถามยืนยันจริง
2. ข้อความลูกค้าล่าสุด **ทั้งข้อความ** (anchor `^...$` ทั้งสองด้าน) เป็นคำยืนยันล้วนๆ ไม่มีเนื้อหาอื่นปน ("ใช่"/"ใช่ครับ"/"ถูกต้อง"/"ยืนยัน"/"ยกเลิก"/"ค่ะ"/"ครับ" เดี่ยวๆ ฯลฯ) ไม่เกิน 20 ตัวอักษร

เข้าเงื่อนไขแล้ว → ข้ามขั้น Pending Image Check ไปทั้งขั้น ปล่อยข้อความไหลเข้า `generateReply()` ปกติแทน (มี `step1AckException` จากเรื่องที่ 31/44 รองรับส่งต่อคำยืนยันให้ protocol เดิมอยู่แล้ว) — รูปที่ค้างยังอยู่ใน `img_list` ต่อไปเหมือนเดิม (ไม่ถูก DEL) รอข้อความบรรยายรูปจริงๆ ตามมาทีหลัง

**จุดที่ระวังเป็นพิเศษระหว่างออกแบบ:** เดิม regex ใช้ `^(...)` ไม่ปิดท้ายด้วย `$` เช็คแค่ "ขึ้นต้นด้วย" คำยืนยัน — จะทำให้แคปชั่นบรรยายรูปจริงที่ขึ้นต้นด้วย "ครับ"/"ค่ะ" เช่น "ครับ ตามรูปนี้เลย" โดนเข้าใจผิดว่าเป็นคำยืนยันไปด้วย (false positive ทำให้รูปที่ควรถูกวิเคราะห์จริงๆ ไม่ถูกวิเคราะห์) — แก้เป็น anchor เต็มทั้งสองด้านก่อน commit เพื่อจำกัดเฉพาะข้อความที่เป็นคำยืนยันล้วนๆ เท่านั้น

**ทดสอบก่อน commit:**
- `tsc --noEmit` ผ่าน — ✅
- unit test regex ล้วน (ไม่ใช้ Gemini API) 11 เคส: "ค่ะ"/"ครับ"/"ใช่ครับ"/"ถูกต้องค่ะ"/"ยกเลิก"/"ok" เดี่ยวๆ → match (ควรข้าม Pending Image Check) ✅ / "ครับ ตามรูปนี้เลย"/"ค่ะ ใช่รุ่นนี้แหละ"/"รุ่น GM-350" → ไม่ match (ควรรวมรูปตามปกติ) ✅ — ผ่านครบ 11/11

**ทำไมไม่กระทบอย่างอื่น:** เงื่อนไขใหม่ต้องครบทั้ง 3 อย่างพร้อมกันเท่านั้นถึงจะเปลี่ยนพฤติกรรม — (1) มีรูปค้างใน `img_list` (2) ข้อความบอทล่าสุดเป็นคำถามยืนยันชัดเจน (3) ข้อความลูกค้าเป็นคำยืนยันล้วนๆ ไม่เกิน 20 ตัวอักษร — กรณีอื่นทั้งหมด (ไม่มีรูปค้าง, บอทถามคำถามทั่วไป, หรือข้อความลูกค้ามีเนื้อหาอื่นปนมากกว่าคำยืนยันเปล่าๆ) เดินตาม path เดิมทุกประการ · ไม่แตะ `analyzeImageWithText()`/`analyzeImageIntent()`/`step1AckException` เอง เลยแม้แต่จุดเดียว

**ขอบเขต:** แก้ไฟล์เดียว [app/api/line-webhook/route.ts](app/api/line-webhook/route.ts) — **Facebook ไม่ต้องแก้** เพราะ [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts) ไม่มีกลไก `fb_img_list` แบบ "รอข้อความตามหลัง" นี้เลย (Facebook ส่งรูป+ข้อความมาในอีเวนต์เดียวกันเสมอผ่าน caption ประมวลผลทันที ไม่มีขั้นตอนรอ 5 นาทีแบบ LINE)

---

## เรื่องที่ 46 — ขยาย step1AckException ให้ครอบคลุมคำถามยืนยันรูปภาพก่อนเข้า protocol (LINE + Facebook)

**เคสจริงที่พบ:** บอทถามยืนยันเนื้อหารูปภาพจาก `analyzeImageIntent()` (`kind:'other'`) — "เห็นหน้าจอ login ของ SPENDER NETWORK ขึ้นข้อความ 'Wrong password' ใช่ไหมคะ ลูกค้าแจ้งว่าเข้าระบบไม่ได้ใช่ไหมคะ 😊" — คำถามนี้ถามขึ้น**ก่อน**จะเข้า `repair_protocol`/`self_repair_protocol`/`imei_protocol` เลย (เป็นขั้นยืนยันเนื้อหารูปภาพเปล่าๆ) ลูกค้าตอบ 38 นาทีให้หลังด้วย **"ใช่ค้บ"** (สะกดเพี้ยนจาก "ครับ") — บอทกลับตอบ "ยินดีให้บริการค่ะ" ตัดจบสนทนาทันที ไม่เดินหน้าต่อเรื่องที่ค้างไว้เลย

**สาเหตุ:** `step1AckException` (เพิ่มจากเรื่องที่ 31/44) ระบุชื่อไว้แค่ 3 protocol เท่านั้น (`repair_protocol` ขั้นที่ 2, `self_repair_protocol`, `imei_protocol` ขั้นที่ 3) — ไม่ครอบคลุมคำถามยืนยันเนื้อหารูปภาพจาก `analyzeImageIntent()`/`analyzeImageWithText()` ที่ยังไม่เข้า protocol ไหนเลย (เป็นขั้นก่อนหน้า protocol ทั้ง 3 ตัวอีกที) Gemini เลยไม่รู้ว่าคำถามนี้คือ "คำถามที่รอยืนยัน" ตามเงื่อนไขข้อยกเว้น ใช้กฎ acknowledgment-shortcut ทั่วไปแทน (ข้อความ "ใช่ค้บ" ถูกมองว่าเป็นแค่คำตอบรับ) → ตัดจบด้วย "ยินดีให้บริการค่ะ"

**วิธีแก้:** แก้ `step1AckException` ใน [lib/prompts.ts](lib/prompts.ts) จุดเดียว 2 ส่วน:
1. **ขยายเงื่อนไข "อะไรคือคำถามที่รอยืนยัน"** — เพิ่มให้ครอบคลุมคำถามยืนยันเนื้อหารูปภาพที่ยังไม่เข้า protocol ไหนเลยด้วย พร้อมบอกสัญญาณที่สังเกตได้จริงใน history: user turn ก่อนหน้าขึ้นต้นด้วย `"[ลูกค้าส่งรูปภาพ:"` หรือ `"[รูปภาพ+ข้อความ]"` (marker ที่โค้ดใส่ไว้จริงตอนบันทึก history — [route.ts:378](app/api/line-webhook/route.ts#L378), [route.ts:331](app/api/line-webhook/route.ts#L331))
2. **แยกคำสั่ง "ทำอะไรต่อ" เป็น 2 แบบ** — เดิมสั่งแค่ "ส่งต่อให้ protocol ที่ค้างอยู่ทำงานต่อ" ซึ่งอ้างอิงว่าต้องมี protocol ตั้งชื่อไว้แล้ว ใช้ไม่ได้กับเคสรูปภาพที่ยังไม่มี protocol ให้ส่งต่อ — แยกเป็น: protocol มีชื่อ (repair/self_repair/imei) → ส่งต่อทำงานต่อตามเดิม / คำถามยืนยันรูปภาพเปล่าๆ → ถือว่ายืนยันเนื้อหาที่สรุปไว้แล้ว ดำเนินการต่อทันทีตามเนื้อหานั้น (ค้นหาคำตอบปกติถ้าเป็นคำถามทั่วไป, เข้า repair_protocol ถ้าเป็นอาการเสีย, เข้า imei_protocol ถ้ามี IMEI)

ระหว่างทางเพิ่มตัวอย่างคำสะกดเพี้ยน/ย่อ **"ค้บ"/"คับ"** เข้าไปในรายการคำยืนยันด้วย (จากเคสจริงที่ลูกค้าพิมพ์ "ใช่ค้บ" ไม่ใช่ "ใช่ครับ" เป๊ะๆ)

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. เคสจริง: image confirm ("เห็นหน้าจอ login...") + "ใช่ค้บ" → ผลจริง: `HANDOFF: ซ่อม/เคลม | รุ่น: ไม่ระบุ (รอช่างตรวจสอบเพิ่มเติม) | อาการ: เข้าสู่ระบบ SPENDER NETWORK ขึ้น Wrong password` ไม่ตัดจบแล้ว — **ผ่าน**
2. regression: "ครับ" เดี่ยวๆ ไม่มี protocol ค้าง (ตอบรับจริง) → ยังตอบ "ยินดีให้บริการค่ะ" เหมือนเดิม — **ผ่าน**
3. regression: `repair_protocol` ยืนยันด้วย "ใช่ครับผม" (เดิม) → ยังไปต่อ `HANDOFF: ซ่อม/เคลม` ปกติ — **ผ่าน**
4. image confirm + คำสะกดเพี้ยน "คับ" → ผลจริง: `HANDOFF: ซ่อม/เคลม | รุ่น: TC-5M | อาการ: ขึ้นข้อความ login timeout` — **ผ่าน**
5. regression เรื่องที่ 44 เดิม ("สอบถามอาการเครื่องหน่อยครับ" ไม่มี protocol ค้าง) → ยังถามอาการต่อ ไม่ตัดจบ — **ผ่าน**

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะโค้ด `app/api/line-webhook/route.ts`/`app/api/fb-webhook/route.ts` เลยแม้แต่บรรทัดเดียว (แก้ prompt ล้วนๆ) · ไม่แตะเงื่อนไข acknowledgment-shortcut ข้ออื่นหรือ `reasoning_protocol` ข้ออื่นเลย · ตัวอย่างคำยืนยันเดิม ("ใช่" "ใช่ครับ" "ใช่ค่ะ" ฯลฯ) ยังอยู่ครบ แค่เพิ่มเงื่อนไข/ตัวอย่างเสริมเข้าไป

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) จุดเดียว (`step1AckException`) branch `channel !== 'web'` เหมือนเดิม (LINE/Facebook เท่านั้น)

---

## เรื่องที่ 47 — กัน Gemini ผสมข้อมูลจาก FAQ คนละแถวเข้าด้วยกัน (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าถามเรื่องย้ายเครื่องเข้าบัญชี SAS ของตัวเอง — บอทตอบ FAQ แถว "เปลี่ยนผู้ดูแลระบบ" (`spender-network-16`) ถูกแถว แต่**ผสมค่าธรรมเนียม "500 บาท/เครื่อง" เข้ามาด้วย** ทั้งที่ไม่เกี่ยวกับคำถามลูกค้าเลย

**การตรวจสอบ:** ดึงชีต FAQ จริงมาดู (ผ่านลิงก์ Publish to web ที่ผู้ใช้แชร์) พบว่าแถว `spender-network-16` (คอลัมน์ `answer`) **ไม่มีเลข 500 บาทอยู่เลย**:
> "กรณีย้าย Department หรือเปลี่ยนผู้ดูแลระบบในระบบ Spender Network รบกวนแจ้งข้อมูลดังนี้ค่ะ 1.เจ้าของเดิมและเจ้าของใหม่ (ชื่อ, เบอร์โทร, อีเมล) 2.หมายเลข IMEI ของเครื่อง"

ค้นหาคำว่า "500" ทั่วทั้งชีตแล้วเจอว่าเลขนี้มาจาก **แถวอื่นโดยสิ้นเชิง** — `spender-network-11` เรื่อง "เปลี่ยนแพ็คเกจ" (คนละหัวข้อกับเปลี่ยนผู้ดูแลระบบ) พิสูจน์ว่า **Gemini หยิบข้อมูลจริงจาก 2 แถว FAQ ที่ไม่เกี่ยวข้องกันมาผสมเป็นคำตอบเดียว** ไม่ใช่แค่จับคู่ FAQ ผิดแถวเฉยๆ

**สาเหตุ:** กฎเดิมใน `<guardrails>` ("ห้ามแต่งข้อมูลที่ไม่มีใน `<faq>`" — [lib/prompts.ts:141](lib/prompts.ts#L141)) ไม่ครอบคลุมเคสนี้ เพราะเลข 500 บาท**มีอยู่จริงใน `<faq>`** (แค่อยู่คนละแถว ไม่ใช่การแต่งขึ้นเอง) — ไม่มีกฎใดห้าม Gemini หยิบรายละเอียดจากแถวหนึ่งมาปนกับคำตอบของอีกแถวหนึ่งเลย

**วิธีแก้:** เพิ่มกฎใหม่เข้า `<guardrails>` ใน [lib/prompts.ts](lib/prompts.ts) (ไม่มี channel branching อยู่แล้ว จึงมีผลทุกช่องทางเท่ากัน — LINE/Facebook/Web Chat):
> "`<faq>` แต่ละแถวเป็นชุดคำถาม-คำตอบอิสระจากกัน ห้ามหยิบรายละเอียด (ตัวเลข ค่าธรรมเนียม ขั้นตอน เงื่อนไข) จากแถวอื่นมาผสมกับคำตอบของแถวที่กำลังตอบเด็ดขาด แม้หัวข้อจะดูใกล้เคียงหรือเกี่ยวข้องกันก็ตาม ... ให้ตอบตามเนื้อหาของแถวที่ตรงกับคำถามลูกค้าที่สุดเพียงแถวเดียวเท่านั้น"

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local` พร้อมจำลอง `<faq>` จากชีตจริง 3 แถว, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. เคสจริง: ถามเรื่องย้าย Department/เปลี่ยนผู้ดูแลระบบ → ผลจริง: ตอบตามแถว `spender-network-16` ล้วนๆ ไม่มี "500" ปนมาแล้ว — **ผ่าน**
2. regression: ถามเรื่องเปลี่ยนแพ็คเกจตรงๆ → ยังตอบ "500 บาท/เครื่อง" ถูกต้องตามแถวของมันเอง (ไม่ได้ถูกลบทิ้งไปด้วย) — **ผ่าน**
3. regression: ถามลงทะเบียนเครื่อง (แถวที่ 3 ไม่เกี่ยวกับ 2 แถวแรกเลย) → ตอบถูกต้องปกติ ไม่สับสน — **ผ่าน**

**ทำไมไม่กระทบอย่างอื่น:** เป็นการ**เพิ่ม**กฎใหม่ 1 ข้อเข้า `<guardrails>` เท่านั้น ไม่แก้/ลบกฎเดิมข้อไหนเลย · ไม่แตะโค้ด webhook/`lib/sheet.ts`/`lib/gemini.ts` เลยแม้แต่บรรทัดเดียว · ไม่มี channel-conditional wrapper (เหมือนกฎ `<guardrails>` ข้ออื่นทั้งหมดที่มีอยู่แล้ว) จึงไม่ต้องแยกทดสอบราย channel

**หมายเหตุ:** ยังมีอีกชั้นหนึ่งของปัญหาเคสนี้ที่ไม่ได้แก้ในรอบนี้ — FAQ แถว `spender-network-16` เอง (ไม่นับเรื่องผสมแถว) ก็ยังไม่ตรงกับสถานการณ์ "ย้ายเครื่องเข้าบัญชีตัวเอง เจ้าของเดิม-ใหม่เป็นคนเดียวกัน" อยู่ดี (คำตอบเขียนไว้สำหรับกรณีเปลี่ยนเจ้าของเป็นคนอื่น) — เป็นปัญหาฝั่งเนื้อหาชีต ไม่ใช่โค้ด ผู้ใช้รับจะไปแก้เนื้อหาเองแยกต่างหาก

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) จุดเดียว (`<guardrails>`) — **มีผลทุกช่องทาง** ต่างจากงานส่วนใหญ่ในเซสชันนี้ที่จำกัดแค่ LINE/Facebook เพราะเป็นการแก้บั๊กพื้นฐานเรื่องการอ่าน `<faq>` ที่ใช้ร่วมกันทุกช่องทางเท่ากัน

---

## เรื่องที่ 48 — กันหยิบราคาตัวเครื่องเต็มมาตอบแทนราคาอะไหล่/อุปกรณ์เสริม (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าถาม "แบตราคาเท่าไหร่" — บอทถามกลับถูกต้องว่า "ลูกค้าสอบถามเป็นแบตเตอรี่รุ่นไหนคะ" ลูกค้าตอบกลับมาแค่ **"ix5"** (ชื่อรุ่นเปล่าๆ ไม่มีคำว่า "แบต" ซ้ำ) — บอทตอบว่า **"Speeder SP-IX5 Plus ราคา 2,200 บาท ค่ะ..."** ซึ่งเป็นราคา**ตัวเครื่องวิทยุเต็ม** ไม่ใช่ราคาแบตเตอรี่เลย ทั้งที่ลูกค้าถามเรื่องแบตแยกต่างหากจากตัวเครื่อง

**สาเหตุ:** ขั้นที่ 2 ของ `<reasoning_protocol>` ("ถ้าเป็นข้อความสั้นๆ เช่น ชื่อรุ่น...ให้ตีความว่าลูกค้าต้องการทราบราคาหรือข้อมูลรุ่นนั้น แล้วค้น `<faq>` ทันที") ตีความข้อความสั้นๆ "ix5" ว่าเป็นคำถามราคา**ตัวเครื่อง**ใหม่เสมอ ไม่เคยเช็ค history ว่าบอทเพิ่งถามยืนยันรุ่นเพื่อตอบคำถามเรื่อง**อะไหล่/อุปกรณ์เสริม**ไปก่อนหน้า — บริบท "แบต" จากเทิร์นก่อนหน้าจึงหายไปหมด หลุดไปเข้า `<product_template>` จับคู่กับสินค้าในชีตตรงๆ แล้วตอบราคาตัวเครื่องเต็มแทน — สาเหตุรอง: ชีตไม่มีข้อมูลราคาอะไหล่แยกต่างหากเลย (มีแต่ราคาตัวเครื่องเต็ม) และไม่มีกฎห้ามหยิบราคาที่ใกล้เคียงที่สุดมาตอบแทนแบบที่มีอยู่แล้วสำหรับผลค้นเว็บ (`search_tool`)

**วิธีแก้:** แก้ 2 จุดใน [lib/prompts.ts](lib/prompts.ts) — **ใช้ร่วมกันทุกช่องทาง** (ต่างจาก `step2AmbiguousRefException`/`imeiSnDistinctionNote` ที่จำกัด LINE/Facebook เพราะเป็นปัญหาการตอบราคาสินค้าทั่วไปที่ใช้ร่วมกันทุก channel ไม่ใช่ protocol เฉพาะทาง):
1. **`<guardrails>`** — เพิ่มกฎห้ามหยิบราคาตัวเครื่องเต็มมาตอบแทนราคาอะไหล่/อุปกรณ์เสริม (แบตเตอรี่ เสาอากาศ สายชาร์จ เคส คลิปหนีบ ฯลฯ) เมื่อไม่มีราคาแยกใน `<faq>` — ให้ตอบว่าไม่มีข้อมูลราคาส่วนนี้แยกไว้ แนะนำติดต่อแอดมินแทน
2. **`step2AccessoryContextException`** (ขั้นที่ 2 ของ `<reasoning_protocol>`) — ถ้าบอทเพิ่งถามยืนยัน "รุ่นไหน" เพื่อตอบคำถามอะไหล่/อุปกรณ์เสริม แล้วลูกค้าตอบกลับมาด้วยชื่อรุ่นเปล่าๆ ห้ามตีความว่าลูกค้าถามราคาตัวเครื่องเต็มใหม่ ให้คงบริบทอะไหล่/อุปกรณ์เสริมไว้ แล้วทำตามกฎห้ามหยิบราคาตัวเครื่องมาตอบแทนใน `<guardrails>`

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. เคสจริง: "แบตราคาเท่าไหร่" → บอทถาม "รุ่นไหน" → ลูกค้าตอบ "ix5" → ผลจริง: "ขออภัยค่ะน้องใจดีไม่มีข้อมูลราคาแบตเตอรี่ของรุ่น SP-IX5 Plus แยกไว้โดยเฉพาะค่ะ 🙏 รบกวนสอบถามราคาแบตเตอรี่กับแอดมิน..." ไม่หยิบราคาตัวเครื่อง (2,200 บาท) มาตอบแทนแล้ว — **ผ่าน**
2. regression: "ix5 ราคาเท่าไหร่" ถามตรงๆ (ไม่มีคำว่าแบต ถามราคาตัวเครื่องจริง) → ยังตอบ "Speeder SP-IX5 Plus ราคา 2200 บาทค่ะ" ถูกต้องตามปกติ — **ผ่าน**
3. regression: `repair_protocol` เดิม (บอทถามยืนยันอาการ ลูกค้าตอบรุ่นเปล่าๆ "TC-11HW") → ยังไปต่อ `HANDOFF: ซ่อม/เคลม | รุ่น: TC-11HW | อาการ: เปิดไม่ติด` ปกติ ไม่หลุดไปตอบราคา — **ผ่าน**

**ทำไมไม่กระทบอย่างอื่น:** เป็นการ**เพิ่ม**กฎ/ข้อยกเว้นใหม่เท่านั้น ไม่แก้/ลบกฎเดิมข้อไหนเลย · ตรวจสอบแล้วว่าไม่ซ้อนทับกับข้อยกเว้นเดิม (`repairStep2`'s exception ใช้เฉพาะบริบทซ่อม/อาการเสีย, `step2AmbiguousRefException` ใช้เฉพาะกรณีอ้างอิงกำกวมแบบไม่มีชื่อรุ่นเลย — เคสนี้เป็นสถานการณ์ใหม่ที่ยังไม่มีกฎไหนครอบคลุมมาก่อน) · ไม่แตะโค้ด webhook ไฟล์ไหนเลย (แก้ prompt ล้วนๆ)

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) 2 จุด (`<guardrails>`, ขั้นที่ 2 ของ `<reasoning_protocol>`) — **มีผลทุกช่องทาง** (LINE/Facebook/Web Chat) เหมือนเรื่องที่ 47

### เพิ่มเติม (2026-08-14 รอบ 2) — กฎที่เพิ่มไปข้ามขั้นค้นเว็บก่อนแจ้งแอดมิน

**พบหลัง commit แรกไปแล้ว:** กฎที่เพิ่มใน `<guardrails>` เขียนไว้แค่ *"ไม่มีราคาแยกใน `<faq>` ... ให้ตอบว่าไม่มีข้อมูล แนะนำติดต่อแอดมิน"* — ไม่ได้สั่งให้ลอง `search_spender_specs` ก่อน ทำให้ Gemini ข้ามขั้นค้นเว็บไปเลย ต่างจาก flow หลักที่ยืนยันไว้เสมอว่าต้องเป็น **ชีต → เว็บ → handoff** — ยืนยันจาก log การทดสอบเดิม (ไม่มี event `search_spender_specs` โผล่มาเลย ตอบเสร็จใน 2.7 วินาที ซึ่งเร็วเกินกว่าจะผ่านการค้นเว็บจริง)

**วิธีแก้:** เพิ่มประโยค *"ให้เรียก search_spender_specs ค้นหาราคาอะไหล่/อุปกรณ์เสริมนั้นก่อนเสมอเหมือนขั้นตอนปกติ ถ้าเจอราคาที่ตรงกับอะไหล่นั้นโดยตรงให้ตอบได้เลย ถ้าไม่เจอทั้ง `<faq>` และผลค้นเว็บ ค่อยตอบว่าไม่มีข้อมูล...แนะนำติดต่อแอดมิน"* เข้าไปในกฎเดิมจุดเดียว

**ทดสอบ:** log ยืนยันมี event `search_spender_specs` (query: "แบตเตอรี่ Speeder SP-IX5 Plus ราคา") เกิดขึ้นก่อนตอบ "ไม่พบข้อมูล...แนะนำติดต่อแอดมิน" แล้ว — **ผ่าน**

---

## เรื่องที่ 49 — กันหยิบ FAQ ทั่วไปที่ไม่เกี่ยวข้องมาแทนการค้นเว็บ + สอนคำสะกดเพี้ยน "แบด" (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าถาม **"แบด 741 ราคาเท่าไหร่คะ"** (สะกด "แบด" แทน "แบต" + ถามครบในข้อความเดียว ไม่ใช่ 2 เทิร์นเหมือนเรื่องที่ 48) — บอทตอบด้วย FAQ ทั่วไปเรื่อง **"ช่องทางติดต่อ"** (`contact-hours-04`: "รบกวนสอบถามรายละเอียดเพิ่มเติมได้ที่ แอดไลน์ @spenderclub...") ทั้งที่ไม่เกี่ยวกับคำถามเรื่องราคาแบตเลย แทนที่จะไปค้นเว็บ (`search_spender_specs`) ตามขั้นตอนปกติ

**สาเหตุ:** สองอย่างประกอบกัน:
1. ขั้นที่ 5-6 ของ `<reasoning_protocol>` ("ค้นหาคำตอบใน `<faq>` ถ้ามี ตอบจาก `<faq>` ทันที" / "ถ้าไม่มีใน `<faq>` → เรียกใช้ `search_spender_specs` ทันทีเสมอ ห้ามข้าม") ไม่มีข้อห้ามหยิบแถว FAQ ทั่วไปที่ไม่เกี่ยวข้องมาตอบแทนตอนไม่มั่นใจ — มีกฎแบบนี้อยู่แล้วสำหรับ**ผลค้นเว็บ**เท่านั้น (`search_tool`: "ถ้าผลค้นหาไม่ตรงกับคำถามโดยตรง...ห้ามหยิบสินค้าใกล้เคียงมาตอบแทน...ให้ตอบว่าไม่พบข้อมูล") แต่ไม่มีกฎเดียวกันสำหรับขั้นตอนเช็ค `<faq>` เอง
2. Gemini อาจไม่รู้จักคำสะกดเพี้ยน "แบด" ว่าหมายถึง "แบต"/แบตเตอรี่ ทำให้ตีความคำถามไม่ออกตั้งแต่ต้น พอไม่มั่นใจจึงเลือกหยิบ FAQ ที่ดูเป็น "คำตอบปลอดภัยทั่วไป" แทนการค้นเว็บ

**การตรวจสอบ:** ทดสอบซ้ำด้วยคำเดียวกัน ("แบด 751 เท่าไหร่") ครั้งถัดมากลับตอบราคาแบตเตอรี่ถูกต้อง (ค้นเว็บเจอจริง TC-751H Plus 910 บาท) — ยืนยันว่าเป็นความไม่แน่นอนของ Gemini (non-deterministic) ไม่ใช่บั๊กที่เกิดทุกครั้ง แต่ยังเป็นช่องโหว่ที่ควรปิดเพื่อลดโอกาสเกิดซ้ำ

**วิธีแก้:** แก้ 2 จุดใน `<guardrails>` ([lib/prompts.ts](lib/prompts.ts), ใช้ร่วมกันทุกช่องทาง):
1. เพิ่มตัวอย่างคำสะกดเพี้ยน **"แบด" = "แบต"/แบตเตอรี่** เข้าไปในกฎเดิมเรื่องราคาอะไหล่/อุปกรณ์เสริม (เรื่องที่ 48)
2. เพิ่มกฎใหม่: ตอนค้นหาคำตอบใน `<faq>` (ขั้นที่ 5) ถ้าไม่มีแถวไหนตรงกับคำถามลูกค้าโดยตรงจริงๆ ห้ามหยิบแถว FAQ ทั่วไปที่ไม่เกี่ยวข้องมาตอบแทนเด็ดขาด ให้ถือว่าไม่พบใน `<faq>` แล้วไปเรียก `search_spender_specs` ต่อทันทีตามขั้นที่ 6 เสมอ

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. เคสจริง: "แบด 741 ราคาเท่าไหร่คะ" → log ยืนยันเรียก `search_spender_specs` (query: "แบตเตอรี่ SPENDER TC-741H Plus ราคา") ก่อน แล้วตอบ "ไม่พบข้อมูลราคาแบตเตอรี่...แยกต่างหาก...รบกวนสอบถามกับแอดมิน" ไม่หยิบ FAQ ติดต่อทั่วไปมาแทนแล้ว — **ผ่าน**
2. เคสจริง 2: "แบด 751 เท่าไหร่" → พฤติกรรมเดียวกัน ค้นเว็บก่อนเสมอ — **ผ่าน**
3. regression: ถามเบอร์ติดต่อจริงๆ "มีเบอร์โทรติดต่อไหมคะ" → ยังตอบเบอร์ติดต่อถูกต้องตามปกติ (คำถามนี้ควรได้ FAQ นี้จริง ไม่ได้ถูกบล็อกไปด้วย) — **ผ่าน**
4. regression: "TC-741H Plus ราคาเท่าไหร่" ถามตัวเครื่องตรงๆ → ยังตอบราคาตัวเครื่องถูกต้องปกติ — **ผ่าน**

**ทำไมไม่กระทบอย่างอื่น:** เป็นการ**เพิ่ม**กฎ/ตัวอย่างใหม่เท่านั้น ไม่แก้/ลบกฎเดิมข้อไหนเลย · กฎใหม่ข้อ 2 จำกัดเฉพาะกรณี "ไม่มีแถวไหนตรงกับคำถามโดยตรงจริงๆ" เท่านั้น คำถามที่ตรงกับ FAQ จริง (เช่น ถามเบอร์ติดต่อตรงๆ) ยังตอบจาก `<faq>` ได้ปกติไม่ถูกบล็อก (ยืนยันจาก regression ข้อ 3) · ไม่แตะโค้ด webhook ไฟล์ไหนเลย (แก้ prompt ล้วนๆ)

**หมายเหตุ:** เป็นการแก้ด้วย prompt เหมือนทุกครั้ง **ไม่การันตี 100%** เพราะ Gemini ไม่ตอบเหมือนกันเป๊ะทุกครั้ง (non-deterministic) — ช่วยลดโอกาสเกิดซ้ำได้มาก แต่ยังมีโอกาสพลาดได้บ้างเป็นบางครั้ง โดยเฉพาะคำสะกดเพี้ยนรูปแบบอื่นที่ยังไม่ได้สอนไว้

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) จุดเดียว (`<guardrails>`) — **มีผลทุกช่องทาง** (LINE/Facebook/Web Chat) เหมือนเรื่องที่ 47-48

---

## เรื่องที่ 50 — เปลี่ยนกฎอะไหล่/อุปกรณ์เสริมจากลิสต์ตัวอย่างเป็นนิยามด้วยหลักการ (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าถาม **"หูฟัง"** — บอทถามกลับถูกต้องว่า "ลูกค้าสอบถามเป็นหูฟังวิทยุสื่อสารรุ่นไหนคะ" ลูกค้าตอบกลับมาแค่ **"id2462"** (ชื่อรุ่นเปล่าๆ) — บอทตอบ **"Spender ID2462 ราคา 1,650 บาท หรือแพ็คคู่ 2,990..."** ซึ่งเป็นราคา**ตัวเครื่องวิทยุเต็ม** ไม่ใช่ราคาหูฟังเลย — เป็นบั๊กรูปแบบเดียวกับเรื่องที่ 48 (แบตเตอรี่) เป๊ะๆ

**สาเหตุ:** กฎที่เขียนไว้ตอนแก้เรื่องที่ 48 ยกตัวอย่างอุปกรณ์เสริมไว้แค่ "แบตเตอรี่ เสาอากาศ สายชาร์จ เคส คลิปหนีบ" — **ไม่มี "หูฟัง" อยู่ในลิสต์** แม้จะเขียนคำว่า "เช่น" (ไม่จำกัดแค่นี้) แต่ Gemini มักตีความรายการตัวอย่างแบบแคบเกินจริง — เจอปัญหารูปแบบนี้ซ้ำหลายรอบแล้วในเซสชันนี้ (เช่น `step1AckException` ที่เคยระบุแค่ 3 protocol ก็ไม่ครอบคลุมเคสรูปภาพจนต้องแก้เพิ่มในเรื่องที่ 46) — การเขียนกฎแบบ "ยกตัวอย่างเป็นรายการ" จะตามไม่ทันอุปกรณ์เสริมทุกแบบที่ร้านมีเสมอ (แก้เพิ่มทีละชนิดจะเจอปัญหาเดิมซ้ำเรื่อยๆ เมื่อลูกค้าถามอุปกรณ์เสริมชนิดใหม่ที่ยังไม่อยู่ในลิสต์)

**วิธีแก้:** เปลี่ยนวิธีเขียนกฎ 2 จุดใน [lib/prompts.ts](lib/prompts.ts) จาก **"ยกตัวอย่างเป็นรายการ"** เป็น **"นิยามด้วยหลักการ"** แทน — เพิ่มวลี "ชนิดใดก็ตาม" / "ไม่จำกัดเฉพาะตัวอย่างต่อไปนี้" ให้ชัดเจนขึ้นว่าครอบคลุมอะไหล่/อุปกรณ์เสริม**ทุกประเภท** ไม่ใช่แค่รายการที่ยกตัวอย่างไว้ (ยังคงยกตัวอย่างไว้เพื่อความชัดเจน แค่เน้นย้ำว่าไม่จำกัดแค่นั้น) พร้อมเพิ่ม "หูฟัง"/"ไมโครโฟน" เข้าไปในตัวอย่างด้วย:
1. **`<guardrails>`** — กฎห้ามหยิบราคาตัวเครื่องเต็มมาตอบแทนราคาอะไหล่ (เรื่องที่ 48)
2. **`step2AccessoryContextException`** — ข้อยกเว้นรักษาบริบทอะไหล่/อุปกรณ์เสริมตอนลูกค้าตอบรุ่นเปล่าๆ

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. เคสจริง: "หูฟัง" → บอทถาม "รุ่นไหน" → ลูกค้าตอบ "id2462" → ผลจริง: "ขออภัยค่ะ ยังไม่พบข้อมูลราคาหูฟังสำหรับรุ่น Spender ID2462 โดยตรง 🙏 หากลูกค้าต้องการข้อมูลเพิ่มเติม สามารถสอบถามกับแอดมินได้เลยนะคะ" ไม่หยิบราคาตัวเครื่อง (1,650 บาท) มาตอบแทนแล้ว — **ผ่าน**
2. regression: "id2462 ราคาเท่าไหร่" ถามตัวเครื่องตรงๆ → ยังตอบ "Spender ID2462 ราคา 1650 บาท..." ถูกต้องปกติ — **ผ่าน**
3. regression: เคสเดิมจากเรื่องที่ 48 ("แบตราคาเท่าไหร่" → "ix5") → ยังผ่านเหมือนเดิม ไม่กระทบ — **ผ่าน**

**ทำไมไม่กระทบอย่างอื่น:** เป็นการ**ปรับถ้อยคำ**กฎเดิมให้กว้างขึ้นเท่านั้น ไม่ได้เขียนกฎใหม่ที่ไปกระทบพฤติกรรมอื่น — เงื่อนไขที่ทำให้กฎทำงานยังต้องอาศัยคำพูดของลูกค้าเองว่าถามถึงอะไหล่/อุปกรณ์เสริม ไม่ได้ไปงับคำถามราคาตัวเครื่องปกติที่ไม่มีคำพวกนี้เลย (ยืนยันจาก regression ข้อ 2) · ไม่แตะโค้ด webhook ไฟล์ไหนเลย

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) 2 จุด (`<guardrails>`, `step2AccessoryContextException`) — **มีผลทุกช่องทาง** เหมือนเรื่องที่ 47-49

---

## เรื่องที่ 51 — กันฟันธงว่า "ไม่มีขายแยก" ผิดๆ ตอนหาไม่เจอด้วยชื่อจากรูป (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าส่งรูปอะแดปเตอร์ชำรุด บอทอ่านป้ายบนตัวเครื่องได้ว่า **"ASD-X45"** ค้นเว็บเจอว่าเป็นอะแดปเตอร์ที่ขายเป็นชุดกับแท่นชาร์จ **"HERO-X4/X5"** — ลูกค้าถามต่อ **"ซื้อแยกเฉพาะ adapter ได้ไหมครับ"** บอทตอบว่า **"ไม่มีจำหน่ายเฉพาะอะแดปเตอร์แยกต่างหากจากชุดแท่นชาร์จนะคะ"** ทั้งที่**ร้านมีขายแยกจริง** (340 บาท ที่ `/product/adapter-spender-hero-x4-x5/`)

**การตรวจสอบ:** ตรวจสอบร่วมกับผู้ใช้หลายรอบ (ดึงชีต, ค้น Google `site:spenderclub.com ASD-X45`, ดูหน้า WordPress admin จริงทั้งหน้าขายแยกและหน้าขายชุด) ยืนยันได้ชัดเจนว่า **"ASD-X45" เป็นรหัสภายใน (พิมพ์บนตัวเครื่องจริง) ที่ไม่ปรากฏบนหน้าเว็บที่ไหนเลยสักหน้าเดียว** — ทั้งหน้าขายแยกและหน้าขายชุดใช้ชื่อ **"HERO-X4/X5"** เหมือนกันหมด ไม่มีหน้าไหนใช้คำว่า "ASD-X45" เลย

**สาเหตุ:** บอทค้นหาด้วย "ASD-X45" ซ้ำทุกรอบ (ยึดตามชื่อจากรูปที่อ่านมาตั้งแต่ต้น เพราะเป็นชื่อเฉพาะล่าสุดที่ปรากฏในบทสนทนา) จึงหาไม่เจอซ้ำๆ ไม่ว่าจะถามกี่รอบ — แล้ว**ฟันธงผิดว่า "ไม่มี"** ทั้งที่ควรตอบแค่ว่า "หาไม่เจอด้วยชื่อนี้" เท่านั้น เป็นการสรุป "ไม่พบหลักฐานว่ามี" เป็น "ยืนยันว่าไม่มี" ซึ่งเป็นคนละเรื่องกัน — กฎเดิม (`search_tool`: "ห้ามหยิบสินค้าใกล้เคียง/รุ่นอื่นมาตอบแทน") ครอบคลุมแค่กรณีค้นแล้วเจอสินค้าอื่น ไม่ครอบคลุมกรณีค้นแล้วไม่เจออะไรเลยแต่ไปฟันธงเชิงลบแบบนี้

**ทางเลือกที่พิจารณาแล้วไม่เลือก:** เพิ่มคำว่า "ASD-X45" ลงในหน้าเว็บขายแยก (Meta Description/เนื้อหาสินค้า) — ผู้ใช้ทักท้วงว่า "ASD-X45" อาจเป็นรหัสจากโรงงานที่ใช้ซ้ำกับอะไหล่รุ่นอื่นที่ไม่ใช่ตัวนี้ ถ้าผูกรหัสนี้ไว้กับสินค้าตัวเดียว เสี่ยงจับคู่สินค้าผิดกับลูกค้าที่มีอะแดปเตอร์รุ่นอื่นที่บังเอิญมีรหัสเดียวกันในอนาคต — จึงเลือกแก้ที่ฝั่งพรอมต์ทั้งหมดแทน ไม่พึ่งพาความแม่นยำของรหัสภายในที่ไม่แน่ใจว่าไม่ซ้ำ

**วิธีแก้:** แก้ 2 จุดใน `<guardrails>` ([lib/prompts.ts](lib/prompts.ts), ต่อท้ายกฎเรื่องที่ 48-50, ใช้ร่วมกันทุกช่องทาง):
1. **ขยายขอบเขตจาก "ราคา" เป็น "ราคา/การมีขายแยก"** — ห้ามฟันธงว่า "ไม่มี" จากการหาไม่เจอเพียงอย่างเดียว ให้ตอบว่า "ไม่พบข้อมูลยืนยันชัดเจน" แทนเสมอ แนะนำติดต่อแอดมิน
2. **อนุญาตสลับไปใช้ชื่อที่เคยยืนยันแล้วจริงในบทสนทนา** (เช่น "HERO-X4/X5" ที่เคยค้นเจอแล้วว่าตรงกับ "ASD-X45") แทนชื่อตั้งต้นจากรูปได้ — เมื่อถามยืนยันให้ใช้รูปแบบ "...ใช่ไหมคะ" ตามสไตล์มาตรฐานของระบบ (แทนคำถามปลายเปิด เพื่อให้เข้ากับกลไก `step1AckException`/`step2AccessoryContextException` ที่มีอยู่แล้ว) — **มีกันชนสำคัญ:** ใช้ได้เฉพาะกรณีเคยยืนยันความสัมพันธ์นี้มาแล้วจริงในบทสนทนาเดียวกันเท่านั้น ห้ามนำไปอ้างเพื่อหยิบสินค้าใกล้เคียง/รุ่นอื่นมาตอบแทนตอนค้นหาไม่เจอเด็ดขาด (กฎเดิมใน `<search_tool>` ยังบังคับใช้เต็มรูปแบบ)

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. เคสจริง: ยืนยันชื่อ "ASD-X45" แล้วถาม "ซื้อแยกเฉพาะ adapter ได้ไม่ครับ" → ผลจริง: "ไม่พบข้อมูลยืนยันชัดเจนว่าสามารถซื้ออะแดปเตอร์แยกต่างหากได้ค่ะ 🙏 รบกวนสอบถามกับแอดมินโดยตรง..." ไม่ฟันธงว่า "ไม่มี" อีกแล้ว — **ผ่าน**
2. regression: ถามราคาชุดตรงๆ → ยังตอบ "ชุดแท่นชาร์จ + อะแดปเตอร์...ราคา 680 บาท" ถูกต้องปกติ — **ผ่าน**
3. regression: ถามอะไหล่ที่ `<faq>` ไม่มีข้อมูลเลย (ไม่มี history เกี่ยวข้อง) → ยังค้นเว็บก่อนแล้วตอบ "ไม่พบข้อมูล...รอแอดมิน" ตามปกติ ไม่ฟันธงผิด — **ผ่าน**

**ทำไมไม่กระทบอย่างอื่น:** เป็นการ**ขยาย**กฎเดิม (เรื่องที่ 48-50) + **เพิ่ม**กฎใหม่ที่มีเงื่อนไขจำกัดชัดเจน (ต้องเคยยืนยันความสัมพันธ์มาก่อนจริงเท่านั้น) — ไม่แก้/ลบกฎเดิมข้อไหนเลย · กฎห้ามหยิบสินค้าใกล้เคียงมาตอบแทนใน `<search_tool>` ยังคงบังคับใช้เต็มรูปแบบ ตรวจสอบแล้วว่าไม่ขัดแย้งกัน · ไม่แตะโค้ด webhook ไฟล์ไหนเลย (แก้ prompt ล้วนๆ)

**ขอบเขต:** แก้ไฟล์เดียว [lib/prompts.ts](lib/prompts.ts) จุดเดียว (`<guardrails>`) — **มีผลทุกช่องทาง** (LINE/Facebook/Web Chat) เหมือนเรื่องที่ 47-50

---

## เรื่องที่ 52 — กันผสมราคา/ลิงก์ข้ามผลค้นเว็บคนละหน้า (ทุกช่องทาง)

**เคสจริงที่พบ (ต่อเนื่องจากเรื่องที่ 51):** ลูกค้าถาม **"มีอะแดปเตอร์รุ่น 741ไหม"** — บอทค้นเว็บเจอถูกต้อง ตอบพร้อมลิงก์หน้าขายแยก (`adapter-spender-tc-741h-751h`) — ลูกค้าถามต่อ **"ราคาเท่าไหร่"** — บอทตอบ **"680 บาท"** พร้อม**ลิงก์เดิม** (หน้าขายแยก) — แต่ผู้ใช้ยืนยันจากหน้าเว็บจริงว่า **ราคาของหน้าขายแยกคือ 340 บาท** ส่วน 680 บาทคือราคาของ**อีกหน้าหนึ่ง** (ชุดแท่นชาร์จ+อะแดปเตอร์) — สินค้า TC-741H/751H มีจริง 3 รายการชื่อคล้ายกันมาก: อะแดปเตอร์อย่างเดียว (340), แท่นชาร์จอย่างเดียว (340), ชุดทั้งสอง (680)

**สาเหตุ:** พิสูจน์ว่า Gemini หยิบราคาจากผลค้นเว็บหน้าหนึ่งมาผสมกับลิงก์ของอีกหน้าหนึ่งที่ไม่ตรงกัน — **เป็นปัญหารูปแบบเดียวกับเรื่องที่ 47 (ผสมข้อมูลข้าม FAQ คนละแถว) ทุกประการ แค่เกิดกับผลค้นเว็บแทน** — พบต้นตอระดับโค้ดด้วย: [lib/websearch.ts](lib/websearch.ts) ฟังก์ชัน `searchWithSerper()` เดิมรวมชื่อ+คำอธิบายของ**ทุกผลลัพธ์**เป็นก้อนเดียวให้ Gemini อ่าน แต่ส่งลิงก์แยกกลับมาแค่ของ**ผลลัพธ์แรกเท่านั้น** (`organic[0].link`) — ทำให้ Gemini ไม่มีทางรู้เลยว่าลิงก์ไหนคู่กับราคาไหนจริงๆ แม้จะสั่งด้วยพรอมต์ก็ตาม เป็น**ข้อจำกัดเชิงโครงสร้างของโค้ด ไม่ใช่แค่เรื่องพรอมต์**

**วิธีแก้:** แก้ 2 จุด (ทั้งโค้ดและพรอมต์ ตามแนวทางเดียวกับเรื่องที่ 47):
1. **[lib/websearch.ts](lib/websearch.ts)** (`searchWithSerper`) — แนบลิงก์ของแต่ละผลลัพธ์ไว้ในเนื้อหาด้วย (ไม่ใช่แค่คืนลิงก์เดียวของผลลัพธ์แรกแยกต่างหาก) ให้ Gemini จับคู่ราคา-ลิงก์ถูกต้องได้จริง — กระทบ `generateReply()`'s `search_spender_specs` และ `generateReplyWithImage()` (ปุ่ม "สอบถามสเปก") ทั้ง 2 จุดที่เรียกใช้ `searchSpenderSites()` — ไม่กระทบ `searchSpenderRecommend()` (ฟังก์ชันแยกต่างหาก มีระบบเก็บลิงก์หลายอันอยู่แล้ว ไม่ได้แก้ตัวนี้)
2. **[lib/prompts.ts](lib/prompts.ts)** (`<search_tool>`) — เพิ่มกฎห้ามหยิบราคา/ลิงก์/สเปกจากผลค้นคนละหน้ามาผสมกันตอบเป็นคำตอบเดียว เขียนเป็น**หลักการทั่วไป** (ไม่ผูกกับสินค้าตัวใดตัวหนึ่ง ตามแนวทางที่พิสูจน์แล้วจากเรื่องที่ 50) — ถ้าไม่แน่ใจว่าหน้าไหนตรงที่สุด ให้ถามลูกค้ายืนยันก่อน (ขายแยกหรือขายชุด) แทนการเดา/ผสมข้อมูล

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):** เนื่องจากไม่มี `SERPER_API_KEY` ในเครื่องทดสอบ (ค้นเว็บจริงไม่ได้) จึงจำลอง **"Call 2" ของ `generateReply()`** ตรงๆ (ยิง `ai.models.generateContent()` ด้วย `systemPrompt`+`functionResponse` ที่จำลองเนื้อหาแบบใหม่ที่ `searchWithSerper()` ที่แก้ไปจะผลิตจริง — มีลิงก์ต่อท้ายแต่ละรายการ):
1. เคสจริง: ถามราคาหลังผลค้นมี 2 รายการ (ขายแยก 340 / ขายชุด 680) → ผลจริง: "อะแดปเตอร์วิทยุสื่อสาร SPENDER รุ่น TC-741H /751H ราคา 340.00 บาทค่ะ...ดูรายละเอียดเพิ่มเติม: .../adapter-spender-tc-741h-751h/" — ราคา+ลิงก์ตรงกันถูกต้อง ไม่มี "680"/ลิงก์ชุดปนมาแล้ว — **ผ่าน**
2. regression: ค้นเจอผลเดียว (กรณีปกติทั่วไป) → ยังตอบราคา+ลิงก์ถูกต้องปกติ — **ผ่าน**
3. regression: ลูกค้าถามชุดตรงๆ → ยังตอบราคา 680 + ลิงก์ชุดถูกต้อง ไม่ได้ถูกบล็อกไปด้วย (กฎใหม่ไม่ได้ปิดกั้นการตอบชุดเมื่อลูกค้าต้องการชุดจริง) — **ผ่าน**

**ทำไมไม่กระทบอย่างอื่น:** การแก้ `lib/websearch.ts` เป็นการ**เพิ่มข้อมูล**เข้าไปในข้อความที่ส่งให้ Gemini อ่านเท่านั้น ไม่เปลี่ยนโครงสร้างฟังก์ชัน (`{text, url}`) หรือวิธีเรียกใช้เลย ไม่มีจุดไหนใน codebase parse รูปแบบข้อความนี้ด้วย regex เฉพาะเจาะจง (ยืนยันจากการ grep) จึงไม่เสี่ยงพังจากการเปลี่ยนรูปแบบ · ไม่แตะ `analyzeImageIntent()`, `analyzeImageWithText()` (ไม่เรียกค้นเว็บเลย), FAQ ในชีต, `repair_protocol`, `imei_protocol` เลย

**ขอบเขต:** แก้ 2 ไฟล์ ([lib/websearch.ts](lib/websearch.ts), [lib/prompts.ts](lib/prompts.ts)) — **มีผลทุกช่องทาง** (LINE/Facebook/Web Chat) เหมือนเรื่องที่ 47-51

---

## เรื่องที่ 53 — Carry-forward เลข Group ID ที่เคยเอ่ยแล้ว ไม่ถามซ้ำ (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าส่งรูป Account Info 2 เครื่อง (862786088923645, 8690900886505246) — บอทเดาถูกว่ามีเลขกลุ่มปนมาด้วย พิมพ์ **"188271"** เดี่ยวๆ ตามหลัง — บอทเอ่ยเลขนั้นในคำถามยืนยันรวมกับรายละเอียดรูปภาพถูกต้อง ("...และแจ้งเลข 188271 ใช่ไหมคะ") — แต่ลูกค้าไม่ได้ตอบ "ใช่" ตรงๆ กลับพิมพ์คำขอใหม่แทน ("ฝากตึ๊ง 2 เครื่องนี้เข้ากลุ่มให้หน่อยนะครับ") — บอทกลับ**ถามชื่อกลุ่ม/Group ID ซ้ำ** ทั้งที่ลูกค้าให้ไปแล้วและบอทเองก็เพิ่งเอ่ยไปเอง

**สาเหตุ:** `imei_protocol` ขั้นที่ 2 บรรทัด "เพิ่มเข้ากลุ่ม → ถามชื่อกลุ่มที่ต้องการเพิ่ม" เขียนเป็นคำสั่งแบบไม่มีเงื่อนไข ไม่ได้บอกให้เช็ค conversation history ก่อนว่าเคยมีเลขกลุ่มปรากฏมาแล้วหรือยัง — เป็นปัญหาตระกูลเดียวกับเรื่องที่ 46/48/51 (ไม่ carry-forward ข้อมูลที่เคยให้/เคยยืนยันไปแล้วในบทสนทนา) ต่างกันแค่ครั้งนี้ลูกค้าไม่ได้ตอบคำถามยืนยันตรงๆ แต่เปลี่ยนไปพิมพ์คำขอใหม่แทน ซึ่งยังไม่เคยมีข้อยกเว้นรองรับมาก่อน

**วิธีแก้:** เพิ่มข้อยกเว้นใน [lib/prompts.ts](lib/prompts.ts) (`<imei_protocol>` ขั้นที่ 2) **เฉพาะบรรทัด "เพิ่มเข้ากลุ่ม" เท่านั้น** — ถ้าบอทเคยเอ่ยเลข/ชื่อกลุ่มในบริบทเรื่องกลุ่มมาก่อนแล้วในบทสนทนานี้ (แม้ลูกค้าจะยังไม่เคยตอบยืนยันตรงๆ ก็ตาม) ห้ามถามชื่อกลุ่ม/Group ID ซ้ำอีก ให้นำค่าที่เคยเอ่ยนั้นไปสรุปยืนยันต่อในขั้นที่ 3 ทันที — **จงใจไม่ขยายไปกรณี ลบ/ย้าย/เปลี่ยนกลุ่ม** เพราะมีมากกว่า 1 slot เลข (ต้นทาง+ปลายทาง) ทำให้กำกวมว่าเลขที่เคยเห็นคือฝั่งไหน เสี่ยงมากกว่าคุ้ม ไม่มีเคสจริงรองรับ — และผูกเงื่อนไขให้แคบว่าต้องเป็นเลขที่ **"บอทเคยเอ่ยเองในบริบทกลุ่มมาก่อน"** เท่านั้น ไม่ใช่เลขสั้นๆ ตัวไหนก็ได้ใน history (กันหยิบเลขไม่เกี่ยวข้อง เช่น รหัสรุ่นสินค้าที่คุยไปก่อนหน้า มาใช้ผิดๆ) — ยังคงบังคับผ่านขั้นที่ 3 (ยืนยันสรุปก่อน HANDOFF) เหมือนเดิมทุกประการ เผื่อบอทเดาเลขผิดจะได้มีจุดให้ลูกค้าแก้ไขก่อน

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. เคสจริง: จำลอง history ตามบทสนทนาจริง (รูปภาพ+ยืนยันเลข 188271 → ลูกค้าขอเพิ่มกลุ่มโดยไม่พูดเลขซ้ำ) → ผลจริง: "ยืนยันการเพิ่มกลุ่ม IMEI: 862786088923645 และ 8690900886505246 เข้ากลุ่ม 188271 ถูกต้องไหมคะ?" — ดึงเลข 188271 มาใช้ถูกต้อง ไม่ถามซ้ำ — **ผ่าน**
2. regression: เคสปกติไม่มีเลขกลุ่มปรากฏใน history มาก่อนเลย ลูกค้าขอเพิ่มกลุ่มพร้อม IMEI ตรงๆ → ผลจริง: "ต้องการเพิ่มเข้ากลุ่มชื่ออะไรคะ" — ยังถามหาชื่อกลุ่มตามปกติเหมือนเดิม ไม่ได้ข้ามขั้นตอนไปเอง — **ผ่าน**

**ทำไมไม่กระทบอย่างอื่น:** แก้แค่บรรทัดเดียวใน `imei_protocol` ขั้นที่ 2 (กรณี "เพิ่มเข้ากลุ่ม") ไม่แตะ ลบ/ย้าย/เปลี่ยนกลุ่ม, ไม่แตะ `repair_protocol`, `recommend_protocol`, `product_template`, `guardrails`, หรือ `<search_tool>` เลย — เงื่อนไขผูกกับ "บอทเคยเอ่ยเลขในบริบทกลุ่มมาก่อน" เท่านั้น ไม่ใช่เลขสั้นๆ ตัวไหนก็ได้ จึงไม่ไปกระทบการถามราคาสินค้า/รุ่นสั้นๆ (ขั้นที่ 2 ของ `reasoning_protocol` หลัก) — ยังคงบังคับขั้นยืนยันก่อน HANDOFF เหมือนเดิม ไม่ได้เปิดช่องให้ข้อมูลผิดหลุดไปแบบเงียบๆ

**ขอบเขต:** แก้ 1 ไฟล์ ([lib/prompts.ts](lib/prompts.ts)) — **มีผลทุกช่องทาง** (LINE/Facebook/Web Chat) เพราะ `imei_protocol` ไม่มี channel branching อยู่แล้ว — จำกัดเฉพาะกรณี "เพิ่มเข้ากลุ่ม" เท่านั้น

---

## เรื่องที่ 54 — ขยาย timeout รูปภาพเดี่ยว+ข้อความสั้น จาก 25s เป็น 35s (LINE + Facebook เท่านั้น)

**เคสจริงที่พบ:** ลูกค้าส่งรูป 1 รูป (เครื่องขึ้นข้อความ "The account has expired, please renew it" — ชัดเจน อ่านง่าย) พร้อมพิมพ์ **"แก้ไงครับ"** ตามมาเร็ว — บอทตอบ **"ขออภัยค่ะ ระบบกำลังประมวลผลนานกว่าปกติ 🙏"** (UNAVAILABLE_MSG) ทั้งที่เนื้อหารูปไม่มีอะไรที่ควรทำให้ Gemini วิเคราะห์ยาก/ช้าเลย

**สาเหตุ:** [app/api/line-webhook/route.ts](app/api/line-webhook/route.ts) จุดเรียก `analyzeImageWithText()` ตั้ง `dynamicTimeout` แบบไดนามิกตามจำนวนรูป (รูปแรก 25s + รูปเพิ่มรูปละ 10s capped 45s, ปรับมาจากเรื่อง 2026-08-10) — ถ้า Gemini ตอบไม่ทันภายในเวลานี้ (ไม่ว่าจะช้าจริงหรือมี error) จะตัดจบและตอบ UNAVAILABLE_MSG ทันที — กรณีนี้เป็นแค่ 1 รูป + ข้อความสั้น ควรใช้เวลาไม่ถึง 5 วิตามปกติ (อ้างอิงจากการทดสอบ Gemini จริงหลายครั้งในเซสชันนี้) การใช้เวลาเกิน 25 วิจึงน่าจะเป็นความหน่วงชั่วคราวของ Gemini มากกว่าปัญหาเนื้อหา — **ไม่มี log ยืนยันสาเหตุแน่ชัด 100%** (ดู Vercel log ไม่ทันเพราะหมดเวลาเก็บ) แต่หลักฐานจากภาพ (รูปชัดเจน ไม่กำกวม) สนับสนุนสมมติฐาน timeout มากกว่ากรณีอื่น

**วิธีแก้:** ขยับค่าฐานของ `dynamicTimeout` จาก 25000ms เป็น 35000ms (บรรทัดเดียว ไม่แตะโครงสร้างสูตรหรือค่า cap 45000ms เดิม) — ยังอยู่ในงบ `maxDuration=60s` ของทั้งฟังก์ชันสบายๆ (เหลือ buffer มากกว่าเดิมสำหรับดาวน์โหลดรูป/ส่งข้อความ)

**ทดสอบก่อน commit:** เป็นการปรับค่าตัวเลข timeout ล้วนๆ ไม่ใช่การเปลี่ยน logic การตีความคำถาม (ต่างจากเรื่องที่ 45-53 ที่เป็นการแก้พรอมต์) จึงไม่มีพฤติกรรมใหม่ให้ทดสอบผ่าน Gemini จริงแบบ ผ่าน/ไม่ผ่าน — ตรวจสอบด้วย `tsc --noEmit` ผ่านแล้ว และยืนยัน diff จำกัดอยู่บรรทัดเดียวใน `route.ts` เท่านั้น (ไม่มีไฟล์อื่นถูกแตะ)

**ผลกระทบที่ต้องรู้ (ไม่ใช่ปรับได้ฟรีๆ):** ถ้า Gemini ค้าง/error จริง (ไม่ใช่แค่ช้า) ลูกค้าจะต้องรอนานขึ้นกว่าจะได้รับข้อความขอโทษ (จาก 25s เป็น 35s) — เป็นการแลก "ลดโอกาสตอบ unavailable ผิดๆ ตอน Gemini แค่ช้ากว่าปกติ" กับ "ลูกค้าที่พังจริงๆ ต้องรอนานขึ้นเล็กน้อย" — ยังไม่มั่นใจ 100% ว่าจะช่วยได้จริงเพราะมีข้อมูลตัวอย่างแค่ 1 ครั้ง (ไม่ใช่ pattern ที่ยืนยันซ้ำแล้ว)

**ทำไมไม่กระทบอย่างอื่น:** แก้แค่ค่าตัวเลขเดียว ผูกกับ branch "รูป+ข้อความสั้นตามมาเร็ว" (`analyzeImageWithText`) เท่านั้น ไม่กระทบข้อความล้วน, `repair_protocol`, `imei_protocol`, กรณีส่งรูปเกิน 5 นาที (`analyzeImageIntent`), หรือ Web Chat (ไม่มี flow รูปภาพแบบนี้อยู่แล้ว) — ไม่แตะ `lib/prompts.ts`, `lib/gemini.ts`, หรือไฟล์อื่นใดเลยตามที่ตกลง

**ขอบเขต:** แก้ 1 ไฟล์ ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts)) บรรทัดเดียว — **LINE + Facebook เท่านั้น** (Web Chat ไม่มี flow นี้)

---

## เรื่องที่ 55 — ตัวกรอง "ข้อความไร้สาระ" เข้าใจผิดว่าเลขราคา/จำนวนเป็นสแปม (Web Chat เท่านั้น)

**เคสจริงที่พบ:** ลูกค้าพิมพ์ **"วิทยุงบประมาณที่มี ประมาณ ตัวละ 2,000 นะครับ"** ซ้ำหลายรอบใน Web Chat — บอทตอบวนสลับกันแค่ 2 แบบ ("มีอะไรให้น้องใจดีช่วยไหมคะ 😊" กับข้อความเบอร์ติดต่อ) ไม่เคยตอบคำถามจริงเลย ทั้งที่มีกฎกันถามคำถามซ้ำอยู่แล้ว 2 ชั้น (ตัวนับซ้ำใน Redis + กฎใน `<guardrails>`)

**สาเหตุ:** [app/api/web-chat/route.ts](app/api/web-chat/route.ts) มีตัวกรอง `isNonsenseMessage()` เช็คว่ามีตัวอักษรซ้ำกัน 3 ตัวขึ้นไปติดกันไหม (ออกแบบมากันสแปมแบบ "aaaa"/"555"/"!!!!") ด้วย regex `/(.)\1{2,}/u` — ข้อความลูกค้ามี **"2,000" ซึ่งมี "000" (เลข 0 ติดกัน 3 ตัว)** เข้าเงื่อนไขนี้พอดี ทำให้ทุกครั้งที่ส่งข้อความนี้ถูกเข้าใจผิดว่าไร้สาระ — ตัวกรองนี้ทำงาน**ก่อน**ทั้งกลไกกันถามซ้ำ (Redis) และก่อนถึง Gemini/`<guardrails>` เลย เมื่อโดนสกัดตรงนี้ก่อน กฎกันถามซ้ำทั้ง 2 ชั้นเลยไม่มีโอกาสได้ทำงานเลยสักครั้ง — ที่สลับ 2 แบบ เพราะมี session rate limit (บรรทัดก่อนหน้า) คอยสกัดสลับกันไปมาตามความถี่ที่ลูกค้าส่ง

**วิธีแก้:** แก้ regex ให้ไม่นับตัวเลขเป็น "ตัวอักษรซ้ำ" เลย — จาก `/(.)\1{2,}/u` เป็น `/([^\d])\1{2,}/u` (เพิ่ม `[^\d]` = ไม่ใช่ตัวเลข) — ยังจับสแปมตัวอักษร/สัญลักษณ์ซ้ำได้เหมือนเดิม (aaaa, !!!!, ดดดด) แต่ไม่แตะตัวเลขซ้ำเลยไม่ว่าจะกี่ตัวก็ตาม เพราะตัวเลขซ้ำแทบไม่เคยเป็น "ขยะ" จริงในบริบทร้านขายของ (ราคา/จำนวน/รหัสสินค้า) — เลือกวิธีนี้แทนการขยับ threshold เป็น 4+ ตัว เพราะครอบคลุมกว่า (ไม่ขึ้นกับว่าลูกค้าจะใส่คอมม่าคั่นหลักพันหรือไม่ เช่น "20000" ไม่มีคอมม่าก็ยังผ่าน)

**ทดสอบก่อน commit:** เป็น logic โค้ดล้วนๆ ไม่ผ่าน Gemini เลย ทดสอบตรงด้วยการจำลองฟังก์ชันแล้วรัน 14 เคส — ผ่านครบ 14/14: เคสจริงที่เจอปัญหา + ราคาอื่นๆ (3,000 / 10,000 / 100 / 1,000,000 / 20000 ไม่มีคอมม่า) ได้ `false` (ไม่ใช่ไร้สาระ) ถูกต้องทุกเคส · regression สแปมตัวอักษรจริง (aaaa, ????, ดดดด, ไไไไ, !!!, 555 ทั้งข้อความ, ข้อความสั้นเกินไป) ยังได้ `true` (ยังโดนกรองเหมือนเดิม) ครบทุกเคส

**ทำไมไม่กระทบอย่างอื่น:** แก้บรรทัดเดียวในฟังก์ชันเดียว (`isNonsenseMessage()`) — ไม่มีฟังก์ชันนี้ใน LINE/Facebook webhook อยู่แล้ว (ตรวจแล้วด้วย grep) จึงไม่กระทบ 2 ช่องทางนั้นเลย · ไม่แตะกลไกกันถามซ้ำที่มีอยู่แล้วทั้ง 2 ชั้น (แค่ทำให้มันมีโอกาสได้ทำงานตามที่ออกแบบไว้จริง) · ไม่แตะ `lib/prompts.ts`, `lib/gemini.ts`, หรือ session rate limit/schedule check ที่อยู่ก่อนหน้าเลย

**ขอบเขต:** แก้ 1 ไฟล์ ([app/api/web-chat/route.ts](app/api/web-chat/route.ts)) บรรทัดเดียว — **Web Chat เท่านั้น**

---

## เรื่องที่ 56 — Sync timeout เรื่องที่ 54 ไป Facebook + บันทึก log ถาวรกัน Vercel log หมดเวลา (LINE + Facebook)

**เคสจริงที่พบ:** ลูกค้า LINE เจอ UNAVAILABLE_MSG ("ระบบกำลังประมวลผลนานกว่าปกติ") หลังส่งรูป+ข้อความตามมาเร็ว ซ้ำอีกครั้ง**หลัง**จาก deploy เรื่องที่ 54 (ขยาย timeout 25s→35s) ไปแล้วจริง (ยืนยันจาก Vercel dashboard ว่า deployment ขึ้นสถานะ "Ready" ก่อนเวลาที่เคสนี้เกิด) — พยายามดู Vercel log เพื่อฟันธงสาเหตุแต่ดูไม่ทันเพราะเกินระยะเวลาที่ Vercel เก็บ log ไว้ให้

**สาเหตุ 2 เรื่องที่พบระหว่างตรวจสอบ:**
1. **เรื่องที่ 54 แก้ไม่ครบ** — ตอนแก้ ขยับ `dynamicTimeout` จาก 25s เป็น 35s ใน [app/api/line-webhook/route.ts](app/api/line-webhook/route.ts) เท่านั้น แต่ [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts) มีโค้ด `analyzeImageWithText()` + `dynamicTimeout` แบบเดียวกันเป๊ะ (บรรทัด 706-716) ที่**ไม่เคยถูกแก้ไปด้วย** ยังเป็น 25s อยู่ — ผิดกฎที่ตกลงกันไว้เองว่า "ทุกครั้งที่แก้ line-webhook/lib ให้แก้ fb-webhook พร้อมกัน" (เป็นความผิดพลาดของการแก้เรื่องที่ 54 เอง ไม่ใช่บั๊กใหม่)
2. **ไม่มีทางแยกสาเหตุ "timeout จริง" กับ "Gemini ตอบ unclear เองโดยไม่เกี่ยวกับเวลา" ได้แบบถาวร** — `analyzeImageWithText()` มี try/catch ภายในตัวเอง ถ้า parse ผลลัพธ์ไม่ได้หรือโมเดลสรุปว่า "ดูไม่ออก" จะ return `unclear` แบบปกติ ไม่ error ไม่ timeout เลย ให้ผลลัพธ์ที่ลูกค้าเห็นเหมือนกับ timeout จริงทุกตัวอักษร — เดิมมีแค่ log ชั่วคราวใน Vercel (`gemini.image_text_failed` เฉพาะกรณี error/timeout, `image_text.unclear` ทุกกรณี) ไม่มีการเก็บถาวร พอ log หมดเวลาก็ไม่มีทางย้อนดูได้อีกเลย

**วิธีแก้:** แก้ 2 ไฟล์พร้อมกัน:
1. Sync `dynamicTimeout` ฐานจาก 25000 เป็น 35000 ใน `fb-webhook.ts` ให้ตรงกับ LINE
2. เพิ่มตัวแปร `imageTextFailureReason` เก็บ error message จริงตอน `.catch()` ทำงาน (มาจาก timeout) แล้วบันทึกลง Redis list ถาวร (`image_text_failure_log`, จำกัด 500 รายการล่าสุดด้วย `ltrim` แบบเดียวกับ `unanswered_log` ที่มีอยู่แล้ว) ทุกครั้งที่ตอบ UNAVAILABLE_MSG จากเส้นทางนี้ — เก็บ `latencyMs`, `imageCount`, `channel` ('line'/'facebook'), และ `reason` (`'gemini_timeout'` ถ้ามาจาก race timeout จริง หรือ `'model_unclear'` ถ้า `analyzeImageWithText()` return unclear เองโดยไม่มี error เลย) — ทำใน `fb-webhook.ts` ด้วย (เดิมไม่มี `log.error` ตอน catch เลยด้วยซ้ำ เพิ่มเข้าไปให้ตรงกับ LINE)

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):** ยิง `analyzeImageWithText()` จริงด้วยรูป JPEG ทดสอบขั้นต่ำ (1x1 พิกเซล) 2 เคส:
1. บังคับ timeout สั้น (100ms) ให้ race timeout ชนะแน่นอน → ผลจริง: `kind: unclear`, `latencyMs: 103`, `failureReason: 'gemini_timeout'` — ตรงตามที่ออกแบบ ✅
2. timeout ปกติ (30s) ปล่อยให้ Gemini ทำงานจบเอง → ผลจริง: Gemini ใช้เวลา 15.8 วิ (ไม่เกิน timeout เลย) แต่ตอบ `kind: unclear` เอง เพราะรูปทดสอบว่างเปล่าไม่มีข้อมูลอะไรให้วิเคราะห์ → `failureReason: null` → fallback เป็น `'model_unclear'` ถูกต้อง — **เคสทดสอบนี้พิสูจน์ตัวอย่างจริงของสถานการณ์ "unclear ที่ไม่ใช่ timeout" ที่ระบบเดิมแยกไม่ออกเลย** ✅
- Redis จริงทดสอบไม่ได้ในเครื่อง (ไม่มี `UPSTASH_REDIS_URL`/`TOKEN` ใน `.env.local` เหมือนข้อจำกัดเดิมที่เจอมาตลอดเซสชันนี้) แต่ shape ของ record ที่จะเขียนตรงกับที่ตั้งใจ และใช้ pattern เดียวกับ `unanswered_log` ที่ใช้งานจริงอยู่แล้วในระบบ (ไม่ใช่โค้ดใหม่ที่ไม่เคยพิสูจน์)

**ทำไมไม่กระทบอย่างอื่น:** เป็นการ**เพิ่ม**การบันทึก log ถาวรเท่านั้น ไม่เปลี่ยนสิ่งที่ลูกค้าเห็นเลย (ยังตอบ UNAVAILABLE_MSG เหมือนเดิมทุกตัวอักษร) — Redis write ห่อด้วย try/catch เหมือนทุกจุดอื่นในระบบ ถ้า Redis ล่มก็ข้ามไปเฉยๆ ไม่กระทบการตอบลูกค้า · การ sync timeout ไป Facebook เป็นแค่ทำให้ค่าตรงกับ LINE เท่านั้น ไม่เปลี่ยนโครงสร้างหรือ cap 45000ms เดิม · ไม่แตะ `lib/prompts.ts`, `lib/gemini.ts`, Web Chat (ไม่มี flow นี้อยู่แล้ว) เลย

**ขอบเขต:** แก้ 2 ไฟล์ ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts)) — **LINE + Facebook เท่านั้น** (Web Chat ไม่มี flow รูปภาพแบบนี้)

---

## เรื่องที่ 57 — หน้า Admin ดู `image_text_failure_log` (feature ใหม่ ต่อยอดเรื่องที่ 56)

**ที่มา:** เรื่องที่ 56 บันทึกเหตุการณ์ timeout รูปภาพลง Redis ถาวรแล้ว แต่ยังไม่มีหน้าเว็บให้เข้าไปดู — ต้องเข้า Upstash Console โดยตรงถึงจะเห็นข้อมูล ไม่สะดวกเท่าหน้า Admin อื่นๆ ที่มีอยู่แล้ว (เช่น `/admin/unanswered`)

**สิ่งที่เพิ่ม:** สร้างหน้า Admin ใหม่ตาม pattern เดียวกับ `/admin/unanswered` ทุกประการ (auth ด้วย `ADMIN_SCHEDULE_PASSWORD` ตัวเดิม, สไตล์เดียวกัน, Export Excel, ปุ่มล้างรายการ):
- [app/api/admin/image-failures/route.ts](app/api/admin/image-failures/route.ts) — GET (ดึง 100 รายการล่าสุดจาก `image_text_failure_log`) + DELETE (ล้างทั้งหมด)
- [app/admin/image-failures/page.tsx](app/admin/image-failures/page.tsx) — ตารางแสดง เวลา/ช่องทาง/จำนวนรูป/เวลาที่ใช้/สาเหตุ (แปล `gemini_timeout` → "หมดเวลา (Gemini ช้า)", `model_unclear` → "โมเดลตอบไม่ชัดเจน (ไม่ใช่ timeout)") พร้อมสรุปจำนวนแยกตามสาเหตุด้านบนตาราง — ไม่แสดง `userId` ดิบในตาราง (ตามแนวทางเดิมที่ไม่ log/แสดงข้อมูลระบุตัวตนเกินจำเป็น)

**ทดสอบก่อน commit:** `tsc --noEmit` ผ่าน + `next build` ผ่านสมบูรณ์ (ยืนยัน route `/admin/image-failures` และ `/api/admin/image-failures` compile และ generate ถูกต้อง ไม่มี error)

**ทำไมไม่กระทบอย่างอื่น:** เป็นไฟล์ใหม่ 2 ไฟล์เท่านั้น ไม่แก้ไฟล์เดิมไฟล์ไหนเลย ไม่มีหน้า Admin อื่นลิงก์มาหาหน้านี้ (เข้าตรงผ่าน URL เหมือนหน้า Admin อื่นทั้งหมดในระบบ ไม่มี nav กลาง) — ใช้ password เดิมร่วมกับหน้า Admin อื่น ไม่ได้เพิ่ม env var ใหม่

**ขอบเขต:** เพิ่ม 2 ไฟล์ใหม่ ([app/api/admin/image-failures/route.ts](app/api/admin/image-failures/route.ts), [app/admin/image-failures/page.tsx](app/admin/image-failures/page.tsx)) — ไม่กระทบโค้ดเดิมไฟล์ไหนเลย

---

## เรื่องที่ 58 — เว้นบรรทัดข้อความยืนยันรูปภาพหลายเครื่อง ให้อ่านง่ายขึ้น (LINE + Facebook)

**เคสจริงที่พบ:** ลูกค้าส่งรูป 2 เครื่อง บอทถามยืนยันด้วยข้อความยาวติดกันเป็นพารากราฟเดียว: "เห็นรูปที่ 1 เป็นเครื่อง SPENDER MODEL: TC-4M S/N: T2516003S001925 IMEI: 865828074109545 และรูปที่ 2 เป็นเครื่อง SPENDER MODEL: TC-15HW S/N: T2616101S000033 IMEI: 863457082454065 ใช่ไหมคะ ต้องการให้ดึงเครื่องทั้ง 2 เครื่องเข้ากลุ่ม puntamit-Esan ID 195517 ใช่ไหมคะ 😊" — อ่านยากมาก ต่างจากคำตอบที่มาจาก `<faq>` ซึ่งมีการเว้นบรรทัดอยู่แล้ว

**สาเหตุ:** ข้อความยืนยันรูปภาพมาจากฟังก์ชันคนละตัวกับคำตอบทั่วไป — `analyzeImageIntent()`/`analyzeImageWithText()` ใน [lib/gemini.ts](lib/gemini.ts) มีพรอมต์แยกเป็นของตัวเอง **ไม่ได้ใช้ `buildSystemPrompt()`** เลย ทำให้กฎ `<output_format>` ("ให้ขึ้นบรรทัดใหม่แยกแต่ละประเด็นเสมอ ห้ามเขียนเป็นพารากราฟยาวติดกัน") ไม่มีผลกับข้อความยืนยันรูปภาพเลยสักนิด — ต้นตอลึกกว่านั้นคือตัวอย่าง `confirmMessage` ที่ยกไว้ในพรอมต์ทั้ง 2 ฟังก์ชัน (กรณีหลายรูป) เขียนเป็นประโยคเดียวยาวๆ ไม่มีเว้นบรรทัดเลย Gemini เลยเลียนแบบรูปแบบนั้นตรงๆ

**วิธีแก้:** แก้ 2 จุดในแต่ละฟังก์ชัน (รวม 4 จุด, [lib/gemini.ts](lib/gemini.ts) เท่านั้น):
1. เพิ่มกฎ "ทุกข้อความใน `message`/`confirmMessage` ถ้ามีข้อมูลหลายรายการปนกัน (เช่น รุ่น/S/N/IMEI ของหลายเครื่อง หรือหลายรูป) ให้ขึ้นบรรทัดใหม่ (`\n`) แยกแต่ละเครื่อง/รายการเสมอ ห้ามเขียนรวมเป็นพารากราฟเดียวยาวๆ จนอ่านยาก" เข้าไปในทั้ง `analyzeImageIntent()` และ `analyzeImageWithText()`
2. แก้ตัวอย่าง `confirmMessage` กรณีหลายรูปในทั้ง 2 ฟังก์ชัน ให้มีการเว้นบรรทัดจริง (แยกบรรทัดต่อเครื่อง + เว้นบรรทัดก่อนประโยคถามยืนยันสุดท้าย) แทนประโยคเดียวยาวๆ แบบเดิม

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):** จำลองเคสจริงด้วย `analyzeImageWithText()` — ใช้รูปทดสอบ 2 รูป + แคปชั่นที่มีข้อมูลรุ่น/S/N/IMEI ของ 2 เครื่อง + คำขอเข้ากลุ่ม (เหมือนเคสจริงเป๊ะ) → ผลจริง:
```
เห็นรูปที่ 1 เป็นเครื่อง SPENDER MODEL: TC-4M S/N: T2516003S001925 IMEI: 865828074109545
รูปที่ 2 เป็นเครื่อง SPENDER MODEL: TC-15HW S/N: T2616101S000033 IMEI: 863457082454065

ใช่ไหมคะ ต้องการเพิ่มเครื่องทั้งสองเข้ากลุ่ม puntamit-Esan ID 195517 ใช่ไหมคะ 😊
```
มีการเว้นบรรทัดตามที่ออกแบบไว้ทุกจุด (`confirmMessage.includes('\n')` = true) — ตรงกับผลที่คาดหวังเป๊ะ ✅

**ทำไมไม่กระทบอย่างอื่น:** แก้แค่ 4 จุดใน `lib/gemini.ts` (เพิ่มกฎ 1 บรรทัด + แก้ตัวอย่าง 1 บรรทัด ต่อฟังก์ชัน) ไม่แตะ `lib/prompts.ts`/`<output_format>` เลยเพราะเป็นคนละพรอมต์กัน ไม่แตะ logic การจำแนกประเภทรูป (A-G) หรือเงื่อนไข 0-4 ใดๆ เลย เป็นแค่การปรับรูปแบบข้อความ (formatting) เท่านั้น ไม่เปลี่ยนเนื้อหา/การตัดสินใจ — ไม่แตะ Web Chat เพราะไม่มี flow ส่งรูปแบบนี้อยู่แล้ว

**ขอบเขต:** แก้ไฟล์เดียว ([lib/gemini.ts](lib/gemini.ts)) 4 จุด — **LINE + Facebook เท่านั้น** (ใช้ฟังก์ชันเดียวกันทั้ง 2 ช่องทาง)

---

## เรื่องที่ 59 — กันชื่อ tag ภายในของพรอมต์ (เช่น `<faq>`) หลุดออกมาในคำตอบจริง (ทุกช่องทาง)

**เคสจริงที่พบ:** บอทตอบลูกค้าว่า **"ตอนนี้พบข้อมูลใน `<faq>` ที่ตรงกับเงื่อนไขราคาไม่เกิน 2,000 บาท มีเพียงรุ่น Spender ID2462 ที่แจ้งไปค่ะ"** — คำว่า `<faq>` เป็นชื่อ tag ภายในของพรอมต์ หลุดออกมาปนกับคำตอบจริงที่ส่งถึงลูกค้าตรงๆ ทั้งที่ไม่ควรมีใครเห็นคำนี้เลยนอกจากตัวพรอมต์เอง — เจอทั้ง Web Chat และช่องทางอื่น

**สาเหตุ:** คำว่า `<faq>` ถูกใช้อ้างอิงซ้ำมากกว่า 30 จุดทั่ว `buildSystemPrompt()` ใน [lib/prompts.ts](lib/prompts.ts) (`reasoning_protocol`, `guardrails`, `recommend_protocol`, `output_format`, `product_template` ฯลฯ) แต่ไม่เคยมีกฎห้ามเขียนชื่อ tag ภายในแบบนี้ออกมาในคำตอบจริงเลยสักข้อ — กฎที่มีอยู่ (ใน `reasoning_protocol`) ห้ามแค่การเขียน "ขั้นตอนการคิด" หลุดออกมาเท่านั้น ไม่ครอบคลุมการเอ่ยชื่อ tag แบบนี้ — **เป็นปัญหาตระกูลเดียวกับเรื่องที่ 44 ("คิดก่อน:" หลุด) และเรื่องที่ 36 (HANDOFF sentinel หลุด) ทุกประการ**: คำที่ใช้ซ้ำมากในคำสั่งพรอมต์มีโอกาสหลุดปนเข้าคำตอบจริงได้เอง เพราะ Gemini ไม่ทำตามคำสั่งได้เสมอไป (temperature=1.0 ไม่ deterministic)

**วิธีแก้:** แก้ 2 ชั้นเหมือนเรื่องที่ 44:
1. **[lib/prompts.ts](lib/prompts.ts)** (`<guardrails>`) — เพิ่มกฎ: "ห้ามเขียนชื่อ tag ภายในของคำสั่งนี้ (เช่น `<faq>`, `<guardrails>`, `<repair_protocol>` หรือ tag อื่นใดที่ใช้กำกับโครงสร้างคำสั่งชุดนี้) ออกมาในคำตอบจริงเด็ดขาด คำตอบต้องเป็นภาษาธรรมชาติล้วนๆ ที่จะพูดกับลูกค้าโดยตรงเท่านั้น" พร้อมตัวอย่างก่อน/หลัง — ไม่มี channel branching (ใช้ทุกช่องทาง)
2. **[lib/gemini.ts](lib/gemini.ts)** — เพิ่มฟังก์ชัน `stripLeakedTags()` เป็นตาข่ายรองรับชั้นสุดท้าย (คู่กับ `stripLeakedReasoning()` ที่มีอยู่แล้ว) ตัด tag ที่รู้จักทั้งหมด (`<faq>`, `<guardrails>`, `<repair_protocol>` ฯลฯ — ครบทุก tag ที่ใช้ใน `buildSystemPrompt()`) ออกจากคำตอบก่อนส่งถึงลูกค้า ใช้ใน `generateReply()` ทั้ง 2 จุด (ทั้งเส้นทางค้นเว็บและเส้นทางปกติ)

**ทดสอบก่อน commit (ไฟล์ทดสอบลบออกจาก repo แล้ว):**
1. ทดสอบ regex ตรง (ไม่ผ่าน Gemini เพราะเป็น logic โค้ดล้วนๆ) 5 เคส — ผ่านครบ 5/5: เคสจริงที่เจอปัญหา + tag อื่นในลิสต์ (`<guardrails>`, `</faq>`) ถูกตัดออกถูกต้อง · regression ข้อความปกติที่ไม่มี tag เลย (ราคาสินค้าปกติ) ไม่ถูกแก้ไขแม้แต่ตัวอักษรเดียว
2. ทดสอบ end-to-end ผ่าน Gemini จริง — จำลองคำถามแนะนำสินค้าตามงบ (เงื่อนไขเดียวกับเคสจริงที่เจอ) เรียก `generateReply()` เต็มรูปแบบ ทำงานได้ปกติไม่มี error ไม่พบ `<faq>` หลุดในคำตอบที่ได้ (ไม่การันตี 100% เพราะ non-deterministic แต่ยืนยันว่า pipeline ทำงานถูกต้องไม่มีอะไรพัง)

**ทำไมไม่กระทบอย่างอื่น:** กฎใหม่ใน `<guardrails>` เป็นการ**เพิ่ม**กฎเท่านั้น ไม่แก้ไข/ลบกฎเดิมข้อไหนเลย — `stripLeakedTags()` เป็นฟังก์ชันแยกใหม่ ไม่แตะ `stripLeakedReasoning()` เดิม แค่เรียกต่อกัน (`stripLeakedTags(stripLeakedReasoning(...))`) — regex ตัดเฉพาะ tag ที่มีชื่อตรงกับ tag จริงในพรอมต์เท่านั้น (ไม่ใช่ `<` ตัวเดียวลอยๆ) จึงไม่เสี่ยงตัดข้อความปกติที่ลูกค้า/บอทพิมพ์ปนมาโดยบังเอิญ — ไม่แตะ `generateReplyWithImage()`/`analyzeImageIntent()`/`analyzeImageWithText()` เลยตามที่ตกลง (ขอบเขตเดียวกับที่ `stripLeakedReasoning()` มีอยู่แล้ว)

**ขอบเขต:** แก้ 2 ไฟล์ ([lib/prompts.ts](lib/prompts.ts), [lib/gemini.ts](lib/gemini.ts)) — **มีผลทุกช่องทาง** (LINE/Facebook/Web Chat) เพราะ `<guardrails>` ไม่มี channel branching และ `stripLeakedTags()` ใช้ใน `generateReply()` ที่ทุกช่องทางเรียกร่วมกัน

---

## เรื่องที่ 60 — Requirement Gate ก่อนแนะนำสินค้าด้วยราคา/งบประมาณอย่างเดียว (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าถาม **"มีราคาไม่เกิน2000ไหม"** (ไม่มีบริบทลักษณะการใช้งานเลย) — บอทตอบราคาสินค้าที่เข้าเงื่อนไข (Spender ID2462) ทันที ทั้งที่ยังไม่รู้เลยว่าลูกค้าจะเอาไปใช้งานแบบไหน — เริ่มต้นวิเคราะห์จากอาการ "ตอบไม่ครบทุกรุ่นที่เข้าเงื่อนไขราคา" (มี TCM-246 ราคา 1,750 บาทเข้าเงื่อนไขด้วยแต่ไม่ถูกกล่าวถึง) แต่หลังหารือกับผู้ใช้ พบว่า**ปัญหาจริงลึกกว่านั้น**: บอทไม่ควรเลือก/แนะนำสินค้าด้วยเงื่อนไขราคาอย่างเดียวโดยไม่รู้บริบทการใช้งานของลูกค้าเลย (แนะนำรุ่นผิดบริบท เช่น รุ่นระยะสั้นให้คนต้องการระยะไกล จะทำให้ลูกค้าไม่พอใจได้จริง ไม่ใช่แค่เรื่องตอบไม่ครบตัวเลือก)

**สาเหตุ:** ตรวจ codebase พบว่า**ไม่มีจุดไหนในระบบทำหน้าที่ "เช็คว่ารู้บริบทลูกค้าเพียงพอหรือยัง" เลย**:
- วิเคราะห์ Intent — `RECOMMEND_RE` ใน `lib/gemini.ts` ใช้แค่เลือกฟังก์ชันค้นเว็บ ไม่ควบคุม logic หลัก
- ตรวจสอบ Requirement — ไม่มีเลย `<recommend_protocol>` ชั้นที่ 1 เดิมไม่มีขั้นตอนเช็คบริบทลูกค้าก่อนค้นหาสินค้า
- ตัดสินใจว่าข้อมูลเพียงพอหรือยัง — มีอยู่แต่เช็คผิดสิ่ง: ข้อความ "ถ้าทั้งชั้นที่ 1 และ 2 ไม่มีข้อมูลเพียงพอ" (ท้าย protocol) เช็คว่า **"หาสินค้าเจอไหม"** ไม่ใช่ **"รู้บริบทลูกค้าพอไหม"** — พอชีตมีสินค้าราคาต่ำกว่างบอยู่แล้ว (เกือบทุกร้าน) เงื่อนไขนี้ไม่มีวันถูกกระตุ้นเลย เพราะ "หาเจอ" เสมอ ทำให้ข้ามการถามบริบทไปตอบราคาทันทีทุกครั้ง

**วิธีแก้:** เพิ่ม **"Requirement Gate"** เป็นขั้นตอนแยกต่างหากใน `<recommend_protocol>` ก่อนชั้นที่ 1-2 (แยก Front Gate ออกจาก Product Matching ชัดเจน ไม่ปนกับ logic ค้นหาสินค้าเดิม ตามที่ตกลงกันไว้):
1. เช็คทั้งข้อความปัจจุบัน **และ conversation history ก่อนหน้าทั้งหมด** เสมอ (กันถามซ้ำถ้าลูกค้าเคยบอกบริบทไปแล้ว) ว่ามีสัญญาณบริบทการใช้งาน (สถานที่/ประเภทงาน/ลักษณะการใช้งาน) อยู่บ้างไหม
2. ถ้ามีบริบทพอสำหรับเลือกสินค้าเบื้องต้นแล้ว (**ไม่ต้องครบทุกด้าน** เช่น ไม่ต้องรู้จำนวนเครื่อง/ระยะทางละเอียดก็ตอบได้ — ผ่อนตามที่ปรับแก้ข้อ C หลังทบทวนรอบแรก) → ไปชั้นที่ 1-2 ต่อได้เลย ไม่ถามเพิ่มโดยอัตโนมัติ
3. ถ้าไม่มีบริบทเลยจริงๆ (ถามแค่งบ/ราคาล้วนๆ หรือขอแนะนำเฉยๆ) → ถามคำถามเดียว (ใช้ข้อความเดิมที่มีอยู่แล้วในท้าย protocol ไม่สร้างคำถามใหม่)

**ตามข้อกำกับที่ตกลงกันไว้ก่อนอนุมัติ:** (1) ไม่สร้างระบบ Requirement Extraction แยกต่างหาก — ใช้ดุลพินิจของ Gemini ผ่านพรอมต์เดิมเหมือนเดิม ไม่มี code แยกวิเคราะห์ requirement ใหม่ (2) บังคับเช็ค history จริง ไม่ใช่แค่ข้อความล่าสุด (3) แยก Front Gate ออกจาก Product Matching ชัดเจนเป็นย่อหน้าของตัวเอง ไม่ปนกับชั้นที่ 1-2

**ทดสอบก่อน commit (Gemini API จริงผ่าน `.env.local`, ไฟล์ทดสอบลบออกจาก repo แล้ว):** ทดสอบครบ 5 เคสตามที่ผู้ใช้กำหนด Flow ไว้ ผ่านทุกเคส:
1. ระบุรุ่นชัดเจน ("TC-245MW ราคาเท่าไหร่") → ตอบราคาทันที ไม่ถามลักษณะการใช้งาน ✅
2. งบอย่างเดียว ไม่มีบริบท ("มีวิทยุไม่เกิน 2,000 ไหม") → ถามลักษณะการใช้งานก่อน ไม่ตอบราคาทันที ✅
3. งบ+usage ในข้อความเดียว ("มีวิทยุไม่เกิน 2,000 ใช้ในโรงงานไหม") → ค้นหา/แนะนำได้เลย (TCM-246 ตรงเงื่อนไข) ไม่ถามซ้ำ ✅
4. เคยบอก usage ในเทิร์นก่อน ("ใช้ในโกดังครับ") แล้วถามงบทีหลัง ("แล้วมีรุ่นไม่เกิน 2,000 ไหม") → **ไม่ถามซ้ำ** ใช้ context จาก history ถูกต้อง และแสดงสินค้าครบทั้ง 2 รุ่นที่เข้าเงื่อนไข (TCM-246 + ID2462) ✅
5. ขอคำแนะนำเฉยๆ ไม่มีบริบทเลย ("อยากได้รุ่นแนะนำ") → ถามคำถามสำคัญก่อน ไม่สุ่มแนะนำ ✅

**ทำไมไม่กระทบอย่างอื่น:** แก้เฉพาะใน `<recommend_protocol>` (เพิ่มย่อหน้าใหม่ก่อนชั้นที่ 1 เท่านั้น ไม่แก้ชั้นที่ 1-2 หรือข้อความ fallback ท้าย protocol เลย) — ไม่มี channel branching จึงมีผลทุกช่องทางเท่ากัน — ไม่แตะ `imei_protocol`, `repair_protocol`, `product_template`, `use_case_map`, `<search_tool>` เลย — คำถามที่ระบุรุ่นชัดเจนไม่ผ่าน `<recommend_protocol>` อยู่แล้ว (ไม่ตรง trigger) จึงไม่ถูกกระทบ — เป็นการเปลี่ยนพฤติกรรมที่ตั้งใจ (breaking change ที่ต้องการ): คำถามงบอย่างเดียวไม่มีบริบทจะได้คำถามกลับก่อนเสมอ แทนที่จะได้คำตอบราคาทันทีแบบเดิม

**ขอบเขต:** แก้ไฟล์เดียว ([lib/prompts.ts](lib/prompts.ts)) จุดเดียว (`<recommend_protocol>`) — **มีผลทุกช่องทาง** (LINE/Facebook/Web Chat)

---

## เรื่องที่ 61 — ขยาย log ถาวรครอบคลุม error ทั่วไปของ webhook (ไม่ใช่แค่รูปภาพ) + แท็บใหม่ในหน้า Admin (LINE + Facebook)

**เคสจริงที่พบ:** 2 เคสในคืนเดียวกัน (1) ลูกค้ายืนยันรูปภาพแล้วพิมพ์ต่อ แต่**ไม่ได้รับคำตอบเลย** (เงียบสนิท) — สงสัยว่า Vercel ฆ่าฟังก์ชันทิ้งเพราะเกิน `maxDuration=60s` ก่อน catch-all จะได้ทำงาน (2) ลูกค้าพิมพ์ข้อความล้วนๆ (ไม่มีรูป) แล้วได้ `UNAVAILABLE_MSG` — เกิดจาก timeout 20 วิของ flow ข้อความทั่วไป — ทั้ง 2 เคสไม่มี log ถาวรรองรับเลย ต้องพึ่ง Vercel live log ซึ่งหมดเวลาเก็บไปแล้วก่อนจะเข้าไปดูทัน (ปัญหาเดิมที่เจอซ้ำเป็นครั้งที่ 3 ในเซสชันนี้)

**สาเหตุ:** เรื่องที่ 56 (persistent log) ครอบคลุมแค่ **path วิเคราะห์รูปภาพ** (`analyzeImageWithText()`) เท่านั้น — จุดอื่นที่อาจทำให้ลูกค้าไม่ได้รับคำตอบยังพึ่ง Vercel live log อย่างเดียว: (1) `GEMINI_UNAVAILABLE` ของ flow ข้อความทั่วไป (2) reply+push ล้มเหลวทั้งคู่ (3) catch-all ท้ายสุดที่ดักทุก error ที่ไม่ถูกจับจุดอื่น

**วิธีแก้:**
1. เพิ่มฟังก์ชัน `logWebhookError()` ใน [app/api/line-webhook/route.ts](app/api/line-webhook/route.ts) และ [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts) — บันทึกลง Redis key ใหม่ **`webhook_error_log`** (แยกจาก `image_text_failure_log` เดิมเด็ดขาด ไม่กระทบเรื่องที่ 56/57) เรียกใช้ที่ 3 จุด: `gemini_text_timeout` (timeout 20 วิของข้อความทั่วไป), `reply_push_failed` (ส่งไม่สำเร็จทั้งคู่), `webhook_error` (catch-all ท้ายสุด)
2. เพิ่ม [app/api/admin/webhook-errors/route.ts](app/api/admin/webhook-errors/route.ts) (ไฟล์ใหม่ — GET/DELETE ตาม pattern เดียวกับ `image-failures`)
3. แก้ [app/admin/image-failures/page.tsx](app/admin/image-failures/page.tsx) เพิ่มเป็น **หน้าแท็บ** — แท็บ 1 "🖼️ รูปภาพตอบไม่ทัน" (เนื้อหา/logic เดิมจากเรื่องที่ 57 ไม่แก้เลย) + แท็บ 2 "⚠️ Error อื่นๆ" (ใหม่ เรียก API ใหม่แยกต่างหาก) — **URL เดิมไม่เปลี่ยน** (`/admin/image-failures`) ตามที่ขอ ไม่ต้องจำลิงก์ใหม่

**ทดสอบก่อน commit:** `tsc --noEmit` ผ่าน + `next build` ผ่านสมบูรณ์ (ยืนยัน route `/api/admin/webhook-errors` ใหม่ และหน้า `/admin/image-failures` ที่แก้แล้ว compile/generate ถูกต้อง ไม่มี error) — เป็นการเพิ่ม logging/UI ล้วนๆ ไม่ใช่การเปลี่ยน logic การตีความ จึงไม่จำเป็นต้องทดสอบผ่าน Gemini จริงแบบผ่าน/ไม่ผ่าน (เหมือนแนวทางเดียวกับเรื่องที่ 57)

**ทำไมไม่กระทบอย่างอื่น:** `logWebhookError()` ห่อด้วย try/catch เหมือนทุกจุดในระบบ (Redis ล่ม = ข้ามไปเฉยๆ) — ไม่เปลี่ยนข้อความที่ลูกค้าเห็นในทั้ง 3 จุดเลยแม้แต่ตัวอักษรเดียว (ยังคงตอบ UNAVAILABLE_MSG/DEFAULT_REPLY/notifyAdmin เหมือนเดิมทุกประการ) — ไม่แตะ `image_text_failure_log`/API เดิม (`/api/admin/image-failures`) แม้แต่บรรทัดเดียว แท็บ 1 จึงทำงานเหมือนเดิม 100% — ไม่แตะ timeout values (20s/35s), ไม่แตะ `lib/prompts.ts`, ไม่แตะ Web Chat (คนละไฟล์) — **ยังไม่ได้แก้ "internal safety-timeout" (ส่วนที่ 2 ที่เคยเสนอไว้คู่กัน)** เพราะซับซ้อนกว่าและเสี่ยงส่งข้อความซ้ำ ต้องออกแบบเพิ่มเติมก่อนแยกต่างหาก

**ขอบเขต:** แก้ 2 ไฟล์เดิม ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts)) + แก้ 1 ไฟล์ ([app/admin/image-failures/page.tsx](app/admin/image-failures/page.tsx)) + เพิ่มไฟล์ใหม่ 1 ไฟล์ ([app/api/admin/webhook-errors/route.ts](app/api/admin/webhook-errors/route.ts)) — **LINE + Facebook เท่านั้น** (Web Chat ไม่มีโครงสร้าง webhook เดียวกัน)

---

## เรื่องที่ 62 — กัน `findExactMatch()` ตัดผ่าน `repair_protocol` (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าแจ้งจอวิทยุแตก บอทถามยืนยันรุ่น+อาการตาม `<repair_protocol>` ลูกค้าตอบ "MODEL TC-11HW ครับ" — บอทกลับตอบ**ราคา/สเปค/ลิงก์สั่งซื้อของ TC-11HW ทันที** เหมือนลูกค้าถามซื้อสินค้าใหม่ ทั้งที่กำลังอยู่กลาง flow แจ้งซ่อม

**สาเหตุ:** [lib/sheet.ts](lib/sheet.ts) ฟังก์ชัน `findExactMatch()` เป็นทางลัดที่ทำงาน**ก่อน** Gemini เสมอ (ทุกช่องทาง) มี `BLOCK_INTENT_RE` กันคำอย่าง "ซ่อม/พัง/เสีย" อยู่แล้ว แต่เช็คแค่**ข้อความปัจจุบัน**เดี่ยวๆ ไม่เคยเห็น conversation history เลย — "MODEL TC-11HW ครับ" ไม่มีคำต้องห้ามและตรง keyword สินค้าพอดี 1 แถว จึงหลุดผ่าน shortcut ไปตอบขายสินค้าแทนที่จะให้ Gemini (ซึ่งมี history เต็ม) จัดการต่อ `repair_protocol`

**วิธีแก้:** เพิ่ม parameter `lastBotTurn?: string` ให้ `findExactMatch()` — ถ้าคำตอบล่าสุดของบอทมีคำว่า "รุ่น" และ "อาการ" ปนกัน (ลักษณะเฉพาะของคำถามยืนยันใน `repair_protocol`) ให้ข้าม shortcut นี้ทันที ปล่อยผ่านให้ Gemini ตอบต่อแทน — แก้ 3 จุดเรียกใช้ ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts), [app/api/web-chat/route.ts](app/api/web-chat/route.ts)) ส่ง bot turn ล่าสุดจาก `history` ที่โหลดไว้อยู่แล้วเข้าไปเพิ่ม ไม่ต้องปรับโครงสร้างอื่น

**ทดสอบก่อน commit:** เป็นฟังก์ชัน deterministic (regex + sheet lookup) ไม่เรียก Gemini เลย จึงไม่มี non-determinism ต้องกังวล — ทดสอบผ่านสคริปต์ชั่วคราวเรียก `findExactMatch()` ตรงๆ ยืนยันว่าเมื่อ `lastBotTurn` มีคำว่า "รุ่น"+"อาการ" ปนกัน ฟังก์ชันคืนค่า `null` (ข้าม shortcut) ถูกต้องตามที่ออกแบบ — ส่วน `tsc --noEmit` และ `next build` ผ่านสมบูรณ์ทั้งคู่ (สคริปต์ทดสอบลบออกจาก repo แล้วก่อน commit)

**ทำไมไม่กระทบอย่างอื่น:** เงื่อนไขใหม่ทำงานเฉพาะเมื่อ bot turn ล่าสุดมีคำว่า "รุ่น"+"อาการ" ปนกันเท่านั้น — บทสนทนาปกติที่ไม่เข้าเงื่อนไขนี้ (ส่วนใหญ่ทั้งหมด) พฤติกรรมเดิมทุกประการ ไม่มีการเปลี่ยนแปลง — ไม่แตะ `BLOCK_INTENT_RE`, การจับคู่ keyword, หรือ logic ส่วนอื่นใน `findExactMatch()` เลย — `repair_protocol` ใช้ร่วมกันทุกช่องทางอยู่แล้ว จึงแก้จุดเดียวมีผลถูกต้องเท่ากันทั้ง LINE/Facebook/Web Chat โดยไม่ต้องแยก logic ตาม channel — ความเสี่ยงที่เหลืออยู่: ข้อความยืนยันของ `repair_protocol` เป็น dynamic text จาก Gemini (ไม่ใช่ค่าคงที่) หากบางครั้งไม่มีคำว่า "รุ่น"+"อาการ" ปนกัน การเช็คนี้จะไม่จับ แต่กรณีนั้นแค่กลับไปเป็นพฤติกรรมเดิมก่อนแก้ ไม่ใช่ regression ใหม่

**ขอบเขต:** แก้ 4 ไฟล์ ([lib/sheet.ts](lib/sheet.ts), [app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts), [app/api/web-chat/route.ts](app/api/web-chat/route.ts)) — มีผลทุกช่องทาง (LINE, Facebook, Web Chat)

---

## เรื่องที่ 63 — กันคำว่า "FAQ" เปล่าๆ (ไม่มีวงเล็บ) หลุดในคำตอบจริง ต่อยอดเรื่องที่ 59 (ทุกช่องทาง)

**เคสจริงที่พบ:** บอทตอบลูกค้าเรื่องเฟิร์มแวร์ TC-6M ว่า "...เนื่องจากเครื่อง TC-6M **ไม่ได้ระบุอยู่ในคำตอบของ FAQ** โดยตรง น้องใจดีแนะนำให้สอบถามทีมงาน..." — คำว่า "FAQ" (ศัพท์เทคนิคภายในระบบ) หลุดออกมาปนในประโยคที่ส่งถึงลูกค้าโดยตรง

**สาเหตุ:** เป็นปัญหาตระกูลเดียวกับเรื่องที่ 59 ทุกประการ แค่คนละรูปแบบ — กฎเดิมที่เพิ่มไปในเรื่องที่ 59 ([lib/prompts.ts](lib/prompts.ts)) ห้ามเฉพาะรูปแบบ tag ที่มีวงเล็บมุม (`<faq>`) เท่านั้น ตัวอย่างที่ให้ไว้ในกฎเองก็สอนแค่ให้ตัดวงเล็บออก ("ไม่พบข้อมูลใน `<faq>`" → "ไม่พบข้อมูลในระบบ") ไม่ได้ห้ามพูดคำว่า "FAQ" เปล่าๆ ด้วย — Gemini เลยยังใช้คำนี้เป็นศัพท์อธิบายแหล่งข้อมูลในประโยคปกติได้ (คำนี้ถูกอ้างอิงซ้ำมากกว่า 30 จุดทั่วพรอมต์) ส่วนตาข่ายรองรับโค้ด `stripLeakedTags()` ก็ใช้ regex ที่บังคับต้องมีวงเล็บ `<>` เช่นกัน จึงหลุดทั้ง 2 ชั้นพอดี

**วิธีแก้ (ต่อยอดกฎเดิมจุดเดียวกัน ไม่แยกกฎใหม่):**
1. ขยายข้อความกฎใน `<guardrails>` ([lib/prompts.ts](lib/prompts.ts)) ให้ครอบคลุมคำว่า "FAQ" แบบไม่มีวงเล็บด้วยชัดเจน
2. ขยาย `stripLeakedTags()` ([lib/gemini.ts](lib/gemini.ts)) เพิ่ม `LEAKED_FAQ_WORD_RE` แยกจับคำว่า "FAQ" เปล่าๆ แล้วแทนที่ด้วย "ระบบ" — ใช้ lookbehind/lookahead กัน `/` `.` ตัวอักษรติดกัน เพื่อไม่ไปแก้คำว่า faq ที่อาจเป็นส่วนหนึ่งของ URL (ห้ามแก้ไข URL เด็ดขาดตาม guardrails เดิม)

**ทดสอบก่อน commit:** ส่วน code-level (`stripLeakedTags()`) เป็น regex ล้วนๆ ทดสอบตรงๆ ผ่านสคริปต์ชั่วคราว 4 เคส (เคสจริงจากรายงาน, เคส `<faq>` เดิมของเรื่องที่ 59 ยังทำงานถูกต้อง, กัน false-positive กับ URL ที่มีคำว่า faq, ข้อความปกติไม่เปลี่ยนแปลง) ผ่านครบ 4/4 — ส่วน prompt-level ทดสอบเรียก `generateReply()` จริงผ่าน Gemini API (`.env.local`) ด้วยสถานการณ์จำลองใกล้เคียงเคสจริง ยืนยันไม่มีคำว่า "FAQ" หลุดในคำตอบ (หมายเหตุ: เครือข่ายทดสอบท้องถิ่นเข้าเว็บ spenderclub.com ไม่ได้ ทำให้ไม่สามารถจำลอง branch ที่ทำให้เกิดบั๊กเป๊ะๆ ได้ 100% แต่ตาข่ายรองรับโค้ดชั้นสุดท้ายยืนยันทำงานถูกต้องแน่นอนไม่ว่า Gemini จะตอบอย่างไร) — `tsc --noEmit` และ `next build` ผ่านสมบูรณ์ทั้งคู่ (สคริปต์ทดสอบลบออกจาก repo แล้วก่อน commit)

**ทำไมไม่กระทบอย่างอื่น:** เป็นการขยาย/ต่อยอดกฎ+ตาข่ายรองรับเดิมของเรื่องที่ 59 ที่มีอยู่แล้ว ไม่ได้เปลี่ยน logic อื่นใดในระบบเลย — `LEAKED_FAQ_WORD_RE` ออกแบบด้วย lookbehind/lookahead ป้องกัน URL โดยเฉพาะ ทดสอบยืนยันแล้วว่าไม่แก้ URL — คำว่า "FAQ" ไม่ใช่คำที่ลูกค้าควรเห็นอยู่แล้วตามเจตนาเดิมของเรื่องที่ 59 การแทนที่ด้วย "ระบบ" จึงสอดคล้องกับตัวอย่างที่กฎเดิมกำหนดไว้เป๊ะ

**ขอบเขต:** แก้ 2 ไฟล์ ([lib/prompts.ts](lib/prompts.ts), [lib/gemini.ts](lib/gemini.ts)) — เป็น shared lib ใช้ร่วมกันทุกช่องทางอยู่แล้ว จึงมีผลทุกช่องทาง (LINE, Facebook, Web Chat) โดยไม่ต้องแก้ไฟล์ webhook แยก

---

## เรื่องที่ 64 — กัน handoffMsg ส่งซ้ำเป๊ะไม่จำกัดรอบเมื่อ HANDOFF ซ้ำติดกัน (LINE + Facebook)

**เคสจริงที่พบ:** ลูกค้าเครื่องหาย กังวลว่าจะถูกนำไปใช้ผิดกฎหมาย/กังวลชื่อตัวเอง/ถามเรื่องแจ้งความ ถามคำถามต่อเนื่องหลายรอบในหัวข้อเดียวกัน — แต่ละรอบที่ Gemini ตัดสินใจ HANDOFF (ผ่าน `repair_protocol`/`self_repair_protocol`) ลูกค้าได้รับข้อความ `handoffMsg` เดิมซ้ำเป๊ะทุกครั้งไม่มีวันเปลี่ยน รู้สึกเหมือนบอทตอบวนเวียนไม่ฟังจริง

**สาเหตุ:** ระบบมีกลไกกันซ้ำ (`routed`/`handoff_notified` Redis key) อยู่แล้วสำหรับ path `shouldHandoffImmediate()` (keyword ตรงๆ เช่น "ขอแอดมิน") — ถ้าลูกค้าโดน handoff ซ้ำภายใน 5 นาที จะได้ข้อความสั้นแทน (ครั้งแรก) แล้วเงียบ (ครั้งถัดไป) กันตอบซ้ำ — แต่ path ที่ Gemini เขียน sentinel **"HANDOFF:"** เองผ่าน `repair_protocol`/`self_repair_protocol`/`imei_protocol` ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts)) **ไม่เคยเช็ค `alreadyRouted` เลย** ส่ง `replyMsg` เต็มซ้ำทุกครั้งไม่จำกัดรอบ และรีเซ็ต `handoff_notified` ทิ้งทุกครั้งด้วยซ้ำ

**วิธีแก้ (ต่อยอด mechanism เดิม ไม่สร้างกลไกใหม่):** เพิ่มเช็ค `alreadyRouted` (จาก `routed:${userId}`) ก่อนตัดสินใจว่าจะส่งอะไร ในบล็อก "HANDOFF:" — ถ้า routed อยู่แล้ว: increment `handoff_notified` counter เหมือน path เดิมทุกประการ (ครั้งแรก = ข้อความสั้น "แอดมินจะรีบติดต่อกลับในเวลาทำการนะคะ...", ครั้งถัดไป = เงียบ) ถ้ายังไม่เคย routed: ทำงานเหมือนเดิมทุกประการ (ส่ง `replyMsg` เต็ม + set `routed`/`takeover` + del `handoff_notified`) — แก้ทั้ง LINE และ Facebook (Facebook มีเงื่อนไข IMEI redirect เพิ่มเติมที่ไม่แตะเลย) — **ไม่แตะ Web Chat** เพราะ `web-chat/route.ts` ใช้ pattern คนละแบบเลย (ไม่มี `routed`/`handoff_notified` ในระบบ)

**ทดสอบก่อน commit:** เป็น control-flow logic ล้วนๆ ไม่เรียก Gemini เลย ไม่มี non-determinism — โครงสร้างโค้ดคัดลอกมาจาก pattern ที่มีอยู่แล้วและพิสูจน์แล้วในโปรดักชัน (`shouldHandoffImmediate` block) แบบคำต่อคำ (Redis key เดียวกัน, TTL เดียวกัน, ข้อความเดียวกัน) — ตรวจสอบด้วย `tsc --noEmit` (ผ่าน) และ `next build` (ผ่านสมบูรณ์) — ไม่ได้ทดสอบ live กับ Redis จริงเพราะ `.env.local` ไม่มี `UPSTASH_REDIS_URL`/`TOKEN` ในเครื่อง (ข้อจำกัดเดิมของ local dev environment)

**ทำไมไม่กระทบอย่างอื่น:** ไม่แตะ path `shouldHandoffImmediate()`/`isOwnerRequest()` เดิมเลยแม้แต่บรรทัดเดียว — เพิ่มแค่เงื่อนไข gate ก่อนบล็อก "HANDOFF:" เท่านั้น ถ้ายังไม่เคย routed พฤติกรรมเดิมทุกประการไม่เปลี่ยนแปลง (ลูกค้าส่วนใหญ่ที่โดน handoff ครั้งแรกจะไม่รู้สึกอะไรเปลี่ยนเลย) — ไม่แตะ `notifyAdmin()`/`notifyAdminFacebook()` เลย (ยังคงเป็นไปตามนโยบายเดิมเรื่องที่ 37/35/42 ไม่ notify แบบเร่งด่วน) — ไม่แตะ Web Chat เพราะสถาปัตยกรรมคนละแบบ ไม่มี state ที่เกี่ยวข้องให้แตะ

**ขอบเขต:** แก้ 2 ไฟล์ ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts)) — LINE + Facebook เท่านั้น (Web Chat ไม่มีโครงสร้าง state เดียวกัน)

---

## เรื่องที่ 65 — บันทึกข้อความลูกค้าลง history แม้ Gemini ตอบไม่ทัน (text-flow, LINE + Facebook)

**เคสจริงที่พบ:** ลูกค้าพิมพ์ข้อมูลแจ้งเปิดระบบใช้งานเครื่องก้อนใหญ่ (อีเมล, ชื่อ-นามสกุลจริง, เบอร์โทร, ร้านที่ซื้อ, ชื่อใช้งาน, Default Group, ห้องที่เพิ่ม) — Gemini พลาด (503 high demand ตามหลักฐานใน `webhook_error_log` เรื่องที่ 61) ลูกค้าได้ `UNAVAILABLE_MSG` แล้ว**ส่งข้อมูลก้อนเดิมมาซ้ำทั้งหมดอีกรอบ**ในอีกไม่กี่นาทีถัดมา — Gemini พลาดซ้ำอีกรอบ (503 เหมือนเดิม) ลูกค้าต้องพิมพ์/copy ข้อมูลชุดใหญ่ซ้ำโดยไม่จำเป็น

**สาเหตุ:** ตรวจพบว่าบล็อก `if (reply === GEMINI_UNAVAILABLE)` ([app/api/line-webhook/route.ts:645](app/api/line-webhook/route.ts#L645), [app/api/fb-webhook/route.ts:539](app/api/fb-webhook/route.ts#L539)) เป็น**จุดเดียวในไฟล์ทั้ง 2**ที่ไม่เรียก `saveHistoryExtended()` เลย ต่างจากทุก path อื่นในไฟล์ที่บันทึกเสมอ — ทำให้ข้อความที่ลูกค้าเพิ่งพิมพ์ไปหายจาก `history` ทันทีที่ Gemini พลาด ถ้าลูกค้าพิมพ์ตามมาใหม่ภายใน 10 นาที (history TTL) บอทก็เริ่มนับหนึ่งใหม่ ไม่มีความจำเหลือจากความพยายามครั้งก่อนเลย

**วิธีแก้:** เพิ่ม `saveHistoryExtended(userId, [...history, { role: 'user', text: userMessage }, { role: 'model', text: UNAVAILABLE_MSG }])` ในบล็อกนี้ ให้ตรงกับ pattern เดิมที่ใช้ทุกจุดอื่นในไฟล์ — แก้ทั้ง LINE และ Facebook

**ทดสอบก่อน commit:** เป็นการเพิ่มบรรทัดเดียว (pure addition ไม่แก้ไข/ลบโค้ดเดิมเลย) เรียกฟังก์ชันที่มีอยู่แล้วและถูกใช้/ทดสอบมาแล้วนับสิบจุดในไฟล์เดียวกัน ความเสี่ยงต่ำมาก — `tsc --noEmit` และ `next build` ผ่านสมบูรณ์ทั้งคู่ — `git diff --stat` ยืนยันเป็น pure addition (9 insertions, 0 deletions) ทั้ง 2 ไฟล์

**ทำไมไม่กระทบอย่างอื่น:** จำกัดอยู่แค่บล็อกเดียวที่ Gemini ตอบไม่ทันเท่านั้น (`GEMINI_UNAVAILABLE` ของ text-flow 20 วิ) — ไม่แตะ path ที่ Gemini ตอบสำเร็จ, `findExactMatch()`, HANDOFF, หรือ image-flow เลย — ไม่เปลี่ยนความเร็ว/ข้อความที่ลูกค้าเห็น (`UNAVAILABLE_MSG` เหมือนเดิมทุกตัวอักษร) — ไม่ set `takeover` (บล็อกนี้ไม่ใช่ HANDOFF trigger) บอทยังตอบข้อความถัดไปของลูกค้าตามปกติ ไม่เงียบเพิ่มขึ้นจากเดิม — **ข้อจำกัดที่ทราบและยอมรับแล้ว:** ถ้าแอดมินเข้ามาคุยกับลูกค้าโดยตรงนอกระบบ (ไม่ผ่าน webhook) ในช่วง 10 นาทีนั้นพอดี บอทจะไม่รู้เรื่องที่คุยไปแล้ว และอาจหยิบข้อความที่ค้างใน history มาอ้างอิงซ้ำได้ — เป็นข้อจำกัดเชิงโครงสร้างเดิมของระบบ history ทั้งหมดอยู่แล้ว (ทุก path ที่บันทึก history มีความเสี่ยงแบบเดียวกัน) ไม่ใช่ความเสี่ยงใหม่ที่เกิดจากการแก้นี้โดยเฉพาะ และจำกัดอยู่แค่หน้าต่าง 10 นาทีเท่านั้น

**ขอบเขต:** แก้ 2 ไฟล์ ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts)) เฉพาะ text-flow — LINE + Facebook เท่านั้น (ไม่แตะ image-flow ที่มี `GEMINI_UNAVAILABLE` ของตัวเองแยกต่างหากใน Facebook, ไม่แตะ Web Chat)

---

## เรื่องที่ 66 — สลับตำแหน่งบรรทัด "แอดมินจะตอบกลับ..." ในข้อความทักทายนอกเวลาทำการ (LINE + Facebook)

**คำขอ:** สลับตำแหน่งบรรทัด "แอดมินจะตอบกลับในวันและเวลาทำการ 08:00–17:00 น. ค่ะ" ในข้อความทักทาย (`getWelcomeMessage()`) จากเดิมที่ขึ้นต้นก่อนแม้แต่ก่อนบล็อก "Spender CLUB ยินดีต้อนรับ" ให้ย้ายไปอยู่**หลัง**บล็อกทักทายหลักแทน (เฉพาะช่วงนอกเวลาทำการเท่านั้น — ในเวลาทำการไม่มีข้อความนี้อยู่แล้ว)

**วิธีแก้:** แยก `offHoursPrefix` เดิม (ท่อนเดียวรวมกัน) ออกเป็น 2 ตัวแปร `offHoursHeader` ("🙏 ขณะนี้อยู่นอกเวลาทำการ") และ `offHoursNote` ("แอดมินจะตอบกลับในวันและเวลาทำการ...") แล้วแทรก `offHoursNote` หลังบล็อก "Spender CLUB ยินดีต้อนรับ" แทนที่จะรวมอยู่ใน prefix เดิม — เงื่อนไขเวลา (`thaiHour >= 18 || thaiHour < 8`) เหมือนเดิมทุกประการ ที่ [lib/greeting.ts](lib/greeting.ts)

**ทดสอบก่อน commit:** เป็นฟังก์ชัน deterministic ล้วนๆ (คำนวณจากเวลาปัจจุบัน ไม่เรียก Gemini) ทดสอบตรงด้วยสคริปต์ชั่วคราวเทียบ output กับข้อความที่อนุมัติไว้ทั้ง 2 กรณี (นอกเวลาทำการ/ในเวลาทำการ) ตรงกันทุกตัวอักษร ✅ ทั้งคู่ — `tsc --noEmit` และ `next build` ผ่านสมบูรณ์ทั้งคู่ (สคริปต์ทดสอบลบออกจาก repo แล้วก่อน commit)

**ทำไมไม่กระทบอย่างอื่น:** `getWelcomeMessage()` เป็นแหล่งเดียว (single source) ใช้ร่วมกันทั้ง LINE และ Facebook ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts) 4 จุด, [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts) 2 จุด) ไม่มีข้อความซ้ำ/hardcode ที่อื่นต้องแก้เพิ่ม — Web Chat ไม่ใช้ข้อความนี้เลย ไม่ถูกกระทบ — ไม่แตะเงื่อนไขเวลา, `shouldGreet()`, หรือจุดเรียกใช้ใดๆ เลย เป็นการสลับลำดับข้อความล้วนๆ

**ขอบเขต:** แก้ 1 ไฟล์ ([lib/greeting.ts](lib/greeting.ts)) — มีผลทั้ง LINE และ Facebook (Web Chat ไม่ใช้ข้อความนี้)

---

## เรื่องที่ 67 — กันคำว่า "OUT_OF_DOMAIN" เปล่าๆ (ไม่มีวงเล็บ) หลุดในคำตอบจริง (ทุกช่องทาง)

**เคสจริงที่พบ:** ลูกค้าได้รับคำสั่งควบคุมภายในดิบๆ **"OUT_OF_DOMAIN"** ตรงๆ แทนที่จะได้ข้อความ "น้องใจดีเป็นผู้ช่วยด้านวิทยุสื่อสารเท่านั้นค่ะ..." เกิดซ้ำ 2 ครั้งติดกัน (06:32, 06:37)

**สาเหตุ:** `reasoning_protocol` สั่ง Gemini ให้ตอบ **"[OUT_OF_DOMAIN]"** (มีวงเล็บ) เมื่อคำถามไม่เกี่ยวกับวิทยุสื่อสาร ([lib/prompts.ts:395](lib/prompts.ts#L395)) — แต่ทั้ง 3 ไฟล์ webhook เช็คด้วย `reply.includes(OUT_OF_DOMAIN)` โดย `OUT_OF_DOMAIN = '[OUT_OF_DOMAIN]'` (ต้องมีวงเล็บครบถึงจะตรง) — เมื่อ Gemini เขียนคำนี้ออกมาโดยไม่มีวงเล็บ (non-deterministic, temperature=1.0) เงื่อนไขนี้เป็น false ปล่อยคำสั่งควบคุมดิบหลุดไปให้ลูกค้าเห็นตรงๆ เป็นปัญหาตระกูลเดียวกับเรื่องที่ 36 (HANDOFF:) และเรื่องที่ 63 (FAQ)

**วิธีแก้:** ตัดวงเล็บออกจากค่าคงที่ `OUT_OF_DOMAIN` เป็น `'OUT_OF_DOMAIN'` เฉยๆ ในทั้ง 3 ไฟล์ ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts), [app/api/web-chat/route.ts](app/api/web-chat/route.ts)) ทำให้ `.includes()` จับได้ทั้ง 2 รูปแบบ (มี/ไม่มีวงเล็บ) — พร้อมแก้จุดที่ 2 ใน Facebook ([fb-webhook.ts](app/api/fb-webhook/route.ts), flow ปุ่ม postback) ที่เช็คแบบ **strict equality** (`reply === OUT_OF_DOMAIN`) เปลี่ยนเป็น `.includes()` ด้วย เพื่อไม่ให้กลายเป็นตรวจพลาดกรณี Gemini ใส่วงเล็บถูกต้องแทน (`[OUT_OF_DOMAIN]` ไม่เท่ากับ `'OUT_OF_DOMAIN'` เป๊ะ) — **ไม่แตะ `NOT_FOUND`** ที่มีรูปแบบเสี่ยงแบบเดียวกันแต่ยังไม่มีรายงานว่าหลุดจริง อยู่นอกขอบเขตที่รายงานมา

**ทดสอบก่อน commit:** เป็น string matching ล้วนๆ ไม่เรียก Gemini ไม่มี non-determinism — ทดสอบตรงด้วยสคริปต์ชั่วคราว 8 เคส ผ่านครบ (เคสจริงไม่มีวงเล็บ, เคสปกติมีวงเล็บไม่ regress, คำตอบปกติไม่ถูกกระทบ, `NOT_FOUND`/`GEMINI_UNAVAILABLE` ที่ postback flow ยังทำงานถูกต้องไม่ถูกแตะ) — `tsc --noEmit` และ `next build` ผ่านสมบูรณ์ทั้งคู่ (สคริปต์ทดสอบลบออกจาก repo แล้วก่อน commit)

**ทำไมไม่กระทบอย่างอื่น:** กรณี Gemini ใส่วงเล็บถูกต้องตามปกติ (ส่วนใหญ่) พฤติกรรมเดิมทุกประการ เพราะ `[OUT_OF_DOMAIN]` ยังมีคำว่า "OUT_OF_DOMAIN" ปนอยู่ข้างในอยู่ดี — ไม่แตะ `NOT_FOUND`/`GEMINI_UNAVAILABLE` เลย ทั้ง 2 ตัวยังเช็คแบบเดิมทุกจุด — ไม่แตะ `reasoning_protocol`/prompt เลย แก้แค่ฝั่งตรวจจับ (code-level)

**ขอบเขต:** แก้ 3 ไฟล์ ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts), [app/api/web-chat/route.ts](app/api/web-chat/route.ts)) — มีผลทุกช่องทาง (LINE, Facebook, Web Chat)

---

## เรื่องที่ 68 — เปลี่ยนโมเดลจาก Gemini 2.5 Flash เป็น Gemini 3.6 Flash (ทุกฟังก์ชัน)

**สาเหตุ:** เจอ error 503 "high demand" จาก Gemini ถี่ขึ้นเรื่อยๆ (เกิดซ้ำหลายวันติดกัน 20, 21, 25, 26 ส.ค.) — ค้นข้อมูลพบรายงานใน Google AI Developers Forum ว่า Gemini 2.5 Flash เริ่มด้อยประสิทธิภาพ/ใช้งานไม่ได้ก่อนวันเลิกใช้งานจริง (16 ต.ค. 2569) ตรงกับสมมติฐานที่ตั้งไว้ — Gemini 3.6 Flash เป็น GA (เสถียรพร้อมใช้งานจริง) แล้วตั้งแต่ 21 ก.ค. 2569 เป็นรุ่นที่ Google แนะนำให้ย้ายไป

**วิธีแก้:** เปลี่ยน `const MODEL = 'gemini-2.5-flash'` เป็น `'gemini-3.6-flash'` ใน [lib/gemini.ts](lib/gemini.ts) (จุดเดียว ใช้ร่วมกันทุกฟังก์ชัน) — ค้นเพิ่มพบว่า 3.6 Flash **ไม่รองรับ `temperature` ที่กำหนดเองแล้ว** (ใส่มาจะถูกเพิกเฉยเงียบๆ ไม่ error) จึงตัดออกจากทุกจุดที่เคยตั้งไว้ (`generateReply()` 2 จุด, `generateReplyWithImage()` 2 จุด, `analyzeImageIntent()`/`analyzeImageWithText()` 2 จุด) และ **`thinkingConfig.thinkingBudget` (ตัวเลข) ถูกยกเลิกในเจน 3.x** ต้องใช้ `thinkingLevel` (enum: `ThinkingLevel.MINIMAL`/`LOW`/`MEDIUM`/`HIGH`) แทน (ใช้ทั้ง 2 พารามิเตอร์พร้อมกันจะ error 400 ทันที) — แมปตามเจตนาเดิม: budget 1024 (`generateReply()`, งานสนทนา/ใช้ tool) → `ThinkingLevel.MEDIUM` (ค่า default มาตรฐาน), budget 300 (`analyzeImageIntent()`/`analyzeImageWithText()`, งานจำแนกประเภท/สกัดข้อมูล) → `ThinkingLevel.MINIMAL` (ตรงกับคำอธิบายที่ Google ให้ไว้: "for high-volume, simple tasks like extraction, routing, or classification")

**ทดสอบก่อน commit:** เป็นการเปลี่ยนโมเดล + พารามิเตอร์ที่กระทบพฤติกรรม Gemini โดยตรง จึงทดสอบผ่าน Gemini API จริงครบทุกฟังก์ชัน: `generateReply()` 4 เคส (ตอบจาก FAQ ตรง, out-of-domain ตอบ `[OUT_OF_DOMAIN]` ถูกต้อง, ทักทายทั่วไป, ค้นเว็บเมื่อไม่เจอใน FAQ) ผ่านหมด ไม่มี error, ไม่มี tag/sentinel หลุด — `analyzeImageIntent()`/`analyzeImageWithText()` สโมคเทสต์ยืนยัน API call กับ `thinkingLevel: MINIMAL` ทำงานถูกต้องไม่ error — `tsc --noEmit` และ `next build` ผ่านสมบูรณ์ทั้งคู่ (สคริปต์ทดสอบลบออกจาก repo แล้วก่อน commit)

**ทำไมไม่กระทบอย่างอื่น:** แก้ไฟล์เดียว ([lib/gemini.ts](lib/gemini.ts)) เป็น shared lib ใช้ร่วมกันทุกช่องทางอยู่แล้ว — ไม่แตะ prompt (`lib/prompts.ts`), ไม่แตะ logic การตัดสินใจ/routing ใดๆ ในไฟล์ webhook เลย — **ข้อจำกัดที่ต้องยอมรับ (ไม่มีทางเลี่ยงได้):** ไม่สามารถคง temperature=0/1.0 แบบเดิมได้อีกต่อไปเพราะ API ไม่รองรับแล้ว หมายความว่าความแม่นยำแบบ deterministic ของการอ่าน S/N/IMEI จากรูป (เดิมพึ่ง temperature=0) จะขึ้นกับค่าที่ Google กำหนดภายในเองแทน — เป็นข้อจำกัดของแพลตฟอร์ม ไม่ใช่สิ่งที่แก้เพิ่มได้ในโค้ดฝั่งเรา

**ขอบเขต:** แก้ 1 ไฟล์ ([lib/gemini.ts](lib/gemini.ts)) — มีผลทุกช่องทางและทุกฟังก์ชัน (text-flow, image-flow ทั้ง LINE/Facebook/Web Chat)

---

## เรื่องที่ 69 — ขยับ thinkingLevel เป็น MEDIUM + maxOutputTokens เป็น 2000 สำหรับฟังก์ชันอ่านรูป (LINE + Facebook, image-flow)

**สาเหตุ:** ต่อเนื่องจากเรื่องที่ 68 (เปลี่ยนโมเดลเป็น Gemini 3.6 Flash) — `analyzeImageIntent()`/`analyzeImageWithText()` (อ่าน S/N/IMEI จากรูป) ตั้ง `thinkingLevel: MINIMAL` ไว้ (เทียบเท่า `thinkingBudget: 300` เดิม) แต่เดิมความแม่นยำ/ความสม่ำเสมอในการอ่านตัวเลขพึ่ง `temperature: 0` เป็นหลัก ซึ่ง Gemini 3.6 Flash ไม่รองรับให้กำหนดเองแล้ว (เรื่องที่ 68) — เพื่อชดเชยความแม่นยำที่อาจลดลง จึงพิจารณาขยับ `thinkingLevel` เป็น `MEDIUM` ให้โมเดล "คิด" ก่อนตอบมากขึ้น

**ปัญหาที่พบระหว่างทดสอบ (ก่อนแก้):** ทดสอบตรงพบว่า `thinkingLevel: MEDIUM` ใช้ thinking token มากขึ้นมาก (เคสรูปเดี่ยวง่ายๆ ใช้ถึง ~405 token, เคส**ส่งหลายรูปพร้อมกัน**ใช้ถึง ~764 token) — เพราะ thinking token กับคำตอบจริงใช้เพดาน `maxOutputTokens` เดียวกัน (ไม่ได้แยกงบ) ถ้าไม่ขยับ `maxOutputTokens` ตาม (ยังคงที่ 800 เดิม) **เคสส่งหลายรูปพร้อมกันจะโดน MAX_TOKENS ตัดคำตอบก่อนเขียนเสร็จจริง** (ทดสอบเจอ finishReason: MAX_TOKENS จริง คำตอบดิบไม่ครบเป็น JSON ที่ parse ไม่ได้ → ตกไปที่ `unclear` แทน ซึ่งแย่กว่าพฤติกรรมเดิม)

**วิธีแก้:** ปรับ 2 พารามิเตอร์พร้อมกันใน [lib/gemini.ts](lib/gemini.ts) ทั้ง `analyzeImageIntent()` และ `analyzeImageWithText()`: `thinkingLevel` จาก `MINIMAL` เป็น `MEDIUM` และ `maxOutputTokens` จาก `800` เป็น `2000` (ทดสอบยืนยันว่า `1500` ผ่านเคสหนักสุดที่ทดสอบได้แล้ว ตั้ง `2000` เผื่อ margin เพิ่มสำหรับเคสจริงที่อาจซับซ้อนกว่า)

**ทดสอบก่อน commit:** ทดสอบผ่าน Gemini API จริงหลายรอบ: (1) เทียบ `MINIMAL` vs `MEDIUM` เคสรูปเดี่ยว ยืนยัน token ที่ใช้เพิ่มขึ้นจริงแต่ยังไม่ชน MAX_TOKENS ที่ 800 (2) เคสส่ง 2 รูปพร้อมกันที่ `maxOutputTokens: 800` → **พิสูจน์ได้จริงว่าพัง (MAX_TOKENS)** (3) เคสเดียวกันที่ `1500` → ผ่าน (STOP, JSON ถูกต้องสมบูรณ์) (4) เรียกฟังก์ชันจริง (`analyzeImageIntent()`/`analyzeImageWithText()`) ที่ `maxOutputTokens: 2000` ทั้งเคสรูปเดี่ยวและหลายรูป ผ่านหมดไม่มี error — `tsc --noEmit`/`next build` ผ่านสมบูรณ์ (สคริปต์ทดสอบลบออกจาก repo แล้วก่อน commit)

**ทำไมไม่กระทบอย่างอื่น:** แก้เฉพาะ config ของ 2 ฟังก์ชันนี้เท่านั้น ไม่แตะ `generateReply()`/`generateReplyWithImage()` (ซึ่งมี `maxOutputTokens`/`thinkingConfig` ของตัวเองแยกต่างหากอยู่แล้ว ไม่ได้ใช้ค่าเดียวกัน) — ไม่แตะ prompt/logic การจำแนกประเภทรูปเลย แก้แค่พารามิเตอร์เรียก API — **ต้นทุนที่เพิ่มขึ้น (ยอมรับแล้ว):** ใช้ thinking token มากขึ้น = ต้นทุนต่อครั้งสูงขึ้นและอาจใช้เวลาตอบนานขึ้นเล็กน้อย เป็นการแลกที่ตั้งใจเพื่อความแม่นยำ

**ขอบเขต:** แก้ไฟล์เดียว ([lib/gemini.ts](lib/gemini.ts)) เฉพาะ 2 ฟังก์ชันอ่านรูป — มีผล LINE + Facebook เท่านั้น (Web Chat ไม่มี image-flow นี้)

---

## เรื่องที่ 70 — ตรวจสอบ IMEI ด้วย Luhn checksum กัน Gemini อ่านเลขผิดโดยไม่รู้ตัว (LINE + Facebook, image-flow)

**สาเหตุ:** ทดสอบจริงด้วยรูปที่สร้างเอง (รู้คำตอบที่ถูกต้องแน่นอน — ground truth) พบว่า Gemini 3.6 Flash อ่าน IMEI **ตกหล่นไป 1 หลักจริง** (1 ใน 5 ครั้ง แม้รูปจะคมชัดไม่เบลอเลย) โดย**ไม่ส่งสัญญาณว่าไม่มั่นใจเลย** (ไม่ขอให้ยืนยัน/ถ่ายใหม่ตามที่ guardrails สั่งไว้) — เป็นผลจากไม่มี `temperature=0` คุมความสม่ำเสมอแล้ว (เรื่องที่ 68)

**วิธีแก้:** เพิ่มการตรวจสอบด้วยมาตรฐานสากลของ IMEI — ต้องยาว**15 หลักเป๊ะ**และมี**เลขตรวจสอบ (checksum) ตามสูตร Luhn** — เพิ่มฟังก์ชัน `isValidImeiChecksum()` + `allImeiMentionsValid()` ใน [lib/gemini.ts](lib/gemini.ts) ที่ค้นหาเลขที่ Gemini ระบุว่าเป็น "IMEI" ในข้อความ (`summary`/`confirmMessage`) แล้วตรวจสอบทุกตัว ถ้าไม่ผ่าน (ยาวไม่ครบ 15 หลัก หรือ checksum ไม่ตรง) จะแทนที่ข้อความด้วย `IMEI_MISREAD_MSG` ("...รบกวนตรวจสอบเลข IMEI จากเครื่องอีกครั้ง หรือถ่ายรูปให้ชัดขึ้น...") แทนที่จะปล่อยเลขที่อ่านผิดผ่านไป — ใช้เฉพาะ `analyzeImageIntent()`/`analyzeImageWithText()` **เฉพาะ IMEI เท่านั้น ไม่แตะ S/N เด็ดขาด** (S/N ไม่มีมาตรฐาน checksum แบบเดียวกัน การเช็คแบบนี้กับ S/N จะ false-positive ได้)

**ทดสอบก่อน commit:** (1) ทดสอบ unit ด้วยสคริปต์ชั่วคราว 6 เคส ผ่านครบ (IMEI ถูกต้องจริงจากการทดสอบ, IMEI ที่อ่านผิดจริงที่เจอในรอบที่ 3 — ตรวจจับได้ถูกต้อง, IMEI สั้นเกินไปชัดเจน, ไม่มี IMEI เลยต้องผ่าน, ข้อความทั่วไปต้องผ่าน, หลายรูปมีตัวผิดปนต้อง false) (2) สร้างรูปทดสอบจริงด้วย S/N/IMEI ที่รู้คำตอบแน่นอน เรียก `analyzeImageIntent()` จริงผ่าน Gemini API ซ้ำ 10 รอบ ยืนยันไม่มี false-positive เลย (IMEI ที่อ่านถูกต้องไม่เคยถูกปฏิเสธผิดพลาด) — `tsc --noEmit`/`next build` ผ่านสมบูรณ์ (สคริปต์ทดสอบลบออกจาก repo แล้วก่อน commit)

**ทำไมไม่กระทบอย่างอื่น:** แก้เฉพาะ 2 ฟังก์ชันนี้เท่านั้น ไม่แตะ `imei_protocol` ใน `lib/prompts.ts` (คนละกลไก — จัดการตอนลูกค้า**พิมพ์**เลข IMEI เองเป็นข้อความ ไม่ใช่จากรูป) ไม่แตะ `generateReply()`/`generateReplyWithImage()` เลย — จะทำงานก็ต่อเมื่อในคำตอบมีเลขที่ระบุว่าเป็น "IMEI" เท่านั้น เคสอื่นที่ไม่เกี่ยวกับ IMEI (S/N อย่างเดียว, สินค้าทั่วไป, error, ใบเสร็จ) ไม่ถูกแตะเลยเพราะเงื่อนไขไม่ทำงาน — ทดสอบยืนยันแล้วว่าไม่ false-positive กับ IMEI ที่ถูกต้อง — **ข้อจำกัดที่ทราบ:** ตรวจจับได้แค่ IMEI ที่ผิดรูปแบบ (ยาวไม่ครบ/checksum ไม่ตรง) เท่านั้น ถ้า Gemini อ่านผิดแบบที่ยังผ่าน checksum ได้บังเอิญ (เช่น สลับ 2 หลักที่ไม่กระทบผลรวม) จะตรวจจับไม่ได้ — ไม่ครอบคลุม S/N เลยตามที่ตั้งใจ (ไม่มีมาตรฐานให้เช็ค)

**ขอบเขต:** แก้ไฟล์เดียว ([lib/gemini.ts](lib/gemini.ts)) เฉพาะ 2 ฟังก์ชันอ่านรูป — มีผล LINE + Facebook เท่านั้น (Web Chat ไม่มี image-flow นี้)

---

## เรื่องที่ 71 — ขยาย Google Sheet fetch timeout จาก 3s เป็น 8s (ทุกช่องทาง)

**สาเหตุ:** เจอเคสจริง: ลูกค้าส่งข้อมูลลงทะเบียนเครื่องก้อนใหญ่ (ชื่อ/เบอร์/อีเมล/IMEI) แล้วบอทตอบ "ไม่พบข้อมูล" (`DEFAULT_REPLY`) ทั้งที่ไม่เกี่ยวกับคำถามลูกค้าเลย — ตรวจ `webhook_error_log` พบสาเหตุคือดึง Google Sheet ไม่ทันภายใน 3 วินาที (`AbortSignal.timeout(3000)` ใน [lib/sheet.ts](lib/sheet.ts)) — เดิมเคยวิเคราะห์ไว้แล้วแต่ยังไม่แก้ ตอนนี้เห็นชัดว่าเป็นสาเหตุจริงในเหตุการณ์ที่ทำให้ลูกค้าเสียโอกาสส่งข้อมูลสำคัญ

**วิธีแก้:** ขยับ `AbortSignal.timeout(3000)` เป็น `8000` (8 วินาที) จุดเดียวใน [lib/sheet.ts](lib/sheet.ts) — งบเวลารวมยังปลอดภัย (8s + Gemini timeout 20s + ส่งข้อความ ~5s ยังห่างจากเพดาน Vercel `maxDuration=60s` มาก)

**ทดสอบก่อน commit:** ทดสอบเรียก `fetchFAQ()` จริงกับชีตจริง ยืนยันดึงสำเร็จปกติ (ใช้เวลาจริงแค่ ~1.6 วินาที ยังห่างจากทั้ง timeout เดิมและใหม่มาก) — `tsc --noEmit`/`next build` ผ่านสมบูรณ์ (สคริปต์ทดสอบลบออกจาก repo แล้วก่อน commit)

**ทำไมไม่กระทบอย่างอื่น:** เป็นตัวเลขเดียว ไม่แตะ logic อื่นเลย — กรณีปกติ (ชีตตอบเร็วอยู่แล้ว ส่วนใหญ่ไม่ถึง 1-2 วินาทีด้วยซ้ำ) ไม่มีอะไรเปลี่ยนแปลง — กรณีชีตช้าจริง (4-8 วิ) จากเดิมเคยได้ "ไม่พบข้อมูล" ผิดๆ จะได้คำตอบจริงที่ถูกต้องแทน (ดีขึ้น) — ใช้ร่วมกันทุกช่องทาง (LINE/Facebook/Web Chat) เพราะ `loadSheet()` เป็นฟังก์ชันเดียวที่ใช้ร่วมกัน

**ขอบเขต:** แก้ไฟล์เดียว ([lib/sheet.ts](lib/sheet.ts)) — มีผลทุกช่องทาง

---

## เรื่องที่ 72 — บันทึกข้อความลูกค้าลง history แม้ดึง Google Sheet ไม่ทัน (LINE + Facebook text-flow)

**สาเหตุ:** ต่อเนื่องจากเรื่องที่ 71 — ถึงจะขยาย timeout แล้ว ก็ยังมีโอกาสเจอกรณีดึงชีตไม่ทันได้อยู่ดี (เครือข่าย/Google Sheets ล่มจริง) — ตรวจพบว่า `const faqText = await fetchFAQ()` ใน text-flow ของทั้ง [app/api/line-webhook/route.ts](app/api/line-webhook/route.ts) และ [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts) **ไม่มี try/catch ของตัวเอง** — ถ้าล้มเหลว error จะทะลุไปถึง catch-all นอกสุดของไฟล์ ซึ่ง**ไม่บันทึก history** (ต่างจาก `GEMINI_UNAVAILABLE` ที่แก้ไปแล้วในเรื่องที่ 65) — เคสจริงที่เจอ: ลูกค้าส่งข้อมูลลงทะเบียนเครื่องก้อนใหญ่ชนจุดนี้พอดี ทำให้ข้อมูลหายจากความจำบอททันที

**วิธีแก้:** เพิ่ม try/catch เฉพาะรอบ `fetchFAQ()` ในทั้ง 2 ไฟล์ (แยกจาก catch-all เดิม ไม่ใช่แก้ที่ catch-all เหมารวม เพราะจุดนั้นดักจับ error จากหลายส่วนของโค้ดพร้อมกัน ไม่รู้แน่ชัดว่ามีข้อมูลลูกค้าที่ถูกต้องให้บันทึกเสมอไป) — ถ้าล้มเหลว: log ผ่าน `logWebhookError()` (type `webhook_error` เดิม) → ตอบ `DEFAULT_REPLY` (ข้อความเดิมทุกตัวอักษร ไม่เปลี่ยน) → **บันทึก history** ก่อน return — ให้ผลเหมือน `GEMINI_UNAVAILABLE` ทุกประการตาม pattern เรื่องที่ 65

**ทดสอบก่อน commit:** เป็น control-flow logic ล้วนๆ ไม่เรียก Gemini เลย ไม่มี non-determinism — โครงสร้างคัดลอกมาจาก pattern ที่พิสูจน์แล้วจากเรื่องที่ 65 แบบเดียวกัน — `tsc --noEmit`/`next build` ผ่านสมบูรณ์ทั้งคู่

**ทำไมไม่กระทบอย่างอื่น:** ข้อความที่ลูกค้าเห็นตอน error เหมือนเดิมทุกตัวอักษร (`DEFAULT_REPLY`) ไม่เปลี่ยน — ไม่แตะ catch-all นอกสุดเดิมเลย (ยังทำงานเหมือนเดิมสำหรับ error ประเภทอื่นที่ไม่ใช่ fetchFAQ()) — ไม่แตะ path ที่ Gemini ตอบสำเร็จ/`GEMINI_UNAVAILABLE`/HANDOFF เลย — ไม่มีความเสี่ยงตอบซ้ำซ้อน (return ทันทีหลัง catch จึงไม่มีทางไปโดน catch-all อีกรอบสำหรับ error เดียวกัน) — **ข้อจำกัดเดิมที่ยอมรับแล้วเหมือนเรื่องที่ 65:** ถ้าแอดมินคุยกับลูกค้าโดยตรงนอกระบบในช่วง 10 นาทีนั้น บอทจะไม่รู้เรื่องที่คุยไปแล้ว

**ขอบเขต:** แก้ 2 ไฟล์ ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts)) เฉพาะ text-flow — LINE + Facebook เท่านั้น (ไม่แตะ Web Chat)

---

## เรื่องที่ 73 — ข้าม replyToken ที่หมดอายุแล้วในบล็อก GEMINI_UNAVAILABLE ใช้ push ตรงๆ แทน (LINE เท่านั้น)

**สาเหตุ:** วิเคราะห์ log ดิบจาก production 3 เคสจริงพบรูปแบบซ้ำเป๊ะ: ลูกค้าเจอ `GEMINI_UNAVAILABLE` (ทั้ง 20s timeout และ 503 เร็ว) → บอทพยายามส่งข้อความกลับด้วย `replyToken` (เพราะเป็นข้อความแรกของวัน มีทักทาย) → ได้ **"400 - Bad Request"** ทุกครั้ง (3/3) → ตกไปใช้ push แทนสำเร็จ — ตรวจโค้ดพบว่า**นี่คือบั๊กเดียวกับที่เคยแก้ไปแล้วสำหรับฝั่งวิเคราะห์รูปภาพ**เมื่อ 4 ส.ค. 2569 (คอมเมนต์เดิมใน [line-webhook.ts:291-294](app/api/line-webhook/route.ts#L291) อธิบายไว้ตรงเป๊ะ: "replyToken มีโอกาสหมดอายุสูงมากกว่าจะถึงจุดส่ง...เสียเวลาลอง reply ที่รู้อยู่แล้วว่าจะพัง") — ตอนนั้นแก้แค่ `pushOnly()` (ฝั่งรูปภาพ) แต่ `ans()` (ฝั่งข้อความทั่วไป) ยังคงพยายาม `safeReply()` ก่อนเสมอถ้า `greetFirst` ทั้งที่ผ่านการรอ Gemini นาน (สูงสุด 20 วิ) มาก่อนแล้วเสมอเมื่อมาถึงบล็อก `GEMINI_UNAVAILABLE`

**วิธีแก้:** ที่บล็อก `if (reply === GEMINI_UNAVAILABLE)` ข้าม `ans()`/`safeReply()` ไปเลย สร้างข้อความทักทาย (ถ้ายังไม่เคยส่ง) + `UNAVAILABLE_MSG` รวมเป็นชุดเดียว ส่งผ่าน `safePush()` ตรงๆ — ไม่แตะ `ans()` เอง (ยังใช้ปกติทุกจุดอื่นที่ตอบสำเร็จ) — **เฉพาะ LINE เท่านั้น** ตรวจแล้ว Facebook ไม่มีแนวคิด replyToken เลย (`fbSend()` ใช้ PSID ส่งตรงเสมอ ไม่มีปัญหานี้) จึงไม่ต้องแก้ฝั่ง Facebook

**ทดสอบก่อน commit:** เป็น control-flow logic ล้วนๆ (การจัดเรียง/เลือกช่องทางส่งข้อความ) ไม่เรียก Gemini เพิ่ม ไม่มี non-determinism — ตรวจโค้ดแล้วยืนยันโครงสร้างถูกต้อง (ยังคง logic การ mark `greetReplied` เหมือนเดิม, เนื้อหาข้อความเหมือนเดิมทุกตัวอักษร เปลี่ยนแค่ช่องทางส่ง) — `tsc --noEmit`/`next build` ผ่านสมบูรณ์ทั้งคู่ — ไม่ได้ทดสอบยิง LINE จริงเพราะเป็นการเรียก LINE Messaging API จริงที่กระทบบัญชีจริง

**ทำไมไม่กระทบอย่างอื่น:** แก้เฉพาะบล็อก `GEMINI_UNAVAILABLE` จุดเดียว ไม่แตะ `ans()`/`safeReply()`/`pushOnly()` ที่ใช้ร่วมกับจุดอื่นเลย — จุดที่ตอบสำเร็จปกติ (ส่วนใหญ่ของระบบ) ยังใช้ `ans()` เหมือนเดิมทุกประการ ไม่มีความเสี่ยง — **ไม่เพิ่มโควต้า push message** เพราะปัจจุบันก็ตกไปใช้ push อยู่แล้วทุกครั้งอยู่ดี (ยืนยันจาก log ทั้ง 3 เคส) แค่ข้าม reply ที่รู้อยู่แล้วว่าจะพังทิ้งไปเท่านั้น จำนวนข้อความที่ส่งจริงเท่าเดิม — เนื้อหาข้อความที่ลูกค้าเห็นเหมือนเดิมทุกตัวอักษร เปลี่ยนแค่กลไกภายใน

**ขอบเขต:** แก้ไฟล์เดียว ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts)) จุดเดียวในไฟล์ — LINE เท่านั้น (Facebook ไม่มีปัญหานี้ตามสถาปัตยกรรม, Web Chat ไม่เกี่ยวข้อง)

---

## เรื่องที่ 74 — แก้ข้อความ ORDER_CONFIRM_OFF_HOURS_MSG ขัดแย้งกันเอง (ระบุ "วันทำการถัดไป")

**สาเหตุ:** เคสจริงจาก log แชท — ลูกค้ายืนยันสั่งซื้อตอน 06:56 น. (ใกล้เวลาเปิดทำการ 08:00 น. อีกแค่ ~1 ชม.) แต่ข้อความ `ORDER_CONFIRM_OFF_HOURS_MSG` มี 2 ประโยคขัดแย้งกันเอง: ประโยคแรกบอก "เจ้าหน้าที่จะเริ่มให้บริการในช่วงเวลา 08:00–17:00 น." (ใกล้แล้ว) แต่ประโยคสองบอก "จะรีบตอบกลับใน**วันทำการถัดไป**" (ฟังดูต้องรอข้ามวัน) — ทำให้ลูกค้าที่ทักใกล้เวลาเปิดเข้าใจผิดว่าต้องรอนานกว่าความเป็นจริงมาก ข้อความนี้ใช้ตัวเดียวกันทั้งช่วง 18:00–07:59 โดยไม่แยกแยะว่าใกล้หรือไกลจากเวลาเปิด — ตรวจสอบข้อความนอกเวลาทำการอื่นในระบบ (`OWNER_REQUEST_OFF_HOURS_MSG` ใน [lib/handoff.ts](lib/handoff.ts#L94), `offHoursNote` ใน [lib/greeting.ts](lib/greeting.ts#L11)) พบว่าไม่มีปัญหานี้เลย เพราะไม่เคยระบุวันเจาะจง บอกแค่ช่วงเวลาทำการเฉยๆ

**วิธีแก้:** ตัดคำว่า "วันทำการถัดไป" ออกจาก `ORDER_CONFIRM_OFF_HOURS_MSG` เปลี่ยนเป็น "เจ้าหน้าที่จะรีบตอบกลับในเวลาทำการนะคะ" (ไม่ระบุวันเจาะจง) ใช้โทนเดียวกับ 2 ข้อความข้างต้นที่พิสูจน์แล้วว่าไม่มีปัญหานี้ — แก้เฉพาะเนื้อหาข้อความ (string constant) ไม่แตะเงื่อนไข `isOffHours`/`ORDER_CONFIRM_RE` หรือ logic การตัดสินใจใดๆ เลย

**ทดสอบก่อน commit:** เป็นการแก้ string constant ล้วนๆ ไม่มี logic เปลี่ยนแปลง ไม่เรียก Gemini เพิ่ม ไม่มี non-determinism — `tsc --noEmit` ผ่านสมบูรณ์ ไม่จำเป็นต้องทดสอบกับ Gemini จริงเพราะไม่ใช่พฤติกรรม AI

**ทำไมไม่กระทบอย่างอื่น:** แก้แค่เนื้อหาข้อความ 1 ตัวแปร มีจุดใช้งานแค่ 2 จุดในทั้งระบบ ([line-webhook.ts](app/api/line-webhook/route.ts#L611) และ [fb-webhook.ts](app/api/fb-webhook/route.ts#L504)) ไม่ถูกอ้างอิงที่อื่น ไม่กระทบเงื่อนไขการตัดสินใจว่าจะส่งข้อความนี้เมื่อไหร่เลย

**ขอบเขต:** แก้ 2 ไฟล์ ([app/api/line-webhook/route.ts](app/api/line-webhook/route.ts), [app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts)) จุดเดียวต่อไฟล์ — sync กันทั้งคู่เพราะข้อความซ้ำกันเป๊ะ

---

## เรื่องที่ 75 — ปรับข้อความชี้ทาง Spender Network/กลุ่ม ให้อ่านง่ายขึ้น (Facebook เท่านั้น)

**สาเหตุ:** ตรวจสอบข้อความชี้ทางไปแอดไลน์ @spenderclub ที่ใช้เมื่อลูกค้าถามเรื่องเข้า/ลบกลุ่มสาธารณะหรือปัญหาระบบ Spender Network (เฉพาะ Facebook) พบว่าเป็นพารากราฟยาวก้อนเดียว อ่านยากบนหน้าจอมือถือ และใช้ชื่อ "Spendernetwork" (คำเดียวติดกัน) ไม่ตรงกับชื่อ "Spender Network" (มีเว้นวรรค) ที่ใช้ในข้อความต้อนรับลูกค้าจริงอยู่แล้ว ([lib/greeting.ts:20](lib/greeting.ts#L20))

**วิธีแก้:** จัดข้อความใหม่เป็นหลายบรรทัด (หัวข้อ / เหตุผล+วิธีติดต่อ / ทีมซัพพอร์ต+เวลาทำการ) แก้ชื่อเป็น "Spender Network" ให้ตรงกับข้อความต้อนรับ เพิ่มประโยคอธิบายเหตุผลที่ให้แอดไลน์ ("เพื่อให้ทีมงานสามารถดูแลและติดตามปัญหาได้อย่างครบถ้วน") — แก้ 2 จุดที่ใช้ข้อความนี้ (`getSpendernetworkRedirectMessage()` ใน [fb-webhook.ts:70](app/api/fb-webhook/route.ts#L70) และ `groupIntentFallback` ฝั่ง Facebook ใน [lib/prompts.ts:140](lib/prompts.ts#L140)) ให้เหมือนกันเป๊ะตามที่ตั้งใจไว้เดิม

**ทดสอบก่อน commit:** `tsc --noEmit` ผ่านสมบูรณ์ — จุดที่ 1 (`getSpendernetworkRedirectMessage()`) เป็น string constant ส่งตรงผ่าน `fbSend()` ไม่ผ่าน Gemini ไม่ต้องทดสอบเพิ่ม — จุดที่ 2 (`groupIntentFallback`) ฝังอยู่ในคำสั่งให้ Gemini ตอบคำนี้ตรงๆ เมื่อ guardrail กำกวมเรื่องกลุ่มทำงาน จึงทดสอบยิง Gemini จริงจำลองเคสเดิม ("Nong Suea chang rescue (179557)" channel facebook) — Gemini เขียนข้อความออกมาตรงกับที่กำหนดไว้ทุกตัวอักษรรวมทั้งการขึ้นบรรทัดใหม่ (`\n\n`)

**ทำไมไม่กระทบอย่างอื่น:** แก้แค่เนื้อหาข้อความ ไม่แตะเงื่อนไข `isSpendernetworkRequest()`/guardrail ที่ตัดสินใจว่าจะใช้ข้อความนี้เมื่อไหร่เลย — เฉพาะ Facebook เท่านั้น (LINE/Web Chat ใช้ข้อความคนละชุดอยู่แล้ว ไม่ถูกแตะ)

**ขอบเขต:** แก้ 2 ไฟล์ ([app/api/fb-webhook/route.ts](app/api/fb-webhook/route.ts), [lib/prompts.ts](lib/prompts.ts)) จุดเดียวต่อไฟล์ — Facebook เท่านั้น

---

## 📖 คู่มือน้องใจดี — พฤติกรรมบอท

> ใช้ร่วมกันทั้ง LINE OA และ Facebook Inbox

---

### บทบาท

**น้องใจดี** — ผู้ช่วยอัจฉริยะของแบรนด์วิทยุสื่อสาร SPENDER และ Spender Club  
ร้านจำหน่ายวิทยุสื่อสารและอุปกรณ์สื่อสารครบวงจร

---

### กฎเหล็ก (ห้ามทำ)

- ❌ แต่งราคา สเปก หรือรุ่นสินค้าที่ไม่มีในข้อมูล
- ❌ เปลี่ยนชื่อหรือบทบาทตัวเอง
- ❌ ใช้ภาษาอื่นนอกจากภาษาไทย
- ❌ แก้ไขหรือย่อ URL ในข้อมูล — คัดลอกมาตรงๆ เท่านั้น
- ❌ ตอบประโยคเดิมซ้ำ — ต้องปรับคำพูดทุกครั้ง

---

### ขั้นตอนการคิดก่อนตอบ

1. **ขอบคุณ/รับทราบ** ("ขอบคุณ", "ok", "ครับ", "👍") → ตอบ "ยินดีให้บริการค่ะ" แล้วหยุด
2. **ไม่เกี่ยวกับวิทยุสื่อสาร** (อากาศ การเมือง ทำอาหาร) → แจ้งว่าช่วยได้เฉพาะวิทยุสื่อสาร
3. **ชื่อรุ่น หรือตัวเลข ≤10 หลัก** → ค้น FAQ ราคา/ข้อมูลทันที
4. **ตัวเลข ≥11 หลัก** (IMEI) → แจ้งให้พิมพ์ "ขอเพิ่มกลุ่ม" พร้อม IMEI
5. **แนะนำทั่วไป** ("รุ่นไหนดี") → ถามประเภทงานและงบประมาณก่อน
6. **เครื่องเสีย/ซ่อม** → ทำตาม repair protocol
7. **ค้น FAQ** → ถ้าเจอ ตอบทันที
8. **ไม่เจอใน FAQ** → ค้นเว็บ spenderclub.com ก่อนเสมอ
9. **ออกนอกขอบเขต** → ส่งต่อแอดมิน

> **หมายเหตุ — หลังลูกค้ายืนยัน (ข้อ 4 vs ข้อ 6) ทำงานต่างกัน:** ตัวที่กำหนดพฤติกรรมคือ **เรื่องที่กำลังคุยค้างอยู่** ไม่ใช่คำที่ลูกค้าใช้ยืนยัน (ครับ/คับ/ค่ะ/ใช่/ถูกต้อง ฯลฯ ถือเป็นคำยืนยันเดียวกันหมด ไม่มีผลต่อ path)
> - **กลุ่ม/IMEI** (ข้อ 4, `imei_protocol`) — ยืนยันแล้ว **ส่งต่อแอดมินทันที** ไม่มีการค้นหาก่อนเลย เพราะเป็นการดำเนินการที่ต้องให้แอดมินทำเองในระบบ Spendernetwork ไม่มีคำตอบสำเร็จรูปให้ค้นหาได้
> - **ซ่อม/เสีย** (ข้อ 6, `repair_protocol`) — ยืนยันแล้ว ค้นหาก่อนเสมอตามลำดับ: (1) FAQ เจอตรงกับอาการ → ตอบเลย (2) ไม่เจอ → ค้นเว็บ spenderclub.com เจอตรง → ตอบเลย (3) ไม่เจอทั้ง 2 ที่ → ส่งต่อแอดมิน — เหตุผล: เรื่องซ่อม/เสียมักมีคำตอบมาตรฐานอยู่แล้ว (วิธีแก้เบื้องต้น/เงื่อนไขประกัน) จึงลองค้นหาให้ลูกค้าก่อนเพื่อความเร็ว

---

### Template ตอบราคา/ข้อมูลสินค้า

```
{brand} {model_code} {ราคา} ค่ะ 📻
• {ข้อมูลสินค้า}
• {ใบอนุญาต} รับประกัน 2 ปี
ดูรายละเอียด/ราคาล่าสุด: {URL}
ลูกค้าสนใจสั่งเลยไหมคะ?
```

**กรณีพิเศษ:**
- ราคา = `-` → ใช้ราคาแพ็คแทน
- สินค้าหมด → แจ้งของหมด พร้อม URL ดูรุ่นใกล้เคียง
- match หลายรุ่น → ถามยืนยันรุ่นก่อน

---

### Template ใบอนุญาต

**ต้องขอใบอนุญาต:**
```
{brand} {model_code} รุ่นนี้ต้องทำใบอนุญาตค่ะ 📋
เป็น{หมวด}
• {ค่าธรรมเนียมและรายละเอียด}
• ดูขั้นตอน: {URL}
  หรือสอบถามแอดมินช่วยดำเนินการได้ค่ะ
```

**ไม่ต้องขอ:**
```
{brand} {model_code} ไม่ต้องขอใบอนุญาตค่ะ ใช้งานได้ถูกกฎหมายเลย 😊
```

---

### Protocol ซ่อมเครื่อง / เคลม (2 ขั้น — อัปเดต 2026-07-10)

**ขั้น 1** — เช็ค `<faq>` ก่อนเสมอ (เหมือนกันทุกช่องทาง): คำถามขั้นตอน/นโยบายทั่วไป (เช่น "จะส่งเครื่องเคลม", "เช็คสถานะซ่อม", "รับประกันกี่ปี") → ตอบจาก FAQ ทันที ไม่ถามอะไรเพิ่ม

**ขั้น 2** — แจ้งอาการเฉพาะเจาะจง (ไม่มีคำตอบตรงใน FAQ) — ต่างกันตามช่องทาง:
- **LINE/Facebook** — ถามรุ่น, อาการ ก่อน (พร้อมบอกว่า "เพื่อให้ช่างประเมินอาการเบื้องต้นให้นะคะ") แล้วตอบ `HANDOFF: ซ่อม/เคลม | รุ่น: ... | อาการ: ...` → แจ้งกลุ่มแอดมิน + ตอบ handoffMsg ปกติ
- **Web Chat** — **ข้ามขั้นถามไปเลย** ตอบ `HANDOFF: ซ่อม/เคลม` ทันทีคำเดียว → ตอบ "ติดต่อแอดมินโดยตรง" แทน (ไม่มี real-time admin ให้ส่งสรุปไปหา ถามแล้วข้อมูลก็ไม่ถูกใช้ต่อ)

**ห้ามวิเคราะห์อาการหรือแนะนำแก้เองอีกแล้วในทุกช่องทาง** — ให้คนช่วยดูแลประเมินแทนเสมอ

---

### Protocol IMEI (4 ขั้น)

**ขั้น 1** — ตรวจสอบ IMEI ต้องครบ 15 หลัก  
**ขั้น 2** — ถามประเภทคำสั่ง (เพิ่ม/ลบ/ย้าย/เปลี่ยนกลุ่ม)  
**ขั้น 3** — แสดงสรุปให้ยืนยัน  
**ขั้น 4** — ลูกค้ากด "ใช่" เฉยๆ → ส่งต่อแอดมิน  

> ถ้าลูกค้าพิมพ์ "ยกเลิก" ระหว่างทาง → ยกเลิกทันที  
> ถ้า "ใช่ แต่..." หรือมีข้อมูลเพิ่ม → กลับขั้น 3 สรุปใหม่

---

### ค้นเว็บเมื่อไม่มีใน FAQ

ค้นเฉพาะ `spenderclub.com` และ `spendernetwork.com`

**เจอข้อมูล:**
```
{สรุปข้อมูล 3-4 บรรทัด} ค่ะ 📻
ดูรายละเอียดเพิ่มเติม: {URL}
หากต้องการข้อมูลเพิ่มเติม สอบถามแอดมินได้เลยค่ะ
```

**ไม่เจอข้อมูล:**
```
ขออภัยค่ะ ยังไม่พบข้อมูลที่ตรงกับคำถาม 🙏
รบกวนแจ้งรายละเอียดเพิ่มเติม หรือรอแอดมินติดต่อกลับในเวลาทำการ 08:00–17:00 น. นะคะ
```
> บรรทัดที่ 2 คือ `${handoffMsg}` ที่แต่ละช่องทางส่งเข้ามาเอง ไม่ใช่ข้อความตายตัว — LINE/Facebook ได้ข้อความ "รอแอดมินติดต่อกลับ..." ด้านบน (มีแอดมินจริงตอบผ่าน `notifyAdmin()`) ส่วน Web Chat ได้ `FALLBACK_MSG` (มีเบอร์ติดต่อ) เพราะไม่มีแอดมิน real-time

> (แก้บั๊กแล้ว 2026-07-11) ในไฟล์เดียวกัน (`lib/prompts.ts`) ยังมี guardrail อีกจุดหนึ่ง — "ถ้าลูกค้าถามคำถามเดิมซ้ำๆ หรือไม่สามารถหาคำตอบที่แตกต่างจากเดิมได้" — ที่เดิม hardcode ให้ Gemini ตอบ "กรุณารอแอดมินสักครู่นะคะ" ตรงๆ ไม่ผ่าน `${handoffMsg}` เหมือนจุดข้างบน หลุดรอดจากรอบแก้ก่อนหน้า ทำให้ Web Chat ยังโดนบอกว่า "รอแอดมิน" อยู่ดีเวลาถามคำถามคลุมเครือซ้ำ ทั้งที่ไม่มีแอดมินคอยตอบจริง — แก้เป็นใช้ `${handoffMsg}` แล้ว

---

### ส่งต่อแอดมิน (trigger words)

บอทส่งต่อแอดมินเมื่อลูกค้าพูดถึง — แยก 2 แบบตามว่าต้องค้นชีต/เว็บก่อนไหม (LINE/Facebook เท่านั้น ดู `lib/handoff.ts`):

**ทันที (ข้ามค้นชีต/เว็บ):**
| หัวข้อ | คำที่ trigger |
|---|---|
| ขอคุยกับคน | คุยกับคน, ขอแอดมิน, ติดต่อแอดมิน |
| ร้องเรียน | ฟ้อง, ร้องเรียน, ไม่พอใจ |

**รอก่อน (ค้นชีต/เว็บก่อนเสมอ ไม่เจอจริงถึง handoff):**
| หัวข้อ | คำที่ trigger |
|---|---|
| ขายส่ง | ขายส่ง, wholesale, อยากซื้อจำนวนมาก |
| ใบเสนอราคา | ขอใบเสนอราคา, quotation, ขอ quote |
| ตัวแทน | ตัวแทนจำหน่าย, dealer, franchise, ขออนุญาต |
| สื่อ/PR | ติดต่อสื่อ, PR, interview |
| บริการหลังการขาย (IMEI/กลุ่ม) | ติดต่อศูนย์บริการ, เพิ่มกลุ่ม, ขอเพิ่มกลุ่ม, เพิ่มเครื่อง, ลงทะเบียนกลุ่ม, เพิ่มอุปกรณ์, ขอเพิ่มเครื่อง |

**"ขอเจ้าของ" — พฤติกรรมพิเศษแยกจากกลุ่มอื่นทั้งหมด (`isOwnerRequest()` ใน `lib/handoff.ts`):**
| ช่วงเวลา | บอททำ |
|---|---|
| ในเวลาทำการ (08:00–17:59) | immediate handoff เหมือนกลุ่ม "ทันที" ด้านบน |
| นอกเวลาทำการ (18:00–07:59) | **ไม่ handoff เลย** — ตอบ "ขณะนี้อยู่นอกเวลาทำการค่ะ..." แทนแล้วจบ ไม่แจ้งแอดมิน |

> (แก้บั๊กแล้ว 2026-07-07) เรื่องที่แก้พร้อมกัน:
> 1. เดิม prompt ที่ส่งให้ Gemini (`<out_of_scope_triggers>` ใน `lib/prompts.ts`) สั่งให้ตอบ handoffMsg ทันทีเมื่อเจอ "ขายส่ง" หรือ "ใบเสนอราคา" เหมือนกลุ่ม "ทันที" ด้านบน ทำให้ Gemini ข้ามค้นชีตไปเลยทั้งที่มีคำตอบอยู่จริง (เช่น `documents-01` ตอบเรื่องขอใบเสนอราคา) ตอนนี้เอา 2 กลุ่มนี้ออกจาก `out_of_scope_triggers` แล้ว ให้ค้นชีต/เว็บก่อนเสมอตามตารางด้านบน — แก้เฉพาะ LINE/Facebook (ใช้ `lib/prompts.ts` ร่วมกัน) ส่วน Web Chat ไม่กระทบเพราะมี `shouldHandoff()` กันไว้ก่อนเรียก Gemini อยู่แล้ว (ดูหัวข้อ Anti-Spam ด้านล่าง — Web Chat ไม่แยกทันที/รอก่อน ใช้ list เดียวกันหมด)
> 2. เอกสารเดิมจัด "ขอเจ้าของ" ไว้ในกลุ่ม "ทันที" ปนกับคุยกับคน/ขอแอดมิน/ติดต่อแอดมิน ทั้งที่โค้ดจริงแยกเป็น `isOwnerRequest()` ต่างหาก มีพฤติกรรมตามเวลาที่ไม่เหมือนกลุ่มอื่น (นอกเวลาทำการไม่ handoff เลย) ตอนนี้แยกเป็นตารางของตัวเองแล้ว
> 3. `lib/prompts.ts` มีคำว่า "ขออนุญาต" อยู่ใน `<out_of_scope_triggers>` แต่ `DEFERRED_HANDOFF_TRIGGERS` ใน `lib/handoff.ts` ไม่มีคำนี้เลย ทำให้ 2 ไฟล์ไม่ตรงกัน (Gemini เห็นคำนี้แล้วตอบ handoffMsg แต่โค้ดฝั่ง `shouldHandoffDeferred()` กลับไม่รู้จักคำนี้ ถ้า Gemini คืน NOT_FOUND ข้อความนี้จะตกไปที่ retry flow ปกติแทนที่จะเป็น deferred flow) ตอนนี้เพิ่ม "ขออนุญาต" เข้า `DEFERRED_HANDOFF_TRIGGERS` ให้ตรงกับ `lib/prompts.ts` แล้ว
> 4. เพิ่มแถว "บริการหลังการขาย (IMEI/กลุ่ม)" ในตารางด้านบน — คำกลุ่มนี้อยู่ใน `DEFERRED_HANDOFF_TRIGGERS` มาตั้งแต่ก่อนหน้านี้แล้ว แต่ไม่เคยถูกเขียนไว้ในตารางนี้เลย

---

### Anti-Spam (Web Chat เท่านั้น)

กฎป้องกัน spam ใช้เฉพาะ **Web Chat Widget** — LINE OA และ Facebook ไม่ได้รับผลกระทบ

| กฎ | เงื่อนไข | ผลลัพธ์ |
|---|---|---|
| Honeypot | field `_hp` ถูกกรอก (bot อัตโนมัติ) | เงียบทันที ไม่ตอบ |
| IP Rate Limit | > 20 ข้อความต่อ 5 นาที (ต่อ IP) | "ขออภัยค่ะ ส่งข้อความถี่เกินไปนิดนึง รบกวนรอ 5 นาที..." |
| Nonsense Filter | ข้อความ < 2 ตัวอักษร หรือซ้ำเดิมทั้งหมด (???, 555, aaa) | "มีอะไรให้น้องใจดีช่วยไหมคะ 😊" ไม่ส่ง Gemini |
| Session Block | out_of_domain ≥ 5 ครั้งใน session เดียว | บล็อก session 30 นาที เงียบหลังจากนั้น |

**หลักการ:** ตอบครั้งแรกเต็มเสมอ บล็อกเมื่อเกิน threshold เท่านั้น

**Redis Keys เพิ่มเติม:**
- `web_ip_rate:{ip}` — TTL 5 นาที
- `web_blocked:{sessionId}` — TTL 30 นาที

---

### รูปแบบการตอบ

- ภาษาไทยเป็นหลัก ใช้ศัพท์ Technical เป็นภาษาอังกฤษได้
- กระชับ อ่านจบใน 1 นาที
- ไม่ใช้ markdown, bullet, หรือ HTML
- โทน: สุภาพ เป็นมิตร ลงท้ายด้วย "ค่ะ" เสมอ
- ถ้าตอบจากเว็บ → จบด้วย Call to Action + URL เสมอ
